# done.md — índice do que FECHOU, agrupado por demanda

> **Este arquivo é ÍNDICE, nunca narrativa.** Cada linha traz id, tarefa, data e a **âncora no
> `CHANGELOG.md`**. O porquê, a medição e os achados moram lá — o `CHANGELOG.md` já é o arquivo de
> raciocínio deste repositório (625 entradas narrativas), e repetir aqui criaria uma quinta casa
> afirmando o mesmo fato, com a mais nova vencendo em silêncio.
>
> Os grupos espelham os de `pending.md`, inclusive quando vazios: o lugar de pousar uma tarefa
> fechada existe antes de ela fechar.

---

## `docs/adr/adr-contextstore-allowlist.md` — ContextStore como ALLOWLIST

*(V0–V3, V1b, V2b, D6, D7-metade, D8 e a FATIA 1 da D9 fecharam antes deste ledger existir —
história no `CHANGELOG.md`.)*

| id | tarefa | data | âncora no `CHANGELOG.md` |
|---|---|---|---|
| ALW-17 | **`echo_to_operator` saiu — catalogo, tela, encanamento, gate e teste.** Decisao do dono: desde 2026-09-10 `none` deixou de remover o campo e `plain` ja era rebaixado a `masked` pelo *"o tipo aperta"*, entao os tres modos produziam byte a byte a mesma saida e a tela oferecia uma escolha que nao existia. **Nada muda para o operador** — ele ja lia sempre o mascarado. O que saiu: o campo do `MaskingDisplayRuleSchema` e das 8 entradas semeadas, o seletor da `MaskingPage`, o parametro `echo_policy` do `redact_customer_reply`, a consulta ao config-api por submissao mascarada, e o modulo `masking_types.py` INTEIRO — ele existia so para essa politica e ficou sem chamador. ⚠️ **Dois instrumentos foram movidos junto, e essa e a parte que se esquece:** o `test_none_e_masked_produzem_a_mesma_saida`, que fixava a INERCIA como fato, saiu — um teste sobre parametro removido nao protege nada — e foi substituido por um que cobra a AUSENCIA do parametro; e o ramo F4 do `probe_masking_display_domain` foi INVERTIDO (exigia `echo_policy` em exatamente 1 destino; passou a exigir zero). ⚠️ A primeira versao desse ramo acusou o proprio comentario que explicava a remocao — hoje ele distingue mencao em crase de codigo, mesmo criterio do ramo A. Deixou ficha: ALW-19. Gate VERDE nos 6 ramos, `probe_masked_field_echo_parity` verde, 156 testes do bridge e 61 do config-api verdes, typecheck limpo nos dois pacotes TS | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (8) |
| CNS-19 | Chave de `delegate.context`/`collect.context` cujo CANÔNICO é do `core.*` passa a ser gravada no canônico, não no alias `session.<k>`. Fecha a regressão da CNS-11, que migrou os LEITORES (Console `isFormFillSnapshot`, `skill_dialog_runner_v1`, `skill_survey_runner_v1`, o step de desarme) e deixou o produtor prefixando tudo com `session.` — item de fila PULL (**aprovação e wrap-up**, os dois `delegate` com `dialog_form_id`) abria no chat com *'Awaiting messages…'* em vez de renderizar o formulário. Escopo deliberado: **só** aliases cuja canônica é `core.*`; os de canônica `session.*` são a fase V4 do ADR da ALLOWLIST e não entram de carona | 2026-09-03 | `CHANGELOG.md` § 2026-09-03 CNS-19 |
| CNS-21 | Diagnóstico do NPS invisível e do OTP travado: **os dois vinham de SNAPSHOT de slot anterior à CNS-11**, que lê `@ctx.session.*` enquanto o produtor escreve a canônica `core.*` — o ref resolve `null` e o efeito é mudo (no NPS a visibilidade cai para `agents_only` e o cliente nunca vê; no runner o `form_id` chega `null` e o `form_get` recusa). Provado por instrumentação do `resolveCtxRef` (`[CTXDBG] tag=session.dialog_form_id … valor=null`) e fechado por re-promoção de `dialog_runner` e `nps_ia`. ⚠️ **A CNS-19 ANTECIPOU o defeito no OTP**: enquanto o produtor escrevia o alias, o snapshot velho funcionava por coincidência | 2026-09-03 | `CHANGELOG.md` § 2026-09-03 CNS-21 |
| ALW-06 | D7 (metade) — a Config API passa a DIZER qual escopo está em vigor: rota `/_provenance` por key (escopo efetivo × presença nos dois lados × divergência), `shadowed_by[]` na resposta do `PUT` global, e banner na tela de Mascaramento. No caminho, a `GET /{namespace}/raw` foi medida INALCANÇÁVEL (literal registrada depois da paramétrica) e removida | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 ALW-06 |
| ALW-07 | Duas casas para `mention_command set_context` viram UMA — e a decisão foi REMOÇÃO, não merge: a casa TS (tool `mention_command_dispatch`) não tinha chamador nenhum, e a própria descrição dela nomeava um chamador que nunca chamou. Fica a do bridge, que é a que roda; some uma superfície MCP sem credencial (CAP-09) | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 ALW-07 |
| ALW-10 | `display_voice`, `echo_to_customer` e `echo_to_operator` tinham zero consumidores; decisão do dono foi **padronizar**, não remover: domínio CHANNEL-ABSTRACT (`display_voice` absorvido por `token_display`) e `EchoMode = plain\|none\|masked` **sem parcial**, porque eco é INPUT e não renderiza token. Fronteira eco × armazenamento escrita e imposta; ponta do OPERADOR fiada nas duas casas (bridge + Console), com o tipo APERTANDO e nunca afrouxando | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 ALW-10 |
| ALW-11 | Censo do ContextStore vira **PORTÃO** (`probe_contextstore_cadastro.sh`, exit 0/1/2, as duas metades reprovam). Nasceu verde porque o único achado foi consertado antes: `core.workflow.reviewer_id`, escrita sem leitor em namespace fechado, id de pessoa `agents_only` sem tipo. ⚠️ Ao promover, o extrator revelou perder tag por DISTÂNCIA (janela de 6 linhas) — hoje é o ESCOPO da chamada, com `--autoteste` guardando a regressão | 2026-09-03 | `CHANGELOG.md` § 2026-09-03 ALW-11 |
| ALW-13 | `journey.processo.origin_process_session` removida do arquivo do tenant e do mapa vivo (97 → 96 folhas, efeito conferido nos dois lados); no guia o exemplo virou nota datada, porque nome que sai do mapa e fica em exemplo volta como pendência redescoberta | 2026-09-03 | `CHANGELOG.md` § 2026-09-03 ALW-11 |
| ALW-04 | Lista de domínios da plataforma FECHADA por critério de PAPEL: os 7 domínios específicos do tenant (20 campos, 21 aliases) saíram da declaração e viraram cadastro; `PLATFORM_CONTEXT_DOMAINS` + oráculo impõem, e o gate passou a medir a config viva por CONTENÇÃO | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 ALW-04 |
| ALW-03 | Onde mora o cadastro: divisão por FATO — catálogo de tipos fica em `config.masking` (compliance), o MAPA ganha `config.context_map` com o **developer** no preset e tela própria em `/config/context-map` | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 ALW-03 |
| ALW-02 | Choke point de escrita do ContextStore: carimbo do `atributo` (D9.6) nos dois funis, gate de paridade TS × Python sobre fixture única, e os 21 sítios roteados. Censo 8/22 → 2/2 | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 ALW-02 |
| ALW-12 | Provisionamento do mapa de contexto do TENANT: `infra/context-map/tenant_demo.json` + `infra/seed/seed_context_map.py` + serviço `context-map-seed`, com o orchestrator-bridge ordenado depois dele. Pós-`--wipe` deixa de perder 20 canônicas | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 ALW-12 |
| ALW-01 | **V4** — portão de cadastro do ContextStore no `PUT /v1/skills`: coletor das 5 superfícies de autoria em `@plughub/schemas`, gate de paridade TS × Python sobre fixture única, e o runtime passa a LOGAR a tag não cadastrada nos dois funis | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 V4 |
| ALW-08 | Afordância preventiva no editor: dry-run `POST /v1/skills/validate` rodando o MESMO `validateSkillPayload()` do `PUT` (D2/D3 do ADR do editor), com painel que lista os erros e oferece atalho para `/config/context-map` quando o código é `unregistered_context_tag` | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 ALW-08 |
| MSK-03 | **Remove-se o VALOR, nunca o CAMPO — e as três casas do eco passam a concordar.** O dono viu 1 campo na tela de histórico onde o cliente preencheu 3; medido, não era perda: o bridge aplicava `echo_to_operator: none` (que APAGAVA a chave) e a lista Redis que a MESMA tela relê num F5 não conhece política nenhuma — um F5 mudava o que ele via da mesma submissão. ⚠️ **O argumento da ALW-10 não sobreviveu à medição:** o `MenuCard` renderiza o `label` de todo campo do formulário, mascarado incluído, então `none` não escondia a existência do campo — fazia o eco contradizer o cartão acima dele. Decisão do dono aplicada nas TRÊS casas (bridge · webchat · eco otimista do Console), mais a correção do campo VAZIO, que virava `••••••` e afirmava um valor inexistente (agora `""`, a mesma gramática do campo livre em branco). Mecanismo: `probe_masked_field_echo_parity.sh` COMPILA com o `tsc` e EXECUTA as três contra a mesma tabela — a ALW-10 já pedia o acordo em prosa. 12 casos com as duas classes exercidas (senão INCONCLUSIVO), 5 mutações. Suítes 148/148 e 771/771; o teste da semântica antiga foi REESCRITO com a razão ao lado, não apagado. ⚠️ Consequência declarada: `echo_policy` ficou INERTE, fixada como fato por teste próprio (`ALW-17`) | 2026-09-10 | `CHANGELOG.md` § 2026-09-10 (4) |

---

## `docs/product/contextstore-core-namespace-spec.md` — namespace do CORE do ContextStore

| id | tarefa | data | âncora |
|---|---|---|---|
| CNS-20 | **A assercao 7b do `smoke_limite_tres_acessos` ganhou o ANTES que faltava — e a suspeita (b) foi REFUTADA.** (a) A 7b provava o DESARME do pacote exigindo duas tags VAZIAS, forma que passa por AUSENCIA: ate a CNS-19 o produtor escrevia `session.dialog_form_id` e a assercao lia `core.workflow.dialog_form_id`, entao o nome NUNCA existia e o defeito que ela existe para pegar ficou invisivel. Agora ha controle positivo (pacote ARMADO antes da decisao, senao INCONCLUSIVO) e as tags sao declaradas em `PKG_TAGS`, **fonte unica lida pelo controle e pela assercao** — duas listas reintroduziriam o verde por ausencia com o carimbo de 'tem controle positivo'. Falseado por mutacao, e a M2 e demonstracao e nao argumento: sem o controle e com o nome velho o smoke fica VERDE. (b) **Refutada**: `limpar_form_do_pacote` escreve na sessao certa — todo `type: delegate` roda como especialista de conferencia DENTRO da sessao do chamador (`persistDelegate` → `/delegate-conference`), e o `child_session_id` devolvido E o proprio `session_id`; sessao-filho e coisa de `task()` | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 CNS-20/MEN-03 |
| CNS-07 | Tela de tipos completa — `mascara.by_role` e `lgpd` editáveis | 2026-09-01 | `CHANGELOG.md` § 2026-09-01 CNS-07 |
| CNS-02 | Reserva do ContextStore fechada no root `core.*` (e não no `session.*`) | 2026-09-01 | `TODO.md` § CNS-02 |
| CNS-09 | **D-C dissolvida pela CNS-02** — tirar `portabilidade`/`reembolso` da estrutura deixou de ser tarefa: sob `core.*` aquilo é espaço de TENANT, e a plataforma não dita vocabulário de negócio. Sobrevive como conselho de modelagem na spec §3, não como trabalho | 2026-09-01 | `TODO.md` § CNS-02 |
| CNS-03 | Rota de `core.` DECLARADA (o escopo do core é o 2º segmento: `core.customer.`→90 d, `core.journey.`→30 d) + `core.segment.` em `dynamic_prefixes` | 2026-09-01 | `CHANGELOG.md` § 2026-09-01 CNS-03 |
| CNS-04 | Tabela de roteamento única em `@plughub/schemas`; oráculo troca `unknown_scopes` por `mismatched_retention` (coerência, não enum) — desarma o root `customer` prometendo 90 d e entregando 4 h | 2026-09-01 | `CHANGELOG.md` § 2026-09-01 CNS-04 |
| CNS-18 | Causa raiz isolada por A/B: é o `stdout` do python em cp1252 — não `sys.stdin` (CNS-12) nem a variável de shell (CNS-17). Duas migrações minhas revertidas por consertarem defeito inexistente | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 CNS-18 |
| CNS-17 | Diagnóstico da CNS-12 REFUTADO (não era `sys.stdin`, era a variável de shell); `PYTHONIOENCODING` fica nos 26 com a justificativa correta | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 CNS-17 |
| CAP-17 | Os DOIS testes de `role: "orchestrator"` removidos (a regra era inalcançável — zero portadores em 44 skills). O irmão VERDE era o perigoso: `.not.toThrow()` sem refinement. Suíte 177/177 | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 CAP-17 |
| CNS-06 | Critério de contagem dos escritores diretos DECLARADO + gate cruzando instrumento × oráculo: **8 arquivos · 22 sítios**, e o achado de que um funil (`writeContextTag`) já existe | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 CNS-06 |
| CNS-12 | O seed NUNCA reescreveu key divergente — o probe é que corrompia UTF-8 ao ler (`sys.stdin` cp1252) e culpava o produto. Oito ramos verdes | 2026-09-02 | `CHANGELOG.md` § 2026-09-02 CNS-12 |
| CNS-08 | Mapa do ContextStore ganha tela (`core.*` somente leitura, gravando no global) e o portão passa a recusar QUALQUER override de tenant nesta chave | 2026-09-01 | `CHANGELOG.md` § 2026-09-01 CNS-08 |
| CNS-15 | Destino dos 12 sem deploy decidido caso a caso: **3 apagados**, 9 mantidos com razão nomeada (quarentena declarada · Fase 2 encenada · dependência funcional · motor legado com remoção já prescrita) | 2026-09-01 | `CHANGELOG.md` § 2026-09-01 CNS-15 |
| CNS-01 | Inventário de skills medido AO VIVO: 30 com deploy · 12 sem · os 3 pools fantasma não existem. As duas metades da tarefa refutadas; nada apagado | 2026-09-01 | `CHANGELOG.md` § 2026-09-01 CNS-01 |
| CNS-05 | **D-A** — o `put_config` RECUSA (422) root `core` vindo de tenant; medido antes: dava 200 e o override apagava as 94 folhas da plataforma para aquele tenant | 2026-09-01 | `CHANGELOG.md` § 2026-09-01 CNS-05 |
| CNS-11 | **O commit único** — 35 nomes do core migram para `core.*`: 84 arquivos na passada mecânica + 34 folhas movidas nos dois mirrors do mapa | 2026-09-01 | `CHANGELOG.md` § 2026-09-01 CNS-11 |
| CNS-10 | **Absorvida pela CNS-11** — sob `core.*` nenhum skill renomeia vocabulário próprio; o que muda são as **referências de leitura** a nomes do core em 14 skills, e isso é parte do commit único | 2026-09-01 | `TODO.md` § CNS-02 |
| CNS-24 | **O wrap-up do operador abria VAZIO — a CNS-11 moveu o pacote do formulário para `core.workflow.*` e o portão de namespace do operador o escondeu.** O `DialogFormRenderer` exige `dialog_form_id` + token no snapshot; até 2026-09-01 eles eram `session.*`, e `DEFAULT_OPERATOR_NAMESPACES = ["service","session"]` não contém `core`. Admin/supervisor pulam o portão. Medido como experimento controlado: operator **5 concluídos → 0** (2 hang up, 1 pendurado), admin 24 → 28. ⚠️ **O gate que existia para isto ficou verde dez dias:** o P2 de `gate_session_state_pool_scope.sh` conferia só o HTTP 200 — o operador recebia 200 com o corpo sem o formulário. Conserto: `PLATFORM_CONSOLE_TAGS` (tags EXATAS, nunca o namespace) SOMADAS às do pool, nunca substituídas (a sobrescrita por pool substitui o default; 0 de 41 pools sobrescrevem hoje). Entregar o token ao browser do operador foi decisão do dono contra medição (A5 recusa não-detentor; AUT-47 limita quem lê). P2 passou a exigir o CORPO; bateria no JS compilado: sem o conserto exit 1, restaurado exit 0. Ao vivo, a tarefa do dono passou a receber as duas tags e seguia recuperável | 2026-09-11 | `CHANGELOG.md` § 2026-09-11 (1) |
| CNS-26 | **`core.workflow.dialog_form_version` entra no cadastro.** Medido antes de escolher: e VIVA — escrita no delegate (`webhook.py`) e lida pelo wrap-up como pin de versao (`segment.ts`); a irma `dialog_form_id` estava cadastrada, entao era esquecimento. Tres casas: semente TS, gemeo Python e o mapa VIVO do config-api, que o seed-if-absent nao alcanca. O `--overwrite` foi recusado (descartaria 20 dominios so do vivo); escrita aditiva pela API com backup, plano +1/-0/~0 e releitura identica (99 -> 100 folhas). config-api rebuildado. Cadastro, seed, audit e drift verdes; py 73 passed; tsc limpo | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (13) |

---

## `docs/adr/adr-historico-unificado-duas-visoes.md` — ler um processo num lugar só

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| HIS-03 | F4 — as duas visões na tela | 2026-08-25 | `CHANGELOG.md:5559` |

---

## `docs/adr/adr-journey-session-segment-model.md` — D12: a janela de espera

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| WAI-02 | Produtor de espera para o tier `max_wait_exceeded` (fatia B) | 2026-08-24 | `CHANGELOG.md:6304` |

---

## `docs/adr/adr-voice-media-plane.md` — voz própria / Arc 15 WebRTC

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| VOZ-05 | **Bot leg STT/TTS (V-F2): a chamada WebRTC ganhou ouvido, voz e coleta.** Em cinco fatias: bot entra só com STT de verdade (1); speaches self-hosted (2); o agente de IA fala, com barge-in (3); ouvinte oculto + voz visível e transcrição de cada falante como mensagem marcada (4); coleta por teclado e fala com a semântica numa casa só, fila de sinal (MEN-07) e teclado do widget pelo domínio, com o PIN mascarado fora do SFU (5a-5c). Deixou VOZ-18 | 2026-09-16 | `CHANGELOG.md` § 2026-09-16 (6) — fatias anteriores em § 2026-09-15 (22)-(26) e § 2026-09-16 (2)-(5) |
| VOZ-18 | **Parâmetros de fala da coleta aplicados:** confiança MEDIDA do speaches (`verbose_json`, nunca o 1,0 default) contra `min_confidence`, e `end_silence_ms`/`max_speech_s` por menu num ajuste lido a cada quadro da fala do cliente, desligado com a coleta. Deixou VOZ-19 | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (1) |
| VOZ-19 | **Ruído não é mais fala:** o STT do speaches vai com o VAD do serviço ligado — sem ele, 44 de 44 trechos de não-fala viravam texto ("Obrigado.") e gastavam a tentativa do menu; com ele, 1 de 44 e nenhuma fala perdida. Sem default de `min_confidence` (certas de 0,35 a 0,92). Deixou VOZ-20 | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (2) |
| VOZ-21 | **Segmentação da fala do bot leg é config do tenant:** as 6 constantes do provedor (energia, silêncio de fim, lacuna, fala mínima/máxima, VAD) viram `webrtc.stt_*` no config-api, validadas por faixa no gateway, lidas a cada chamada nova com a procedência no log, invalidadas pelo `config.changed`, e editáveis na aba WebRTC da tela de canais. Pré-requisito da recalibragem (V13) | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (3) |
| VOZ-22 | **Telemetria passiva da fala, só números:** tópico `speech.metrics` com resumo do fluxo do cliente (chão de ruído, falas, descartes, confiança, segmentação em vigor) e desfecho de cada coleta por voz; duas tabelas no ClickHouse e `GET /reports/speech/quality` por pool, com amostra pequena dita. Camada A da recalibragem (V13) | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (5) |
| VOZ-26 | **Endpoint WebRTC cadastrável:** o registro aceita `channel=webrtc` e a aba WebRTC da tela de canais ganha Endpoints (sem conta de integração) ao lado das Configurações; o alias cai no pool do endpoint e trocar o pool vale na chamada seguinte | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (6) |
| VOZ-25 | **Perfil de fala por ponto de entrada:** config-api `speech_profiles` apontado pelo endpoint WebRTC; sobrepõe a segmentação e escolhe modelo/língua/voz no mesmo serviço (menu → perfil → tenant → global → default); telemetria e relatório por pool × perfil; tela de perfis e seletor no endpoint | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (7) |
| VOZ-23 | **Verificação ativa do caminho de fala (camada B):** processo `speech-check` faz uma chamada sintética por endpoint temporário no pool de calibração, mede na saída do caminho real, grava `speech_check_result` e compara com a linha de base marcada por pessoa (`/reports/speech/checks/compare`); disparo por pool webhook + tool MCP. Achou e corrigiu o `receive` que segurava a instância no desligamento | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (8) |
| VOZ-27 | **Periodicidade e tela da verificação de fala:** agenda declarativa (`infra/scheduler/*.json` + job `agenda-seed`, seed-if-absent) disparando o pool de verificação; tela na aba WebRTC (histórico, executar agora, marcar linha de base, comparação) e rota `POST /v1/speech-checks` com Bearer + `config.channels`; a verificação RECUSADA passa a deixar linha (`check_running`) | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (9) |
| VOZ-15 | **Token de mídia exige ATENDER o contato:** `role=agent` só recebe token se estiver entre os atendentes do plano de mídia (`human-{sub}`), não apenas com `agent_assist.atender` no pool; supervisor é exceção declarada; sem atendente conhecido responde `room_not_ready` em vez de 403 | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (11) |
| VOZ-12 | **`file_upload` sai da tabela do WebRTC:** era declarado nos dois gêmeos sem caminho de upload no adapter, e podia fazer um `collect` eleger o canal e não entregar; zero eleições mudaram (nenhum skill vivo pede o requisito). Volta com a `VOZ-28` | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (12) |
| SCH-01 | **As rotas de Agenda passam a exigir credencial:** `scheduler.configurar` para criar/editar, `scheduler.operacao` para disparar/pausar/cancelar e para ler, verificados por `plughub_authz`; tenant do TOKEN; porta de serviço aditiva para o job de seed; 6 smokes migrados para login de admin; censo do terceiro eixo estendido ao scheduler | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (10) |
| VOZ-16 | **O cliente WebRTC recebe o aviso e o motivo quando a plataforma fecha o contato.** Vermelho ao vivo antes (7 falhas): num pool sem agente com teto curto, o routing tirou o contato da fila e publicou `farewell_text`, e o cliente recebeu só o *"Aguardando…"* de antes e `webrtc.session_closed` com `session_timeout`. O adapter lia `close_reason` — que **nenhum** produtor de `session.closed` escrevia, então todo fechamento chegava assim — e o teste unitário passava porque ELE escrevia a chave. Hoje: os três fechamentos do routing (teto de fila, sem recurso, outage) carregam `close_reason` de negócio; o adapter manda o `farewell_text` como `webrtc.message` de sistema ANTES do fechamento, usa `close_reason` ou o `reason` de negócio de bridge/`conversation_end`, não trata o marcador de transporte `agent_done` como motivo e, sem motivo, fecha sem inventar um e diz no log; o widget não fabrica `session_ended`. Gate `probe_webrtc_close_farewell.sh` (contrato nos 3 produtores × leitor · ao vivo F1–F5 com controle de que o fechamento é o do routing); testes com as formas REAIS dos produtores e bateria de 5 mutações, todas reprovando; suítes gateway 1025 e routing 296 | 2026-09-15 | `CHANGELOG.md` § 2026-09-15 (21) |
| VOZ-09 | **Mídia é fato do PARTICIPANTE, e o SFU obedece.** Vermelho ao vivo antes: humano de vídeo atende, cliente publica microfone, especialista de IA de texto entra — o cliente recebe `webrtc.renegotiate` mandando ir para `text` e o estado da sessão vira `text`, enquanto o SFU segue com `can_publish=True, can_publish_sources=[]` (no LiveKit: TODAS as fontes). Por baixo, a capacidade que decidia o meio (`media_capabilities`) não tinha produtor, e 204 de 204 `routing.assigned` vivos saíam sem nada que a substituísse. Hoje: `media_policy.py` com três camadas (política por papel, com procedência `platform_default` até a `VOZ-10` · consumo por framework do atendente · escolha no cliente); teto do cliente = política ∩ UNIÃO do que os atendentes presentes consomem; o bridge carimba `framework`; o adapter aplica nos dois lados — `update_participant` no SFU e `webrtc.media` com token novo — e refaz no `participant_left`; token recortado por fonte, com teto vazio virando `can_publish=False`. Console e widget passaram a ler tetos, e a visão de supervisor deixou de pedir `role=agent`. ⚠️ **Três medições no SFU real mudaram o código:** `update_participant` sobre participante ausente responde `unavailable`/503 (a cara de SFU fora), logo pergunta-se `get_participant` antes; `ParticipantPermission` exige o ENUM (`MICROPHONE`) onde o token aceita a string; e revogar a permissão RETIRA a trilha já publicada. Gate `probe_webrtc_participant_media.sh` (contrato medido no leitor · imagem · cenário ao vivo com a permissão perguntada ao SFU · 2 mutações que TÊM de reprovar · produtor vivo, INCONCLUSIVO sem amostra), vermelho contra as imagens anteriores, verde contra as novas; suítes gateway 1004 e bridge 151; typecheck do platform-ui limpo. Deixou achados em `VOZ-04` e `VOZ-11` | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (12) |
| VOZ-10 | **As mídias do WebRTC são config do POOL, e é ela que chega ao token.** Antes: o teto vinha de `PLATFORM_DEFAULT`, tabela em código (todo pool de todo tenant oferecia áudio e vídeo ao humano), e o adapter disparava gravação lendo `pool.webrtc_recording`, campo sem schema, tabela, tela nem produtor. Censo ao vivo: 0 pools webrtc, nada a migrar. Hoje: `pool.media_policy` `{customer_publish, agent_publish}` (zod `.strict()`, coluna JSONB por migração versionada, `PoolsPage` com i18n nos dois locales), **obrigatória** em pool de contato com `webrtc` — o registry recusa o estado resultante no POST e no PUT (limpar a política ou acrescentar o canal) nomeando o campo; o bridge a lê FRESCA só para sessão webrtc e a leva no `routing.assigned` com procedência (`registry` · `registry_unavailable` · `not_webrtc`); o gateway calcula teto do cliente = UNIÃO de (política do pool de cada atendente ∩ consumo) e do atendente humano = UNIÃO do `agent_publish`, e **ausência nunca vira permissão** (pool sem política, registry fora, evento sem pool ou estado anterior contribuem nada, com procedência nomeada). Sem ordem de preferência, por decisão (degradar é a `VOZ-11`). O gatilho de gravação saiu com lápide e volta na `VOZ-06`. Gate `probe_webrtc_pool_media_policy.sh` (contrato zod×tabela×rota×tela×bridge×gateway · registry ao vivo · produtor→leitor com imagens reais e tokens assinados · 2 mutações que TÊM de reprovar · produtor vivo), vermelho contra as imagens anteriores, verde contra as novas | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (13) |
| VOZ-04 | **Contato de voz/vídeo ponta a ponta por WebRTC — validado com gente no browser.** Cinco fatias, cada uma com vermelho ao vivo antes: **(1) entrada** — widget e servidor não falavam o mesmo handshake, o pedido de roteamento ia sem `started_at`, a mensagem sem `content.type` e a sessão sem `ws_alive` (casa única `adapters/contact_lifecycle.py`, compartilhada com o webchat); **(2) Console** — `conversation.assigned` sem canal, o caminho real de ativação humana não escrevia `routing.assigned` (a sala nunca nascia), token pedido ao mcp-server; **(3) texto** — premissa refutada (já chegava, via `conversations.outbound`); consertada a hora (leitor pedia `ts`) e o rótulo do aviso de sistema; **(4) mídia do host** — loopback + UDP único + TURN com dois nomes, medido num browser real; **(5) validação assistida** — três rodadas do dono: a 1ª achou o SFU v1.8.4 sem responder à oferta de vídeo dos clientes 2.20/2.22 (SFU → v1.13.6, widget com versão exata); a 2ª achou o Console sem tocar áudio remoto nenhum e o autoplay bloqueado mudo (`RemoteAudio`, aviso de áudio bloqueado); a 3ª: **vídeo, áudio e texto nos dois sentidos**. Gates: `probe_webrtc_contact_entry.sh` · `probe_webrtc_agent_console.sh` · A5/A6 do `probe_webrtc_media_plane.sh` · assistido `gate_webrtc_console_live.sh` (L5 mede sinal de áudio) · roteiro `docs/guias/roteiro-validacao-webrtc-console.md`. Achados à parte: `CAP-19`, `VOZ-15`, `AGH-01`. Fora: browser em outra máquina da rede; menu por DataChannel (`VOZ-05`) | 2026-09-15 | `CHANGELOG.md` § 2026-09-14 (14) · 2026-09-15 (15)–(19) |
| VOZ-01 | **O plano de mídia existe, e sem ele o canal RECUSA em vez de fingir.** Vermelho ao vivo antes: provider sem credencial devolvia `dev-token-…`/`RM_dev_…`/`EG_dev_…`, SDK ausente da imagem, zero SFU/TURN em compose, e `GET /webrtc/token/{sid}` emitia **sem credencial** token de supervisor OCULTO com identidade escolhida na query — rota que o provisionamento transformaria em chave real, por isso fechada JUNTO. Hoje: `livekit` (v1.8.4, `auto_create: false`) + `coturn` (4.6.3) no compose demo; SDK no pyproject; `LiveKitProvider` levanta `WebRTCProviderUnavailable` nomeando a env/SDK que falta; o adapter não derruba o boot e fecha a porta do WS antes de autenticar e rotear; a rota exige Bearer + `agent_assist.atender` (agente) ou `contacts.monitorar` (supervisor) no pool da sessão, com identidade do JWT. ⚠️ **O SFU real achou um defeito que o placebo escondia:** `with_ttl(int)` levantava `TypeError` — nenhum token real jamais pôde ser assinado. Gate `probe_webrtc_media_plane.sh` (6 ramos, join só por relay, 7 negativos e 2 positivos que o SFU aceita, mutação embutida), vermelho contra a imagem anterior, verde contra a nova; bateria de 5 mutações externas; suíte 1000 verdes. Fora: browser no host (VOZ-04), egress (VOZ-06), degradação muda do bot leg (VOZ-05) | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (10) |
| VOZ-07 | **O expurgo de anexos existe — estava descrito em tres casas e em nenhum codigo.** `expires_at` era carimbado com a politica do tenant e nunca aplicado; o serving so recusa por `deleted_at`, que ninguem escrevia. Hoje: task de boot supervisionada `attachment-expiry` com os dois estagios no Protocol (`expire_due` marca e o serving passa a 410; `purge_deleted` apaga o blob apos 24 h de carencia e zera `file_path`), SQL unico para os dois backends, primeira passada na SUBIDA, falha de blob contada por arquivo e nomeada no log. ⚠️ **O demo roda S3/MinIO, nao filesystem** — medido antes de escrever. O censo C do `probe_adapter_self_calls` passou a ligar parametro anotado com o tipo, senao as chamadas do job ficavam fora da populacao. Gate novo `probe_attachment_expiry.sh`: invariante viva (VACUA hoje, e diz), exercicio do codigo da imagem contra Postgres e MinIO reais com dois controles e a rota de serving (410/200), e mutacao nos dois sentidos. 14 testes novos, suite 798 | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (3) |
| VOZ-08 | **Tres escritores do AttachmentStore chamavam o store fora do contrato, e nenhum arquivo deles foi armazenado.** `voice.py` chamava `self._store.store()` (metodo inexistente) e `whatsapp.py`/`email.py` chamavam `commit(mime_type=...)` sem `tenant_id`; os tres caiam num `except Exception` e viravam uma linha de log. Achado ao medir a premissa da VOZ-06. Conserto: `reserve`+`commit` como o `webrtc.py` ja fazia. ⚠️ **O instrumento que existia declarava nao ver a forma do defeito** (`probe_adapter_self_calls` so contava `self.NOME()`); ganhou os ramos C/D — censo do contrato do store por AST sobre o pacote inteiro (nome E kwargs, com testemunha de receptor nao reconhecido) e mutacao das tres formas. Contraprova contra `HEAD`: 5 defeitos acusados. Testes novos com store LIGADO a assinatura do proprio Protocol: contra os adapters antigos 3 reprovam com os erros exatos. Suite 784 verdes | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (1) |

---

## `docs/adr/adr-human-agent-pool-scoped-identity.md` — instância do agente humano por pool

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| AGH-01 | **O login de um agente não cancela mais o logout de OUTRO.** O timer de desregistro (2,5 s) era chaveado só pelo pool, e qualquer conexão nova ao mesmo pool fazia `clearTimeout` nele — o agente que saiu ficava `ready`, sem socket, recebendo contato (medido: contato do widget atribuído a uma instância assim). Hoje a chave é (usuário, pool), e o timer só apaga a própria entrada. Vermelho ao vivo antes (G1: A continuava instância com B entrando em 0,3 s), com os controles C1 (sair sozinho desregistra) e G2 (F5 do próprio agente mantém). Gate `probe_agent_ws_ghost_instance.sh`. A segunda causa do mesmo sintoma (reinício do processo) ficou na `AGH-02` | 2026-09-15 | `CHANGELOG.md` § 2026-09-15 (20) |
| AGH-02 | **Reinício do mcp-server não deixa mais agente humano FANTASMA no pool — nem o contato preso a ele.** O desregistro só rodava no `close` do `/agent/ws`, e processo recriado derruba os sockets sem `close`: a instância ficava `ready`, sem TTL, recebendo contato. Vermelho ao vivo antes (G1: 150 s depois, chave com TTL -1; C1: o contato seguia em `human_agents` do fantasma). ⚠️ A cura não é TTL na chave da instância (já feito e desfeito em 2026-07-28: agente conectado sumia sem DEL); o que ganha TTL é um fato separado, *conexão viva de (instância, pool)* (`{t}:human_liveness:{iid}:{pool}`, 90 s), escrito no login e nos dois pongs. Um varredor no mcp-server (15 s, carência de boot de um TTL) sai pelo MESMO `unregisterHumanAgent`, com a liveness relida DENTRO do Lua (`alive` aborta), e publica `agent_disconnect` só para as sessões do semáforo servidas NAQUELE pool em que o humano ainda está. Ficou no mcp-server, não no reconcile do routing que a ficha sugeria: é o dono do ciclo de vida humano e da prova de vida (o pong), e o `crash_detector` do routing pula `human-*` de propósito. Gate assistido `probe_agent_ws_restart_ghost.sh` (G1 · C1 · controles L1/L2 · L3: a liveness se sustenta pelos pongs, não pelo cinto local), mutação ao vivo nas duas direções; 11 unitários, 9 mutações mortas. Achado à parte: `AGH-03` (vaga órfã no semáforo de instância apagada) | 2026-09-15 | `CHANGELOG.md` § 2026-09-15 (28) |

---

## `docs/product/identity-resolver-fase-a-plano.md` — identidade e comércio conversacional

*(Fases A e B fecharam antes deste ledger — história no `CHANGELOG.md`.)*

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| PID-09 | **A chegada pelo WhatsApp do telefone autoritativo prova a posse — e toda prova passou a dizer DE QUEM é.** Medido antes: a evidência de identidade dizia *"alguém provou algo nesta sessão"*, sem cliente. **Vermelho ao vivo** pelo transporte MCP: a sessão que provou por OTP o telefone do cliente A pediu a pendência do cliente B (posse durável) e LEVOU o token, e o `workflow_resume` com ele passou o atestado. Decisões do dono: esta ficha faz só a metade de identidade (`princ` virou PID-21), a chegada EQUIVALE ao OTP (satisfaz `["otp"]`, não é exigível por nome), e o cliente entra também no OTP. A evidência ganhou `customer_id`; `judgeResumeEvidence` exige o cliente das pendências (liberação) e o cliente DO TOKEN (retomada, pelo índice `pending_by_customer`), e sem cliente nada satisfaz — a porta legada por `contact_identifier` fecha, com zero chamadores medidos. O portão de posse do `pending_workflow_get` aceita também a posse provada NESTA sessão para o cliente resolvido. O gateway conta a chegada ao escritor único (`POST /internal/identity-evidence`): `verified` só com assinatura da Meta CONFERIDA e o `from` como âncora `authoritative`, `failed` para número sem dono, `not_run` sem segredo — e erro do cadastro não apaga prova. De passagem: sem `whatsapp_app_secret` o adapter aceitava webhook sem conferir nada, e a chegada teria virado prova forjável; o demo ganhou segredo. Sem conta Meta, o ao vivo é webhook assinado simulado. WhatsApp sem endereço de pool virou PID-22. Gate `probe_arrival_evidence.sh` | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (8) |
| PID-16 | **Um skill em várias portas é promovido em LOTE: um snapshot, a config de cada pool, tudo ou nada.** Medido: `skill_intake_runner_v1` em duas portas com UM snapshot e DUAS configs; o release era `set-next` + `promote` por pool — snapshots congelados em instantes diferentes, e vermelho ao vivo nos fixtures: `skill_nps_v1` pool a pool promoveu `probe_batch_a` e parou na recusa de perfil de `probe_batch_b`. Decisões do dono: lista EXPLÍCITA de pools (nunca "todos que rodam o skill"), tudo ou nada, rollback por pool, e só API + script nesta ficha (tela e tool viraram PID-20). `POST /v1/pool-slots/promote-batch` julga todos os portões em todos os pools antes de mexer em qualquer slot e troca os slots numa transação; config herdada só do `current` que já roda o skill; `next` pendente recusado salvo `replace_pending_next`; pool idêntico fica `unchanged` e o `previous` sobrevive; capacidade somada sobre o lote (dois aumentos que cabem sozinhos estouravam juntos). Os portões do candidato e a mecânica do promote ganharam UMA casa cada (`lib/slot-candidate.ts`, `lib/slot-promotion.ts`), usadas por set-next, promote e lote — e quatro censos que liam o texto antigo das rotas foram movidos junto. De passagem: o `deploy_skill_to_slot.sh` descartava a resposta do promote. Ao vivo nas portas reais, o release do runner em `limite_ia,portabilidade_ia` saiu `unchanged` nos dois, com os slots idênticos. Gate `probe_promote_batch.sh` | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (7) |
| PID-17 | **A portabilidade entra pela porta de plataforma, com a âncora na própria LINHA a portar.** Decisões do dono: âncora `phone` e prova `phone` (o número provado é o número portado; o formulário só coleta operadora e contato de retorno), ociosidade na confirmação preserva a pendência, e a porta mantém a D11 (falha de OTP abre pedido novo; a duplicata é do N3, e confirmar exige OTP pelo piso). O caminho de continuidade nunca tinha rodado (0 segmentos em `portabilidade_confirmacao`) e, exercido ao vivo pela primeira vez, trouxe **três defeitos além dos três da ficha**: cancelar retomava com `input` e o processo registrava SUCESSO; ficar ocioso ou desconectar matava a portabilidade aprovada; e — **vermelho ao vivo** — a âncora da pendência vinha do campo `contact_identifier`, que com a porta é do formulário: a pendência nasceu sob um cliente PROVISIONADO a partir do texto digitado. O processo passa `phone` (a âncora) e grava número e operadora na journey; o agente lê de lá, cancela com `rejected` e devolve a conversa à porta sem tocar no processo. O mapa padrão ganhou `session.phone`/`session.email` como aliases (o cadastro recusava o `delegate`), com escrita aditiva no mapa vivo. Achado lateral: `suspend reason: approval` decidido sem credencial pela rota interna (APR-11). Gatilho da PID-16 atingido. Gate `probe_portabilidade_door.sh`; o censo do `probe_intake_runner.sh` passou a cobrir TODA porta | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (6) |
| PID-04 | **A porta do aumento de limite é o runner de plataforma `skill_intake_runner_v1`, e tudo o que é do processo virou config do pool.** Medido antes: o `skill_limite_entrada_v1` (194 sessões; os três acessos, OTP e degradação ao SAC) fazia identificação, prova, pendência e coleta num YAML de domínio. Decisões do dono: runner + migrar só o limite (a portabilidade virou PID-17); a continuidade é do DOMÍNIO (`skill_limite_continuidade_v1`, pool `limite_continuidade`), porque `offer` significa coisas diferentes por processo e o menu mostra dados que não moram em roteiro; o mecanismo de prova nasce dentro da porta (PID-05 com gatilho). O runner declara 8 parâmetros obrigatórios; `auto` delega ao pool que parqueou a pendência, `offer` ao pool de continuidade. **Defeito fechado de passagem, vermelho ao vivo antes:** o intake montava `context_json` por template com o que o cliente digitou, e um número de cartão com `", "session.cpf": "<outro>"` fez o processo nascer com o CPF de outra pessoa depois de o cliente provar o próprio por OTP — o `workflow_trigger` ganhou `context_fields` + `anchors` (objeto, âncora por último). **Três defeitos de mecanismo achados e fechados:** o `choice` não enxergava `$.config.*` e lia `@ctx.journey.*` no hash da sessão (a entrega narrou recusa de pedido aprovado); o seed não sabia semear config de deploy além da capacidade (6 slots vivos dependiam disso); e o `deploy_skill_to_slot.sh` publicava sem `config_params`, apagando a declaração. Paridade: acessos 2 e 3, cancelamento, pertença e roteiro verdes pelo chat. D11 (abrir pedido novo sem prova) virou PID-19; `door_mode`/`accept_resume_key`, PID-18. Gate `probe_intake_runner.sh` | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (5) |
| PID-08 | **O deploy em lote que registrava sem mudar foi aposentado; o promote é o único registro de deploy.** `POST /v1/skills/:id/deploy` gravava `skill.flow` e um `SkillDeployment` "implantado nos pools X" sem tocar slot — e a produção executa só o snapshot do slot `current`. Medido antes: 115 registros, 110 de promote e 5 de um seed de demonstração no `sac_ia` (o slot nunca mudou; eram triângulos de deploy na lente sobre deploys que não houve), zero usos reais, nenhum chamador de UI, e o workflow `skill_scheduled_deploy_v1` fora de pool nenhum. Decisão do dono: aposentar, e o lote de verdade vira ficha (PID-16). A rota e a listagem agendada respondem 410 apontando o caminho do pool, com o portão de escrita na frente; saíram a tool `skill_deploy` e o workflow; as 5 linhas foram apagadas com aprovação; o seed da lente passou a re-promover o pool — e recusa quando o `previous` guarda outra versão, porque re-promover apagaria o rollback (medido no `sac_ia`). A falha do promote ao gravar o registro deixou de ser muda. Gate `probe_batch_deploy_retired.sh` | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (3) |
| PID-15 | **O cliente que provou identidade cancela o próprio pedido em análise — e não o decide.** Reproduzido ao vivo antes: com OTP provado, "Cancelar solicitação" respondia "Não consegui processar sua solicitação agora" e o processo seguia `suspended` — o token de `aprovar` é tarefa de aprovação, e a AUT-46 exige Bearer humano para retomá-la; o MCP não tem. O processo já separava os papéis (`input` = aprovador decide; `rejected` = `encerrar_cancelado_cliente`). Decisão do dono: o cliente provado encerra, nunca decide. A rota interna dispensa o Bearer só com as quatro condições juntas — tarefa com capacidade + atestado de evidência da PID-13, nenhum Bearer, `decision=rejected`, exigência de identidade no token. Provado no mesmo token: `input` 401 e token vivo; `rejected` retoma e o processo termina pelo ramo do cliente. População do botão antes: zero em 188 sessões | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (2) |
| PID-13 | **A exigência de identidade vale na RETOMADA, em todas as portas.** A PID-06 retinha o token na liberação, mas quem já o tinha retomava sem prova — e medido, não era só o `workflow_resume`: SETE caminhos chegam ao `handle_resume`, um deles público e anônimo (`/channel/webhook/resume/{token}`). Decisão do dono: a regra mora no `handle_resume`, e numa pendência com exigência só três atores passam — o mcp-server, que julga a evidência da sessão chamadora e atesta com a credencial de serviço; principal humano verificado (aprovador, supervisor), porque a exigência é sobre o cliente retomar; e o scanner de prazo. A exigência mora no registro do token (`resume_meta`), gravado fechado. População medida antes: aprovador 90 retomadas, identidade 28, anônimo zero. `resume_door` virou PID-14; o cancelamento pelo cliente, barrado pelo AUT-46 antes desta ficha, virou PID-15 | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (1) |
| PID-06 | **O token de retomada só sai para quem provou NESTA sessão o que a pendência exige.** Medido antes: a liberação tinha um portão só, a âncora `possessed` (posse DURÁVEL) — uma sessão que nunca fez OTP recebeu o `resume_token` de um cliente cujo celular outra sessão acabara de provar. Três correções de premissa: a exigência não mora no `suspend` (o único vivo é aprovação de operador) e sim nos steps `customer_resumable` (`delegate`/`collect`, três em produção); o piso mora no próprio step (`resume_requires_floor`), ao lado do ref; e nenhuma ficha era dona de APLICAR — decisão do dono: exigir na liberação. `resume_requires` (lista ou `$.config.<chave>`) viaja engine → gateway → pendência; `judgeIdentityFloor` recusa no set-next e no promote; `pending_workflow_get` retém, nas duas portas (âncoras e legado), o token cuja evidência não é `verified`, desta sessão e de até 15 min. Os três fluxos vivos declaram `["otp"]`; a jornada do limite segue verde. A retomada por token já em mãos e o `resume_door` ficaram na PID-13 | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (19) |
| PID-07 | **A escrita no agent-registry age em nome da credencial, e o deploy responde ao campo da tela de Deploy.** A ficha dizia "o deploy não tem portão", e a medição a refutou: sem credencial já dava 401, porque `/v1/pools` casava por prefixo e cobria slots/promote/rollback com `config.resources` — portão por acidente, no campo errado (o devops via a tela e tomava 403). O que estava aberto, medido ao vivo: um token de `tenant_outro` com `x-tenant-id: tenant_demo` GRAVOU um slot do tenant_demo (200), em qualquer router; e o autor era o `x-user-id`, que a UI não manda (33 slots como `system`) e qualquer um forja. Decisão do dono: deploy + tenant em tudo. Com Bearer, tenant e autor saem do TOKEN (header divergente é recusado com `tenant_mismatch`); o deploy tem portão próprio em `skill_flows.operacao`, montado antes de pools; serviço segue agindo pelo header, com o autor que declara. Trocar o `skill_id` no slot deixou de ser anônimo e cross-tenant, que era o que a PID-06 esperava | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (18) |
| PID-03 | **A prova de quem acabou de provar chega ao processo retomado.** Reproduzido ao vivo antes: duas consultas da jornada fizeram OTP `verified` e se uniram ao processo, e o processo ficou com a prova da PRIMEIRA — o merge fazia "canônica vence" campo a campo. A ficha dizia que só o `workflow_resume` transportaria, e a medição mostrou que não basta: no limite o merge roda ANTES da retomada, e a retomada principal dos dois intakes é por `delegate`. Decisão do dono: os dois. A evidência viaja como REGISTRO inteiro e a mais recente vence (`adoptNewerEvidence`), no merge e no `workflow_resume` — este antes de acordar o processo, lendo a sessão do token. O filho de delegate herda a raiz de journey, então quem retoma já enxerga a prova do intake. Provado pela jornada: o processo guarda a prova da última consulta | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (17) |
| PID-02 | **A evidência de identidade só é gravada por quem verifica.** Medido antes, numa sessão sintética: `context_set` e `/api/inject-context` gravaram `core.journey.identity.otp.status = verified`, e o `otp_verify` sem token agiu — e **nada** produzia a evidência de verdade (zero escritores e zero leitores do namespace). Hoje: a reserva (`core.identity.*`, `core.journey.identity.*`) mora em `@plughub/schemas` com gêmeo em `py-contextstore`, e os quatro funis genéricos (`writeContextTag`, os dois `ContextStore.set`, `write_context_tags`) recusam — o de webhook descarta e loga. O único escritor é `writeIdentityEvidence`, chamado por `otp_challenge`/`otp_verify`, que passaram a ser tools ligadas à sessão e gravam o formato do D4 na journey da sessão do token (`status` sempre; a prova só quando `verified`, removida nos outros). Provado pela jornada: `verified` com a sessão que provou, `verified_at` e `source: authoritative` | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (16) |
| PID-01 | **As tools de retomada só agem para uma sessão que o bridge ativou.** Medido antes: pelo `/sse` anônimo, `pending_workflow_get` respondia e `workflow_resume` chegava ao gateway. A ficha dizia *"o mecanismo já existe"*, e a medição o refutou: o `session_token` do `agent_login` não carrega sessão, é auto-serviço, e o caminho bridge → skill-flow-service nunca recebeu nenhum (11 skills usam `workflow_resume`). Decisão do dono: token **ligado à sessão** (`audience` próprio, `session_id`), emitido só pelo mcp-server em `POST /internal/session-token` a pedido do bridge na ativação; o skill-flow-service o injeta, por cima do YAML, só nas tools de `SESSION_BOUND_TOOLS` (`@plughub/schemas`); as tools tiram o tenant dele. Zero YAML alterado. Provado pela jornada no chat: `pending_workflow_get` com `found: true` e o `workflow_resume` do runner de diálogo concluídos depois do deploy, sem recusa | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (15) |
| IDN-06 | **As rotas de identidade e pendência do channel-gateway exigem credencial.** Medido antes: pela porta da UI (5174), sem login, a busca devolvia o cadastro, o resolve respondia e `pending/by-customer` entregava `resume_token` — quatro 200 anônimos. Hoje: um portão, duas portas (`identity_auth.py`). **Serviço** por `X-Service-Token` (mcp-server e mailing-api, que não têm usuário), tenant do pedido, principal nomeado; token vazio no gateway **fecha** a porta. **Usuário** por Bearer só onde a rota declara campo — a busca e o get do cadastro (`contacts.visualizar` ou `agent_assist.atender`), tenant do JWT, divergente é 403; as outras sete são internas e recusam usuário com qualquer grant. Os dois chamadores internos migraram junto, e credencial recusada virou erro nomeado nas tools em vez de *"sem pendência"*. Censo AST das 11 rotas + probe vivo nas duas portas, pela borda da UI e pelos chamadores | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (14) |
| IDN-08 | **O cadastro do operador é durável e carimba `operator` — só por quem atende aquele contato.** A ficha dizia que faltava portador para o carimbo; ao ler a aba, apareceu o defeito que ela escondia: o seletor mandava `kind: "telefone"`, que não é kind, e o resolve **descartava a âncora calado** — cadastrar pelo telefone criava cliente sem âncora, só com o nome (sem identificador, caía num `contact_identifier` que também não é kind). Hoje: rota `POST /identity/operator/register` com Bearer, `agent_assist.atender` **recortado ao pool da sessão** e tenant do JWT; `register_by_operator` grava no cadastro durável com procedência `operator`, recusa âncora inválida nomeando o kind, recusa âncora que já é de OUTRO cliente sem movê-la, não duplica cliente existente (anexa a ele o que falta) e não sobrescreve nome. A aba usa kinds do índice com rótulo i18n e exige identificador. Provado ponta a ponta pelo proxy da UI com o token real do operador | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (13) |
| IDN-14 | **O mesmo celular com e sem código do país é o MESMO cliente — com o país vindo da config do tenant.** A normalização fazia `"+" + dígitos`: `11 99999-0001` virava `+11999990001` e não casava com `+55 11 99999-0001`. **Decisão do dono (2026-09-13): país padrão por tenant**, e recusar valor com letra. Hoje o telefone vira E.164 pelo `phonenumbers` (versão EXATA: a regra de tamanho vem dos metadados, e o resultado é hasheado) com `identity.default_phone_region` do config-api (seed `BR`, editável em Configuração → Plataforma → Identidade, invalidado pelo `config.changed` — inclusive o do default global, que chega como `__global__`); sem `+` e sem país, RECUSA — o país nunca é adivinhado. Aceita-se por tamanho possível, nunca por validade de faixa. Hash de quem já tinha DDI não muda (provado); âncora gravada SEM DDI fica órfã, e não é mensurável (o hash não reverte). `region` é parâmetro obrigatório de `hash_anchor` e o índice e o OTP hasheiam pela MESMA função, com o MESMO resolvedor. O `contact_identifier` `cli_…` do webchat deixou de virar âncora phone | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (12) |
| IDN-13 | **Posse só existe em âncora entregável — na leitura, na escrita e no dado.** A PID-10 fechou o produtor; ficaram **25 CPFs `possessed`** gravados pelo OTP ao CPF, e o dano era real: **1** deles, com a chave quente no índice, tinha pendência `policy: auto` viva com `resume_token` — digitar o CPF entregava o resultado. Três pontas, uma regra (`effective_verification_class`): a LEITURA a aplica em todo ponto que lê a classe guardada (resolve, dono no cadastro, reapontamento da importação, preservação do `attach_anchor`), então o legado deixa também de ganhar o bônus de posse no desempate; a ESCRITA recusa `possessed` em kind não-entregável; e a MIGRAÇÃO do boot rebaixa o cadastro e a chave quente do mesmo cliente (TTL mantido, `verified_at` nulo) — rodou no deploy: 25 linhas, 1 chave. Dois testes usavam CPF `possessed` como fixture genérica de posse — eram o próprio legado. O `probe_otp_gate` passou a julgar o legado (ramo A) e ganhou o ramo E, com três mutações | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (11) |
| IDN-12 | **Resolve ambíguo deixa de devolver um cliente — e com isso deixa de entregar `resume_token` alheio.** O Lookup 1 devolvia `matched_by="ambiguous"` com o `customer_id` do PRIMEIRO candidato, e nenhum dos seis consumidores lia o `matched_by`: o `pending_workflow_get` entregava as pendências e o token de retomada do cliente escolhido ao acaso (se ele fosse `possessed`), duas gravações de pendência do webhook gravavam sob ele, a aba Cliente gravava atributos nele, os skills de intake o carimbavam em `caller.customer_id`. ⚠️ **O conserto foi na ORIGEM, não em seis remendos:** ambíguo volta com `customer_id` vazio, e todo consumidor já tinha o caminho de *"não resolvido"* — o id arbitrário era o valor plausível que os desviava dele. Nos consumidores ficou só a degradação nomeada: `pending_workflow_get` devolve `ambiguous: true`; `customer_resolve` vira `isError: ambiguous` (os skills caem no `on_failure` "segue sem carimbar" em vez de gravar tag vazia); o webhook loga a pendência não indexada; a aba Cliente diz por quê (i18n nos dois idiomas); o importador do mailing loga. 5 testes TS novos (com controle positivo nas duas tools), asserção do id vazio nos testes Python e no probe. Quatro imagens rebuildadas | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (9) |
| IDN-11 | **"Estas âncoras identificam quem?" tem uma resposta, qualquer que seja a temperatura do índice.** Eram duas regras: com hit no Redis, empate entre clientes → `ambiguous`; com o Redis frio, `_pg_resolve` pegava o primeiro estritamente maior — vencia a ORDEM das âncoras. E, com o Redis parcialmente quente, o dono das âncoras frias nem entrava na decisão (phone quente de A vencia CPF frio de B). Hoje é UMA computação de candidatos (Redis quando quente, cadastro quando frio) e vencedor, empate e escrita no índice são decididos uma vez; `_pg_resolve` foi REMOVIDO em vez de corrigido, e as duas regras viraram funções de módulo (`_vencedores`, `_pode_apontar`) para o gate mutar uma de cada vez. Sob ambiguidade nada é escrito no índice. 4 testes que reprovam 3 contra `HEAD` (o controle passa). O `probe_identity_index_owner` ganhou o empate a frio e a mutação da regra de empate; a mutação da IDN-10 passou a atingir só a escrita, porque calar o cadastro agora mataria o caminho frio inteiro. Achado: IDN-12 | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (8) |
| IDN-10 | **O índice Redis deixou de apontar âncora para quem o cadastro não reconhece.** Os dois escritores do índice — a identidade progressiva do caminho quente (anexava ao vencedor as âncoras que eram miss NO REDIS, sob um comentário que prometia não tocar âncora de outro cliente) e a reidratação do caminho `durable` (apontava TODAS as âncoras da chamada para o vencedor) — passaram a perguntar ao cadastro (`_pg_key_owner`) antes; âncora sem linha segue anexada, e com o mesmo dono herda a classe durável em vez de `claimed`. Exposição no dia: 4 âncoras no índice, 0 divergentes — o caminho existia, o dano medido era zero. ⚠️ **O contador que a ficha pedia NÃO foi criado**: chave Redis sem leitor é o *"existe ≠ está pronto"*; o instrumento é o censo Redis × PG do gate. Gate novo `probe_identity_index_owner.sh`. ⚠️ **A primeira versão do ramo de mutação sujou o Redis vivo**: restaurava o valor anterior passando-o pelo shell, o extrator imprimia `None` para JSON nulo, e a string `"None"` virou dono da âncora de CPF de um cliente real por cerca de um minuto — pega pelo próprio censo, removida, e o ramo refeito num processo só com `finally`. Achado: IDN-11 | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (7) |
| IDN-07 | **A âncora tem eixo de procedência, na escrita e na leitura.** A escrita veio com a PID-12; a leitura, aqui: `CustomerRef.provenance`, o campo `provenance` na resposta do `/identity/resolve` e `anchor_provenance(tenant, customer_id, kind, value)` — a pergunta que a PID-10 fará antes de emitir OTP. ⚠️ **Lida SÓ do cadastro durável, nunca copiada no Redis:** `authoritative` só nasce no PG (pela importação), e uma segunda casa para a mesma confiança é exatamente o que a IDN-09 mediu divergindo. ⚠️ **E confere o CLIENTE:** o índice Redis pode apontar a âncora para quem o cadastro não reconhece (IDN-10, achada aqui); sem a conferência, a leitura emprestaria a confiança de um cliente a outro — o furo que a PID-12 fechou na escrita, reaberto na leitura. Divergência loga e devolve `None`. O `probe_identity_provenance` ganhou os casos (Redis quente, cadastro frio, Redis divergente, a pergunta da PID-10) e a terceira mutação, que tira a conferência e faz o caso reprovar | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (6) |
| IDN-09 | **A prova de posse deixou de ser herdada por outro cliente.** O upsert de chave mantinha `possessed` e `verified_at` sempre que a linha já era `possessed` — mesmo quando o `customer_id` mudava. O índice Redis já fazia o certo, então as duas casas discordavam e a durável, que responde quando o Redis esfria, era a errada: o OTP provado por um cliente passava a valer para quem a âncora fosse atribuída depois (`promote_to_durable` reatribui quando o id nativo e as âncoras do contexto divergem). Hoje a posse, como a procedência da PID-12, é fato do par (âncora, CLIENTE). ⚠️ **A exposição passada não é mensurável:** nada guarda o histórico de `customer_id` de uma chave, então não há como saber se alguma das 31 posses vivas foi herdada — o conserto vale daqui para a frente. A fake do teste espelhava a regra errada sob *"mirror the SQL"* e foi corrigida junto; quem prova é o `probe_identity_provenance.sh`, que ganhou o caso (PG e Redis têm de CONCORDAR após a reatribuição) e a mutação que devolve a regra antiga | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (5) |
| IDN-02 | **Nao era trabalho aberto: era a mesma decisao, com vocabulario de junho.** A ficha tinha quatro palavras (*"Gate de identificacao"*); a fonte dela (`delegate-contrato-por-pool-spec.md` §4-5) define o gate como LOGICA DE FLUXO — coletar ancora, repassar a retaguarda por MCP (`identity_verify`), e chamar o `delegate` com ou sem `customer_resumable`. O `adr-identity-door-evidence.md` redecidiu isso inteiro com mecanismo: orquestrador de plataforma, evidencia em `core.journey.identity.*`, `resume_requires` na retomada e piso julgado por `judgeIdentityFloor` no emissor do token — as fatias sao PID-01..12. O unico pedaco nao absorvido era o `identity_verify` por MCP, e a PID-12 acabou de eleger outra porta (importacao com credencial de admin). Fechada por ABSORCAO, com o dono confirmando em 2026-09-12 | 2026-09-12 | `CHANGELOG.md` 2026-09-12 (4)  ADR adr-identity-door-evidence |

---

## `docs/adr/adr-identity-door-evidence.md` — porta de identidade

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| PID-10 | **OTP só contra âncora entregável e autoritativa para o cliente que pede, com recusa explícita.** O `OtpService` aceitava qualquer kind e dizia `sent: true` sempre — fora do modo dev a entrega era `TODO(prod)`, e o default do modo dev era LIGADO; o `skill_limite_entrada_v1` desafiava o CPF e deixava a posse DURÁVEL nele. Hoje o mecanismo recusa sobre si mesmo (`undeliverable_kind`, `delivery_unavailable` — o modo dev é um canal e diz que é: `delivery: dev_log`), o adaptador recusa âncora que `anchor_provenance` não dá como `authoritative` para AQUELE `customer_id`, e a tool devolve a recusa como erro. ⚠️ **Furo irmão fechado junto:** o verify anexava a posse a qualquer `customer_id` informado — o desafio agora é amarrado ao cliente, sem o que o portão da procedência seria contornável na conferência. O fluxo do limite pede o celular cadastrado e cai no SAC na recusa, com uma frase só (anti-oráculo). Default do modo dev passou a desligado; o compose do demo liga. Legado de 25 CPFs `possessed` e o corte de DDI na normalização viraram IDN-13 e IDN-14 | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (10) |
| PID-12 | **`authoritative` tem uma porta, e ela exige credencial.** Decisão do dono (2026-09-12): a importação com credencial de admin é a única que carimba procedência autoritativa. Entregue: rota `POST …/identity/import` com Bearer + `contacts.importar_cadastro` (`read_write`, campo novo, preset só de `admin`; papel não é portão) e o tenant vindo do JWT, nunca do corpo; `import_customers` idempotente por `(system, external_id)`, que **recusa e nomeia** a linha cuja âncora já é de outro cliente em vez de fundir; coluna `provenance` (IDN-07, metade escrita). ⚠️ **A trava mora no ÍNDICE, não na rota:** as rotas irmãs seguem sem credencial (IDN-06), então todo escritor que não é a importação passa por `_writer_provenance`, que recusa `authoritative` alto — e a procedência **zera quando a âncora muda de cliente**, senão quem não tem credencial anexaria um telefone importado ao próprio cadastro e herdaria a confiança. Gate `probe_identity_provenance.sh` (censo AST · portão com controle positivo · semântica no Postgres · mutação do upsert). Backfill do campo em `admin@`/`probe@`. Achado: IDN-09 | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (4) |

---

## `docs/adr/adr-human-approval-workflow-step.md` — aprovação humana

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| APR-11 | **Pela porta externa de resume, um sistema decide o que é dele — aprovar OU recusar — e só isso.** Decisão do dono: num `suspend reason: approval` o aprovador é um SISTEMA (a operadora) e o token de uso único é a credencial. Implementar a decisão mediu três defeitos na MESMA porta, os três **vermelhos ao vivo**: a porta externa descartava `decision` e o bridge assume `input` — a operadora RECUSOU a portabilidade e o processo seguiu APROVADO; a porta externa não aplicava a regra da AUT-46 — a promoção de deploy, 401 na rota interna, foi APROVADA sem credencial (`efetuar_promocao`); e o carimbo `verification_class` só existia com aprovador, por `setdefault` — o resume anônimo passava sem carimbo pelo portão `== claimed` do fluxo, e o chamador podia declarar `possessed`. Agora a porta externa aceita `approved`/`rejected` só onde o registro do token diz `suspend_reason: approval` (e ali sem decisão é 422, nunca aprovação por default), recusa com 401 a tarefa que declara capacidade, e o `handle_resume` carimba a confiança do autor do servidor, em todo resume. Casa única `resume_authority.py`. O exercício da portabilidade passou a aprovar pela porta externa. Gate `probe_external_resume_authority.sh`; o `probe_resume_requirement` aceita a recusa anônima pela capacidade | 2026-09-14 | `CHANGELOG.md` § 2026-09-14 (9) |
| APR-08 | v1 da aprovação humana — A1–A5, validadas E2E | 2026-07-17 | `CHANGELOG.md:16316` |

---

## `docs/adr/adr-work-item-requeue-and-agent-affinity.md` — pull direcionado e wrap-up

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| PUL-02 | **A Camada F fechou o arco de detach validada A MÃO, e a ficha registrava desde julho: *"arco completo sem gate versionado é lembrança, não verificação"*.** Hoje `gate_camada_f.sh` afere as proposições que sobrevivem no dado durável — **F1** (a classificação do wrap-up pousa no segmento da ORIGEM: 194 segmentos de origem com `issue_status` contra **0** nos 85 de `-int`) e **F2** (sessão interna não conta como contato: 1118 × 1200, com **0** linhas internas na lista de contatos e o controle positivo de 10 aparecendo em `scope=all`). ⚠️ **Não re-encena a coreografia de julho** (ambiente montado, `acw_timeout_hours: 0.03`) — afere o que regride em SILÊNCIO, e a F2 é a que mais custa perder porque `handle_time_ms` é NULL nas internas: a contaminação não apareceria na média, e sim na CONTAGEM. ⚠️ **A triagem achou um instrumento VERMELHO havia semanas**: `smoke_detached_hook.sh` estava como `?` não-triado (logo, nenhum runner o colhia) e falhava com 422 — um guard posterior recusa `dispatch: detached` + `side: agent` em pool sem fila interna, e o hook omitia `side`, cujo default é `agent`. Defeito do INSTRUMENTO, não do produto; hoje declara `side: customer` (o caso NPS/survey exercita o mesmo despacho) e volta a 2/2. `smoke_internal_work_queue.sh` saiu de `?` verde na primeira execução. Bateria de mutação: 2, ambas vermelhas, e verde de volta após restaurar | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (14) |
| PUL-04 | **A tela de pendências abria em LISTA, e o filtro de estado duplicava um contador que já existia.** Pedido do dono depois da ORQ-10. O argumento não foi simetria, foi AMBIGUIDADE: a tela estava honestamente vazia (0 no ledger; 85 wrap-ups no total, o último em 2026-09-07) e dizia isso com o mesmo *"nenhum item"* que a aba Processos exibia com 51 processos de pé — *um zero em contador afirma, uma lista vazia só sugere*. Hoje abre em consolidado, o cartão É a porta da sua lista, o eixo primário continua sendo o AGENTE (a pendência é author-bound) e `orphaned` aparece mesmo em zero. ⚠️ **Duas rodadas do dono acharam o que eu não vi:** o recorte era `useState` e morria ao navegar para outra rota (foi para a URL); e o "voltar" da tela de sessão é da Analytics — hoje o link leva `?from=`, o que conserta os QUATRO módulos que linkam para lá, com guarda contra destino externo (inclusive `/\` , que o browser normaliza para `//`) | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (12) |
| PUL-03 | Arco A–F: posse conferida no submit, devolução por queda, resume terminal-uma-vez, Console lendo o 409 | 2026-08-04 | `CHANGELOG.md` § Fase F (D7) |
| PUL-05 | **O Console reencontra o item EM POSSE pelo LEDGER, não só pela chave de 300 s.** A reconexão lia apenas `pool:pending_assignment:{pool}` (TTL 300 s, uma por pool): item em posse que não reabrisse em 5 min sumia da tela do dono enquanto o A5 impedia qualquer outro de pegá-lo (`0596f383`, medido). Agora o mcp-server lê o ledger `work_task` na conexão e reentrega o `conversation.assigned` do que é MEU (`decideLedgerRehydration`, pura: posse pela classificação lease→registro, filtro por `claimed_by`, descarta sessão fechada e token cancelado — os mortos da PUL-06 —, `assigned_at` = claim, todo descarte com motivo). 12 testes, suíte 305/305. Ao vivo as duas metades: `59485f70` REENTREGUE após reinício do mcp-server, `5120fe90`/`4841c60d` descartados. ⚠️ Um primeiro teste com Ctrl+Shift+R recuperou o formulário pela RESERVA DE QUEDA, não por esta mudança — só os logs separaram | 2026-09-11 | `CHANGELOG.md` § 2026-09-11 (2) |
| PUL-07 | **Encerrar TAREFA DE FORMULÁRIO pendente devolve à fila; nunca fecha.** Os wrap-ups `5120fe90`/`4841c60d` morreram assim: a tela abriu vazia (CNS-24), o Console não reconheceu a tarefa, mostrou "Encerrar", e o fechamento apagou o `resume_token` (RET-03) — tabulação perdida, irrecuperável. ⚠️ O guarda NÃO podia morar na tela: `isFormFillSnapshot` depende do snapshot renderizado, que é o que falha. Ele está em `POST /api/agent_done` e decide pelo LEDGER (`decideFormTaskClose`, pura, 5 ramos): sem item fecha; item morto fecha; item vivo meu vira `return_to_queue` (mesmo `releaseTask` do botão, resposta 200 com `returned_to_queue`); item de outro ou sem dono é 409. Token não conferido conta como VIVO — devolver por engano é reversível, fechar não. 8 testes, suíte 313/313. Ao vivo com a condição REPRODUZIDA (tag removida do ctx): Close reapareceu, não fechou, item voltou reservado com token vivo, e depois de restaurar a tag o formulário foi enviado (`task_done`) | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (1) |
| PUL-06 | **A pendência não sobrevive ao contato: o fechamento encerra o ITEM, não só o token.** `cancel_pending_resumes` cancelava o `resume_token` e deixava ledger, posse e pipeline suspenso por ~25 h — `/monitor/work-items` mostrava `5120fe90`/`4841c60d` como "Being filled in" (trabalho morto) e o *Close* do supervisor respondia **404**, porque ele encerra RETOMANDO pelo token cancelado. Agora `_close_work_task_on_session_close` roda na MESMA casa, antes do hash: expira no árbitro com causa própria `acw_session_closed` (nem prazo, nem supervisor) e só então apaga o ledger. ⚠️ **Sem confirmação do árbitro o ledger FICA** — é dele que o scanner de prazo reidrata o endereço. ⚠️ **A hipótese de que o prazo nunca dispararia foi MEDIDA e REFUTADA** (2026-09-12): o scanner reidrata do registro durável e encerrou os dois no prazo com `acw_expired`; o dano real era a JANELA, não a evaporação. 7 testes (o que decide é o de FALHA DO ÁRBITRO), RET-03 intacto, suíte 778/778 sobre a imagem. Ao vivo, publicando o `session.closed` do bridge: ledger apagado, posse liberada, `work_task_expire reason=acw_session_closed` | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (7) |

---

## `docs/arcos/customer-surveys.md` — módulo de pesquisas

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| SUR-05 | **O S2 fica ABSORVIDO: o dono do runner e o Dialog Primitive, e survey e consumidor.** A ficha perguntava se o runner generico voltava a ter dono proprio. No cruzamento do plano contra o codigo (`TODO.md` §item 17) o S2 ja estava classificado como *feito-por-substituicao*: o que o spec pedia como runner dedicado virou `dialog-api` (store canonico) + `DialogForm` (script linear) + `form_get` + `skill_dialog_runner_v1`. Dar dono proprio reconstruiria um runner de pesquisa ao lado do generico — o oposto do que a casa fixou quando o mesmo primitivo passou a servir survey E OTP (*"survey e o 1o consumidor, nao o dono"*), com as quatro costuras separadas (conteudo · controle · canal · segredo). ⚠️ *Absorvido* nao e *completo*: o residuo vivo do plano de survey continua na SUR-02 (nenhum produtor emite CES/PMF/FCR). Confirmado pelo dono em 2026-09-12 | 2026-09-12 | `TODO.md` § item 17 · `CLAUDE.md` § Dialog Primitive |

---

## `docs/arcos/arc7-auth.md` — ABAC e escopo

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| AUT-52 | **O probe contava GRUPOS; o produto recortava LINHAS, e estava certo o tempo todo.** Montado o mesmo cenário do `probe_report_row_scope` (sonda escopada ao pool que ele escolhe), as duas grandezas lado a lado: `evaluations/summary` grupos **13 = 13** mas linhas **82 → 54**; `quality` grupos **12 = 12** mas linhas **38 → 14**, com o controle movendo 138 → 27. O recorte descartava um TERÇO das linhas sem mexer no número de grupos, porque um pool sozinho contém ao menos uma linha de cada campanha — ramo honesto e falseável, evidência da proposição ERRADA (D14.1, com o sinal invertido). E a escolha do pool com MAIS sessões, que existe para não cair em `SEM AMOSTRA`, é justamente a que maximiza a colisão: o cuidado com um modo de falha criou o outro. ⚠️ **Eram QUATRO casos, não dois** — `agent-events/summary` e `/categories` estavam verdes pelo mesmo acaso, do lado oposto; hoje movem 66 → 41. Os quatro passaram a somar a contagem de linha DENTRO do grupo (`total_evaluated` · `n` · `count` · `event_count`), com `add // 0` porque `add` de lista vazia é `null` e `null` compara igual dos dois lados. **Mecanismo contra a quinta cópia:** o probe reprova qualquer caso julgado por `(.data|length)` — comentário não impede cópia, e foi copiando a vizinha que as quatro entraram. Mutação: M1 (voltar a contar grupos) e M2 (métrica cega ao escopo) reprovam | 2026-09-12 | `CHANGELOG.md` 2026-09-12 (3) |
| AUT-45 | **A sétima cópia do verificador era nossa, e convivia com o canônico DENTRO DO MESMO ARQUIVO.** `pool_auth.py` já importava `plughub_authz` para o eixo de ESCOPO (`abac_can`, `resolve_scope`) e mantinha `import jwt` com dois `jwt.decode` próprios para o eixo de IDENTIDADE — *um censo de um eixo não prova nada sobre o vizinho*, desta vez dentro de um arquivo só: o import fazia a migração parecer feita. Os dois sites (header em `optional_pool_principal`; `?token=` em `accessible_pools_from_token`, que é o caminho do `sse_pool_principal`, porque `EventSource` não manda header) passaram a `verify_user_jwt`, e o `import jwt` saiu. **Segredo ausente não mudou de postura**: os dois já o barram ANTES, cada um com a sua decisão (o do header degrada aberto e loga; o do SSE recusa). O que mudou foi a recusa — `Token expired` e `Invalid token` viraram **um** `invalid or expired token`, o mesmo da evaluation-api, com o log separando as causas; medido antes de trocar que **nenhum** consumidor lê essas strings (zero no `platform-ui`). C1 de **2 → 1**, 4 ramos verdes; **766 testes** da analytics-api passando na IMAGEM. Deixou ficha: AUT-51 | 2026-09-12 | `CHANGELOG.md` 2026-09-12 (2) § 1 |
| AUT-31 | **A ficha estava VELHA — o gate já tinha sido consertado, e o conserto já estava escrito.** `gate_sla_segment_target.sh` roda VERDE nos três veredictos (`pool_a` 1/1 · `pool_b` 0/1, com a violação da SEGUNDA fila aparecendo · `pool_c` com `sla_unstamped=1` fora do denominador e aderência AUSENTE), e o `/reports/pools/queue` devolve `series`/`by_pool` cheios para a janela sintética — o oposto do que a ficha afirmava. Causa e conserto em **2026-09-07 (11) § 5**: as fixtures `pool_a/b/c` só existem no ClickHouse, `mk_unrestricted_principal.sh` enumerava o registry, e o recorte por linha da AUT-01 descartava as esperas — *o gate acusava o RELATÓRIO por um defeito do escopo do CHAMADOR*, e a ficha nasceu do lado errado dessa distinção. ⚠️ **Rodado do Git Bash ele volta a imprimir 🔴 por falta de `jq`** — gate que não conseguiu rodar e gate que reprovou têm a mesma cor; este se roda de dentro do WSL | 2026-09-12 | `CHANGELOG.md` 2026-09-07 (11) § 5 |
| AUT-40 | **`write_only` saiu do modelo de ABAC, em DOIS passos — e a ordem era a ficha inteira.** O grau era o ÚNICO ponto em que as duas ordens da casa discordavam: o `ACCESS_RANK` do `py-authz` o COLAPSAVA com `read_only` em 1, e a lista INDEXADA da `permissions.ts` o punha estritamente acima — a *"divergência 2"* que o cabeçalho do pacote dava por fechada em Python enquanto seguia viva na UI. Fechada por **REMOÇÃO do elemento que a produzia**, não por patch: com três degraus a ordem é TOTAL dos dois lados. ⚠️ **Passo 1 antes do 2, e não é estilo:** `abac_can` levanta `ValueError` para `min_access` desconhecido, então tirar do rank com os dois call sites ainda pedindo o literal daria **500 em vez de negação**. A equivalência foi MEDIDA, não presumida: **0** domínios do catálogo vivo ofereciam `write_only`, `validate_module_config` recusa access fora do domínio com 422 (logo grant algum podia sê-lo), e os 6 portadores de `approvals.decide` são todos `read_write` — os dois mínimos selecionam o MESMO conjunto. Para o call site DINÂMICO (`main.py`, par vindo de `session.resume_abac`): **0 skills** o declaram, então o único par que chega é o retrocompat `("approvals","decide")`. ⚠️ **O censo da ficha estava curto**: eram **10** tabelas de rank, não 7 — 3 helpers de `infra/test/` não estavam na lista, e 2 contadas como `infra/test/` eram `infra/scripts/`. Mais 3 suítes que pinavam o literal, todas reescritas para medir a proposição SOBREVIVENTE. Prova viva: rank `{none,read_only,read_write}` na IMAGEM reconstruída, admin ACEITO pelo call site migrado (o controle positivo) e `min_access='write_only'` levantando `ValueError` | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (17) |
| AUT-46 | **A ficha perguntava quem deve ter `approvals.decide`; a medição respondeu que o campo nunca foi a barreira — e achou a que era.** Medido ao vivo, numa tarefa real de promoção de deploy criada pelo caminho oficial: `operator@` (que **não** alcança `aprovacao_deploy`) DECLARANDO `pool_id` levou **403**; **omitindo** o campo levou **200** e reprovou a promoção — e a linha durável gravou `verification_class: possessed`, atribuindo a decisão a um aprovador *verificado* que o próprio sistema recusaria se o corpo tivesse dito a verdade. Sem header nenhum: **200** também. ⚠️ **O `if body.pool_id` era o defeito**: um portão que só enforça quando o chamador pede para ser enforçado não é portão, e quem concede acredita ter negado. Duas correções, a mesma regra (*quem pergunta não escolhe a pergunta*): **(a)** tarefa que DECLARA capacidade exige credencial (**401** sem ela); **(b)** o POOL vem do SERVIDOR (`resume_task_pool`, irmão do `resume_required_abac`, que já dizia no docstring *"NUNCA de valor client-asserted"*) — nenhuma tag servia, porque `core.pool.id` e `session:{sid}:meta` nomeiam o pool do WORKFLOW, não o pool PULL onde a tarefa foi parqueada. **Ramo legado morreu CONTADO:** em 30 dias, **85** decisões de aprovação com credencial × **1** sem — e essa 1 era a própria sonda; do outro lado, **322 de 323** resumes anônimos não decidem nada, e é para eles que a porta fica aberta (ramo P2 do gate). A pergunta da ficha ficou respondida por consequência: o `operator` **mantém** o campo — ele é o decisor previsto no Console, e o que separa aprovação de negócio da promoção de deploy é o escopo de pool. Quatro casas de prosa que diziam *"o supervisor não tem o campo"* foram corrigidas | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (18) |
| AUT-47 | **O estado da sessão passou a pertencer ao pool dela — e o motivo pelo qual o token não vazava era MASCARAMENTO POR PAPEL, não escopo.** A ficha mandava medir isso primeiro, e a medição virou o achado: com tarefa de promoção de deploy reivindicada, `admin@` (alcança o pool) e **`supervisor@` (não alcança)** receberam **200 com o `core.workflow.delegate_resume_token` no corpo** — a CREDENCIAL da tarefa —, enquanto `operator@` recebeu 200 sem ele. O único filtro em jogo era papel; eixo de pool não existia, e a analytics-api recorta o conteúdo do MESMO contato por pool desde 2026-08-30 (*duas portas, uma trancada*). Fechado com a **mesma união** da analytics (`entrou por pool meu OU um pool meu atendeu`), lida das duas casas do Redis que a guardam: `session:{sid}:meta.pool_id` (quem ATENDE — o bridge o reescreve na alocação) e `{t}:ctx:{sid}.core.pool.id` (por onde ENTROU). ⚠️ **Duas ausências separadas:** sessão com estado e sem pool resolvível → **403** nomeado; sessão sem `meta` nem `:ai` → **404** `session_expired_or_unknown`, porque *expirou* chegando como *você não pode* manda procurar permissão onde falta dado. ⚠️ **Resíduo fechado de carona:** `accessible_pools` traz `pool_id` CRU e pool_id **não é único entre tenants** — o tenant do token é comparado ANTES do pool, e esse ramo está declarado NÃO-EXERCÍVEL (um tenant na instalação) em vez de ganhar verde vazio. Gate `gate_session_state_pool_scope.sh` com 5 ramos | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (19) |
| AUT-48 | **A recusa do estado da sessão aparece na tela — e a premissa da ficha (*"não dá para conferir em navegador"*) era FALSA.** A metade visível da AUT-47: `SessionStateDeniedNotice`, um banner acima da coluna central com **quatro textos distintos**, porque as quatro respostas do servidor não têm a mesma cara — `pool_not_accessible` (peça escopo) · `session_scope_undeterminable` (ninguém tem o que conceder) · `tenant_mismatch` (não há caminho) · e **404 `session_expired_or_unknown`, que NÃO é recusa**: colapsá-lo nos 403 mandaria o operador pedir permissão para ver uma sessão que não existe mais. O código cru do servidor fica visível de propósito — é o que a pessoa repassa ao suporte. ⚠️ **Conferido a olho, nos dois idiomas**, com o Console real: login por clique/`Tab` funciona (a ficha o dava por impossível porque `form_input` não dispara os eventos do React — clique e digitação de verdade disparam), e a recusa foi forçada apagando `meta`+`ai` da sessão, sem tocar em provisionamento. O eixo de escopo foi exercido à parte: tirar `aprovacao_deploy` do `accessible_pools` do admin pela API oficial fez o pool sumir do seletor do Console (7→6), e a restauração foi conferida idêntica (41 pools) | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (20) |
| AUT-49 | **O poll de 3 s não existia, e o write-back do operador não voltava para a tela.** Medido: `useSupervisorState` não tem `setInterval` nenhum (busca no mount + 3 retries curtos + evento de WS), e `POST /api/inject-context` grava no Redis **sem publicar evento** — o único produtor de `supervisor_state.updated` é o `ai-gateway`, depois de um turno de LLM. Três comentários em dois arquivos prometiam o poll; o `handleTagSaved` era um **no-op** apoiado nele. ⚠️ **Estava METADE consertado:** o vínculo de cliente já chamava `onLinked`, e a prop dele dizia a razão certa (*"o hook só refetcha em evento WS"*) — a correção foi aplicada num lugar e a prosa dos outros manteve a história antiga. Saída **(b)** da ficha: event-driven continua e o call site avisa — `onCustomerLinked` virou **`onStateStale`** (o nome descrevia o primeiro caso, não o fato) e passou a chegar também à aba Contexto. Sem intervalo: seria uma chamada por sessão aberta por operador a cada 3 s para cobrir dois cliques. **Provado ao vivo, no Console real:** gravei `service.aut49_prova` e o painel foi de **13 → 14 campos com a tag renderizada, sem remontar**. ⚠️ E a mesma prova achou a AUT-50: `agent.*` é escrevível e ILEGÍVEL — 200, 14 no Redis, 13 servidos | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (21) |
| AUT-50 | **`agent.*` era ESCREVÍVEL e ILEGÍVEL, e a ficha pedia para alinhar duas listas que NÃO devem ser iguais.** A assimetria da leitura é deliberada — a decisão P7 de `context-masking-rules.md` manda descartar `agent.*` **antes** do matching de regras, para que um admin não crie `"agent.* × operator → plain"` e exponha nota privada de outra pessoa. O que não era deliberado é que a feature **nunca existiu**: a taxonomia promete `visibility: ["<participant_id>"]` e diz que *"o `supervisor_state` filtra por `participant_id` do JWT antes de entregar"*, e **nenhuma das duas metades tem produtor** — a escrita gravava `visibility: "agents_only"` FIXO e resolvedor por participante não existe em caminho nenhum. Logo a nota não voltava **nem para quem a escreveu**: 200, entra no Redis, some da tela. **População medida: 0 tags `agent.*` nos hashes vivos e 0 no stream durável** — não é "pouco usado", **não dava para usar**. Decisão: fechar a **AFORDÂNCIA**, não construir a feature (construir reabriria a superfície que a P7 fechou de propósito, por uma demanda medida em zero). `POST /api/inject-context` recusa com **422 `namespace_sem_leitor`** — nomeado, porque um 403 genérico manda pedir permissão para uma porta que não existe — e o `ManualTagForm` parou de oferecê-lo nas **duas** listas. Três comentários que prometiam o mecanismo ausente (server.ts, `context-masking.ts`, e a "Regra" da taxonomia) foram corrigidos no lugar, marcados, em vez de apagados. ⚠️ **Achado de carona, e ele era um instrumento mentindo:** o `probe_context_withheld.sh` (não triado) publicava *"o campo sumiu em silêncio, que é exatamente o defeito da V1"* — e a causa real era o **403 de escopo da AUT-47**, porque ele compara `operator@` e `admin@` numa sessão qualquer e o operator alcança 3 pools, nenhum com sessão viva. Vermelho fiel ao ramo, adjacente à proposição. Hoje o 403 sai INCONCLUSIVO nomeando a causa, a listagem mostra sessão × pool, e ele foi triado de `?` para ASSISTIDO. Controle positivo rodado concedendo o pool ao `operator@` pela API oficial e restaurando: **6 ramos verdes** — a V1 está intacta | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (22) |
| AUT-43 | **Os dois instrumentos quebrados foram consertados — e os dois números da ficha estavam errados, cada um para um lado.** **(1) O aviso da AUT-03 nunca chegou a log nenhum**, e não era só o `analytics-api`: o `CMD` destes serviços é `uvicorn …:app`, que configura apenas os loggers `uvicorn*`, então o root fica em WARNING e **todo `logger.info` do repositório é descartado** — assimétrico e por isso invisível, porque `logger.warning` continua saindo pelo handler de último recurso. O `TODO.md` já catalogava isso desde 2026-08-07 com o conserto prescrito, e listava **seis** serviços: medido, são **sete**, e a lista errava dos dois lados — 2 falsos positivos (`scheduler-api`/`mailing-api` já configuravam, por outro mecanismo) e 3 ausentes (`workflow-api`, `quality-ingest`, `quality-export`, dados como sadios *por regra* e sem nenhuma linha de configuração no pacote). ⚠️ **`basicConfig` recusado de propósito** — ele liga INFO na RAIZ e traria asyncpg/aiokafka/httpx/clickhouse junto, que é o "aumento real de volume" que o próprio TODO temia; com handler no namespace `plughub`, o volume medido em 3 min nos sete foi de **1** linha nova. Provado ao vivo com controle negativo: chamador de escopo vazio produz a linha nomeando o `sub`; chamador com escopo, **zero**. **(2) O guarda de pool órfão estava anestesiado por uma fixture** — `probe@` (senha no repositório) carregava 43 pools, e desativar `admin@`, único vigia de 36, avisaria sobre **0**; sem ela, **36**. ⚠️ A premissa da ficha (*"36 de 41 pools sem vigia"*) **venceu**: o `admin@` foi provisionado desde então, e hoje órfãos = 0 mesmo excluindo fixtures — o que sobrevive é a anestesia. O conserto **não** foi ensinar o produto a reconhecer conta de teste (não é eixo do domínio, e a fixture faz login como qualquer pessoa): o escopo total virou **EFÊMERO** — `mk_unrestricted_principal.sh --revogar`, chamado no `trap EXIT` dos três gates que o consomem, que agora também o **garantem** (eram INCONCLUSIVOS até um humano rodar o helper). Efeito colateral medido: o mesmo fixture estava **CONTESTADO** — `probe_ts_scope_resolvers.sh` exige `[]` no mesmo `probe@` e saía INCONCLUSIVO desde que outro gate o encheu; **um fixture, duas exigências opostas, e quem vencia era a ORDEM de execução**. Hoje: VERDE. Dois gates novos, ambos falseáveis por mutação | 2026-09-10 | `CHANGELOG.md` § 2026-09-10 (1) |
| AUT-44 | **Quem cria um usuário passou a administrá-lo — contratando PARA UM TIME.** Resíduo nomeado da AUT-39, que fez do organograma o eixo de administração e não tocou na criação. Medido ao vivo com `supervisor@` (tem `config.users`, supervisiona zero grupos): cria → **201**; e no instante seguinte **403 para editar**, **403 até para VER a ficha**, e o criado **ausente da lista dele** (1 usuário visível: ele mesmo). ⚠️ **É pior do que a ficha dizia** — ela falava de editar; a conta some inteira da vista de quem a emitiu, sobrando ao criador só a senha que ele próprio digitou. Criar e não administrar não é contratar: é produzir órfão. Das três saídas, venceu **(a) declarar o grupo**: `group_ids` entra na certidão de nascimento, **obrigatório** para quem administra por delegação e opcional para `admin` (que administra todos por definição — exigir dele trancaria o dono num tenant com zero grupos, que é como todo tenant começa). **(b) proveniência** foi recusada por ser um SEGUNDO eixo ao lado do organograma recém-escolhido, onde o delegado montaria um time que ninguém acima enxerga como time; **(c) delegado não cria** contraria a ordem G1→G2→G3 do ADR de delegação. ⚠️ Obrigatório e não *"opcional com aviso"*: a omissão seria o chamador desligando o portão, a forma exata do `if body.pool_id` da AUT-46. As **duas** portas seguem a regra (`POST /users` e `/users/from-template/{id}`, a rota que existe para a contratação delegada) e as duas escritas — linha e vínculo — vão na **mesma transação**, porque são um fato só. Medido de carona: o delegado **não se declara supervisor** (403, exige `config.permissions`), então organograma é do admin e contratação é do supervisor — e a recusa nomeia a tela e o pedido a fazer. Gate `gate_hire_into_your_own_team.sh` (P1 confere QUATRO coisas: criar não basta) · 95 testes no auth-api, 4 novos | 2026-09-10 | `CHANGELOG.md` § 2026-09-10 (2) |
| AUT-41 | **O módulo `audit` passou a EXISTIR no catálogo — o item do DPO era invisível e inconcedível.** `infra/modules.yaml` e o `auth.module_registry` vivo traziam 11 módulos e `audit` em nenhum, enquanto `Sidebar.tsx:170` o gateava e a analytics-api o enforçava (`_check_audit_access`). Sob grant-first: **0 portadores**, item oculto para todos, e a tela de Access sem como concedê-lo (o `ModulePermissionForm` renderiza o catálogo) — só um `PUT` à mão o alcançava, e nem isso: `validate_module_config` recusava com *"Módulo 'audit' não encontrado no registro"*. Declarados **2 campos** (`sessions`, `mcp_calls`), não os 5 do `CLAUDE.md`: os outros três são deferred (`AUD-01..04`) e campo sem portão vivo é promessa sem mecanismo. **Sem `role_defaults` por decisão do dono** — auditoria é ortogonal a papel, ninguém nasce com ela, e o mecanismo já garante (`build_module_config` pula campo sem preset). `domain: [none, read_only]` (o portão é de leitura) e `scopable: false` (o gate não passa `scope_id`, e o ramo 3 do `abac_can` faria um escopo declarado *parecer* restringir). Provado ponta a ponta com contraprova: concessão `read_only` → **200**; `read_write`, fora do domínio → **422** nomeando os valores aceitos; estado do usuário-cobaia restaurado idêntico | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (2) |
| AUT-03 | **`accessible_pools: []` passou a significar NENHUM pool.** `LEGACY_EMPTY_MEANS_UNRESTRICTED = False`. O caminho vazio **não ficou mudo**: virou `logger.info` que nomeia a origem e diz que é config válida, porque *"não vejo nada"* é o sintoma que chega e sem a linha ele é indistinguível de tela quebrada. **Provado ao vivo, com controle positivo e negativo no mesmo teste:** `admin@` (36 pools explícitos) → 100 linhas; `probe@` (0 pools) → 0 linhas. Antes da virada os dois viam as mesmas 100 | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-10 | **A tela passou a dizer que o vazio é ESCOPO, e o criador do pool recebe a oferta.** *(a)* `EmptyScopeNotice` — componente único cujo texto diverge no ponto que importa: quem tem `config.permissions` recebe link para Acesso, quem não tem recebe *"peça a quem administra"* (mandá-lo a uma tela onde só levaria 403 seria pior que silêncio). Ligado no `PoolDomainSelect` — onde o combo de domínio vazio é uma **mentira**, oferece "Todos" e o backend devolve nada — e no Monitor › Pools, onde *"sem escopo"* e *"sem pool no tenant"* produziam a mesma tela vazia e só a primeira tem conserto de quem olha. *(b)* Oferta ao criar pool, **gateada por `config.permissions`**: criar exige `config.resources`, conceder exige outro módulo, e sem o gate a oferta seria uma segunda porta para a mesma decisão. Inclui `reauthorize()` — o escopo vive no TOKEN, e sem renovar o sintoma seria *"me atribuí e continua vazio"*. Falha na concessão **não desfaz o pool**. Medido que o servidor recusa (403, com controle positivo 200 no `name`) e o **guardado**: `probe_config_permissions_split.sh` ganhou o **S4c** — ele cobria `roles` e `unrestricted` e não cobria `accessible_pools`, justo o campo que esta tela passou a enviar. Falseável por mutação | 2026-08-31 | `CHANGELOG.md` § afordância de escopo |
| AUT-27 | **As 14 suítes Python voltaram ao verde: 2 629 testes, zero vermelhos.** Eram **10** falhas em 3 serviços, todas com uma raiz só — instrumentos escritos quando `[]` significava "todos" e quando o claim `unrestricted` existia. Cauda do arco de 2026-08-31, **não regressão**. Reescritos como **TESTEMUNHAS, nunca apagados**: apagar deixaria o caminho livre para a porta larga voltar sem nada acusar. Dois merecem registro: **(a)** `test_ramo_LEGADO_pools_vazio_ainda_libera_e_e_CONTADO` trazia no docstring *"é este teste que vai ficar vermelho para avisar, em vez de o escopo vazar mudo"* — **o instrumento funcionou exatamente como prometido**, e o que faltava era escrever a versão pós-virada; **(b)** os dois do gate de credencial do `/stream` mediam uma proposição ADJACENTE à que os nomeia — aferem se o `?token=` é LIDO e falhavam por ESCOPO, então o vermelho fiel teria publicado *"o `?token=` parou de ser aceito"*, que é falso. Hoje o veredicto é sobre o **401** (única resposta que significa "credencial não foi lida"), com teto de 5xx para não passar por acidente. Entrou junto `test_ramo_LEGADO_esta_DESLIGADO`, que guarda o interruptor: religá-lo devolve o tenant inteiro a todo token de lista vazia, **sem erro e sem log de erro** | 2026-08-31 | `CHANGELOG.md` § suítes Python |
| AUT-01 | **Os agregados de `/reports` passaram a recortar LINHA por pool — 35 rotas escopadas, 2 isentas declaradas, 2 dívidas com gatilho.** Vazamento medido ao vivo com controle positivo na MESMA rodada: `admin@` (36 pools) e um chamador escopado a UM pool liam números IDÊNTICOS em `/usage` (20), `/evaluations` (2), `/evaluations/summary` (1), `/evaluations/quality` (2), `/agent-events/summary` (3), `/agent-events/categories` (3) e `/customers/{id}/360` (21 contatos), enquanto `/sessions` movia 386→323. **A decisão é por FAMÍLIA DE FATO, nunca por rota** — a pergunta não é *"qual coluna é o pool?"* (13 escolhas, ao menos uma errada em silêncio) e sim *"a linha carrega o pool como fato PRÓPRIO ou o pool é fato de outra coisa que ela referencia?"*, que tem três respostas e um predicado cada: **F-A** pool-nativo (`_apply_pool_scope`) · **F-B** derivado-de-sessão (`_session_derived_scope_clause`, delegando ao predicado único da F1b) · **F-C** indecidível (dívida DECLARADA). Duas correções de rumo que a medição impôs, ambas registradas no código: a F-C começou **recusando** (403) e foi revertida no mesmo dia — o admin também é chamador escopado, então a recusa defendia ZERO linha causando dano real (a D14.1 ao contrário); e `meta.unattributable` do `/usage` media *"o que o meu recorte deixou de fora"* sob um nome que diz *"o que não é de ninguém"* — duas proposições, e a errada ainda publicava o tamanho da população alheia. Achado colateral: **`/reports/quality` respondia 500 desde sempre** — `query_quality_report` **e** `_fetch_quality` estavam DUPLICADOS, a T11 sombreava o relatório de sentimento, e corrigir só o par de fora trocava o 500 por um `503 data_unavailable` que parecia degradação graciosa | 2026-08-31 | `CHANGELOG.md` § recorte de linha nos agregados |
| AUT-15 | **O campo `unrestricted` saiu da persistência e da API — e a remoção NÃO era mecânica.** Três coisas que só apareceram medindo: *(a)* o campo já **mentia** — `TokenUserInfo` é construído sem ele, então o corpo do login publicava `unrestricted: false` para `admin@plughub.local`, cuja linha no banco diz `t`; um valor constante vestido de dado. *(b)* Ele participava de um predicado de SEGURANÇA (`_is_privileged`), e tirar um disjunto enfraquece — a razão para tirar é que o disjunto protegia um FANTASMA (desde a AUT-12/13 o campo não é emitido nem lido), e a população foi contada ANTES: 8 usuários, 2 com `true`, **1** privilegiado só por ela, `probe@plughub.local`, fixture de portão. *(c)* Remover um campo de ENTRADA não é neutro: pydantic ignora chave desconhecida, e `PATCH {"unrestricted":true}` passou a devolver **200 sobre um no-op** — uma concessão que o chamador acredita ter feito, com dois remetentes reais (`seed_auth.py` e o `S4b` do probe do split). Daí a **LÁPIDE**: 422 nomeando, no precedente do `?pool_id=` de `/reports/resources/tokens`. **422 e não 403** — o 403 diria *"você poderia, com o grant certo"*, e é falso. A coluna física FICA (AUT-32), no precedente de `agent_group_members`. Testemunha invertida, nunca apagada, com controle positivo ao lado. 66 testes verdes | 2026-08-31 | `CHANGELOG.md` § lápide do `unrestricted` |
| AUT-35 | A imagem do `agent-registry` não nascia (testes no build de produção, TS1378); split `tsconfig.json` × `tsconfig.test.json` sem perder cobertura de tipos | 2026-09-01 | `CHANGELOG.md:3` |
| AUT-33 | **O `mk_unrestricted_principal.sh` entregava o OPOSTO do que promete, e derrubava três gates.** Ele criava `unrestricted: true` + lista VAZIA — o único arranjo que sobrevivia à inversão do passo 3 *quando foi escrito*. A AUT-13 tirou o claim do token e a AUT-03 fez `[]` significar NENHUM pool: juntas, converteram *"irrestrito por declaração"* em **escopo vazio**. O `probe_unrestricted_claim.sh` media isso e dizia *"irrestrito vê MENOS que o escopado (0 < 2)"*, cuja leitura óbvia — *"alguém quebrou o escopo"* — era falsa. ⚠️ É o modo de falha mais caro de um helper de teste: **HTTP 200, nome tranquilizador, principal que não serve**, e o gate consumidor reprova por um motivo que parece dele. Hoje ele ENUMERA, lendo os pools do agent-registry (pool novo entra sozinho). Dois defeitos encontrados no próprio conserto: o registry filtra tenant pelo header `x-tenant-id` e não por query param — `?tenant_id=` sai 200 com lista vazia, e foi o ramo INCONCLUSIVO que denunciou na PRIMEIRA execução —, e a conferência comparava o número que o script PRETENDIA escrever, passando com a escrita mutada; hoje **lê de volta**. `gate_queue_report_per_wait`: INCONCLUSIVO → VERDE | 2026-08-31 | `CHANGELOG.md` § lápide do `unrestricted` |
| AUT-34 | **`probe_unrestricted_claim.sh` invertido, não apagado — terceira ocorrência do padrão no mesmo dia** (AUT-27, AUT-30). Ele provava que o claim ERA cunhado, que sobrevivia ao refresh e que a declaração tinha DENTES: o pré-requisito do passo 3. O passo 3 aconteceu e o dono decidiu o oposto do pré-requisito. Hoje mede as três proposições que a remoção precisa que continuem verdadeiras: **N1** o claim não volta ao token — e o refresh é ramo PRÓPRIO, porque o modo de falha clássico é funcionar por uma hora e voltar na renovação; **N2** a API recusa NOMEANDO (com controle positivo: o mesmo corpo sem o campo cria); **N3** o principal dos gates alcança o tenant — sem ele os outros dois passariam num mundo onde ninguém enxerga nada, que é o estado em que o arquivo foi encontrado. Falseável: mutação no tipo da lápide → N2b; mutação no helper → N3 | 2026-08-31 | `CHANGELOG.md` § lápide do `unrestricted` |
| AUT-30 | **O ramo C do `probe_session_content_scope.sh` mudou de VEÍCULO, não de proposição.** Ele cunhava `{"unrestricted": true}` para representar o chamador irrestrito, e esse claim foi REMOVIDO por decisão do dono no mesmo dia (AUT-12) — escopo de pool passa a ser sempre enumerado. O gate seguiu cunhando um claim que ninguém lê e ficou vermelho dizendo *"o irrestrito perdeu acesso às rotas de conteúdo"*, que é **falso**: não existe mais aquele irrestrito. Mesma família das 10 falhas da AUT-27 — instrumento escrito contra um contrato que mudou embaixo, e o vermelho fiel ao ramo publicaria um defeito inexistente. Hoje o ramo usa o único principal irrestrito que restou, o de SERVIÇO (`X-Service-Token`), lido do próprio container para o gate não carregar cópia de segredo que envelhece calada. Encontrado ao rodar os portões vizinhos no fecho da AUT-01 | 2026-08-31 | `CHANGELOG.md` § recorte de linha nos agregados |
| AUT-12 | **Papel semeado virou config — e a mudança revelou um IMPASSE DE BOOTSTRAP.** `roles` era o único campo do admin semeado fora da config (e-mail, senha e nome já eram); default passou de `admin,developer` para **`admin`** (decisão do dono), medido antes: **zero** campos do catálogo concedem a `developer` sem conceder a `admin`, e os dois gates de papel restantes na UI aceitam `admin`. A env **recusa alto** papel desconhecido ou lista vazia, porque papel fora do catálogo não casa com preset e o admin nasceria cego. ⚠️ **O achado:** `create_user` não grava `module_config` — o preset era aplicado só pelo ROUTER, e o seed chama `create_user` direto. Medido: `module_config = '{}'`, isto é, admin **sem menu** sob o grant-first e **sem poder se conceder nada**. Não mordia porque o `seed_auth.py` provisiona pela API — o ambiente funcionava por um caminho DIFERENTE do que o código afirmava. Hoje a aplicação vive em `presets.apply_role_preset`, **um caminho e dois chamadores**. Testemunhas novas (a classe só afirmava `True`/`False`; *"criou"* e *"nasceu com grants"* são dois fatos) mais a **negativa** — papel sem `role_defaults` não grava nada. Suíte **65 verdes**; mutação (seed volta a não aplicar) reprova exatamente a testemunha nova | 2026-08-31 | `CHANGELOG.md` § papel semeado |
| AUT-14 | **Pool sem vigia virou fato visível, e só ele é alarme.** Três peças, como decidido: **(a)** alarme na barra lateral **só para ÓRFÃO** (zero usuários ativos) — "preso a UM" não entra, porque seria exposição publicada como dano (D14.1) e daria 31 de 36, isto é, 86% da população; **(b)** contagem de vigias por pool como **dado** ao lado da linha, nunca emblema; **(c)** a guarda no instante em que exposição vira dano — **três** caminhos tiram vigia de circulação (modal, desativar na lista, apagar) e todos passam por `confirmaSemVigia`, um decisor só. **Avisa, nunca impede:** pool sem vigia é config válida; o que não pode é ser silenciosa. ⚠️ **Achado que teria invertido o valor do alarme:** `GET /auth/users` pagina com `limit` default **100**, e a tela pedia sem parâmetro — um censo sobre lista cortada publica **órfão FALSO**, que é exatamente como se ensina a ignorar alarme. Hoje o teto é explícito e, se a lista puder estar cortada, o censo **declara que não sabe** em vez de afirmar ausência. Módulo puro `access/pool-coverage.ts`; gate `infra/test/probe_pool_coverage.sh` (11 casos, compila o arquivo de PRODUÇÃO em `strict` + `noUncheckedIndexedAccess`), falseável por mutação: contar inativo reprova 3 casos, acusar órfão preexistente reprova 1 | 2026-08-31 | `CHANGELOG.md` § pool sem vigia |
| AUT-24 | **As suítes TS voltaram a ser instrumento de regressão: 596 verdes.** Duas causas, e a segunda não era de autorização. **(a)** `agent-registry` 6 vermelhos `expected 401 to be…` — mas o diagnóstico real era pior que "teste atrás do portão": **o veredicto dependia de ambiente não declarado** e errava dos DOIS lados (sem `PLUGHUB_JWT_SECRET` a suíte ficava VERDE **sem nunca exercer o portão**; com ele, vermelha). Hoje o teste declara o próprio env (`vi.hoisted`, que roda antes dos imports — `config.ts` lê `process.env` no import) e **limpa o service-token de propósito**, para exercer o ramo Bearer+ABAC que a UI usa; consertar pelo atalho teria apagado a cobertura. **(b)** Ao remover o 401 apareceram **três falhas de outra natureza** que ele escondia (mocks velhos: `publishRegistryChanged` e `poolSkillSlot`). **(c)** `mcp-server` 1 vermelho: o produto estava CERTO — `conversation_escalate` passou a recusar sem `tenant_id` (*"identidade não tem fallback"*) e o teste afirmava o contrato antigo. Entraram **4 testemunhas novas** que não existiam: 401 sem credencial, 403 sem `config.resources`, 403 com `read_only`, e a do fail-closed da escalação — sem elas, apagar o portão ou reintroduzir o tenant inventado deixaria tudo verde. Gate novo `infra/test/probe_ts_suites.sh` (gêmeo do Python: execução a partir da IMAGEM + cobertura NOMEADA), provado falseável por mutação | 2026-08-31 | `CHANGELOG.md` § suítes TS |
| AUT-19 | **`[]` passou a ter UM significado.** Credencial ausente/inválida devolve **401**; escopo legitimamente vazio segue **200 + `[]`** — ambiguidade **eliminada**, não rotulada. Servidor: união discriminada (`unrestricted` × `scoped` × `unauthorized`) que o compilador **obriga** a tratar, nas duas cópias TS. **Achado no caminho:** `GET /pools/:id/queue` **não tinha verificação nenhuma** — servia os `session_id` da fila de qualquer pool sem credencial, enquanto a irmã (que devolve só CONTAGENS) já escopava; era a porta destrancada do par, e agora exige credencial + escopo (403 fora do domínio). Cliente: re-auth **reativo** no `apiFetch`, com **single-flight** — que não é otimização: o refresh token é ROTATIVO, e N renovações concorrentes se invalidam, matando a sessão quando ela tenta se salvar. Uma retentativa, nunca duas. Chamador migrado junto (`smoke_admission_licensing.sh` chamava sem credencial). Gates: `probe_ts_scope_resolvers.sh` estendido (401 nas TRÊS rotas + 403 de escopo, com controle positivo) e `probe_apifetch_reauth.sh` novo (compila os arquivos de PRODUÇÃO e os exerce; 7 casos), ambos provados falseáveis por mutação | 2026-08-31 | `CHANGELOG.md` § `[]` com um significado só |
| AUT-23 | **As duas copias TS passaram a seguir a AUT-03 — e o `agent-registry` deixou de degradar ABERTO sem header.** Dois ramos: (a) `[] -> null` (legado, que o py-authz ja tinha virado — Python e TypeScript discordavam sobre o mesmo claim); (b) o early-return de "sem header Authorization" ficava **antes do `try`**, e por isso o ramo homonimo do log era **INALCANCAVEL** — foi o log impossivel que denunciou a AUT-17 registrada por mim como se cobrisse os dois casos. Medido ao vivo, com controle positivo em toda rodada: `admin@` 36 pools / 291 instancias intactos; `probe@` (claim `[]`), sem header e token invalido -> **0/0** nas duas rotas (antes: 36 pools). Gate novo `infra/test/probe_ts_scope_resolvers.sh`, em duas metades provadas NAO redundantes por mutacao: trocar `return []` por `return null` mantendo o log deixa a metade estatica **verde nas quatro** e reprova so a viva. Suites conferidas contra baseline do `HEAD`: identicas (as 7 falhas sao herdadas, AUT-24) | 2026-08-31 | `CHANGELOG.md` § copias TS do resolvedor |
| AUT-06 | **A UI parou de inferir "todos" de lista vazia** — e eram **cinco** sítios, não um: `AgentAssistContext`, `PoolDomainSelect`, `FlowMonitorPage`, `AgentsPage` (×2), `AnaliseSurveysPage` e `CustomerVoicePage`. Os quatro últimos usavam a variável local `dom` e escaparam do primeiro censo, que só casava `accessiblePools` na mesma linha — quinta vez neste arco que um instrumento teve eixo cego | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-11 | **O `admin@` do demo recebeu os 36 pools explicitamente**, pela API oficial (`PATCH /auth/users/{id}`) e não por `UPDATE` — o invariante de provisionamento vale também para dado de demo. Verificado no token: 36 pools, `unrestricted` ausente. Achado no caminho: o serviço rodava o código ANTIGO porque `docker cp` não recarrega processo Python vivo — os 63 testes verdes eram de outro processo. Só depois do `restart` o token saiu correto | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-18 | **116 chamadas do browser migradas para `apiFetch`** (44 arquivos), em duas levas: 58 com rota literal e 58 com URL montada por variável — estas invisíveis ao primeiro censo, que só via literais. `fetch` cru sem credencial: **65 → 24**, e 23 dos 24 são o `registry.ts`, excluído por ser cross-origin (AUT-20). O `probe_ui_credential_coverage.sh` segue **VERDE**. Dois erros do meu script, pegos por verificação e não pelo verde: import inserido dentro de bloco multi-linha (2 arquivos) e uma substituição dentro de COMENTÁRIO (revertida) | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-17 | **O escopo deixou de degradar ABERTO em token INVÁLIDO.** ⚠️ **Corrigido em 2026-08-31 por medição:** esta linha dizia "token ausente/inválido" nas DUAS cópias, e isso é falso para o `agent-registry`, onde `if (!auth.startsWith("Bearer ")) return null` **antecede o `try`** — o caso sem header nunca alcança o `catch` e segue irrestrito. O próprio código denuncia: o ramo `"sem header Authorization"` do log é **inalcançável**. No `mcp-server` a correção vale inteira, porque `verifyJwtPayload` levanta. Resíduo em **AUT-23**. Metade indispensável junto: `CampaignsPage` usava `fetch` cru e só funcionava por causa do fail-open — migrado para `apiFetch`. O consumidor foi conferido: `[]` é truthy em JS, vira `Set` vazio e filtra tudo (não repete o `if not x` do lado Python) | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-16 | **As duas cópias TypeScript do resolvedor de escopo** perderam a cascata do claim — `mcp-server/server.ts` e `agent-registry/routes/operational.ts`. Nenhum censo as contava: o `probe_authz_single_verifier` conta quem DECODIFICA JWT, e estas consomem claims já decodificados | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-08 | **O parâmetro morto saiu de `resolve_supervisor_scope`** — e ao remover descobriu-se que não era morto: `router.py:188` lia a COLUNA do banco e a função ainda concedia escopo total de supervisão no login, mesmo sem o claim no token. `role` e `unrestricted` saíram da assinatura juntos | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-13 | **`unrestricted` deixou de ser emitido e de ser lido.** O ramo `claim → irrestrito` saiu de `resolve_scope` (py-authz) e o claim saiu do token (`jwt_utils`, mais os 2 call sites do builder). Escopo de usuário passa a ser **sempre uma lista**; `None` sobrevive só para principal de SISTEMA, construído explicitamente. Inerte hoje — o ramo legado ainda resolve irrestrito para `[]`, então nada quebrou. Testes reescritos com **testemunha negativa** (com o legado desligado, `unrestricted: True` recebe `[]`, não o tenant): sem ela, reintroduzir o ramo passaria despercebido, porque com o legado ligado os dois desenhos devolvem `None` pelo mesmo caminho. Suítes: **83 + 63 verdes** | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-07 | **O seed do admin deixou de declarar escopo de pool.** `unrestricted=True` saiu de `seed_admin_if_absent` — era afirmação sobre POOLS feita antes de existir pool, e reintroduzia por SEED a porta larga que o passo 8 removeu do runtime. ⚠️ **Corrigido em 2026-08-31 (AUT-12) por medição:** esta linha dizia *"o bootstrap fecha pelo MÓDULO: `config.permissions` tem `role_defaults: admin: read_write`, logo o admin semeado concede escopo"* — e era **falso**. `create_user` grava `roles` e **não** grava `module_config`; quem aplicava o preset era só o router, e o seed chama `create_user` direto. Medido: o admin semeado nascia com `module_config = '{}'`, ou seja, **sem menu** sob o portão grant-first e **sem poder se corrigir**. Era promessa sem mecanismo, e load-bearing — foi com ela que este passo justificou remover o `unrestricted=True`. Hoje o seed aplica o preset (`presets.apply_role_preset`), e a afirmação passou a ser verdadeira. Suíte auth-api: **63 verdes**. Sem efeito hoje (o ramo legado ainda resolve irrestrito) e sem efeito no `admin@` existente (seed é if-absent) | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-05 | **Os 3 sítios que fundiam `[]` com `None` foram consertados** — `query_workflow_summary` (analytics-api), `list_survey_responses` e `list_results` (evaluation-api), este último preservando a regra de que a PRÓPRIA avaliação é sempre visível (sem pool algum sobra a posse, e só ela). Censo: FUNDE 8→6, e os 6 restantes são 1 falso positivo + 5 helpers protegidos por wrapper. Suítes: **237 + 747 verdes**. Gate novo `infra/test/probe_accessible_pools_scope.sh`, duas metades (censo + testemunha dos 3 consertos), ambas provadas falseáveis por mutação | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-04 | **Auditoria dos consumidores de `accessible_pools`** — censo AST (`infra/test/_accessible_pools_census.py`): **142 funções** tocam o campo · 41 distinguem `[]` de `None` · 93 apenas repassam · **8 decidem por truthiness**, das quais 4 são protegidas pelo wrapper e 1 é falso positivo ⇒ **3 vazamentos reais**, nomeados em AUT-05. O instrumento fica versionado e re-executável | 2026-08-31 | `CHANGELOG.md` § `accessible_pools: []` |
| AUT-02 | **ABAC TOTAL — arco de 8 passos completo**: split `config.users` → `users`+`permissions` · campos `config.calendars`/`dialog_forms`/`dashboards` e `nav.channels`→`config.channels` · papel vira **preset de seed** aplicado em `create_user` · grants do supervisor · portão único **grant-first** · queda dos dois bypasses (`unrestricted` vira a única porta larga) · remoção dos 7 `roles:` do `Sidebar.tsx` · cauda de papel no backend (4 sítios) | 2026-08-27 | `TODO.md:621` + `CHANGELOG.md` |
| AUT-36 | **A hipótese estava certa: era o INSTRUMENTO, e provar isso exigiu o caso que faltava.** O ramo `customers/360` reprovava com `admin=21 escopado=21`, e a ficha suspeitava do próprio ramo — *igualdade só prova ausência de filtro se o dado PUDER diferir*. Duas medições fecharam a questão: **(1)** com escopo num pool que não entra nem atende nenhuma sessão do cliente, a rota devolveu **0 contra 21** — o recorte funciona, e a UNIÃO do predicado foi conferida no ledger antes (os 3 pools que atenderam não incluem o do escopo); **(2)** o probe sorteava `POOL` e `CID` INDEPENDENTES, e numa rodada caiu em `limite_ia` com um cliente cujas 21 sessões estão todas ali — `21 = 21` era o comportamento CERTO. Conserto: o cliente passa a ser escolhido DEPOIS do principal escopado, entre os que **não aparecem na lista de contatos daquele escopo** — perguntando ao PRODUTO, porque reimplementar a união no gate seria a segunda casa do predicado que a AUT-01 fechou. A asserção virou exata (`escopado == 0`, não `!=`), e ausência de par vira `SEM AMOSTRA`, nunca verde. Mutação no serviço rodando: sem o `sess_conds.append(_scope)`, o ramo acusa | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (13) |
| AUT-55 | **Quem DETÉM o item passa a enxergar o formulário dele.** O escopo da sessão tinha duas fontes (pool do `meta`, `core.pool.id` do ctx) e numa tarefa delegada as duas apontam o pool do WORKFLOW, enquanto o operador alcança o pool HUMANO do claim: `403 pool_scope` às dezenas, medido. ⚠️ O mesmo operador podia SUBMETER (o A5 autoriza pela posse) e não podia LER o estado que renderiza o formulário — e o sintoma sumia sozinho quando a ativação escrevia o `meta`, o que é corrida, não rede instável. Conserto: terceira fonte = pool do ITEM parqueado (`work_task.pool_id`), sem alargar `accessible_pools` — o teste que carrega peso é esse. A função saiu do `server.ts` para `lib/session-scope.ts`, onde pode ser testada. 9 casos, suíte 341/341, `gate_session_state_pool_scope.sh` verde com o corpo do operador trazendo `dialog_form_id` e `resume_token` | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (8) |
| AUT-53 | **O probe de rank volta a medir RANK.** A bateria deu os tres POSITIVOS do `probe_rank_grant_guard` vermelhos com 422 (*grupo de nascimento*) contra 201: instrumento atras da AUT-44, nao defeito — e positivo vermelho PARECE protecao. Conserto so na fixture: grupo criado, os dois delegados supervisores antes do login, e TODA criacao delegada manda `group_ids`, para cada cenario isolar o eixo de rank (o do grupo tem gate proprio). De passagem: `POST /auth/v1/groups` devolve `group_id`, nao `id`, e a 1a rodada vazou o grupo por registra-lo depois da checagem — corrigido e expurgado. VERDE, 10 cenarios, residuo 0 | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (10) |
| AUT-54 | **O `auth-seed` morria no admin desde a MOD-11.** O S6 lia seis `workflows.*` no seed como divergencia de politica; medido, era FATAL: o modulo saiu do catalogo, o PUT recusa o config inteiro com 422 e o seed faz `die` no PRIMEIRO usuario — `Exited (1)` em toda subida, sem semear `supervisor@`/`operator@`. Contado antes: 0 portadores, 0 linhas no registro, 0 templates (o boot ja limpava o dado). Conserto: bloco fora do seed, e o comparador passa a nomear a parte campo FORA DO CATALOGO, com a consequencia. Verificado nos dois sentidos (seed antigo exit 1, novo exit 0), os 3 configs com 200 no catalogo vivo e o probe verde. Seed NAO re-executado (reescreve usuarios reais). Causa de ninguem ver: `up.sh` ignora one-shots pelo nome, sem olhar o exit code | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (11) |
| AUT-56 | **O `auth-seed` deixa de apagar o escopo do `admin@` a cada subida.** A primeira rodada verde do seed (pos AUT-54) levou o admin de 41 pools para `[]` — NENHUM pool desde a AUT-03 —, restaurado pela API no mesmo minuto; os logs antigos mostram que ja acontecia antes do `die`. Opcao 2, do dono: a entrada do admin perde `accessible_pools`, e a guarda por PRESENCA do `set_scope` o pula (novo nasce com o default, existente mantem o que lhe atribuiram). Mantido por decisao: o `module_config` completo segue regravado em toda subida. Verificado: seed exit 0, tres usuarios sem mudanca, admin com 41 pools, S6 verde | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (12) |

---

## `docs/adr/adr-abac-module-granularity-and-delegation.md` — granularidade e delegacao ABAC

| id | tarefa | data | âncora no `CHANGELOG.md` |
|---|---|---|---|
| ORQ-10 | **A aba Processos perguntava a uma tabela que o Arc 19 esvaziou, e o dono viu antes de qualquer gate.** Ela dizia *"No processes found"* com **51 processos suspensos vivos**: `useWorkflowInstances` chamava `workflow-api /v1/workflow/instances`, cuja tabela tem **0 linhas** desde que processo virou sessão de canal webhook — e o endpoint respondia **`[]` com HTTP 200**, vazio bem-formado que não acende nada. ⚠️ **A ficha nasceu com a lista no Monitor e o DONO corrigiu o desenho**: Monitor é estado agregado (a aba irmã abre com mostradores + distribuição por pool), Analytics é lista com período. Hoje a aba abre no CONSOLIDADO — Em execução · Suspensos · **Sem endereço** · Vencendo em 24 h, por pool — e a lista só aparece no drill-down, onde a ação da APR-10 vive. ⚠️ **Mas o consolidado NÃO tem janela de tempo, e isso é medição**: das 51 suspensas, **2** abriram nas últimas 24 h e **49** antes — uma janela mostraria 2 e esconderia 49 na tela que existe para revelar trabalho parado; suspenso há sete dias é presente. Os mostradores da aba Sessões (`busy`/`available`/`queue`) são igualmente de estado. ⚠️ **`/reports/sessions` não servia** justamente por ter janela default de 7 dias: devolvia **45** das 51. ⚠️ **Três defeitos meus, pegos por medição:** escrevi `pool_id IN (…)` à mão nas rotas novas — a cópia que a F1b fechou — e ela já divergia, perdendo a sessão de `pool_id = ''` (7 ativas na fonte, 6 na resposta) e o ramo *"um pool meu ATENDEU"*; o consolidado quebrou duas vezes por variável removida na edição, e **as duas falharam FECHADO** (503, nunca número errado); e o ramo C do gate aceitava metade do contrato até a bateria de mutação mostrar que trocar só a rota da lista o deixava verde. ⚠️ **O `resume_token` saiu da tela**: ela o exibia com um botão de copiar, e quem o tem retoma o processo pela porta externa sem portão nenhum — agora viaja `has_resume_token`, e o número *sem endereço* (25 de 51) é o que diz quem nem o botão de encerrar alcança | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (11) |
| APR-10 | **A ficha dizia *"lacuna de SUPERFÍCIE, não de autorização"* — e a medição achou uma terceira: ALCANCE.** O `force-complete` resolvia o endereço da sessão pelo ledger `work_task` do **Redis**, que neste deploy não persiste: medido em 2026-09-09, **zero** chaves para **54** sessões suspensas. Ou seja, o botão que a ficha pedia responderia **404 em 100% dos casos** — pior que botão nenhum, porque ensina que a tela não funciona. Duas metades, e a segunda sem a primeira seria decorativa: **(a) ALCANCE** — `force-complete` ganhou o ramo do PARQUE DURÁVEL (RET-11), delegando a `POST /v1/channels/webhook/sessions/{sid}/encerrar-parque` no channel-gateway, que é o dono da tabela; o gateway EXECUTA e **não devolve o token** (pedir a ação não é receber a credencial de retomada), e o chamador continua com UMA porta. **(b) SUPERFÍCIE** — `AcoesDoProcesso` no painel de detalhe do Monitor: encerrar (gateado por `agent_assist.supervisionar`) e *Abrir no Console*, com os quatro desfechos do backend traduzidos em mensagens DIFERENTES (404 = sem endereço, e nomeia o mutirão; 501 = passo em execução; 409 = outro já encerrou). ⚠️ **A tela não adivinha se a ação vai funcionar** — ela sabe só do `resume_token` do Redis, e uma sessão sem token à vista pode ter parque durável; quem sabe é o backend. ⚠️ **Eu abri um buraco e a medição o pegou antes do commit:** a primeira versão da rota devolvia **200 e encerrava a sessão sem `Authorization` nenhum** (herança do `_resolve_approver_principal`, que trata header ausente como sistema — correto no resume, errado numa ação de supervisor). Fechado com o verificador canônico e provado ao vivo: 401 sem Bearer · **403** com credencial e sem o campo, nomeando quem foi barrado · 200 com supervisor, encerrando de fato (54 → 53 suspensos, `close_reason=flow_complete` porque o flow seguiu o próprio `on_timeout`) | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (10) |
| RET-17 | **As 4 cópias do supervisor de boot viraram uma, e o eixo inteiro fechou numa casa só.** `plughub_tasks` passou a ter as DUAS funções — `supervisionar` (boot: avisa na morte E no término espontâneo) e `disparar` (efêmera: dá dono e CALA no término normal) —, que continuam separadas porque os ciclos de vida são opostos e trocar uma pela outra é o defeito nos dois sentidos. ⚠️ **Medido ANTES de mover:** as 4 cópias eram comportamentalmente IDÊNTICAS (o AST normalizado diferia só em acentuação e no nome da variável interna), então consolidar foi mudança de ENDEREÇO, não escolha de comportamento — se tivessem divergido, a consolidação teria de ESCOLHER, e isso seria decisão a registrar. ⚠️ **O gatilho foi decisão do dono, não a condição que a ficha declarava** (o quinto serviço, ou alguém tocando naqueles `lifespan` por outra razão); fica registrado assim porque um gatilho que *"disparou"* sem ter disparado corrói o valor de declarar gatilhos. ⚠️ **O workflow-api mudou de build context**: era o único Python sem nenhuma dependência interna e construía a partir do próprio diretório, o que punha `packages/*` fora de alcance — 4 composes ajustados. Era exatamente essa parede que a RET-13 citou para não fazer a lib. ⚠️ **A mensagem perdeu o nome do serviço** (*"Reinicie o analytics-api"* → *"Reinicie o servico"*): quem identifica o serviço é o log do container, e um parâmetro só para repetir o prefixo seria contrato novo sem fato novo. Controle positivo ao vivo pela lib: falha injetada no scanner do workflow-api → *"task de background 'workflow-timeout-scanner' MORREU"*, com o serviço `running healthy` | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (9) |
| RET-16 | **Os 21 disparos soltos dos serviços vivos ganharam dono, e o helper virou pacote.** `routing-engine` (17), `rules-engine` (2), `ai-gateway` (1) e `evaluation-api` (1) criavam tasks efêmeras com `asyncio.create_task(...)` como statement: exceção presa na Task e coleta possível no meio da execução. ⚠️ **A decisão de casa MUDOU, e o que mudou foi o número, não o argumento:** a RET-13 copiou o supervisor de boot em 4 serviços porque *"alarme não decide nada"* — segue verdadeiro; mas com o SEXTO consumidor do mesmo helper a cópia vira dívida, e o precedente do repositório é literal (`py-authz` nasceu de seis cópias de JWT). Hoje `packages/py-tasks` é a casa, e o `channel-gateway/tarefas.py` da RET-15 foi REMOVIDO para não haver duas. ⚠️ **A dívida do `.debt` foi a ZERO**, e o arquivo passou a distinguir `!isento` de dívida — o único que sobra é `conversation-writer`, **fóssil em quarentena declarada**, que não é dívida porque migrá-lo contraria a quarentena. ⚠️ **Duas regressões minhas, ambas nos TESTES e não no produto:** dois espiões de `asyncio.create_task` no routing-engine quebraram, primeiro por não aceitarem `name=` e depois por devolverem `None` onde o produto registra um done-callback — corrigi o ESPIÃO, porque um mock que dita a assinatura do que espia mede o formato da chamada, não o fato. ⚠️ **Controle positivo ao vivo no caminho mais crítico que migrei**: com falha injetada em `_process_message` (uma task por mensagem Kafka do roteador), o log disse *"tarefa 'process-message' MORREU"* e o `docker ps` seguiu **`running healthy`** | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (8) |
| RET-15 | **As tarefas efêmeras não tinham dono, e a ficha achava que o problema era o `try` que falta.** Medido: são DOIS danos independentes, e o segundo vale até para quem já embrulha o corpo inteiro — `asyncio.create_task(...)` como statement devolve uma Task que **ninguém referencia**, então (a) a exceção fica presa nela e some, e (b) o loop é o único dono e o CPython pode **coletá-la no meio da execução**. Por isso o conserto alcançou **10 disparos** no channel-gateway, e não os 6 da ficha: `email`, `sms` e `webrtc_room_client` tinham `try` pega-tudo e entraram assim mesmo. ⚠️ **Uma correção da própria ficha:** `voice.py:374` foi listado como exposto e **não é** — tem 2 instruções fora do `try` e nenhuma que possa levantar; entrou só pelo dano (b). Casa única: `channel-gateway/tarefas.py::disparar()`, que guarda a referência num conjunto de módulo e loga a morte. ⚠️ **E ele NÃO é o supervisor de boot da RET-13:** lá uma task que retorna é incidente; aqui retornar é o que a tarefa existe para fazer, e um WARNING por mensagem afogaria o log — a mutação M4 prova que copiar aquele comportamento reprova aqui. ⚠️ **O gate mediu o REPOSITÓRIO e achou mais 22 disparos soltos em 5 serviços** (17 no routing-engine, 4 deles perdendo também a exceção): dívida DECLARADA COM TETO em `task_ownership.debt` (gate vermelho crônico ensina a ser ignorado), fila em RET-16. ⚠️ **Controle positivo ao vivo, mesma mensagem no mesmo tópico**: produto são → silêncio; `_dispatch` mutado para levantar → `ERROR tarefa 'outbound-dispatch' MORREU` | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (7) |
| RET-13 | **A ficha apontava UMA task e o censo achou ONZE.** Ela dizia que o scanner de prazo era *"a única task de background do channel-gateway sem `_supervise`"* — verdade, e irrelevante: medido por AST, são **16 tasks de boot em 4 serviços**, e o `_supervise` só existia num deles. **analytics-api (2), evaluation-api (6) e workflow-api (1) não tinham alarme nenhum** — nenhum `add_done_callback` em lugar algum. É a GAT-01 outra vez: *"rodou tudo o que a lista cita"* e *"a lista cita tudo"* são dois fatos. ⚠️ **O caso da ficha era o MENOS grave**, e isso foi medido antes de embrulhar: o laço do scanner tem `except Exception` por iteração e re-levanta `CancelledError`, então é praticamente imortal — o alarme ali não muda o comportamento de hoje, fecha a CLASSE. ⚠️ **Duas armadilhas achadas ao implementar, ambas de alarme que MENTE:** (a) `_run_consumer_safe` e `run_performance_job_loop` faziam `break` no `CancelledError`, então a task terminava *com sucesso* ao ser cancelada; (b) — **e esta a suíte não pegou** — `run_consumer` instala handlers de SIGTERM/SIGINT e **RETORNA** no `docker stop`, o que fazia o supervisor gritar *"TERMINOU sozinha"* em **todo shutdown normal**. Quem pegou foi o teste AO VIVO; o meu mock dormia até o cancel e media a proposição adjacente. Alarme que grita quando nada há ensina a ser ignorado, que é pior que alarme nenhum. ⚠️ **Controle positivo ao vivo**: com uma falha injetada no scanner do workflow-api, o log disse *"task de background 'workflow-timeout-scanner' MORREU"* e o `docker ps` seguiu **`running healthy`** — a cegueira em uma linha | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (6) |
| RET-12 | **A ficha dizia que o `collect` era um SEGUNDO mecanismo de suspensão, e a medição REFUTOU: a máquina sempre foi uma, e o que divergia era o NOME da chave.** O `collect` escreve no MESMO hash `{tenant}:resume_tokens` e chama o MESMO `_write_resume_meta` — `adapters/webhook.py:2226` diz em prosa *"the collect_token DOUBLES AS the resume_token"*. O defeito inteiro morava numa string: os três steps que parqueiam gravam o token no `pipeline_state.results` com sufixos diferentes (`suspend.ts:53` e `delegate.ts:41` → `:__resume_token__`; `collect.ts:38` → `:__collect_token__`) e o leitor do orchestrator-bridge casava **só o primeiro**. Consequência medida: **49 sessões parqueadas sem endereço de volta**, 41 delas do `limite_entrega` — o `session_suspended` saía sem token, a analytics não escrevia `session_transitions` (`if payload.get("resume_token")`) e a RET-11 gravava o parque vazio. ⚠️ **É um contrato ENTRE pacotes** (o engine TS escreve, o bridge Python lê), a mesma família do `payload["answers"]` × `payload["result"]` — por isso o gate `probe_park_token_key_contract.sh` **DESCOBRE** os sufixos que o engine escreve e os cobra do leitor; declarar a lista mediria os produtores contra o gate. ⚠️ **Gate e teste medem proposições DIFERENTES, e isso foi provado por mutação:** recortar o `step_id` com um sufixo fixo deixa o gate de nomes VERDE e quebra o leitor — por isso o laço saiu de dentro de uma função de 10 mil linhas e virou `descobrir_parque`, com 10 testes. ⚠️ **Controle positivo ao vivo, lado a lado no mesmo banco**: o leitor novo gravou `token=0434fc6d… step=parquear_resultado reason=input prazo=+7d`; o antigo, montado por `docker cp`, gravou `<VAZIO>` e disparou o aviso que a RET-11 já deixara pronto — *"parque SEM endereco de volta … (mecanismo `collect`?)"*, com a hipótese escrita na véspera confirmada palavra por palavra | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (5) |
| RET-14 | **Mutirão executado: 163 sessões que estavam `suspended` para sempre foram encerradas — 212 → 49, e as 49 restantes são todas RECENTES.** Elas não tinham a que voltar (o token e o `pipeline_state` viviam num Redis sem persistência) e nada as alcançava: o scanner varre tokens que não existem, e o `force-complete` responde 404 porque o estado 1 dele exige o item no ledger. Ferramenta: `infra/scripts/reap_parques_orfaos.sh`, **dry-run por padrão**, que emite `contact_closed` em `conversations.events` — pela porta de EVENTO, nunca escrita direta em ClickHouse (o precedente é o `/admin/backfill-events`, que também re-emite). ⚠️ **`close_reason` NOVO, e a escolha é o ponto:** `suspend_orphaned`. Não podia ser `flow_complete` (o flow não completou — reusá-lo faria a estatística de resolução contar como sucesso um processo que ninguém terminou) nem `session_timeout`, **que já tem dono**: o `orchestrator-bridge` o usa para timeout de TRANSPORTE (`main.py:3830`). ⚠️ **Duas guardas, e a primeira foi PROVADA ao vivo:** sessão com token vivo nunca entra — inseri um token apontando para uma candidata e ela saiu como `PULADA (token vivo)`, 163 candidatas → 162 emitidas; e o corte de idade (default 7 dias) protegeu as 49 recentes, que a medição original mostrava incluir 3 ainda no prazo. ⚠️ **A linha vai COMPLETA** — `sessions` é `ReplacingMergeTree` de linha inteira, e evento parcial apagaria `pool_id`/`channel`/`opened_at`; conferido depois: 0 sem pool, 0 sem canal, 0 sem abertura em 162. **Três defeitos meus, todos pegos rodando:** o `<<'PY'` ocupava o **stdin** e a lista canalizada nunca chegava ao Python (emitiu ZERO); o veredicto olhava só o `exit` e imprimiu **"OK — eventos emitidos"** sobre zero emissões, que é a degradação de SINAL TROCADO que motivou a reescrita do `force-complete`; e a consulta de candidatas não usava `FINAL`, então a segunda execução repropunha as 163 já fechadas, porque o RMT ainda mostra a versão antiga até fundir | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (4) |
| RET-11 | **O parque de uma sessão suspensa ganhou registro DURÁVEL, e o scanner passou a restituir o endereço perdido.** Até aqui o token e o prazo viviam só no Redis, que neste deploy roda `--save ""` com `appendonly no`; o scanner varre `*:resume_tokens` **no Redis**, então sem token ele não enxerga nada — e o prazo durável que existia (`analytics.session_transitions`, 680 linhas, todas com `resume_expires_at`) nunca era lido por quem podia agir. ⚠️ **A prescrição original da própria ficha foi REFUTADA antes de virar código:** ela mandava o scanner ler `session_transitions`, que é ClickHouse — analytics, `ReplacingMergeTree` alimentado por Kafka — e isso tornaria a analytics load-bearing para operação, sem precedente no repositório (medido em `rules-engine` e `routing-engine`). O store escolhido pelo dono foi **Postgres**: `parking.session_parks`, no `channel-gateway`, que já abre o pool e já é dono do ingress de resume e do scanner. ⚠️ **Não se reabriu o Arc 19** (o dono deu a opção): ressuscitar `workflow.instances` recriaria a `WorkflowInstance` como contêiner paralelo, contra *"never create a wide container for a fact that fits a narrow one"* — o parque é fato da SESSÃO. ⚠️ **O escritor é um CONSUMIDOR, não o caminho do suspend:** quem cunha o token é o engine, via callback `persistSuspendWebhook`, Redis-only por desenho do Arc 19; mas o evento `session_suspended` já existe, já é publicado pelo `orchestrator-bridge` e já carrega tudo — escutá-lo cobre os DOIS mecanismos de parque sem tocar em produtor nenhum. ⚠️ **E o alcance é menor do que parece, o que está escrito no módulo:** o registro **não** torna retomável um processo cujo Redis se perdeu inteiro (o `pipeline_state` do flow é igualmente volátil). Ele recupera o caso REAL e documentado de *sessão viva, token perdido* — o TTL de `{tenant}:resume_tokens` é do HASH e é compartilhado, então um `collect` de 1 h encurta um suspend de 48 h; e dá CONHECIMENTO sobre o resto, que é o que a RET-14 encerra. Parque sem `resume_token` (o caso `collect`) vira **linha contável com aviso**, nunca silêncio (RET-12). **Três defeitos latentes meus foram pegos antes de valerem**: `datetime` usado e não importado (só quebraria no ramo de fallback), `self._db_pool` lido e nunca atribuído (o adapter só repassava o pool ao `IdentityIndex`), e `AIOKafkaConsumer` sem o import local que os irmãos têm — este último só apareceu **ao rodar**, e apareceu porque a task foi ligada com `_supervise`, que é justamente o que falta ao scanner (RET-13). 10 testes novos, 766 na suíte, 2 mutantes conferidos | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (3) |
| AUT-37 | **A ficha apontava para `analise/*` e estava VENCIDA; o buraco estava no vizinho que ela não olhou.** Reaberta por uma pergunta do dono (*"tem tarefa para disponibilizar a permissão de Monitor e Analytics pela interface?"*). Medido: **conceder já funcionava** — Monitor é `contacts.monitorar` e Analytics é `contacts.visualizar`, ambos no card *Service & Contacts* desde 2026-08-27, quando o portão de PAPEL a montante da ABAC foi removido. O que faltava era **impor**. Censo NAV → ROTA das 31 entradas de menu com regra ABAC: **11 gateadas · 6 "por dentro" · 14 NUAS**, alcançáveis por URL por qualquer autenticado — a mesma forma do item (b) da MOD-11. ⚠️ **A ficha nasceu vencida no ledger:** as 6 rotas `analise/*` ganharam `RequireAbac` entre 27 e 29/08, e ela foi movida do `CLAUDE.md` para o `pending.md` em 05/09 carregada verbatim, sem remedir — exatamente o defeito que a § *Pending* do `CLAUDE.md` narra ter custado seis semanas em Customer History. ⚠️ **Das 6 que "gateavam por dentro", DUAS não gateavam nada:** `AccessPage` e `GroupsPage` tinham `canGrant = perms.can('config','permissions','read_write')` — o portão de CAMPO da MOD-02, que decide um BOTÃO, não a página. Um `grep` por `perms.can` as dava por protegidas; *presença de checagem não é evidência de que ela decide o render*. Eram **16** abertas, não 14. Hoje: **31 de 31 gateadas**, e o par (módulo, campo) de cada guarda é **copiado** da regra do menu, nunca reescolhido — duas casas decidindo o mesmo divergem. ⚠️ **E uma já divergia:** `/analise/quality` exigia `contacts.visualizar` enquanto o menu exige `evaluation.report`, e a rota era a **mais permissiva** (`operator` e `devops` têm o primeiro no preset e não o segundo, logo alcançavam por URL uma tela que o menu lhes esconde). Alinhada ao menu, que é o campo semanticamente certo. Fechou junto a nomeação que originou a pergunta: `contacts.monitorar` e `contacts.visualizar` passaram a **nomear a tela** (*"Monitor: …"*, *"Analytics: …"*) — o rótulo de `monitorar` já dizia "Monitor" e o de `visualizar` nunca dizia "Analytics", e era essa assimetria que fazia a permissão parecer ausente. ⚠️ **E eu derrubei o boot do `auth-api` no meio do caminho**: o rótulo *"Monitor: …"* virou escalar YAML **não-citado com `: `**, o serviço morreu com `ScannerError` e o `platform-ui` não subiu junto. O `probe_abac_field_labels_i18n.sh` ficou **verde** com o arquivo quebrado — ele lê o catálogo por regex, de propósito, e leitor tolerante foi a razão de não pegar; e a checagem que faltou era a que eu fizera nas vizinhas (validei `json.loads` nos locales, não validei o YAML). Virou o **ramo G** daquele gate. Gate: `probe_nav_route_guard_agreement.sh`, 2 ramos (**A** rota nua · **B** par divergente do menu), os dois provados falseáveis por mutante conferido | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (2) |
| AUT-42 | **Os rótulos de campo do ABAC não seguiam o idioma da tela — reportado pelo dono, com `/config/access` em inglês e os campos em português.** O `ModulePermissionForm` renderizava `{schema.label}`, o rótulo CRU da API vindo do `infra/modules.yaml`; o nome do MÓDULO logo acima já resolvia por `t('moduleNames.<id>', { defaultValue: mod.label })` desde sempre, então **as duas metades do mesmo cartão discordavam de idioma**. Entraram 41 chaves `fieldNames.<módulo>.<campo>` nos DOIS locales — a chave é **(módulo, campo)** e nunca só o campo, porque `visualizar` existe em `contacts`, `campaigns`, `billing` e `skill_flows` com sentidos diferentes. O `defaultValue` fica: campo novo do catálogo aparece com o rótulo do YAML, **nunca com a chave crua**. ⚠️ **A ficha dizia 44 campos; são 41** — ela foi escrita antes de a MOD-11 remover os 6 de `workflows`. ⚠️ **A paridade EN × pt-BR não pegaria isto, e é por isso que o gate mede outro eixo**: antes da correção nenhum dos dois locales tinha `fieldNames`, então eles estavam em paridade PERFEITA sobre uma ausência total — o eixo do gate é CATÁLOGO → locale, não locale → locale. ⚠️ **E o vizinho na mesma tela mentia parado:** a lista ROLES dizia *"Developer"* porque `AccessPage.tsx` tinha um `ROLE_LABELS` **hardcoded** que 4 call sites liam em vez do locale — e ele guardava o rótulo que a MOD-10 aposentara **na véspera**. O locale estava certo; a tela não o lia. **Removido, não corrigido** (remover a alternativa não depende de ninguém lembrar). Gate: `probe_abac_field_labels_i18n.sh`, 6 ramos (**A/B** todo campo do catálogo tem rótulo nos dois locales · **C** nenhuma chave órfã · **D** o formulário RESOLVE o rótulo, senão a cobertura é decorativa · **E** o nome do módulo, a outra metade do cartão · **F** o rótulo de papel, com as duas metades: a chave existe E ninguém a contorna). ⚠️ **O ramo E entrou porque a metade de cima estava completa por ACIDENTE** — a MOD-11 tinha acabado de tirar a chave órfã de `workflows` na véspera. Os seis provados falseáveis por mutante; **um mutante saiu no-op e foi refeito** (a string de busca não casava com o valor real, `"LGPD Audit"`), que é a mesma família do teste que não pode reprovar. ⚠️ O ramo D também nasceu errado e foi consertado: lia o fonte linha a linha e reprovou no **comentário que documenta esta própria correção** — *grep contaria o comentário que documenta a migração*, exatamente como a D14 registra | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (1) |
| MOD-11 | **O módulo `workflows` não era órfão à espera de fiação: era uma modularização SUPERADA, e foi encerrada.** A ficha oferecia *"ligar ou remover"*; a medição resolveu para remover, campo a campo, cada um com sucessor já gateado — `operacao` (o rótulo tinha três nomes, e os três se separaram: editor em `skill_flows.operacao`, monitor em `contacts.monitorar`, calendário em `scheduler.operacao` + `config.calendars`) · `visualizar` (Arc 19 Fase E: workflow é sessão `channel=webhook`, com filtro `suspended` sob `contacts.visualizar`/`contacts.monitorar`) · `cancelar` (a rota `/cancel` foi APAGADA em 2026-08-07; encerra-se por SESSÃO em `force-complete`, `agent_assist.supervisionar`) · `webhooks` (registro único é o `ChannelEndpoint`, `config.channels` — o ADR já carimbava esta tabela como `legacy_token`) · `journey_read`/`journey_resume` (a fila pull de aprovação no Console: `approvals.operacao` e `approvals.decide`). Saíram: 2 telas, 3 rotas de UI (viraram redirect para o sucessor, não 404), o namespace i18n, os 6 campos do catálogo e 8 rotas da workflow-api. ⚠️ **Duas afirmações da própria ficha foram REFUTADAS na medição.** (a) *"quem alcança o serviço dispara qualquer flow"* — o proxy `/v1/workflow/trigger` repassa **sem header nenhum** para uma rota do gateway que resolve pelo registro e exige `X-Webhook-Token` quando o endpoint declara `auth_required`: é repassador na frente de fechadura, não porta aberta, e ele **fica** (5 cenários e2e dependem dele). (b) *"o fail-open morre com o CRUD"* — `_require_admin` também gateia `POST /admin/backfill-events`, que **sobrevive**; o portão foi CONSERTADO para fechar (segredo ausente ⇒ 503, não 200), medido no container: nenhuma env de admin configurada, logo ele nunca recusou ninguém. ⚠️ **Remover do YAML não remove do catálogo VIVO** — `upsert_module` é upsert puro sem poda, então entrou `DDL_DROP_MODULE_WORKFLOWS` ao lado da que tira a chave do `module_config`; as duas são guardadas por PRESENÇA e rodam incondicionais. Medido depois: 2 → 0 portadores, catálogo 12 → 11 módulos. ⚠️ **`api/hooks.ts` NÃO foi apagado** — o `MonitorTab`, que é o sucessor de `visualizar`, o consome; foram podados só os exports que morreram com as telas. As 3 classes de teste de webhook foram **substituídas**, não apagadas (rota removida responde 404 **com controle positivo** de que o app segue servindo; portão fecha, com controle positivo de que o token certo atravessa), e os dois ramos novos foram provados falseáveis por mutante — um mutante, um vermelho, cada vez. A lacuna de superfície que a revisão do dono encontrou virou `APR-10` | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (15) |
| MOD-10 | **O papel `developer` virou `devops`** (E3 do ADR, adiada por sequência até a AUT-38 remover as 7 listas de `requireJwtRole` — a sobra do `mcp-server` foi zero). O preset era de **autor de fluxo** (`skill_flows.*` + `config.context_map`), papel que os DialogForms tornaram obsoleto; quem existe na operação é infraestrutura. Superfície: catálogo (5 campos + o par de `hiring_pairs`), `auth-api` (`Role` Literal, `_ROLES_VALIDOS`, docstring do emissor), seed, 7 arquivos do `platform-ui`, 4 locales — incluindo a CHAVE `bannerDeveloper` → `bannerDevops` e os textos visíveis —, 3 probes e 1 teste. ⚠️ **A troca foi por VALOR, nunca pela palavra**: a prosa que narra medições antigas (a hierarquia refutada, o default de seed) fica, porque reescrevê-la apagaria decisões que continuam valendo. ⚠️ **O dado migra por MECANISMO**: a ficha previa que re-semear bastaria, e isso é promessa — o seed é `seed-if-absent` e não passa por uma instalação cujo admin já existe; pior, papel fora do `Role` Literal atravessa a LEITURA (as respostas são `list[str]`) e só estoura no primeiro `PATCH` que reenvie `roles`. Entrou `DDL_MIGRATE_ROLE_DEVOPS` com marcador; medido: `admin@` `{admin,developer}` → `{admin,devops}` no boot. ⚠️ O `probe_role_preset_on_create.sh` ficou INCONCLUSIVO no papel antigo — a lista dele é LITERAL de propósito (é o controle do ramo que deriva do catálogo), foi atualizada à mão e o comentário agora diz por quê, para que ninguém a "conserte" derivando | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (14) |
| AUT-39 | **`config.users` era irrestrito, e a tomada LATERAL de conta foi medida ao vivo antes do conserto.** Supervisor com o campo e **ZERO pools** listou os 9 usuários do tenant, trocou a senha de um usuário de outro time (**HTTP 200**), **entrou na conta**, e a senha original deixou de valer — o dono perde o acesso. E era **silencioso**: o handler não emitia log e não havia trilha. ⚠️ **A saída por POOL foi recusada por CENSO:** sob `pools(alvo) ⊆ pools(ator)` o `admin@` ficaria sem administrador e só administrava os outros porque ENUMERA os 41 pools — *um pool novo o tiraria dessa condição sem erro em lugar nenhum* (o discriminador da AUT-29). Venceu o **organograma** (`agent_groups`, Arc 9, greenfield 0/0/0): membership explícita, e o recorte da lista vai no **SQL**, nunca depois do `LIMIT` — filtrar depois de paginar devolve página errada. O caminho do `admin` é DECLARADO, não bypass mudo: sem ele, ligar o organograma com zero grupos trancaria o dono do lado de fora. Duas fatias: a **trilha** (`auth.user_admin_log`, sem FK no alvo para o `CASCADE` não apagar o registro de quem apagou; WARNING nomeando reset por terceiro) e o **enforcement** em list/get/patch/delete. Gate `gate_user_admin_org_chart.sh` com 4 ramos — e o ramo 3 (com grupo, PASSA) é o que impede o verde por negar tudo | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (16) |
| AUT-38 | **Os 17 portões de PAPEL do `mcp-server-plughub` viraram portões de GRANT — a autorização dependia da ORDEM em que os papéis foram digitados.** `requireJwtRole` lia `payload["role"] ?? roles[0]`, e o primeiro é claim que a auth-api não emite: decidia sempre pelo PRIMEIRO papel. Medido ao vivo, mesma rota, mesmos papéis e mesmos grants: `roles=["admin","developer"]` atravessa e `["developer","admin"]` leva **403 "Insufficient role"** — nomeando um papel que o chamador TEM. `admin@` passava por sorte de ordenação. **Foi troca, nunca remoção**: 15 rotas para `agent_assist.atender` e 2 (`force-complete`, `work_queue/expire`) para `agent_assist.supervisionar`, cada uma declarando o seu campo e o seu mínimo (`read_only` em GET, `read_write` em mutação) — fato da ROTA, como no `requireAbacWrite` (MOD-06) e no `campo` do `authorize_session_scope` (MOD-07). ⚠️ **Três rotas com "supervisor" no nome ficaram em `atender`, por MEDIÇÃO**: `supervisor_state`, `supervisor_capabilities` e `inject-context` são chamadas pelo Console de todo operador (o `ClienteTab` usa `inject-context` para vincular cliente) — o nome é legado, a audiência não é supervisão. ⚠️ **`supervisor` entrou no preset de `supervisionar`** (era admin-only), senão a troca seria REVOGAÇÃO silenciosa do botão de encerrar item de fila; backfill dos 4 supervisores pela API oficial. A tela parou de espelhar a allowlist (`WorkItemsPage.canExpire` virou `perms.can`), e **dois outros leitores de papel do mesmo arquivo saíram junto** — o bypass do `approval_audit` e a política de namespace do `inject-context`, que decidia por `roles[0]` quais tags o chamador escreve. Gate: `probe_mcp_agent_assist_grants.sh`, 5 ramos (**A** estático: nenhum `roles[0]` volta e `requireJwtRole` fica sem chamador · **B** o campo abre · **C** o vizinho NÃO abre · **D** papel graúdo sem grant não abre · **E** a ordem não decide). O ramo B é o que impede a "correção" mais barata e mais errada — sem ele, remover o guard passaria em todos os negativos | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (13) |
| MOD-07 | **G6 — corte #4: `contacts.visualizar` não era exigido em backend NENHUM, e o corte trouxe o eixo que faltava.** A D6 pedia recortar Analytics por SUPERFÍCIE; medido antes de cortar, a premissa não se sustentava — a analytics-api tinha o eixo de ESCOPO (recorta linhas e sessões por pool) e **não tinha o de CAPACIDADE**, então o campo só escondia menu. Contraprova ao vivo: token com `contacts.monitorar`, **sem** `visualizar`, escopado ao pool da sessão, lia a **transcrição inteira** do contato (**200**). Recortar por superfície teria criado N campos decorativos — e eles *pareceriam* proteger o diálogo. O corte foi o que o rótulo já dizia (*"Visualizar contatos **e** transcrições"*, a regra do "e" outra vez): **`visualizar`** (listas, agregados e traços de execução: `/workflow-trace`, `/pipeline-state`) × **`contacts.transcricao`** (o verbatim: `/v1/transcript/…`, `/sessions/{id}/stream`). `authorize_session_scope` passou a receber o **`campo` de quem chama** — fato da ROTA, mesma forma do `requireAbacWrite` (MOD-06) e do `_NS_FIELD_OVERRIDES`; **capacidade decide ANTES do escopo** (recusa não paga Redis + ClickHouse) e **`module_config` ausente ≠ vazio** (serviço × usuário sem grants, que NEGA). ⚠️ **Único corte da D6 que ESTREITA a população** (`business`/`developer` ficam fora do preset), e por isso o backfill copia o que cada um JÁ TINHA: 7 portadores migrados. ⚠️ **A migração precisa do MARCADOR**, ao contrário da do corte #1 — lá a chave de origem sumia; aqui `visualizar` sobrevive, e guardar por ausência do destino RESSUSCITARIA a transcrição de quem a revogou (o defeito da MOD-04 na forma inversa). Provado: revogar → reiniciar → a revogação sobrevive. Gates: o `probe_session_content_scope.sh` ganhou o **ramo A2** (mesmo escopo do ramo verde, capacidade faltando ⇒ 403 nomeando o campo) e teve os tokens corrigidos — eles só traziam escopo, e sem isso o probe ficaria vermelho **pelo eixo vizinho**, medindo a proposição errada; mais `test_content_capability_axis.py` (6 casos: ordem provada por contador de consultas, isenção do principal de serviço, e a testemunha de que rota sem `campo` segue só no escopo) | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (12) |
| MOD-06 | **G5 — corte #3: `config.resources` deixou de ser um campo em frente a CINCO telas; o corte #2 não tinha sujeito.** Medido antes de cortar: o campo gateava as mutações de `/v1/pools`, `/v1/skills`, `/v1/channels` e `/v1/channel-endpoints` no agent-registry **e**, pelo catch-all `platform` do config-api, duas das três abas da própria tela Recursos. Três dessas telas **já declaravam campo próprio no menu** — não havia campo novo a criar, havia campo alheio a devolver. Dois defeitos medidos ao vivo com o preset REAL de cada papel: `PUT /v1/skills` com o preset do `developer` → **403** *"requires config.resources"* (o menu OFERECE o Editor de Fluxo a ele e `skill_flows.editar` existe dizendo *"Criar e editar skill flows"*, mas quem salvava era grant admin-only) e `POST /v1/channels` com `config.channels` → **403** — este último é o mesmo defeito que o config-api fechou em 2026-08-27, sobrevivendo **um store adiante** porque aquele censo perguntava pelo config-api. Hoje o campo é resolvido **por ROUTER** (`requireAbacWrite`, mesma forma do `_NS_FIELD_OVERRIDES`), a recusa **nomeia o campo que falta**, e `competency_skills`/`llm_accounts` passaram a responder a `config.resources`, alinhando as três abas da tela ao campo do seu menu. **Sem renomear campo e sem migração de dados**, e isso é medição: os 2 portadores de `config.resources` já detinham os outros dois campos. ⚠️ **O corte #2 (`workflows.operacao`) não foi feito porque não tem o que cortar**: os SEIS campos do módulo `workflows` têm zero consumidores no repositório, `/workflows` é redirect gateado por `contacts.monitorar`, e dois deles nomeiam tools (`journey_list_suspended`, `journey_resume`) removidas na Fase F do Arc 19 — dividir renderia **dois órfãos**, o defeito que o as-built do corte #1 nomeia. O que a medição achou no lugar virou a **MOD-11** (trigger de workflow sem autorização nenhuma). Gates: `probe_nav_backend_field_agreement.sh` ganhou o **segundo store** (o eixo era o mesmo, o store é que era outro) com par positivo+negativo por rota, mais a seção do Editor onde ver×escrever divergem **por desenho** — ali o que se cobra é o CATÁLOGO (todo papel com `operacao` tem `editar`), provado falseável por mutante; `abac-field-per-router.test.ts` (7 casos) e o `smoke_agent_registry_write_auth.sh`, que passou a recusar o campo VIZINHO — sem esse caso, reverter o `app.ts` não ficaria vermelho | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (11) |
| MOD-05 | **G4 — corte #1: `contacts.operacao` cortado, e o campo estreito JÁ EXISTIA órfão.** O rótulo listava dois fatos com detentores diferentes (*"Monitor em tempo real, Agent Assist"*): o supervisor **observa**, o operador **atende**. O plano da D6 pedia `contacts.atender`; medir os consumidores antes de cortar revelou **`agent_assist.atender`** — *"Receber e atender contatos"*, declarado no catálogo desde sempre e **sem uma entrada de menu sequer**. Criá-lo de novo deixaria dois campos para o mesmo fato, com o velho continuando órfão e **indistinguível de campo em uso**; o corte repontou os consumidores (Console e fila de trabalho → `agent_assist.atender`; Monitor de sessões/agentes/pools → `contacts.monitorar`) e deu ao órfão o consumidor que faltava. Isso **dá alvo declarado à AUT-38**, que mirava esses mesmos órfãos. Migração **presence-guarded** (`WHERE (module_config->'contacts') ? 'operacao'`), portanto executada incondicionalmente — o oposto das absence-guarded que desfaziam a MOD-04 a cada boot; `read_write` vira `monitorar` **e** `atender`, abaixo disso só `monitorar` (quem observava não ganha o direito de atender por causa de um rename). Medido ao vivo: **6 portadores migrados, 0 restantes**. Preset e seed mudaram juntos (`_seed_vs_preset.py` verde). ⚠️ **No caminho, um fail-open da MOD-02**: `CreateUserRequest.roles` tem default `["operator"]` e o preset é aplicado em toda criação, mas o guard rodava sob `model_fields_set` — **o corpo mais CURTO era o que passava**, e um delegado com apenas `config.users` criava um operator completo (201) enviando o mesmo papel explicitamente (403). O discriminador vale no PATCH; na criação **não existe "não conceder"**. O `POST /users` passou a chamar o guard incondicionalmente, com par negativo+positivo em unidade e os ramos **N2b/N2c** do probe (delegado MÍNIMO, porque o delegado do probe cobre o preset e não veria o buraco). Dois testes que afirmavam a premissa errada foram **trocados, não apagados** — um deles passava pelo próprio buraco. ⚠️ E o `smoke_config_usuarios_auth.sh` estava vermelho por mintar **`config.usuarios`**, campo em português já retirado: as quatro asserções positivas viviam vermelhas e as negativas passavam pelo motivo errado | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (10) |
| MOD-09 | **G2 — criação por template, com PROVENIÊNCIA carimbada.** `POST /auth/users/from-template/{id}`: **a capacidade vem da linha do template, nunca do corpo** (D3) — `CreateUserFromTemplateRequest` não declara `roles` nem `module_config`, o que fecha o caminho da cópia-no-cliente (o formulário mandava a capacidade num `POST /users` comum e o servidor não sabia que havia template). **`accessible_pools` fica no corpo, e é a exceção com razão** (E4): o template **não** carrega pools — pool é do APLICADOR, escolhido dentre os seus e conferido pelo guard de RANK, de modo que um mesmo template *"Operador"* serve todos os supervisores e a pergunta *"qual template para qual time"* deixa de existir; template com `accessible_pools` (formato antigo da tela) tem o campo **ignorado com log que diz isso**, em vez de aplicado em silêncio. **Proveniência**: `created_from_template_id` + **hash canônico** do que foi aplicado, gravados no **INSERT** (carimbo em `UPDATE` posterior pode faltar, e carimbo que às vezes existe não responde auditoria), imutáveis e **nunca consultados para autorizar**. O hash é o que faz o carimbo valer: sem ele *"veio do template X"* não distingue quem nasceu do X de ontem de quem nasceu do X de hoje, e o template é editável. **Sem `delegable`** — ele existia para aprovar um pacote que atravessaria o guard, e sem travessia não há o que aprovar; quem protege é o mesmo `_assert_pode_conceder` das outras portas, sobre `preset(role) ∪ module_config`. Medido ao vivo com delegado real: template no alcance → **201** com proveniência e pools do aplicador; *"Admin Total"* → **403**; pool fora do escopo → **403**. Gate: os ramos **T1..T3** do `probe_rank_grant_guard.sh` | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (9) |
| MOD-04 | **G3 — `config.permissions` revogado de quem o detinha CONTRA a declaração; censo 2A/4B/0C → 2A/0B/0C.** A regra veio do CENSO, não de lista: revoga-se a **classe B** (detém e nenhum papel declara), e o script **RECUSA** tocar na A sem `--forcar` — quem detém porque o papel declara não é deriva, e revogá-lo contrariaria a declaração em vez de reconciliá-la. Sobraram `admin@` e `probe@`, ambos papel `admin`. ⚠️ **Só foi seguro porque o backfill veio antes:** sob o guard de RANK o revogado vira delegado, e supervisor sem os campos do preset é administrador de pessoas **incapaz de contratar**. O `revoke_field.py` mede isso ANTES de escrever (`--simular-contratacao`) e imprime por alvo. Verificado **ao vivo, não simulado**: `supervisor@` sem `config.permissions` no token cria operator no próprio pool (**201**) e é barrado ao tentar criar admin (**403** nomeando o campo). ⚠️ **`useradmin@` ficou sem contratar de propósito** (faltam-lhe `contacts.operacao`/`contacts.visualizar`) — opção (a) do dono: aquela é a fixture MÍNIMA do `probe_config_permissions_split`, cujo assunto é *"administra pessoas e não pode conceder"*, e completá-la apagaria o único exemplar desse caso. A revogação **restaura a premissa** daquele probe, que aborta se o token do `useradmin@` declarar o campo. ⚠️ **CORREÇÃO no mesmo dia: esta linha esteve FALSA por algumas horas.** A revogação era **desfeita a cada boot** — `DDL_MIGRATE_ABAC_PERMISSIONS` roda em todo `ensure_schema` e é guardada pela **ausência** do destino (`NOT ... ? 'permissions'`), o que a torna idempotente contra re-execução e **não** contra revogação; e por ser SQL cru não bumpa `updated_at`, então a evidência apontava para o momento errado. Achado ao testar a MOD-09. Corrigido com marcador (`auth.schema_migrations`, linha de base MEDIDA, não presumida) e **re-verificado**: revogar → restart → `2A/0B/0C`, e sobrevive a `up -d --force-recreate` | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (8) |
| MOD-02 | **G1 — o guard de RANK nas duas portas: ninguém concede o que não detém.** `grants.py` na auth-api, predicado único para os dois caminhos de escrita. **Master** (`config.permissions: read_write`) curto-circuita **antes de tocar no catálogo** — sem isso um banco lento derrubaria com 503 quem tem direito a tudo. **Delegado** (`config.users: read_write`) concede `≤` o que detém, campo a campo, com escopo `⊆`. Isso **fecha a Costura 1**: o `_assert_may_grant` recusava em bloco qualquer campo de capacidade a quem não fosse master, e por isso o supervisor criava usuário sem poder dar-lhe papel nem pool — aquela função foi **removida**, não deixada sem chamador (código de segurança morto parece proteger). ⚠️ **Três decisões que a implementação carrega:** o guard roda sobre **`preset(role) ∪ module_config`** (senão `{role:"admin", module_config:{}}` concede 44 campos pela porta do preset) · `scope: []` = GLOBAL e `accessible_pools: []` = NENHUM, semânticas **opostas** no mesmo formulário · e `atual` faz o guard julgar o **AUMENTO**, não o reenvio — sem isso o delegado não conseguiria editar ninguém que tenha um grant acima do dele, porque o `PUT` substitui o config inteiro. ⚠️ **Dois defeitos achados por teste vermelho durante a entrega**, ambos falha-aberta: catálogo VAZIO fazia o papel expandir para `{}` e nada ser conferido (agora 503 nomeando a causa); e o master pagava o custo do catálogo. Prova: **21 testes de unidade** do predicado + **90** na suíte do serviço + `probe_rank_grant_guard.sh` na **borda**, com 1 positivo e 3 negativos nas duas portas — ele existe porque, com o predicado já verde, a medição ao vivo ainda mostrou um `admin` sendo criado: predicado correto e rota guardada são dois fatos | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (5) |
| MOD-08 | **G1b — os presets deixaram de ser engenharia reversa, e o par de contratação virou dado conferido.** O guard de RANK da MOD-02 sozinho produzia **a diagonal**: cada papel só criava um clone de si, e o `supervisor` **não criava um `operator`** — bloqueado em `approvals.decide`, `approvals.operacao` e `evaluation.contestar`, que o preset do operador traz e o dele não tinha. Os três foram acrescentados ao preset do supervisor (13 campos agora) **e ao seed**, que `_seed_vs_preset.py` confere — as duas declarações batem sem lista de exceção. As arestas de contratação viraram **dado declarado** (`hiring_pairs:` em `infra/modules.yaml`, ao lado dos presets que elas restringem, para que editar um sem o outro caia no mesmo diff): `admin → {supervisor, operator, business, developer}` e `supervisor → operator`. ⚠️ **Não é hierarquia** — a cadeia `admin > developer > supervisor > operator` foi proposta e refutada pelo catálogo no mesmo dia; papéis são funções, e o grafo é declarado porque não é derivável de nada. Gate: `probe_hiring_pairs_subset.sh`, **estático** (a propriedade é da declaração; exigir a stack faria o gate depender de ambiente que a pergunta não usa) e com **três ramos provados falseáveis**: remover `approvals.decide` do supervisor derruba o A nomeando o campo · `access: full_access` num preset derruba o B · `hiring_pairs:` ausente vira INCONCLUSIVO, nunca verde. ⚠️ **Ele não olha grants VIVOS, de propósito**: contratante que teve grant revogado pela tela falha no runtime com este gate verde — esse eixo é do censo (classe C), e juntá-los faria um gate reprovar por uso normal. O rename `developer` → `devops` da E3 **saiu do escopo por sequência** e virou `MOD-10` | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (4) |
| MOD-01 | **G0 — o censo de `config.permissions` existe, e é genérico.** `probe_config_permissions_census.sh` + `_config_permissions_census.py` comparam **banco × catálogo DEPLOYADO** e classificam a população em três: **A** detém e o papel declara · **B** detém e nenhum papel declara · **C** não detém e o papel declara. Primeira execução: **2A / 4B / 0C** em 8 usuários. ⚠️ **Ele NÃO julga a população, e isso é decidido:** deter um campo que o preset não declara é legítimo (a tela concede depois do nascimento), e inventar um alvo antes da MOD-04 seria decidir por ela; quando ela fixar o alvo, a classe B vira asserção no helper. O que ele julga é se o censo **consegue medir** — **R1** (campo inexistente em declaração nenhuma: contar zero medindo a coisa errada é pior que não contar) e **R2** (arquivo × deployado discordando, o *"existe ≠ está aplicado"* de quem edita o YAML sem reiniciar). **Os dois provados falseáveis** (`CAMPO=config.nao_existe` → R1; YAML mutante → R2). Sem credencial ou sem serviço: INCONCLUSIVO, nunca 0 — *"nenhum portador"* é resposta do censo, *"não consegui perguntar"* não é. **Não duplica `_seed_vs_preset.py`**: aquele mede seed × catálogo, arquivo × arquivo; este mede o eixo vizinho, e repetir seria segunda casa afirmando o mesmo fato. Sem `jq` de propósito, e sem heurística de fixture ou e-mail fixo — o instrumento serve qualquer `modulo.campo` via `CAMPO=` | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (3) |
| MOD-03 | **G2 — D5 respondida: quem aplica, e sobre quais campos.** Fechada por DECISÃO, não por código: a emenda de 2026-09-08 ao ADR (E1) resolveu a D5 sem campo novo — **master** = `config.permissions: read_write` (só o `admin@`, por seed, concede qualquer campo) × **delegado** = `config.users: read_write` (concede `≤` o que detém, campo a campo, com escopo `⊆`). As três formas que a D5 deixara abertas foram descartadas com razão escrita: a opção *c* (discriminar por NÍVEL dentro de `config.users`) fundiria de volta os dois fatos que o split de 08-27 separou, e as opções *a*/*b* criariam campo para o que o `config.permissions` já declara — *"Conceder permissões (papéis, módulos, escopo de pools)"*. A implementação da UI de criação por template migrou para a **MOD-09**, junto da proveniência carimbada | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (1) |

---

## `docs/product/journey-retorno-modelo-3-niveis-design.md` — Journey

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| JRN-01 | Sinal N3 no drill da Vista Processos | 2026-07-23 | `CHANGELOG.md:17051` |
| JRN-02 | Guard de rota ABAC em `analise/*` | 2026-08-27 | `CHANGELOG.md:4533` |

---

## `docs/adr/adr-remove-agent-role-axis.md` — remoção do terceiro eixo

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| MEN-05 | **O `@alias` deixou de ser texto de mensagem: virou COMANDO com evento proprio.** Decisao do dono — o alias e traduzido em efeitos que nunca aparecem como mensagem, logo carrega-lo no `message` misturava comando com conteudo. Entregue: o alias nao e persistido em lugar nenhum; sobra a prosa (`@billing conta=X, pode conferir?` entrega *pode conferir?*), `agents_only` **para qualquer emissor** — o que fecha o vazamento que a ficha nomeava, porque o override antigo so disparava para `primary`; e alias SEM prosa nao gera mensagem nenhuma, so evento e aviso. ⚠️ **Dois defeitos latentes cairam junto, e os dois sobreviviam por falta de leitor**: `stripped_text` tinha fallback `|| text` (alias puro devolvia o texto INTEIRO) e a ordem das remocoes deixava um `conta=` orfao virar "mensagem" — o campo nao tinha consumidor de producao, entao ninguem o via errado. De carona, o metering passou a medir `effectiveVisibility`: mensagem forcada a `agents_only` nao e entrega ao cliente. 350 testes verdes, typecheck limpo nos dois pacotes | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (7) |
| MEN-06 | **A autoria do convite ganhou casa DURAVEL, e o emissor ganhou retorno.** Sem isto a MEN-05 teria apagado o unico lugar onde *quem convidou quem* existia: o `participant_joined` registra quem ENTROU, nunca quem PEDIU. Entregue: tipo novo `mention_command` no `StreamEventTypeSchema` — escrito por `routeMentions` com o EMISSOR como autor, `agents_only`, alias, pool-alvo e desfecho; e `mention.ack` publicado por alias, nomeando e enderecando o emissor, **inclusive para convite de pool** (o ack antigo do bridge so existe para `mention_commands` declarados, 1 skill em 44) **e inclusive para alias desconhecido**, que antes so ia para o `console.log` — o agente via o proprio texto na tela e nada acontecia, indistinguivel de comando aceito. A Agent Assist consome o evento novo (sem isso o emissor ficaria MUDO, que era a troca errada). ⚠️ **NAO se filtra por `recipient_participant_id` no cliente**: errar o id proprio esconderia o aviso de quem precisa dele. ⚠️ Tipo novo conferido antes: nenhum leitor entrega tipo desconhecido ao cliente (o `stream_subscriber` casa por `if` encadeado). Mutacao: trocar o `author_id` por literal reprova o teste da autoria | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (7) |
| MEN-01 | **A regra passou a dizer o que o gate faz, e o gate passou a decidir sobre identidade ASSINADA.** A ficha exigia responder *qual cenário isto impede* antes de escolher, e a resposta derrubou as tres opcoes que ela listava: o gate nunca falou de ESPECIE — ele barra `specialist`/`supervisor`/`evaluator`, isto e *quem foi convidado nao convida* —, e `role === "human"` era RAMO MORTO (dominio vivo: `primary|specialist|queue`), o que dava ao codigo a aparencia de falar de especie. O modelo do dono (*primary e quem recebeu o contato na entrada ou no escalate, humano ou IA, um so por vez*) foi medido e CONFERE: 597 sessoes com 1 primary, 456 com 2, 229 com 3, e dos 222 pares sobrepostos **220 duram ≤100 ms** (costura da passagem de bastao). **Decisao do dono (2026-09-12): o eixo e POSICAO**, e vale nos dois caminhos. Entregue: `mayRouteMentions` numa casa so (`lib/participant-role.ts`), ramo morto removido, e o gate decidindo sobre `resolveRoleByInstance` — a instancia do `session_token`, nunca o `participant_id` do input, que fechou a segunda linha da tabela de gates do ADR (o mesmo defeito da CAP-01). ⚠️ **As "4 copias" da promessa eram DEZ**: alem do `CLAUDE.md` ×2 e do guia ×3, viviam no comentario de `mention-routing.ts`, no descritivo de produto (.md e .html), num spec de journey e num ADR. 332 testes verdes, typecheck limpo, mutacao reprova | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (6) |
| MEN-02 | **A assimetria acabou: a mesma regra nos dois caminhos que roteiam mencao.** O WS do Console (`server.ts`) chamava `routeMentions` sem checagem nenhuma, por desenho declarado (*"o WS conhece o agente pela conexao"*) — e conhecer o agente pela conexao prova QUEM ele e, nao em que POSICAO esta nesta sessao. Enquanto a regra valeu numa porta so, ela nao era garantia da plataforma: era regra de uma porta, com a outra aberta ao lado. ⚠️ **O `agentRole` daquele socket foi recusado como fonte, e a razao e medida**: e um `let` de escopo de CONEXAO, derivado uma vez de `scard(human_agents) > 1` dentro de um `.then()` e sobrescrito a cada nova sessao atribuida ao mesmo socket — fato de conexao usado como fato de (sessao, participante), o "valor plausivel" classico. A fonte e o ROSTER, igual ao outro caminho. Decidida junto com a MEN-01 pelo dono | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (6) |
| CAP-18 | **As tres tools sem classificacao foram medidas e declaradas — e o motivo das outras 47 tambem estava velho.** `agent_event_record`, `dialog_tree_level` e `pool_route_resolve` entraram no censo (72 → 74) sem linha na tabela, e o gate reprovava com *"o censo MUDOU sem a declaracao acompanhar"*, que e exatamente o que ele existe para pegar. As tres foram MEDIDAS, nao presumidas: as quatro camadas (`token`/`guard`/`permission`/`audit`) saem **false** nas tres, e as tres irmas mais proximas ja classificadas (`form_get`, `pool_status_get`, `segment_outcome_record`) sao `divida` — logo `divida`, com motivo proprio em cada uma: `agent_event_record` ESCREVE KPI de negocio pelo caminho NATIVO, que nao tem token por desenho (o bridge nao emite `session_token`); `dialog_tree_level` e projecao pura que le DialogForm publicado; `pool_route_resolve` expoe o mapa de navegacao do tenant a quem souber um `session_id`. ⚠️ **De carona, as 47 linhas que diziam *"politica pendente (CAP-09)"* foram reescritas**: a politica DEIXOU de estar pendente na CAP-10 (2026-09-12), e motivo que envelhece e a mesma mentira tranquila que este arquivo existe para cacar. O cabecalho passou a registrar a decisao e o que `divida` NAO cobre: `agent_login` e auto-servico, entao as 23 linhas `token|ok` verificam um token que qualquer um cunha. Censo: `ok=23 · isento=1 · divida=50 · total=74`, 5 ramos verdes; mutacao (remover uma das tres linhas) reprova | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (5) |
| CAP-01 | R1 — os dois `if` de `agent_role` fora; smoke invertido em testemunha | 2026-09-01 | `CHANGELOG.md:3` |
| CAP-02 | R2 — procedência do `evaluation_submit` passa a vir do `agent_type_id` do token assinado | 2026-09-01 | `CHANGELOG.md:3` |
| CAP-04 | Validação de que o `evaluator_pool` da campanha roda um flow de avaliação (derivada do artefato) | 2026-09-01 | `CHANGELOG.md:3` |
| CAP-03 | R3 — `agent_role` fora de 5 pacotes + coluna dropada por migração; PUT recusa o campo com 422 nomeando | 2026-09-01 | `CHANGELOG.md:37` |
| CAP-05 | A borda `invoke` deixou de negar 100% das chamadas — o default `permissions: []` era `deny`, não permissivo | 2026-09-01 | `CHANGELOG.md:3` |
| CAP-06 | `permissions[]` ganhou produtor: o `tools[]` da skill é mapeado e ASSINADO no `session_token` | 2026-09-01 | `CHANGELOG.md:3` |
| CAP-09 | Censo das 72 tools por camada de guarda virou gate; duas promessas falsas corrigidas (`deploy.ts`, `CLAUDE.md` do pacote) | 2026-09-01 | `CHANGELOG.md:3` |
| CAP-11 | Superfície de rede do mcp-server medida: a borda existe (nginx no Dockerfile do platform-ui), não publica o transporte MCP, e publica 20 rotas `/api/*` | 2026-09-01 | `CHANGELOG.md:3` |
| CAP-12 | As 9 rotas `/api/*` publicadas pela borda passaram a exigir credencial; o decode-sem-verificar do `verifyJwtPayload` saiu junto (era um portão que não podia reprovar onde o segredo faltava) | 2026-09-01 | `CHANGELOG.md:3` |
| CAP-13 | A 3100 passou a publicar em loopback nos dois composes — o transporte MCP anônimo estava aceitando conexão no IP de LAN, medido | 2026-09-01 | `CHANGELOG.md:3` |
| CAP-16 | Os três chamadores de `/api/conversation_history` pararam de converter falha de leitura em histórico vazio: carregador único, motivo na tela e releitura que falha não apaga mensagem boa | 2026-09-01 | `CHANGELOG.md:3` |
| CAP-19 | **O `/agent/ws` só atende agente com credencial do pool — e só lhe dá as sessões dele.** Antes, sem token e pela borda da 5174: `user_id` inventado virava instância que recebia contato, e `?session_id=` alheio lia os eventos de agente e escrevia no stream como atendente. Hoje a credencial viaja no subprotocolo (`plughub.bearer`, porque o browser não põe `Authorization` em WebSocket e JWT na URL vai para log), é conferida NO UPGRADE por `lib/agent-ws-auth.ts` (identidade = `sub` assinado; `user_id` divergente recusa; `agent_assist.atender` read_write cobrindo o pool), a recusa fecha com 4401/4403 e o motivo, e o `session_id` de reconexão só é assinado se o agente está em `session:{id}:human_agents`. Console (os dois hooks) manda o token e não reconecta em 4403. Vermelho ao vivo antes: 7 de 7 recusas faltando. Gate `probe_agent_ws_credential.sh` (W1–W8 + controles W6/W7b/W9); unitário com mutação (5 mutações, todas reprovam) | 2026-09-15 | `CHANGELOG.md` § 2026-09-15 (20) |

---

## `docs/adr/adr-agent-flow-single-authored-level.md` — um nível autorado

*(grupo aberto em 2026-09-03 com a ADR; nada fechou ainda.)*

| id | tarefa | data | ancora no `CHANGELOG.md` |
|---|---|---|---|
| NIV-01 | Capacidade de canal passa a ter **uma casa** (`channel_capability_registry`, em código, porque é fato do PROTOCOLO) e a política de fallback fica no tenant (`MaskedFallbackPolicySchema`) — a segunda casa era `ChannelCapabilitiesSchema`, com vocabulário DIFERENTE, zero consumidores, e **já discordava em `voice`**. A tabela virou exaustiva sobre `ChannelSchema` (6 → 9 canais; `instagram`/`telegram` nunca eram eleitos, em silêncio) | 2026-09-03 | `CHANGELOG.md` § 2026-09-03 NIV-01 |
| NIV-02 | A exigência de canal do `collect` passa a ser **DERIVADA** da declaração `masked:` do `DialogForm` (um sítio: `collect_requirements.py`) **e** a eleição VIVA (`WebhookAdapter._negotiate_channel`) passa a consultá-la — inclusive no ramo do `channel:` fixo. Medição refutou a premissa da ADR: o portão que se ia alimentar (`select_channel`) está em ramo MORTO, e a eleição que roda era cega, descartando o `requires` que já chegava até ela | 2026-09-03 | `CHANGELOG.md` § 2026-09-03 NIV-02 |
| NIV-03 | Recusa em DOIS momentos. **Runtime**: `notification_send` não publica menu mascarado em canal sem `masked_input` — ANTES do Kafka, e o `isError` vira `on_failure` na hora pelo caminho que já existia. **Deploy**: `set-next`/`promote` recusam (422) skill que mascara em pool sem NENHUM canal capaz, e AVISAM no parcial; rollback isento. Para isso o mapa canal→capacidade virou canônico em `@plughub/schemas` com o gêmeo Python sob gate de paridade — **refina a NIV-01, não a reverte**: a casa única precisa ser legível por toda linguagem que decide | 2026-09-03 | `CHANGELOG.md` § 2026-09-03 NIV-03 |
| NIV-04 | O roteiro sai do YAML: **mecanismo** (`form_get` passa a expor `render.by_node`, `node_id → texto`) + **piloto** (`skill_limite_entrada_v1`, 12 frases estáticas → referência à forma `dialog_limite_roteiro`). Sem o endereçamento por nó a migração era inviável: o `render` é single-turn e os avisos vivem em ramos diferentes, então cada frase pediria uma forma e um `invoke` — ~79 de cada, quase dobrando os fluxos. Provado com CONTATO REAL: a saudação entregue é a da forma, e com o dialog-api parado sai a frase de degradação em vez de silêncio | 2026-09-03 | `CHANGELOG.md` § 2026-09-03 NIV-04 |
| NIV-05 | **`masked_input` passa a ser definida pela GARANTIA, não pelo mecanismo** — redação fechada com o dono em 2026-09-03 (*"o canal coleta entrada sem que o valor apareça em nenhuma superfície de leitura controlada pela plataforma — transcript, histórico durável, e gravação quando a plataforma grava"*), aplicada quando o segundo canal a reivindicou: o webrtc, por campo protegido no widget e descarte da fala e do texto livre durante a coleta (VOZ-05 fatia A). O enum dizia `password-overlay masked field (webchat)` | 2026-09-15 | `CHANGELOG.md` § 2026-09-15 (25) |
| MSK-01 | Campo `masked` chegando a canal que não sabe mascarar deixa de ser silencioso — fechada pela metade de RUNTIME da NIV-03, não pela mitigação barata (tirar `whatsapp` de `limite_ia`). O desfecho é melhor que o previsto: em vez de um pool consertado, **todo** pool ficou coberto, e a exposição some sem depender de config | 2026-09-03 | `CHANGELOG.md` § 2026-09-03 NIV-03 |

## `docs/guias/masked-input.md` — mascaramento de entrada por canal

*(grupo aberto em 2026-09-03 com a MSK-01; nada fechou ainda. O lugar de pousar existe
antes de a tarefa fechar — e a mudanca so e conferivel se o destino ja estiver la.)*

| id | tarefa | data | ancora no `CHANGELOG.md` |
| MSK-02 | **Estava SUPERADA, e provar isso achou a segunda casa.** A ficha dizia *"o mesmo número de cartão chega mascarado num caminho e CRU no outro"*, com dois escopos em aberto — declarar `masked` no campo, ou entender a detecção. **Nenhum dos dois era o trabalho:** o mecanismo saiu no MESMO dia, pelo arco CTX (CTX-04/CTX-07), e a linha nunca foi atualizada — segunda casa afirmando defeito que o código já não sustenta. Prova em duas formas independentes: o ramo F do gate exercita a tag EXATA contra o `dist` deployado (cliente `***4444` × sistema cru), e o stream durável mostra a população crua parando em **2026-09-04** (14 linhas, 2026-08-13→09-04, **zero** depois) enquanto a mascarada segue até hoje — o registro mostrando que deixou de acontecer, não o gate dizendo que funciona. ⚠️ **Finding novo:** `steps/suspend.ts` tem interpolador PRÓPRIO que não passa pelo filtro de plateia; população medida **ZERO** (5 `suspend`, nenhum com `notify`), então virou o **ramo I** do gate, com mutação nos dois sentidos, em vez de mecanismo contra população zero | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (12) |
|---|---|---|---|

## `docs/adr/adr-context-read-audience-policy.md` — leitura de contexto por plateia

| id | tarefa | data | âncora no `CHANGELOG.md` |
|---|---|---|---|
| CTX-01 | **O censo estrutural REFUTOU o ADR escrito horas antes — 20 viraram 2.** O ADR dizia que 10 dos 20 pontos sensiveis eram `credential` em link legitimo; era inferencia a partir do TIPO, sobre um censo por regex. O parse estrutural (que sabe em que step cada interpolacao esta) mostrou que aqueles `credential` vivem em steps `invoke` — plateia `system`, nao chegam a cliente nenhum. Sobram **2**, e sao exatamente as duas da tela do dono. A regex responde *"a tag APARECE no arquivo?"*; a pergunta era *"a tag CHEGA a alguem?"* — uma ordem de grandeza de diferenca. O ADR foi corrigido no lugar, com o motivo. A tabela de excecoes nasceu VAZIA (as seis linhas viraram orfas e o proprio ramo C as acusou). Retrato: 88 interpolacoes, 7 ao cliente, 2 bloqueariam, 6 tags fora do mapa contadas e nao decididas | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 CTX-01/CTX-02 |
| CTX-02 | **A auditoria de plateia roda no runtime, e ainda nao aplica nada.** `interpolate` ganhou o parametro `sitio` (opcional de proposito) e os tres sitios de plateia o declaram; a auditoria nao e aguardada (observar nao entra no caminho critico) e nunca lanca (auditar nao pode derrubar atendimento). Faltava `CONFIG_API_URL` no `skill-flow-service` — sem ela nao roda, e o log vazio seria indistinguivel de parque limpo; o aviso NOMEIA o que deixa de valer em vez do *"using default values"* que ninguem leu no bridge. ⚠️ **Tres provas saíram enganosas e as tres viraram conserto:** o smoke saiu mudo (consistente — aquele flow nao tem `notify`), a mutacao M1 deu verde (o gate fazia `grep` no compose INTEIRO e outro servico declara a mesma env), e o ramo D deu vermelho FALSO (media alcance com `wget`; hoje usa o `fetch` do proprio engine). Garantia re-executavel: 7 testes com controle positivo | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 CTX-01/CTX-02 |
| CTX-03 | **A F2 nao tinha conteudo, e descobrir isso foi o resultado.** A tarefa era a excecao declarada; a populacao dela era ZERO sob o eixo antigo e 1 sob o novo — e esse 1 (`limite_aprovado` -> `financial` -> `R$ ****,**`) nao era excecao, era **declaracao errada**. A decisao do dono fechou o desenho: quem compoe um `notify` e um AGENTE, e agente so mostra o que ele proprio ve, entao `by_role.operator` e o TETO por construcao e `by_role.customer` nunca precisa existir. O que faltava era tipo para *"valor que o processo existe para comunicar"* — consulta de saldo que mascara o saldo nao e produto. Criado `valor_informado_ao_cliente` (2a ponta do eixo de `valor_declarado_pelo_cliente`, cujo criterio de assimetria respondia *"e dado do titular?"* e nao *"o processo existe para comunicar?"*) e retipadas 3 tags. Censo 3 -> 2. ⚠️ Nome e FINALIDADE, nunca formato: `moeda` daria a permissao a todo valor futuro por PARECER dinheiro | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 (3) |
| CTX-07 | **O masker mudou de casa para que o engine o alcançasse — mover, nunca copiar.** `applyMaskingTypeToValue` vivia em `mcp-server-plughub/src/lib/context-masking.ts`, e a CTX-01 deixou o engine sabendo DECIDIR qual mascara aplicar (`resolveMaskForAudience`) sem conseguir APLICA-LA: ele nao importa o mcp-server, e nao deve — a dependencia e na direcao contraria. Copiar faria a QUARTA casa de mascaramento, que e o que o comentario do `_build_pending_preview` existe para impedir. Hoje o corpo mora em `@plughub/schemas` e o mcp-server REEXPORTA. ⚠️ O compilador pegou que `export … from` reexporta mas nao traz o nome ao escopo local — e so pegou porque aquele arquivo LE a funcao; quem apenas a repassasse ficaria verde. O runner de paridade foi reapontado para a casa canonica: importar da que reexporta continuaria compilando e passando, medindo o repassador em vez de quem decide. Provado do engine: cliente `***4444`, sistema o numero cru (controle positivo). Bateria ganhou a M4, no masker | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 (4) |
| CTX-04 | **F3 — o leitor passou a APLICAR em `notify`/`menu`, e e UM so.** `filtrarLeituraCtx` SUBSTITUI o valor no `interpolate` (§D1: nada de `getMasked()` ao lado do `get()`), e e AGUARDADO — a auditoria da CTX-02 era fire-and-forget de proposito, mas um filtro que a resposta nao espera nao filtra nada. `auditarLeituraCtx` foi REMOVIDA: manter as duas seria duas implementacoes da mesma regra, que e o defeito que este arco existe para corrigir. NAO aplica `undecided` (§D5, plateia `model`) nem `unknown` (§D4, e a V4 da allowlist) — os dois sao LOGADOS com o motivo. Catalogo indisponivel deixa passar CRU e o carregador grita nomeando: mascarar tudo faria toda mensagem virar `***` por queda de config. ⚠️ O compilador forcou `maskChangesValue` a virar type predicate — como `boolean` ele exprimia a abstencao sem prova-la, e `undecided` podia chegar ao masker e virar `***`, aplicando justo a decisao de que o ADR se absteve. ⚠️ Gate ganhou o ramo **F**, que separa CODIGO de IMAGEM: no dia da entrega o container ainda servia a versao so-auditoria. Provado VIVO contra o catalogo real (cliente `***4444` x sistema cru) e falseado por mutacao no dist deployado | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 (5) |
| CTX-05 | **F4 — o trabalho era CONFIRMAR, e confirmar produziu duas correcoes.** No `invoke`: a §D2 dizia que o cru era aceitavel porque havia gate (`AuditPolicy.data_categories`) — medido, **zero** tools o declaram, **zero** ocorrencias no mcp-server, e o unico leitor e o `McpInterceptor`, que nunca e instanciado. O cru continua (o CRM precisa do numero); o que sai e a citacao de um gate inexistente (§D10, virou CTX-09). No `reason`: **populacao ZERO** — 6 interpolacoes, 5 tags, 3 `texto` aberto e 2 fora do mapa, nenhuma de tipo que mascare. Decidir a §D5 agora seria politica contra populacao zero, o erro que o repositorio ja registrou; entao o FATO virou gate (ramo G), que reprova quando deixar de ser zero. ⚠️ O ramo existe porque `mudaria` nao cobre `model` (a plateia devolve `undecided`), e um censo que so a olhasse concluiria que nao ha risco num prompt. Falseado ao vivo retipando uma tag do prompt, com restore conferido | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 (6) |
| CTX-06 | **F5 entregue pela REDE, nao pelo carimbo — e o desbloqueio veio de uma medicao.** O censo estrutural corrigiu o numero (228 regex -> 142 estruturais -> **35** ao cliente, 30 chaves; 16 das 35 sao texto de DialogForm ja renderizado) e o bloqueio parecia ser o carimbo: `pendencia.context.*` ja nasce mascarado e re-mascarar daria `*****4444**`. **A rede de deteccao e IDEMPOTENTE** (medido nos 4 tipos: `***4444`, `***.***.***.--`, `(##) ****-4321`, `m***@exemplo.com` nao casam), entao aplicar sobre valor ja mascarado e no-op — o carimbo caiu de pre-requisito para divida de precisao (CTX-10). A rede mudou de casa do `sdk` para `@plughub/schemas` (mover, nunca copiar — mesma manobra da CTX-07) e o sdk reexporta. ⚠️ Ela e **MITIGACAO, nunca cobertura**: 4 de 15 tipos, so por FORMA, e o gate AFIRMA o limite (sensivel por contexto passa inteiro) justamente para nao virar anestesico | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 (7) |
| CTX-11 | **A rede mascarou o ROTEIRO publicado, em voo — e o carimbo de proveniência voltou.** Contato real do `limite_ia`: a forma declara `ex: 52998224725` e o cliente recebeu `ex: (##) ****-####`, com a rede ainda **tipando errado** (o padrão de CPF exige pontuação; 11 dígitos crus casam o de TELEFONE). População com corte limpo: **102 íntegros até 2026-09-04 15:56 · 15 mutilados de 2026-09-04 20:55 a 2026-09-10 11:05**, zero sobreposição, a fronteira sendo o dia em que a F5 entrou. ⚠️ **A premissa que falhou não era falsa — era de outra pergunta:** a idempotência responde *'a rede estraga o que JÁ foi mascarado?'*, nunca *'estraga o que NUNCA foi dado de cliente?'*. Conserto: `DECLARED_CONTENT_TOOLS` (allowlist de TOOL, nunca de chave) + carimbo gravado na MESMA chamada do valor por um escritor único, e **removido por construção** quando outro escreve a chave; ausência é restritiva e a isenção é REGISTRADA. Falseabilidade: 18 unitários com controle par a par e um caso de PONTA A PONTA (sem ele, uma linha esquecida no engine deixaria os outros verdes), 5 mutações; ao vivo, contato de webchat com a frase byte a byte; `smoke_limite_tres_acessos` seguiu 19✅/0❌. Gate: `probe_declared_script_integrity.sh` julga o ENTREGUE e separa exposição de dano — época anterior ao conserto o deixa **vermelho com exatamente as 15** | 2026-09-10 | `CHANGELOG.md` § 2026-09-10 (3) |

## `docs/adr/adr-dialog-input-format-catalog.md` — catalogo de formatos de entrada

| id | tarefa | data | âncora no `CHANGELOG.md` |
|---|---|---|---|
| FMT-05 | **F4 — Console e pagina web passam a GUIAR e JULGAR.** Afordancia (mascara, teclado, comprimento) e veredicto de dois niveis nas duas superficies, com o catalogo lido do STORE e nao do default embutido. No Console o veredicto viaja no `actionsState` (`formatoInvalido`), porque a barra pode ser SUBSTITUIDA por um overlay que senao enviaria o que ela bloqueia — mesmo desenho do `closedElsewhere`. Na pagina o catalogo vem no GET e **nao e congelado no token**: ele muda entre criar o link e responder. So julga o que tem VALOR (vazio e assunto de obrigatoriedade), e a mascara nao briga com quem digita. Gate `probe_dialog_format_surfaces.sh` — **79 vetores x 3 implementacoes**, vetores vindos do CATALOGO e nao do teste, falseado dos dois lados. E o que separa a duplicacao declarada da §D2 do `evaluateAskWhen` triplicado | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 F4 do catalogo de formatos |
| FMT-04 | **F3 — `pattern` saiu do schema, e o fail-open saiu junto.** Censo re-conferido antes de apagar: 0 de 12 publicadas, 0 semeadas, 0 YAMLs; tres consumidores, todos removidos. As tres linhas que saíram do `validateFormat` continham um **fail-open** — o `catch` de regex invalida caia FORA do `if`, entao a funcao retornava `true` e um `^[0-9{6}$` errado liberava tudo, sem log. Remover apaga o defeito **por construcao** e tira regex de terceiro do event loop compartilhado. O campo livre do editor virou **seletor do catalogo lido do STORE** (nao do default embutido), com cache por PROMESSA para as N perguntas compartilharem uma requisicao. Corrigida de passagem a TERCEIRA ocorrencia do tipo espelhado atrasado (`dialog-hooks.ts` sem `format` e com `masked?: boolean` onde o canonico e `false | string`) | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 F3 do catalogo de formatos |
| FMT-08 | **O checkbox de mascaramento virou SELETOR DE TIPO, e a D8 ganhou recusa.** O `MaskedDeclarationSchema` e `false | string` desde a T1 e a referencia tipada ja era usada (`card_cvv`), mas vinha de JSON semeado a mao: o checkbox so emitia `true`, que significa `opaque` (*mascarado SEM tipo*) — **o ADR que aboliu a declaracao anonima deixava a unica superficie de autoria emitindo justamente a anonima**. `opaque` continua, agora como opcao NOMEADA. Junto entrou a D8: formato DERIVADO trava o seletor (o campo nomeia o tipo uma vez), o editor MOSTRA conflito vindo de fora da tela, e o **publish devolve 422** nomeando os dois lados. O guarda cobre pergunta E campo, e le o catalogo embutido com assimetria deliberada — conflito de formato acrescentado pelo tenant **passa despercebido, nunca o contrario**, porque guarda que inventa conflito bloqueia forma valida | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 F3 do catalogo de formatos |
| FMT-03 | **F2 — o engine julga, e validar deixou de depender de retry.** (D5) `retryEnabled = maxAttempts > 1 && !!validation` gateava a PROPRIA validacao, entao declarar regra sem retry nao validava nada — `dialog_nps_v1` tinha `{numeric, 0..10}` inerte desde que foi escrito, e das duas formas publicadas com validacao **uma nao valia nada**. Agora a validacao sempre roda e `retry` so decide o desfecho, que e LOGADO. Populacao contada antes de virar: 1 forma, sem consumidor. (catalogo) `validateFormat` resolve `format` antes dos campos estreitos — um `max_length` da pergunta APERTA o do formato, nunca o afrouxa. (D6) faltavam as DUAS metades: `MenuField` nem tinha `validation` (agora shape compartilhado com o step, para nao divergirem) e o `RenderField` a descartava (agora denylist). `judgeAnswer` julga `form` campo a campo e NOMEIA o campo que reprovou. Gate de 6 ramos que pergunta a **IMAGEM** e nao ao fonte, porque o Zod descarta campo desconhecido em silencio | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 F2 do catalogo de formatos |
| FMT-02 | **F1 — o catalogo `dialog.formats` existe, com uma casa so.** 13 formatos em `@plughub/schemas/dialog-format.ts`, escolhidos por disciplina (so o que tem razao de existir hoje — `cep`/`cnpj` ficaram de fora). Cada entrada carrega AFORDANCIA e VEREDICTO de dois niveis, com `shape` ancorado exigido pelo oraculo e `semantic` de conjunto FECHADO. Entrada com `from_masked_type` **nao declara mascara propria**: herda de `masking.types` e o oraculo reprova quem declarar as duas — e o que torna provavel por construcao que a mascara de DIGITAR e a de REDIGIR sejam a mesma string. Formato desconhecido **RECUSA** (oposto do `catch` que liberava tudo). Gemeo Python GERADO, e o pacote que o hospeda teve o escopo **declarado** em vez de deduzido. Namespace ABAC aponta para `dialog_forms`, nao para o catch-all `platform`. Gate de 5 ramos com os 4 reprovaveis falseados por mutacao | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 F1 do catalogo de formatos |

## `docs/adr/adr-dialog-tree-options.md` — opcoes em ARVORE no DialogForm

| id | tarefa | data | âncora no `CHANGELOG.md` |
|---|---|---|---|
| DTO-01 | **F0 — `flattenBlocks` lossless.** Salvar um DialogForm no editor descartava `capture.kind`, porque o flatten reescrevia o `capture` por ALLOWLIST (`{ metric }`) e a lista nao cresceu quando o schema canonico ganhou o campo. Como `deriveAgentEvents` exige `kind` E `metric`, abrir e salvar desarmava a metrica Arc 12, com vestigio so num `console.log`. A perda era invisivel ao compilador: o tipo espelhado na UI tambem nao tinha `kind`. Allowlist virou DENYLIST (solta `dimension_id`/`weight`, preserva o resto), e o gate exercita o round-trip sobre o form PUBLICADO real | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 wrap-up Arc 12 |

## `docs/arcos/arc12-agent-business-events.md` — eventos de negocio do agente

| id | tarefa | data | âncora no `CHANGELOG.md` |
|---|---|---|---|
| EVT-01 | **O wrap-up real passa a emitir captura Arc 12.** O `dialog_wrapup_arc12_v1` existia, publicado e com o caminho inteiro provado desde 2026-08-30 (9 eventos no ClickHouse, todos do smoke), mas nenhum hook o referenciava — era fixture. Trocado o `dialog_form_id` do hook `on_human_end` de `retencao_humano`, que e **config de pool, nao deploy** (o skill de wrap-up e generico e compartilhado; qual form usar e fato de QUEM invocou). Semente `tenant_demo.yaml` alinhada junto, para o proximo `--wipe` nao reverter em silencio | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 wrap-up Arc 12 |
| EVT-02 | **`checklist` era single-select nas duas superficies de formulario** (Console `DialogFormRenderer` e pagina web de survey): o tipo `Record<string,string>` tornava a segunda marcacao irrepresentavel, entao os tres `interaction` de opcao compartilhavam um ramo single-select. O consumidor (`deriveAgentEvents`) e o smoke ja tratavam array — o produtor humano e que nao. Zero marcacoes REMOVE a chave, nunca grava `[]`, porque o avaliador de `ask_when` testa vazio como `undefined|null|""` e um array vazio faria pergunta nao respondida parecer respondida. ⚠️ Sem gate automatizado na metade de UI: `platform-ui` nao tem runner de teste, e adicionar um e decisao transversal — ausencia DECLARADA em vez de gate de `grep`, que daria conforto sem evidencia | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 checklist |
| EVT-03 | **`/reports/agent-events/series` devolvia 500 COM dado e 200 SEM dado.** `date` nao e subclasse de `datetime` — e o contrario —, entao `_rows_to_dicts` (casa unica de conversao) tratava `datetime` e deixava passar o `toDate(emitted_at) AS period`, que chegava cru ao `JSONResponse` e levantava `TypeError`. As irmas (`/summary`, `/categories`) usam `DateTime64` e por isso nunca mostraram o defeito; o `format=csv` tambem passava, porque nao serializa JSON. ⚠️ **O modo de falha e invisivel em instalacao limpa** — sem linha o endpoint responde 200 e qualquer smoke test passa: e a familia do teste que nao pode reprovar, e por isso o teste que acompanha carrega uma LINHA e usa o proprio `json.dumps` como veredicto (asserir `isinstance(str)` deixaria passar um `repr`). Tres mutacoes vermelhas: remover o ramo · inverter a ordem (o `date` engoliria todo `datetime` e apagaria a HORA de toda a casa — dai o controle positivo) · serializar o dia como meia-noite UTC, instante que ninguem mediu. Suite completa 754/754 | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (13) |

## `docs/guias/mention-protocol.md` — protocolo @mention

| id | tarefa | data | âncora no `CHANGELOG.md` |
|---|---|---|---|
| MEN-07 | **Texto do cliente deixou de poder virar interrupção do fluxo.** As interrupções de @mention viajavam na fila da resposta do cliente e eram reconhecidas por `JSON.parse` do texto; reproduzido ao vivo (o cliente digitou a interrupção e o fluxo saltou). Sinal da plataforma mora agora em `menu:signal`, escrito só pelo bridge; nada em `menu:result` é interpretado. Gate `probe_menu_signal_contract.sh` | 2026-09-16 | `CHANGELOG.md` § 2026-09-16 (4) |
| MEN-03 | **O aviso do gate de @mention apontava para a casa ABANDONADA do campo.** Dizia 'hash `{t}:agent:instance:{id}` sem campo `role`', mas `resolveParticipantRole` le o ROSTER `session:{id}:participants` desde a Fatia B do §1055 — o hash citado tem 0 de 5 com o campo, nenhum escritor e nenhum leitor. Mensagem que nomeia a casa errada custa a mesma investigacao que a ausencia de mensagem, com a agravante de parecer pista. O aviso tambem deixou de re-diagnosticar: o resolvedor ja loga `[role]` com o motivo especifico (roster ausente x participante fora do roster), e duas explicacoes para a mesma falha fazem a generica vencer. O comentario do gate, que prometia a mesma casa errada, foi corrigido junto e passou a registrar o achado MEN-01 (o gate testa `primary`, que e POSICAO e nao especie) | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 CNS-20/MEN-03 |

## `docs/arcos/session-replayer.md` — o que o avaliador de qualidade lê

| id | tarefa | fechada em | âncora |
|---|---|---|---|
| RPL-01 | **O avaliador de qualidade passa a ver o que o CLIENTE escreveu.** Medido: das 252 sessões fechadas em 7 dias com mensagem, 4 íntegras no que o avaliador lê — 70 sem nada persistido e 178 persistidas sem o cliente. Duas causas empilhadas no bridge: a mensagem do cliente ia ao stream só com campos flat, que o Stream Persister e o replayer não leem (linha com autor nulo e payload `{}`), e o `DEL` do stream no fechamento vencia a corrida contra o persister (`0 events persisted`). Hoje: layout canônico do `writeStreamEntry` numa casa só (`customer_message_stream_fields`) e o stream fica 1 h depois do fechamento; o gateway decide *sessão encerrada* pelo marcador `session:{id}:closed`, não pelo sumiço do stream. Gate `probe_replay_customer_text.sh` (ramos IA e humano), mutação ao vivo nas duas metades; 14 unitários, 8 mutações mortas. Histórico: `RPL-02` | 2026-09-16 | `CHANGELOG.md` § 2026-09-16 (1) |

---

## `docs/arcos/platform-ui.md` — platform-ui

| id | tarefa | data | âncora no `CHANGELOG.md` |
|---|---|---|---|
| PUI-02 | **Tela de configuração do webhook que ninguém lia, removida.** `WebhookConfigPage` gravava HMAC, lista de IPs e timeouts no namespace `webhook` do config-api: inalcançável na tela (a sub-aba só existia para webchat), nenhum leitor no backend, namespace vazio ao vivo. Ligá-la daria controle de segurança que não faz nada — a credencial real do webhook é por endpoint (`auth_required` + token) | 2026-09-17 | `CHANGELOG.md` § 2026-09-17 (4) |

## `sem-demanda`

| id | tarefa | data | âncora no `CHANGELOG.md` |
|---|---|---|---|
| GAT-04 | **`probe_ts_suites.sh` não enxergava suíte que falha ao COLETAR — e o conserto do instrumento achou um segundo caso que a ficha não conhecia.** O gate lia só a linha `Tests` do vitest; um arquivo que estoura no import executa **zero** testes, então não entra em contagem nenhuma — `Test Files 1 failed | 18 passed` com `Tests 259 passed` virava *"nenhum vermelho"*. É a família *"uma lista parece completa por ser uma lista"*, do lado do RUNNER. **Os dois consertos, na ordem que prova:** (a) o probe passa a ler `Test Files` e **ficou vermelho sozinho**, apontando `navigation.test.ts` (mcp-server) **e** `dialog-return.test.ts` (schemas) — a ficha só conhecia o primeiro; (b) `infra/test/fixtures/` passou a ser copiada para as **duas** imagens que rodam essas suítes. ⚠️ **A fixture não migrou para dentro do pacote de propósito**: os vetores são lidos por TypeScript e Python de caso pensado, para que um caso novo obrigue as duas casas a concordarem — duplicar o arquivo recriaria a divergência que ele existe para pegar. **Resultado medido: 848 → 900 testes**, 52 que nunca rodavam. Falsificado por mutação: escondida a fixture, o probe acende nomeando COLETA; restaurada, volta a verde | 2026-09-09 | `CHANGELOG.md` § 2026-09-09 (15) |
| GAT-03 | **13 gates não-verdes triados um a um, e 11 eram do INSTRUMENTO.** Metade **(a)**, os 5 que viviam DENTRO do manifesto e faziam o runner reprovar sempre: duas premissas mortas no gate do supervisor (o principal irrestrito que a AUT-15 removeu; a testemunha `noauth` desarmada pelo shim do próprio arquivo) · o ramo de cobertura do `nav_backend` acusando `config.context_map` sem classificação · o seed do demo atrás do `role_defaults` · e os dois de escopo pela MESMA raiz — *ver o tenant inteiro enumerando o REGISTRY não é ver todas as LINHAS do ledger*. Um defeito de PRODUTO caiu de lado: o `PUT /config` descartava o `?tenant_id=` e escrevia no `__global__` com 200 (foi assim que o mapa da plataforma foi a 1 folha durante a própria triagem). Metade **(b)**, os 8 do NÃO TRIADO: um censo que procurava um nome que ganhou indireção — e que se contradizia dentro do próprio relatório (0 emissores × 356 eventos), hoje com ramo que CONFRONTA as duas metades · um INCONCLUSIVO permanente por falta de `node`, hoje rodando num container · um que somava exposição com dano · um resíduo E2E datado, declarado por ID exato · dois consertados e ISENTOS por natureza. **Manifesto: AUTO 112 → 118, ISENTO 72 → 74, NÃO TRIADO 110 → 102, cobertura 63% → 66%** | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (11) e (14) |
| LDG-01 | **O ledger conferia os NÓS e nunca as ARESTAS — dois ramos novos no `probe_task_ledger.sh`.** Ramo **G**: bloqueio cujo bloqueador já está no `done.md` (população 1 — a `MOD-07` esperou sete dias por uma `AUT-01` fechada, com os dois arquivos íntegros o tempo todo). Ramo **H**: `adiado`/`bloqueado` sem condição de retorno em lugar nenhum (população 0 — é guarda, não limpeza). ⚠️ **O H lê a LINHA INTEIRA por cicatriz**: a primeira medição leu só a coluna de status e acusou ONZE fichas sem gatilho; as 38 declaram, espalhado por três colunas (18 status · 6 âncora · 6 corpo) — censo por célula não prova nada sobre a célula vizinha. G provou-se reproduzindo o defeito antes do conserto; H, por bateria de 10 mutações | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (15) |
| RSM-01 | **O `suspend` estendia QUATRO chaves da sessão e não a quinta — a que a retomada lê.** Token de resume vive `timeout_hours*3600 + 3600` (48 h) e o `session:{id}:meta` nasce com 24 h fixas ⇒ todo `timeout_hours > 23` gerava token que sobrevive ao `tenant_id` de que depende, e a retomada morria em `tenant_unknown`. A causa é uma **lista** — `sessionKeys` do `persistSuspendWebhook` parecia completa por ser uma lista. Corrigido nos DOIS funis (TS e `_write_resume_meta`), com a regra do `session_meta_merge`: o prazo só ESTENDE; `-1` aqui **não** é bootstrap (divergência deliberada com `_extend_hash_ttl`, escrita nos dois lados). Prova ao vivo com o mesmo pool antes e depois: 1 440 min de meta → **2 940** contra 2 880 de token. ⚠️ O probe passou a DATAR a exposição (`RSM01_EPOCH`), porque o conserto é forward-only e resíduo esconderia regressão; e ao falseá-lo apareceu um defeito do catálogo no próprio instrumento: **`date -d ""` devolve hoje 00:00**, o que deixava o balde INDATÁVEL morto e — na mesma linha, desde antes — um `expires_at` vazio contado como VENCIDO | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (16) |
| ORF-01 | **Os 4 segmentos abertos de 2026-08-21 expurgados, e a lista de exceção removida do gate no mesmo passo.** Eram resíduo de bateria E2E sobrevivente ao expurgo de 2026-08-18 (`queue`/`retencao_humano`, um segmento cada); desde eles passaram 1 548 segmentos em 867 sessões sem um novo. O `purge_orphan_segments.sh` **abortaria** com o corte default (2026-08-15 — a guarda contra apagar defeito NOVO), então foi movido para `CUTOFF=2026-08-22`; o dry-run NOMEOU as linhas, que conferem com a lista declarada, e a testemunha do script deu `abertos depois do corte = 0`. Apply: 4 apagados por lista explícita de `segment_id`, **2 376 restantes** (testemunha contra tabela truncada). Com o fato encerrado, `RESIDUO_CONHECIDO` saiu do `probe_open_segments_closed_sessions.sh` — exceção que sobrevive ao fato vira permissão — e a baseline é **zero sem exceção**. ⚠️ Gate que perde filtro tem de provar que ainda reprova: bateria em tenant sintético (reprova com 1 aberto · verde com ele fechado · INCONCLUSIVO com tabela vazia), e as duas primeiras execuções saíram verdes por defeito do FIXTURE (tenant passado por env num probe que o lê posicional; coluna `started_at` que não existe) — número plausível, proposição não medida | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (17) |
| PRM-01 | **Parâmetro de deploy declarado e não preenchido deixa de ser deployável — e o modo de falha que isso fecha não é uma queda.** Sem a chave, `$.config.<x>` não resolve, o step falha e o `on_failure` leva tudo embora: o pool sobe, parece saudável em toda métrica de capacidade e escala 100% ao humano. **Duas metades, porque uma não alcança a outra:** `lib/required-config.ts` recusa no `set-next` e no `promote` (irmão de `masked-deploy` e `profile-steps`, rollback isento, conferindo o slot que está sendo PROMOVIDO); `probe_slot_required_params.sh` audita os slots `current`, porque acrescentar um `required` a um skill já deployado torna slots não-conformes **sem que promote nenhum rode**. Vazio = ausente · `null` · string de espaço · `[]` · `{}`; **`0` e `false` não são vazios**, com dois testes só para impedir a 'simplificação' por truthiness. Nasce como GUARDA: 31 slots, 3 declarantes, **0 violações**. Primeiro consumidor: os três `form_id` do `skill_navegacao_v1` viraram `$.config.form_id` com descritor `source: dialogforms`. ⚠️ Falseabilidade: 12 unitários (3 mutações) + 7 casos sobre fixture sintético — e as **duas primeiras tentativas da bateria saíram verdes por defeito do fixture**, medindo população inexistente | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (12) |
| PRM-03 | **O irmão LLM entra no mesmo formato, os dois são publicados — e o probe da PRM-01 media a proposição errada.** `skill_navegacao_llm_v1` teve os 4 literais parametrizados e o mesmo descritor; **dois skills seguem existindo por decisão do dono, e a medição sustenta**: 13 passos com o mesmo id, 9 idênticos, e só o `avaliar` diverge de verdade (o LLM precisa do ramo `escapar`) — são duas estratégias de CONTROLE sobre um conteúdo só, e o interpretador genérico é a tool `dialog_tree_level`, não o skill. Os dois publicados via `x-skill-publish` com credencial de serviço (a 1ª tentativa saiu 401), depois de medir que ambos os pools têm slot `current` — publicar não muda o que roda. ⚠️ **E aí o probe ficou VERMELHO acusando dois pools que funcionavam:** ele comparava declaração × config e ignorava que o `yaml_snapshot` em execução ainda tinha o `form_id` LITERAL. Corrigido para exigir as DUAS metades (declara obrigatório **e** o snapshot referencia `$.config.<chave>`); o estado intermediário virou linha de INFORMAÇÃO. Bateria refeita: 7 casos, e o que carrega peso é o falso vermelho de hoje virado teste | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (13) |
| PRM-02 | **`demo_ia` e `demo_llm_ia` deployados com a árvore escolhida no deploy.** Os dois rodam o snapshot parametrizado, com `form_id: dialog_navegacao_atendimento_v1` no `config_json` do slot `current` e 10 instâncias cada; o `probe_slot_required_params` saiu verde e **sem linha de informação pendente**, que é o desfecho que aquela linha existia para acompanhar. ⚠️ Os dois apontam para a MESMA árvore hoje, e isso é o desenho (eles existem para serem comparáveis na mesma série do Arc 12) — o ganho é que passou a ser POSSÍVEL divergir sem tocar em código. ⚠️ **O primeiro deploy do `demo_ia` levou `max_concurrent_sessions` de 10 para 1** e o bootstrap reagiu na hora (uma instância, uma sessão simultânea), corrigido 4 minutos depois — o campo de capacidade viaja no MESMO `config_json` do parâmetro novo, e a causa é o default do formulário (ficha `PRM-04`) | 2026-09-08 | `CHANGELOG.md` § 2026-09-08 (14) |
| ORF-02 | **Segmento humano aberto em sessao fechada: mecanismo 1 consertado, gate de volta a ZERO sem excecao.** Causa medida: as chaves de participacao do claimante viviam 4 h e o item 25 h, entao o fechamento nao achava o par e o encerramento criava segmento com a identidade POOL-scoped. Conserto: TTL DERIVADO do prazo do item, provado ao vivo (25,0 h; `joined=1/left=1`). ⚠️ A exclusao de fixtures do probe foi RECUSADA por medicao — `sess_*` nunca fecha e nao entrava na conta, e `sess_` e o id de PRODUTO do `conversation_start` (`bpm.ts:31`). Conferidas as 3 linhas (nenhum sucessor em 8 segmentos humanos pos-deploy) e expurgadas por id exato (`2790 -> 2787`). O `3971aea6` revelou que `reap_parques_orfaos.sh` encerra sessao sem fechar segmento — inocuo por ser mutirao unico. O mecanismo 2 segue como ORF-03 | 2026-09-12 | `CHANGELOG.md` § 2026-09-12 (9) |

---

## `docs/product/dialog-primitive-and-runner-design.md` — primitivo de dialogo (survey + OTP)

| id | tarefa | data | âncora no `CHANGELOG.md` |
|---|---|---|---|
| GAT-01 | **O `gates.manifest` passou a prestar contas de TODO `.sh` de `infra/test/`, e 100 gates que já rodavam verdes não eram colhidos por ninguém.** População medida: 281 scripts, 44 declarados. Quatro classes (AUTO · `!`ASSISTIDO · `=`ISENTO com motivo · `?`NÃO TRIADO nomeado), promoção a AUTO só depois de RODAR, e dois gates consertados no caminho. Mecanismo: `probe_gates_manifest_coverage.sh` (5 ramos) + `mut_gates_manifest_coverage.sh` (4 mutações, controle positivo e conferência de restauração). 40 AUTO → 94 | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 (9) |
| GAT-05 | **O `up.sh` julga one-shot pelo exit code.** A conferencia excluia one-shots por LISTA de nomes, sem exit code, e errava nos DOIS sentidos: o `auth-seed` morto lia como concluido (AUT-54), e o `context-map-seed`, fora da lista, reprovava toda subida CORRETA desde 2026-09-02 (medido: `DOWN=[context-map-seed exited]`). Classificacao derivada do compose (`restart: no` ou alvo de `service_completed_successfully`) e julgamento em funcao pura (`_up_state_verdict.py`): long-running fora de `running` e one-shot com codigo != 0 reprovam, one-shot rodando espera ate 180 s e vira INCONCLUSIVO. `probe_up_state_verdict.sh` (AUTO) verde com a stack real (42 containers, 8 one-shots exit 0); tres mutantes reprovam pelo caso certo. `up.sh` nao executado (reconcilia a stack inteira) | 2026-09-13 | `CHANGELOG.md` § 2026-09-13 (2) |
| DLG-01 | **`fields[]` ganhou autoria, e ela é o editor JSON.** `DialogJsonPanel` (importar arquivo · editar em Monaco · exportar · pré-visualizar), com o veredicto vindo de `POST /api/dialog/preview` — a MESMA `validateDialogForm`/`buildRender` que o `form_get` roda. Aplicar **não grava**: troca o rascunho, e Salvar/Publicar segue caminho único. Degradação ALTA (verificador fora do ar ⇒ *"não verificado"*, nunca verde) e **escopo declarado**: não cobre o conflito `format`×`masked`, que precisa do catálogo e segue no publish. O achado do arco foi que o *"canonical validator on the TS side"* prometido pela dialog-api **não existia em lugar nenhum** — os widgets eram validador por acidente de afordância, e o JSON remove o acidente | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 (8) |
| DLG-02 | **`menu_prompt` descartava o prompt da pergunta** — `before.join() \|\| qPrompt` curto-circuitava, e as três formas `form` do repositório perdiam os três prompts. Corrigido com teste falseado por mutação e controle positivo ao lado. **Muda o texto de três formas publicadas**, decidido pelo dono | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 (8) |
| DLG-03 | **O runner genérico passou a alcançar `interaction: form`** — `choice` sobre `render.interaction` → `coletar_form` com `fields`. Passar `fields` sempre não era opção: `render.fields` SEMPRE tem entrada (sintética por pergunta escalar) e o WhatsApp dispara coleta sequencial com `len(fields) > 0`. Contrato declarado: no ramo `form`, `payload.value` é MAPA | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 (8) |
| DLG-04 | **`buildRender`/`duplicateNodeIds` mudaram para `@plughub/schemas/dialog-render.ts`** — função pura com dois consumidores (o `form_get` e o preview do editor). O `resolveLocalizedText` já nomeava *"the editor preview"* como consumidor previsto desde que foi escrito | 2026-09-04 | `CHANGELOG.md` § 2026-09-04 (8) |
| DLG-07 | **`form` deixou de ser valor do seletor de interacao e virou TIPO DE BLOCO**, ao lado de Dialogo e dos instrumentos. Enquanto morasse em `Question/Interaction`, aquele seletor era uma uniao discriminada — o quinto valor mudava o painel inteiro e obrigava a esconder quatro controles; como bloco, **o estado ruim deixa de existir** em vez de ser tratado. Custo ZERO no modelo: bloco e PROJECAO (`dialog-blocks.ts`) sobre o `nodes[]` plano — as quatro formas semeadas passaram a APARECER diferente, sem migracao. Decisoes: a **dimensao vence** (pergunta com `capture.dimension_id` nao entra no bloco form) · o **statement de abertura fica dentro** (o `buildRender` o dobra no `menu_prompt`) · **campo nao e pergunta** (editor proprio; reusar o `QuestionEditor` mostraria quatro controles inertes) · a regra **D8 mora numa casa** (`d8Verdict`) · converter Form→Dialogo **apaga os campos e pede confirmacao** (residual renderizaria como formulario, porque `buildRender` testa `fields?.length` ANTES da interacao). O editor JSON continua, demovido a escape hatch, na linha de acoes do DOCUMENTO. Gate: ramo E de `gate_dialog_capture_roundtrip.sh`, com controle positivo proprio e falseado por mutacao | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 |
| DLG-09 | **As TRES implementacoes de `ask_when` passaram a ser CONFERIDAS entre si.** Medido: nenhum `.sh` de `infra/test/` mencionava `ask_when`, e as tres eram equivalentes (7 ops, mesma regra de ausencia, mesma coercao do `eq`) — risco PROSPECTIVO, cujo gatilho e o op `prefix` da D12. Unificar nao estava disponivel: o platform-ui nao importa `@plughub/schemas` e a copia do `survey_web.py` **nao e Python** (e JS dentro de uma string, o script inline da pagina). O probe RECORTA as tres do fonte real a cada execucao — copiar os corpos para dentro dele seria a quarta copia, e gate que testa a propria copia e verde garantido; funcao renomeada da INCONCLUSIVO. Ramo C le a lista de ops do `switch` recortado, entao op novo NASCE cobrado. Limite declarado: compara as tres entre si, nunca contra expectativa propria — paridade e correcao sao dois fatos. Bateria de mutacao nos tres ramos | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (2) |
| DLG-11 | **F1 do ADR da arvore: a recursao entrou em `DialogOption`, e `nodes` continua PLANO.** `options[]`/`active` (`z.lazy` com anotacao explicita), `optionTreeIssues` (profundidade 5, `id` unico entre IRMAOS, aninhamento so sob `list`/`checklist`, `options: []` recusado) e o op `prefix` com normalizacao escalar→lista. As regras NAO ficaram num `superRefine`: ele devolve `ZodEffects`, que nao entra em `discriminatedUnion` nem aceita `.omit()` — vivem numa funcao pura chamada pelo validador canonico, a mesma casa de `duplicateNodeIds`. Duas decisoes que a D12 nao fixava: `prefix` casa por SEGMENTO (senao `financeiro` casaria `financeiro_avulso`) e `ne` e a NEGACAO de `eq` (senao uma marcacao satisfaria `eq X` e `ne X` juntos). ⚠️ A **D7 nao entrou** — `QuestionNode` nao tem `required`, entao a obrigatoriedade derivada e regra de render e foi para a F3. O `probe_ask_when_parity.sh` (DLG-09) nasceu ontem e **cobrou hoje**: o op novo so na canonica deixou o gate vermelho | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (3) |
| DLG-12 | **F2: resposta de `checklist` virou LISTA no `pipeline_state`, e a multi-selecao chega ao Arc 12 como N eventos.** O `menu` step gravava a string `'["a","b"]'` como escalar e `deriveAgentEvents` a colava em `_a_b_` — uma serie inventada no lugar de duas reais, sem nada ficar vermelho. A promessa existia em DUAS casas sem produtor (o comentario da tool e a tabela do `CLAUDE.md` do engine). ⚠️ **Metade da fase ja estava pronta e o alvo declarado estava errado**: os dois renderizadores ja faziam multi, e o `json.dumps` do bridge e TRANSPORTE, nao defeito — faltava o engine desfazer a codificacao. O achado do ADR foi corrigido no lugar. Semantica escolhida limpa porque a populacao e zero (0 steps `checklist`, 1 pergunta publicada). Consequencia declarada: no survey, NA mudo virou recusa no limite do schema, o que fecha a decisao #3 (arvore e `nominal`, nunca `scored`). Gate: `probe_multi_answer_events.sh`, sobre a CADEIA e nao a ponta, com a mutacao reproduzindo o defeito historico literalmente | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (4) |
| DLG-13 | **F4: a hierarquia morria no EMISSOR, entao o teto da categoria nunca importou.** O achado do ADR (regex de 2-5 segmentos bloqueando uma taxonomia de 8) era metade da verdade: `deriveAgentEvents` saneava a resposta INTEIRA com `replace(/[^a-z0-9_]+/g,"_")`, e o ponto caia nessa classe — `financeiro.cobranca.indevida` virava UM segmento, a categoria nunca passava de 4, e subir a regex sozinha nao teria efeito. Consequencias mudas: o recorte por prefixo da D10 nao casaria com nada, e a pasta `financeiro.cobranca` colidiria com uma folha `financeiro_cobranca`. Hoje saneia POR SEGMENTO (0 de 49 opcoes publicadas tem ponto, entao nada muda no dado existente). O teto virou DERIVADO — `3 + DIALOG_OPTION_MAX_DEPTH` — com a relacao **conferida por teste**, e o emissor RECUSA na origem quando estoura, nomeando. `decomposeCategoryLevels` fica em 4 declaradamente, com teste fixando que `l4` NAO e a folha | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (5) |
| DLG-14 | **F3 (parcial): a subarvore chega a tela, e a PASTA deixa de virar resposta.** `buildRender` descartava os filhos, entao a forma com arvore chegaria PLANA e escolher "Financeiro" gravaria a pasta — o mesmo modo de falha que a D4 recusa no schema, um andar abaixo. Entregues: subarvore no `render` · `options_tree` DERIVADO · colunas Miller no Console (a resposta e o CAMINHO de ids) · `active:false` fora da oferta, sem emitir `options: []` · D7 (arvore e obrigatoria por construcao, com o motivo impresso). A **D5 passou a valer POR CONSTRUCAO** nesta superficie: navegar limpa as marcacoes. ⚠️ A **D11 mudou de casa**: o `form_get` NAO conhece o canal, e dar-lhe um parametro cruzaria a costura conteudo x canal — a exigencia virou declaracao DERIVADA no render, e a recusa ficou aberta (DLG-15). Gates: 3 casos em `dialog-render.test.ts` (2 mutacoes) + ramo S7 do `probe_dialog_json_surface.sh`, falseado AO VIVO com o servico reconstruido do codigo mutado | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (6) |
| DLG-17 | **F5: a taxonomia ficou autoravel sem JSON.** A mesma tabela de opcoes com chevron por linha (sem DnD — nao ha lib no projeto). As regras aparecem na TELA: apagar o ultimo filho avisa antes nomeando a pasta (D2) · o `id` so e editavel na opcao que NAO veio do documento carregado (D6 — comparar com o carregado e o que sobrevive as reescritas imutaveis) · `active:false` aposenta sem apagar · `value` desabilitado em pasta · `+ opcao` desabilita na profundidade maxima. ⚠️ Duas casas espelhadas estavam para tras e so apareceram ao ligar a tela: `AW_OPS` e `AskWhenOp` nao conheciam o `prefix` que a F1 pos no schema — terceira ocorrencia da familia do `capture.kind`. ⚠️ Corrigi aqui uma afirmacao minha: `ask_when` JA era autoravel por pergunta; a dependencia que anunciei nao existia. Gate: ramo F do `gate_dialog_capture_roundtrip.sh` (arvore sobrevive a abrir-e-salvar), falseado | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (7) |
| DLG-19 | **A arvore ganhou dado REAL, e os gates trocaram a fixture por ele.** `dialog_wrapup_arvore_v1` semeado e publicado pelo caminho oficial (`dialog-seed`, que o criou como ausente e nao tocou nos outros 12): `motivo` em 3 niveis · `servico` em 2 · pergunta guardada por `ask_when {op:prefix}` · profundidade VARIAVEL de proposito (o ramo `comercial` tem 2), que e o caso da D10. O ramo F do `gate_dialog_capture_roundtrip.sh` e o `probe_multi_answer_events.sh` deixaram de usar fixture sintetica e passaram a se ancorar nele — fixture que envelhece a parte do dado e a proxima a mentir, e o precedente esta no proprio ADR (o achado 3 ja estava obsoleto quando a F2 chegou nele). ⚠️ Quando esta linha foi escrita a forma **nao estava ligada a pool nenhum**; ligar virou a DLG-20 no mesmo dia, por decisao do dono. Ver la | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (8) |
| DLG-20 | **A arvore virou o wrap-up REAL de um pool, e o README de consumidores estava mentindo ha um dia.** `retencao_humano.on_human_end.context.dialog_form_id` = `dialog_wrapup_arvore_v1`, mudado no **agent-registry** (`PUT /v1/pools/{id}` com o bloco `hooks` inteiro, `x-service-token`), nunca no YAML — seed-if-absent nao muda pool ja semeado. Conferido relendo a rota EXATA que o bridge consome (`GET /v1/pools/{id}` com `x-tenant-id`), nao o eco do proprio PUT. Sem restart: `get_pool_config` se declara **nao cacheada** de proposito. A semente acompanhou, pelo motivo que o comentario de 2026-09-04 ja registrava ali — semente que discorda do que roda faz o proximo `--wipe` reverter o comportamento em silencio. ⚠️ **Achado:** o `infra/dialog/README.md` dizia que o pool referenciava `dialog_wrapup_v1`, e isso era falso **desde 2026-09-04** — a linha tinha sido escrita lendo o YAML de antes. Corolario do `CLAUDE.md` cometido num README que existe para orientar quem nao sabe onde perguntar; as tres linhas foram reescritas e a do `arvore` agora diz ONDE conferir o estado que vale. Falta da F6 so o contato real | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (9) |
| DLG-21 | **A F6 foi re-medida com o Console em dia, e a arvore mediu.** Contato `4919af4a` as 17:20: a verdade de origem (o `__resume_payload__` lido do `pipeline_state` da sessao de wrap-up) mostra `motivo: "financeiro.pagamento.nao_identificado"` — caminho ate a FOLHA, 3 niveis — e `servico: ["cadastro.troca_titularidade"]`, **lista**, nao a string que a F2 removeu. Tres coisas que so contato real prova: a guarda `prefix` **discriminou** (`valor_contestado` ausente porque o caminho foi `financeiro.pagamento`, que comeca com `financeiro.` e nao com `financeiro.cobranca`) · a classificacao virou `outcome: resolved` no segmento por referencia · a categoria de 6 segmentos coube no teto de 8 com os pontos preservados. A asserção da fase confere: prefixo `…motivo.financeiro.` acha 1, e **`l4` acha 0 para a folha** — que e o ponto da D10, agora com dado. ⚠️ Residuo nomeado: os 2 eventos de 17:03 sao valores de PASTA numa serie append-only e continuam contando em agregacao por `…motivo.`. Resta so a multi-folha (DLG-22) | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (11) |
| DLG-22 | **Multi-folha exercida por contato real: duas folhas na MESMA pasta renderam DOIS eventos.** `servico: ["cadastro.segunda_via", "cadastro.troca_titularidade"]` ⇒ 3 eventos no total (1 motivo + 2 servico). Lista de UM elemento nao distinguia *o emissor itera* de *o emissor pega o primeiro*; duas distinguem — e e a metade multi da F2 finalmente exercida fora do gate. O prefixo comum da D5 ganhou testemunha (as duas folhas compartilham `servico.cadastro`). ⚠️ Subproduto que virou insumo de desenho: na pasta `cadastro`, **marcacoes = 2 e contatos = 1** — a divergencia que sustenta a decisao das DUAS colunas deixou de ser argumento e virou dado | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (17) |
| DLG-24 | **O evento passou a dizer de que arvore veio.** `tags[dialog_form_id]` + `tags[dialog_form_version]` em todo evento derivado de DialogForm — na coluna `Map(String,String)` que ja existia, entao zero DDL e zero consumer novo. O par vem do PIN escrito pelo servidor no delegate (S1), nunca do payload de quem submete. Medido ao vivo: as tres linhas do contato `d36639f8` saem com `dialog_wrapup_arvore_v1` v1, e o par resolve na dialog-api. ⚠️ A publicacao exigiu as DUAS metades da pegadinha do seed-if-absent: `PUT /v1/skills` com `x-skill-publish` **e** `set-next`+`promote`, porque o pool usa slot e o bridge executa o snapshot. Gate: `probe_agent_event_form_stamp.sh` (4 ramos, INCONCLUSIVO proprio, 3 mutacoes) | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (17) |
| DLG-26 | **`GET /reports/agent-events/tree` — a contagem sobe da folha para as pastas (D10).** Tres contagens por no, e sao tres porque duas seriam mentira: `own` (eventos parados NESTE no — em pasta e sintoma, e fica em coluna propria para nao sumir dentro do ramo), `branch_marks` (soma) e `branch_contacts` (`uniqExact`, **nao soma**). O rollup e um `arrayJoin` sobre os ancestrais: um evento em `a.b.c` conta em `a`, `a.b` e `a.b.c`, entao `count()` por grupo JA e o ramo — sem segunda passada e sem somar no Python. ⚠️ `branch_contacts` **nao e derivavel no cliente**, e e por isso que a rota existe. `meta.vocabularies` DECLARA quantas formas cairam na janela (com mais de uma, quem desenha recusa os totais em vez de somar em silencio) e `unstamped_events` conta o pre-carimbo sem atribui-lo a forma nenhuma. `root` e obrigatorio (422): floresta somada nao e arvore. ⚠️ `derived_leaf` diz *nenhum evento desceu abaixo deste no*, que NAO e *e folha na forma* — o esqueleto vem dos dados. Medido ao vivo: `servico` = 26 marcacoes / 13 contatos, `servico.cadastro` com `own=1` (a linha orfa do Console velho) e `single_vocabulary=false`. Gate: `probe_agent_event_tree_rollup.sh`, 5 ramos, 4 mutacoes | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (18) |
| DLG-27 | **A lente `taxonomy` na superficie de Contatos — a arvore chegou a tela.** Forma PROPRIA (`taxonomy_tree`), nao `disposition_summary` reaproveitada: aquela responde *como as pendencias terminaram*, esta *o que foi respondido*. Tres colunas (proprio / marcacoes / contatos), tarja de integridade para pasta com `own > 0` e **aviso de vocabulario misturado** lido de `meta.single_vocabulary`. Raizes DERIVADAS do dado (`/agent-events/categories` → `l1.l2.l3`), nunca listadas a mao — lista fixa envelheceria a cada pergunta nova no editor. ⚠️ **Sem coluna de participacao e sem soma de rodape**, de proposito: em `checklist` a soma passa do universo, e um percentual que fecha 100% sobre a base errada e pior que percentual nenhum. `honors: 'period_only'` com o motivo escrito (o pool ja vem dentro da raiz) e `comparability: 'same_form'` ganhando o SEGUNDO consumidor do contrato. O `assertNever` da mesa cobrou a forma nova e ela teve de se declarar. tsc verde; i18n 770x770 | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (19) |
| DLG-28 | **O limite de tempo andava para TRAS, e a epoca ficava vazia sobre os proprios eventos.** `emitted_at` e `DateTime64(3)`; o `_ch_fmt` truncava para o segundo inteiro, entao um recorte pedido em `19:34:45.858` virava `19:34:45` e os eventos daquele milissegundo saiam. Medido no ClickHouse: **39 eventos com o literal em ms, 36 truncado**. Na tela, a epoca carimbada desenhava *"sem amostra no periodo"* logo abaixo do proprio cabecalho dizendo *"2 marcacoes · 1 contato"* — duas afirmacoes contrarias na mesma tela, e a falsa tinha cara de ausencia de dado. Conserto no `_ch_fmt`: sub-segundo do CHAMADOR sobrevive; entrada `YYYY-MM-DD` (a da barra de filtros) sai **byte a byte identica**, medido caso a caso, entao a mudanca e inerte para os outros 67 call sites. ⚠️ O `probe_agent_event_epochs` passou VERDE **nos dois estados** — media *"o form_id filtra"* e nao *"a epoca contem os seus"*, a familia do instrumento que julga a pergunta vizinha. Ramo **E** acrescentado (recorta pelos limites EXATOS, como a tela); sob a mutacao ele nomeia as duas epocas. | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (23) |
| DLG-29 | **A lente de taxonomia nao tinha dono do scroll**, e o fim da arvore era cortado sem barra. Ela vive dentro de um `flex-1 overflow-hidden` (`SessionsPage`), entao o scroll e dela — e e o conteudo que mais cresce por linha, com um bloco por epoca abaixo do outro. Passou a forma da lente irma da mesma superficie (`WrapupSummaryPage`): cabecalho fixo (`flex-shrink-0`), corpo `flex-1 overflow-y-auto`, e o `p-4` que faltava. | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (23) |
| DLG-30 | **A tela afirmava MISTURA onde so havia NAO-SEI.** O aviso da epoca sem carimbo pendurava em `single_vocabulary`, que e `len(formas) <= 1 and sem_carimbo == 0` — dentro daquela epoca ele e falso **por construcao, nao por medicao**, entao o aviso aparecia sempre. Medido: em `motivo` ha **zero** rotulos repetidos e nenhuma forma carimbada, e a tela mesmo assim alegava mistura; em `servico` a mistura e real (`troca_titularidade` em dois caminhos). Mesma familia do vazio que se declarava confiavel, na direcao INVERSA: alegar um defeito que pode nao existir. Agora mistura so e AFIRMADA com evidencia na propria janela — duas formas carimbadas, ou o mesmo rotulo em mais de um caminho —, e o predicado e o MESMO `ambiguos` que desambigua as linhas pelo pai, entao o aviso e verdadeiro **se e somente se** o leitor consegue ver a evidencia na tabela. Sem isso a tela diz que nao sabe. **Segunda casa da mesma afirmacao, achada na tela seguinte:** a nota de rodape dizia *"cada bloco e um trecho com UM vocabulario"* — sem condicao, logo abaixo do aviso que acabara de dizer que nao da para saber. A afirmacao verdadeira e sobre a FORMA (um bloco e um run contiguo de uma forma, verdade por construcao da sessionizacao), nao sobre o vocabulario; e o trecho anterior ao carimbo e a excecao NOMEADA. | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (24) |
| DLG-31 | **D5 REVOGADA: a multi-selecao deixou de exigir mesmo pai.** A regra dizia *"dentro de uma pasta"*, mas impunha **mesmo pai imediato** — logo *o que pode ser registrado junto dependia de onde o autor arquivou a folha*, que e arrumacao e nao dominio. Medido no ClickHouse antes da arvore: `segunda_via` + `alteracao_plano` em **6 contatos**, e **2** marcaram as quatro folhas; ao filar as folhas em `cadastro` e `plano`, a arvore APAGOU essa combinacao. Nao foi reorganizacao de vocabulario, foi **remocao de poder de registro que o dado real exercia**. O ganho analitico da D5 nao exigia a restricao (o rollup por `arrayJoin` + `uniqExact` ja conta cross-pasta certo, e a lente ja desenha a nao-aditividade); o risco que ela citava e de PERCEPCAO e trata-se por exibicao. Navegar deixou de limpar as marcacoes, e cada caminho da cesta virou o botao que o desmarca — necessario, porque com marcacao fora das colunas visiveis o operador alcancaria um estado sem saida a partir de onde esta. **Recusado virar flag** (`multi_scope`): o default teria de ser o permissivo e a alternativa ficaria sendo config que ninguem liga; quem quer *"uma area so"* modela duas perguntas. | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (25) |
| DLG-32 | **Caso cross-pasta EXERCIDO com dado real — o unico do arco que nunca tivera.** Contato gravou `cadastro.segunda_via` + `plano.alteracao`. Prova a coluna `branch_contacts`: as marcas somam (5+1=6), os contatos **nao** (3+1=4 != 3), porque o contato que tocou as duas pastas e UM so. Ate aqui a soma dos irmaos coincidia com a raiz **por acidente da populacao** — um consumidor que somasse filhos acertaria sempre; agora erra em 33%. Contraste util na mesma tela: `motivo` (resposta unica) fecha, `servico` (checklist) nao. ⚠️ **Nao virou ramo de gate:** o erro exposto e do CONSUMIDOR, e o servidor nao consegue produzir a versao aditiva por engano; o ramo C do `probe_agent_event_tree_rollup` ja cobre o erro que o servidor pode cometer. | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (26) |
| ORQ-00 | **F0 do ADR de navegacao: medir antes de desenhar.** Inventario dos 17 skills com `menu`; `agente_triagem_v2` gasta **15 steps** para uma tabela de 5 entradas; os menus sao de DUAS especies (navegacao × coleta) e absorver a segunda coletaria dado do especialista antes de ele existir; **o eixo de DEMANDA nao tem produtor — 0 `agent_event` nos cinco skills de atendimento**, entao toda resposta de menu do cliente e hoje descartada; a descida na arvore **ja e expressavel** (o validador de ciclos sanciona back-edge por `menu`, que bloqueia em I/O); e a UNICA primitiva faltante e o alvo do `escalate`, hoje literal e passado cru. ADR escrito com essa medicao como contexto. | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (27) |
| ORQ-06 | **`agent_event` era inalcancavel de um agente nativo de skill-flow; agora ha uma porta que nao afrouxa o invariante.** A tool exige `session_token` de `agent_login`, e o orchestrator-bridge nao emite nenhum (`session_token` aparece **0 vezes** no `main.py` dele) — por isso o Arc 12 tinha zero chamadores no caminho de atendimento. Das tres saidas levantadas, o dono escolheu a **(b)**: `agent_event_record`, identificado pela SESSAO. ⚠️ **O ponto nao e a credencial, e a COMPOSICAO:** o `agent_event` recebe a `category` pronta e **confere** que o primeiro segmento e o pool; o `agent_event_record` recebe `emitter` + `metric_key` + `path` e **compoe** `{pool}.{emitter}.{metric_key}[.{path}]` lendo o pool do `session:{id}:meta`. O isolamento de namespace passa a valer **por construcao** em vez de por conferencia de uma string que o chamador montou — e o chamador deixa de precisar saber em que pool roda, que era o motivo de o YAML ter de interpolar `{{@ctx.core.pool.id}}`. Recusa NOMEANDO em tres casos (sem meta, sem pool, categoria acima do teto de 8 segmentos), porque emitir produziria evento que o schema rejeita longe dali — buraco na serie em vez de mensagem. `sanitizeCategoryPath` mudou de casa (`mcp-server/segment.ts` → `@plughub/schemas/agent-events.ts`), ao lado do regex e do teto que ela serve. Exercido ao vivo contra o servidor: negativo (sessao inexistente) RECUSA nomeando; positivo (meta real) EMITE com o l1 do meta. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (4) |
| ORQ-07 | **A sentinela de idempotência protegia a QUEDA e esterilizava o CICLO.** Medido em contato real: num laço `menu → invoke → choice → menu`, a segunda visita ao `invoke` devolvia o resultado guardado e nunca mais chamava o mundo — a chave da sentinela é só o `step.id`, então ela dizia *"este step já rodou, alguma vez"* em vez de *"esta chamada já aconteceu nesta execução"*. O validador SANCIONA o ciclo; o runtime o congelava, relatando `on_success` todas as vezes. Conserto: `addTransition` limpa as sentinelas do step em que se ENTRA — entrar por transição é invocação nova, retomar após queda não é, e a distinção é exata pela forma do laço (a retomada executa `current_step_id` sem passar por lá). Controle negativo é o load-bearing: *"re-executa ao reentrar"* sozinho ficaria verde numa implementação que nunca gravasse a sentinela, e essa mandaria duas mensagens ao cliente. Censo: **3 de 40** skills expõem o padrão (`skill_navegacao_v1` — dano medido; `agente_fila_v1` e `agente_copilot_v1` — exposição, dano NÃO confirmado). ⚠️ Achado de método: o ramo D do gate provava que o validador DISTINGUE o ciclo, e ficou verde o tempo todo — media a proposição vizinha à que importava. 6 testes por mutação; engine 211/211. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (5) |
| ORQ-01 | **F1 — o eixo de DEMANDA existe, com contato REAL.** Até aqui a plataforma media com precisão o que o AGENTE diz que o contato foi (wrap-up) e **nada** do que o CLIENTE disse que queria: os cinco skills do caminho de atendimento emitiam ZERO `agent_event`, e toda resposta de menu morria no `pipeline_state`. Entregues: `optionsAtPath` (projeta UM nível; segmento desconhecido devolve vazio, **nunca** a raiz) · tool `dialog_tree_level` · forma `dialog_navegacao_atendimento_v1` (9 folhas, 2 níveis) · `skill_navegacao_v1` promovido no `demo_ia` · `agent_event_record` (ORQ-06) · conserto da sentinela (ORQ-07) · gate de 5 ramos. **Medição de fechamento:** contato real em 2026-09-06 — categoria `demo_ia.navegacao.destino.sac.info_plano`, `pool_id=demo_ia`, época `dialog_navegacao_atendimento_v1` v1; segmentos `demo_ia → sac_ia` em 3 s; e a lente de árvore devolveu os três níveis com `single_vocabulary: true` e `unstamped_events: 0`. ⚠️ **A fase custou dois defeitos que só o contato real expôs** — o `prompt` do `menu` chegava LITERAL ao cliente (`interpolate` só troca `{{...}}`; ref pura atravessa intacta, e a `options` ao lado usa ref pura de propósito porque vai por outro resolvedor), e a sentinela de idempotência esterilizava o ciclo (ORQ-07). Nenhum dos dois ficava vermelho em teste nenhum que existisse antes. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (3), (4), (5) e (6) |
| ORQ-03 | **F3 — a tabela de roteamento sai do fluxo.** `skill_navegacao_v1` foi de **16 para 12 steps**: saíram um `choice` de 5 condições e 5 `escalate` literais, que juntos eram a tabela escrita no controle de fluxo. Quatro peças: `escalate.target.pool` aceita referência, resolvida no engine · `pools.navigation_pools` (`Record<caminho, pool_id>`, forma idêntica à do `mentionable_pools`, migração `20260906120000`) · tool `pool_route_resolve` · superfície de UI em *Configuração → Recursos → Pools*, **reusando** o editor do `mentionable_pools` em vez de copiá-lo. **Duas decisões do resolvedor:** prefixo mais longo (com igualdade, cada folha nova exigiria editar config noutro serviço — o acoplamento que a D2 desfaz) e prefixo por **SEGMENTO** (`startsWith` cru faria `sac_premium` casar com `sac` e despachar errado **com o log dizendo que casou** — caso load-bearing dos 8 testes, derrubado por mutação). **Sem default:** caminho não mapeado é `isError` nomeando; o fluxo manda ao humano. ⚠️ **A guarda da D10 nasceu JUNTO**, como o ADR exigia — ramo G varre as 14 formas publicadas E o `DialogOptionSchema` atrás de 9 nomes que significariam destino; escrevê-la depois seria escrevê-la sobre forma já contaminada. ⚠️ **Uma dependência do próprio ADR foi refutada:** `F3 ← F2` era raciocínio, não medição — a navegação desce um nível por turno e o `menu` renderiza lista plana, então nenhum canal precisa desenhar árvore. Medido ao vivo nos 5 casos; gate em 7 ramos. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (7) |
| ORQ-04 | **F4 — o runner genérico assume, e o menu duplicado morre por CONDIÇÃO.** A triagem já tinha saído como efeito da F1 (medido: **nenhum pool executa `skill_triagem_v2`**). O que faltava era o menu duplicado — e apagá-lo estaria errado: o `sac_ia` tem endpoint de canal próprio, então o cliente pode cair lá sem navegação nenhuma; apagar consertaria o caminho novo e quebraria o antigo. A navegação grava `session.navegacao.path` e o especialista ramifica sobre ele. ⚠️ **O `escalate` não serve de veículo**: `process_context: { pipeline_state }` é publicado e **ninguém lê** (uma ocorrência no repo, na declaração do modelo do routing) — mesma família do `options_tree`. ⚠️ **O registry recusou a 1ª tentativa** (422 `unregistered_context_tag`, tag de 2 segmentos fora do mapa); domínio declarado em `infra/context-map/tenant_demo.json` e aplicado pela API oficial. ⚠️ **A parte que quase escapou:** o seed ainda declarava `skill_triagem_v2` e o mapa de rotas só existia no DB — inerte hoje, mas um `--wipe` ressuscitaria a triagem e faria o `pool_route_resolve` recusar TODO caminho. ⚠️ **Ramo H** existe porque o `default` do novo `choice` é PERGUNTAR: renomear uma folha traria o menu duplicado de volta sem nada ficar vermelho; ele compara de fora folhas-roteadas × ramos-do-skill, verificado por mutação. `agente_triagem_v2.yaml` fica rotulado — os promotes sucessivos tiraram a triagem do slot `previous`, logo rollback não a traz de volta e o arquivo é a última receita fora do git. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (8) |
| ORQ-02 | **F2 — o canal desenha a árvore, ou diz por que não.** A linha dizia *"nenhum canal desenha árvore"*; verdade, e não era o dano. O dano é que os filhos eram descartados **em silêncio** — inócuo quando o fluxo desce nível a nível, beco sem saída quando um runner genérico entrega o `render` inteiro a um `menu` (o cliente vê só as pastas). **Exposição: 4 runners; dano: zero** — nenhuma forma deles tem árvore hoje; guarda, não conserto. Entregue: `option_tree.py` (7 testes) · WhatsApp com seções tituladas onde cabe e log NOMEANDO onde não · webchat agrupando · `chosen_id` pontuado (peça load-bearing: sem ela, desenhar a árvore seria REGRESSÃO — a projeção procuraria a folha na raiz e a navegação reiniciaria parecendo certa) · `DialogOptionSchema` proíbe ponto no id. ⚠️ **O ponto já era separador em TRÊS mecanismos e ninguém o impunha** (`category_path`, prefixo de `navigation_pools`, `chosen_id`); medido 0 de 87 ids afetados. ⚠️ **Ramo I é paridade Python×TS** — duas linguagens, nenhum código compartilhável; trocar o separador de um lado só mantém a tela bonita e devolve `found:false`. Verificado por mutação. Turno-único ≡ turno-a-turno, medido ao vivo. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (10) |
| ORQ-05 | **F5 — paridade LLM: o orquestrador com LLM aterrissa em folha DECLARADA.** O gatilho pedia F4 entregue **e** um pool de orquestração IA candidato; a segunda metade **não existia** (medidos 6 skills com `reason`, nenhum decide DESTINO — respondem, sugerem, extraem, avaliam), então foi criada: `skill_navegacao_llm_v1` no pool `demo_llm_ia`, com o MESMO `navigation_pools`, o MESMO `pool_route_resolve` e o MESMO `escalate` interpolado. ⚠️ **A parte frágil da D6 não é o prompt — é a CONFERÊNCIA**: pôr a lista no prompt é instrução, e um caminho plausível que não existe viraria uma categoria que a lente desenha como se alguém a tivesse autorado. Quem decide é o step `conferir`, com a MESMA projeção do determinístico (F2). Medido ao vivo: inventado RECUSA, pasta RECUSA, vazio RECUSA, declarado ACEITA — e os três recusados caem na folha de escape, que é **contável** (D6.2 + D7). `leafPaths` dá o vocabulário e alimenta as DUAS metades da mesma fonte; separadas, o escape contaria alto por defeito NOSSO. ⚠️ **Ainda não há série** — o pool nasceu agora; o que fechou é a pergunta de mecanismo. Ramo J com duas mutações (pular a conferência; medir noutra unidade). | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (11) |
| CTR-00 | **G0 — medir a assimetria antes de desenhar o contrato.** A trilha de segmentos mostra que o orquestrador **humano** mantem `primary` enquanto especialistas entram como `specialist` e saem, e que o **IA** perde a titularidade no primeiro `escalate` — e essa assimetria, nao a frequencia, e o que justifica o modelo. ⚠️ **A frequencia foi medida e PROIBE o argumento facil**: 2+ pools de IA em **5 de 1 151** contatos — mas o `escalate` e terminal, entao mediria-se demanda por uma capacidade com dados gerados pela ausencia dela. Tambem medidos: nenhum dos 5 especialistas e executor puro; **3 menus de DEMANDA dentro de folhas** (um deles, `auth.menu_continuar`, ainda nao observado); o contrato de contexto com a entrada pronta e ociosa (**1/41** declara, **0/41** le `@ctx.__gaps__`, **0/41** usa `resolve`) e **a saida inexistente**; `task` retorna ao chamador (corrigindo afirmacao minha em contrario) mas endereca skill, contorna o slot de deploy e nao acha 3 dos 5; e a regra que proibe `delegate` em agente **nao tem mecanismo**. ADR escrito com essa medicao como contexto. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (13) |
| CTR-01 | **G1 — a regra de perfil de step ganhou MECANISMO, e perdeu um item por contraprova.** Nao existia allowlist em lugar nenhum, apesar do `CLAUDE.md` afirmar *“validado em parse do YAML + guard no engine”* e do schema do `delegate` dizer *“Agents must never use delegate”*. ⚠️ **Impor a regra escrita recusaria dois pools VIVOS**: `limite_ia` (186 segmentos) e `portabilidade_ia` delegam ao `dialog_runner` (24 segmentos) — entao `delegate` saiu da lista, `suspend`/`collect` ficaram, e com a lista corrigida sao **0 violacoes em 31 deploys vivos**. O portao mora no **DEPLOY** (`set-next` + `promote`), porque o perfil e fato do POOL e o publish do skill nao sabe onde ele vai rodar. Gate de 5 ramos, incluindo mutacao do censo e um controle positivo ao vivo que impede um portao que recuse tudo de passar. Destrava a CTR-03. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (14) |
| CTR-07 | **Instrumento de DELEGABILIDADE — quem pode receber `delegate` sem pendurar o contato.** Nasceu ao executar a CTR-03 e refutar a ordem do ADR. A premissa da D1 se CONFIRMOU na trilha viva (`limite_ia primary` -> `dialog_runner specialist` -> `limite_ia primary`; 24 segmentos do alvo 100% `specialist`, 49 do chamador 100% `primary`), mas **delegar e suspender**: dos 6 destinos distintos de `navigation_pools`, **zero sao delegaveis** — 4 `nao_retorna` (nao invocam `workflow_resume`), 1 `cadeia_delegate` (o token unico da sessao colide, CTR-06) e 1 `sem_deploy`. O gate tem censo + **trava** (reprova orquestrador que delegue a alvo nao-delegavel, e com `pool` por ref cobra o mapa inteiro) + **mutacao** (finge o delegate e exige que a trava acuse: 12 infracoes). Le o SNAPSHOT VIVO do slot, nunca o YAML. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (15) |
| CTR-08 | **CTR-04, metade do RETORNO — os 4 executores devolvem o controle.** Cada desfecho (resolvido · negativo · timeout) passa por um `sair_*` que invoca `workflow_resume` com a sua `decision` antes de fechar o proprio segmento. ⚠️ **Condicional ao token, e a condicao e medida**: `sac_ia` TEM porta propria (`webchat/sac`), entao devolver incondicionalmente faria o resume falhar em todo contato direto. ⚠️ **Dois deles DECIDIAM o destino do contato** — `skill_atendimento_auth_v1` escalava sozinho ao SAC e `agente_auth_form_v1` escalava ao humano nos dois desfechos; agora PROPOEM (`payload.proximo`, `motivo`) e quem dispoe e o chamador. O caminho antigo fica inteiro, porque a condicao e o token. Publicado + promovido nos 4 pools, e o `probe_orchestrator_delegability.sh` virou sozinho: **0 -> 4 delegaveis**. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (16) |
| CTR-09 | **Referência de `invoke` que não resolveu chega à tool como argumento AUSENTE, não `null`.** Medido ao vivo: `skill_auth_form_v1` mandava `customer_id: "@ctx.caller.customer_id"` ao `validate_pin`; sem identidade resolvida o ContextStore devolve `null`, o schema `z.string().optional()` recusava (-32602) e o cliente com a senha CERTA lia *"Não foi possível verificar sua identidade"*. `$.` ausente já sumia do JSON: a mesma ausência em dois formatos, e 164 campos das tools recusam `null`. Conserto no motor (`resolveInputMap`), não no skill nem na tool; `null` literal no YAML e valor falsy viajam. `probe_invoke_absent_ref.sh` | 2026-09-15 | `CHANGELOG.md` § 2026-09-15 (27) |
| RET-01 | **`on_return` na folha + convencao `main` + `buildRender` a partir de uma question.** O campo e UM, e `QuestionNode.id` ja existia — questions identificadas sairam de graca. ⚠️ **O risco nao era o ponteiro, era o render**: `buildRender` serve o `form_get` E a preview do editor, e **5 das 14 formas publicadas tem mais de uma question** (survey, wrap-up) — uma janela incondicional mudaria o `statement_after` delas em silencio. Por isso a janela e OPT-IN: sem `fromQuestionId`, o caminho e o de antes. Ponteiro quebrado e RECUSADO (`returnRefErrors`, irma de `duplicateNodeIds`), inclusive apontando para statement, e inclusive aninhado em qualquer profundidade. 14 testes novos + 296 da suite verdes; gate de 3 ramos com paridade sobre as 14 formas reais e mutacao do comparador. | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 (18) |
| RET-02 | **O orquestrador DELEGA, com o verbo derivado por destino.** `pool_route_resolve` passou a devolver `verb`+`verb_reason` lendo o snapshot PROMOVIDO no destino — derivado do artefato, nunca declarado no mapa (um campo `delegavel: true` viraria mentira no primeiro publish sem retorno). ⚠️ **O default e ESCALATE**: qualquer duvida (registry mudo, sem deploy, cadeia) resolve para o verbo que NAO pendura ninguem. Os dois orquestradores ganharam `decidir_verbo` → `delegar` (com `on_reject`/`on_timeout` → humano) → `continuar` → `nivel_continuacao`, que **reentra no mesmo laco** (no determinista pelo `mostrar`, no LLM pelo `ouvir`, porque aquele nao lista, escuta). `dialog_tree_level` ganhou `question_id` (o `on_return` nomeia o ID do node, e a tool so sabia enderecar por `output_key`) e devolve `on_return` **so quando existe** — `null` faria o `exists` do choice ver continuacao em todo caminho. ⚠️ **Nao liga ciclo nenhum**: 0 de 14 formas declaram o ponteiro. | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (1) |
| RET-04 | **O ciclo tem FIM — e a tarefa achou o defeito que o impedia de GIRAR.** ⚠️ Antes do teto: a lista de sentinelas limpas na transicao cobria so a familia do `invoke` (ORQ-07, que era o caso medido), entao na SEGUNDA volta ao `delegate` o `__resume_decision__` ainda estava preenchido e o step **devolvia o resultado da primeira delegacao** — o especialista nunca chamado, o step relatando sucesso, nada vermelho. Lista estendida para `delegate`, `collect` e `suspend` (3 → 15 sufixos), com o CONTROLE NEGATIVO cobrindo as familias novas. O teto e `delegate.max_iterations` (5 nos dois orquestradores, saindo por `escalar_humano`), contado ANTES de suspender e zerado ao estourar. ⚠️ **A chave e `_delegate_iterations_{id}`, FORA do padrao `{id}:__x__`**: dentro dele a limpeza o zeraria a cada volta e as duas metades se anulariam — laco infinito com `max_iterations` declarado ao lado. Imunidade por construcao. ⚠️ E o publish REMOVIA o campo em silencio (Zod strip) ate o agent-registry ser rebuildado — o gate pegou. 219 testes do engine + 5 novos; gate de 3 ramos. | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (2) |
| CTR-06 | **A cadeia `delegate -> delegate` deixou de colidir — era o risco numero um do ADR, registrado la como questao ABERTA.** `core.workflow.delegate_resume_token` e tag UNICA da sessao e o token e fato da ARESTA: em `A -> B -> C`, a delegacao de dentro sobrescrevia o token que retomaria A, e A ficava pendurado ate o `timeout_hours` com o cliente vendo o especialista atender e **nada ficando vermelho**. Conserto de ESCOPO em duas metades que so valem juntas: o engine **captura** o token no nascimento do pipeline (isolado por segmento, `{sid}--seg--{iso}`, medido no bridge) e o **restaura** na retomada. ⚠️ **E a metade 2 que dispensou migrar os 8 skills que leem a tag** — o nome volta a significar o que promete e nenhum YAML mudou. ⚠️ A chave e `_caller_resume_token`, FORA do padrao `{id}:__x__` (licao da RET-04: dentro dele a limpeza de sentinela anularia a captura). ⚠️ **Dois escritores para uma chave**, mas sao as duas pontas de um protocolo de PILHA — por isso a restauracao so grava quando DIFERE e **loga quando grava**: cada linha e uma colisao reparada; gravar sempre viraria ruido, e e esse o caso negativo que carrega peso. ⚠️ **Tirar `cadeia_delegate` sozinho abriria um buraco:** `nao_retorna` media a presenca da TOOL, proposicao adjacente — `agente_portabilidade_intake_v1` a invoca 5 vezes sem retomar o chamador uma unica vez (retoma um `suspend` proprio). Hoje afere o TOKEN. Medido: **4 de 6 delegaveis antes e depois**, sem regressao. Residuo nomeado: `portabilidade_ia` PODE devolver e ainda nao devolve (metade restante da CTR-04). | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (5) |
| RET-06 | **`exists` sobre `$.` era SEMPRE FALSO — achado pelo primeiro contato real do arco.** O `choice` tem dois avaliadores (`$.` e `@ctx.`) com um `switch` cada, e `exists` so existia no segundo; no primeiro caia no `default` e devolvia `false`. ⚠️ **Nao ha erro em lugar nenhum**: condicao sempre falsa vira o `default` do STEP, e ali o default era `finalizar` — o cliente lia como fim normal. Medido no contato `c97ed196`, cujo `pipeline_state` mostra `continuar -> finalizar default` ao lado de `"on_return": "pos_atendimento"`. **O modelo funcionou**: os tres segmentos que nunca existiram apareceram (`demo_ia primary seq=0 suspended` -> `sac_ia specialist` -> `demo_ia primary seq=1`). ⚠️ **A restricao ja vivia como PROSA** no docstring do `ConditionSchema` e nao impediu nada — terceira ocorrencia do padrao. ⚠️ **Os gates da RET-05 mediam a proposicao vizinha** (o ponteiro existe? a rota existe?) e nenhum perguntava se a condicao podia disparar. Censo: 3 condicoes mortas em deploys vivos contra 16 vivas — a terceira e de outro arco (`avaliacao_ia.check_rag_needed`, e o `search_knowledge` do Arc 6 **nunca rodou**), e ligar e seguro por construcao porque os dois ramos dele convergem em `evaluate`. Criterio do conserto: `!== undefined`, NUNCA truthiness — `0`/`false`/`""`/`null` sao presentes, igual ao ramo `@ctx.`. 17 testes (232 na suite), gate de 3 ramos, tres mutacoes verificadas. | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (6) |
| RET-08 | **`category_path` era composto em DUAS casas, e so uma era o runtime — achado pelo segundo contato real.** Com a RET-06 no ar a continuacao apareceu, e ai QUALQUER escolha derrubava o contato na fila humana: a tool fazia `path.join(".")` para toda question, entao a continuacao media `outra_coisa` enquanto o fluxo comparava `pos_atendimento.outra_coisa` e o `navigation_pools` declarava `pos_atendimento.especialista`. Nenhum dos dois errado sozinho. ⚠️ **O gate da RET-05 ficou verde porque compunha como o FLUXO** — as duas casas que ele comparava concordavam, e a terceira (a tool) nao estava na conversa. ⚠️ **E *falar com especialista* chegava ao destino certo pelo motivo ERRADO** (pela falha de rota, nao pelo mapa) — caminho que parece funcionar sobrevive a qualquer inspecao de tela. O conserto torna explicito ENDERECO (`path`/`leaves`, voltam para dentro da tool, SEM prefixo) x MEDICAO (`category_path`/`root_id`, serie do Arc 12 e chave do mapa, COM prefixo fora da entrada); prefixar o endereco faria a navegacao reiniciar parecendo certa. `categoryPathFor` numa casa so, 9 vetores compartilhados TS x Python (padrao do `verb_vectors.json`), ramo D novo no gate, mutacao derruba 5. | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (7) |
| RET-09 | **Duas casas decidiam *"o contato acabou?"* e discordavam em tres outcomes — terceiro defeito do dia achado por contato real, e o terceiro do MESMO padrao.** `process_routed` tinha `("escalated_human","escalated_ai","transferred","suspended")`; o caminho do RESUME tinha `!= "suspended"`. Ele nasceu para o Arc 19, onde workflow retomado termina em `complete` — **fluxo retomado que termina em `escalate` nao existia** ate a RET-02 dar `delegate` ao orquestrador. Desde entao o desfecho dependia de por onde o fluxo chegou: escalate sem delegate antes -> contato SEGUE; depois de delegar -> contato FECHA. Medido no contato `13484fe6`: o fechamento vence a alocacao por **30 ms** (`abandoned wait_ms=21`) e a tela diz "encerrado". ⚠️ **O comentario do caminho certo JA dizia o motivo** (*"fechar aqui causaria race condition"*) e a outra casa nao o lia. Exposicao x dano medidos separados: **3 de 31** deploys sao retomaveis E escalam; dano confirmado em **2** contatos (`limite_ia` e exposicao, escalou 1 vez pelo caminho nao-resume). Conserto: **predicado** `contato_encerra_com`, nao constante — constante ainda admite comparacao escrita de um jeito novo. Ausencia ENCERRA, por decisao declarada. 14 testes (par `escalated_human` x `escalated` derruba implementacao por `startswith`), gate de 3 ramos em AST e nao grep. **Junto: o dono validou o ciclo de varias voltas com cliente real — a R2-R5 saiu do gate para a producao.** | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (8) |
| RET-07 | **O arco VALIDADO em contato real — e o preco da D2 refutado.** O dono rodou varios ciclos de continuacao e depois escalate para humano com NPS e wrap-up: `folha -> agente -> menu -> folha -> humano -> hooks` ponta a ponta. ⚠️ **A premissa da pergunta era falsa**: o ADR dizia que sem switch *"resolvi"* e *"nao consegui"* apontam para a mesma continuacao, e o `delegate` tem TRES arestas que os quatro executores ja usam (`input`/`rejected`/`timeout`) — so o SUCESSO alcanca a continuacao. O switch recusado no JSON ja existia no YAML, onde o invariante diz que controle mora. Sobrou CONTEUDO: o `notify` do SAC fazia uma pergunta que ele nao coleta e o orquestrador a repetia literal no menu seguinte (visto na captura). Duas edicoes de texto, zero mecanismo: a pergunta saiu do `notify` (redundante nos DOIS caminhos, diferente do `menu_motivo`) e o `pos_atendimento` deixou de ecoar a frase. ⚠️ **O que NAO foi construido tem motivo medido**: `proximo`/`cliente_encerrou` do `auth_sac_ia` ficam sem consumidor porque aquele pool tem ZERO segmentos e ler o `proximo` abre demanda de origem PROCESSO que a CTR-05 nao decidiu (RET-10). | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (9) |
| CTR-03 | **G3 — `delegate` no orquestrador, mantendo `primary`. ENTREGUE sob outro id: a substancia saiu como RET-02** (`decidir_verbo` -> `delegar` com verbo DERIVADO por destino, `max_iterations: 5`, `on_resume: continuar`), promovida nos dois orquestradores e **validada em contato real** em 2026-09-07 — a trilha `demo_ia primary seq=0 suspended -> sac_ia specialist -> demo_ia primary seq=1` existe no banco. ⚠️ **O que se fecha aqui e uma SEGUNDA CASA**: a linha sobreviveu no `pending.md` descrevendo trabalho que o `done.md` ja registrava sob a RET-02 — o defeito que o proprio ledger existe para impedir, e que ja custou seis semanas uma vez (Customer History). Achado ao revisar a validade das CTR depois da entrega do arco, nao por conferencia de rotina: **duas casas para o mesmo fato nao tem dois valores, tem o da casa que ninguem confere.** Nada de codigo foi entregue nesta linha. | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (1) |
| RET-03 | **O `resume_token` do chamador morre com o contato.** A D6 admite que o agente CHAMADO encerre — porque finalizacao por ERRO existe de qualquer forma —, e ai o token nunca e consumido: o `pipeline_state` fica suspenso e o timeout scanner acaba chamando `handle_resume` numa sessao ja fechada, com o orquestrador tentando continuar um contato que acabou. **Duas defesas que nao se substituem:** cancelamento no fechamento (UM lugar — o consumidor de `session.closed`, que ve todo canal; N ganchos por adapter e o esquecido reabrindo o buraco) e guarda no scanner (rede para o que escapou e para o PASSIVO, e so decide sobre token JA VENCIDO, onde a escolha e entre retomar e apagar). ⚠️ O caso load-bearing do teste e o CONTROLE POSITIVO: o token de OUTRA sessao sobrevive — limpar o hash inteiro passaria no teste obvio e derrubaria todo delegate pendente do tenant. 7 testes novos, 750 da suite do gateway, gate de 4 ramos com o D rodando na IMAGEM. | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (3) |
| RET-05 | **O ciclo LIGADO — e a primeira entrega do arco que o cliente ve.** A forma de navegacao ganhou a question `pos_atendimento` (raiz propria, D5) e **6 das 9 folhas** apontam para ela; as 3 que ficam sem ponteiro sao justamente as cujo destino nao devolve (`portabilidade` = cadeia de delegate, `sac.especialista` e `nao_se_aplica` = humano sem deploy) — apontar ali penduraria o contato. Os especialistas PULAM o proprio menu de continuidade quando ha chamador, senao o cliente e perguntado duas vezes; o menu **fica** porque `sac_ia` tem porta propria. ⚠️ **O ramo H do gate da arvore reprovou e estava CERTO**: a primeira versao tinha `pos_atendimento.sac`, folha generica que cairia no menu de motivo do SAC e prenderia o cliente ao SAC quando ele quer a arvore inteira — virou o comando `outra_coisa`, que reinicia na raiz. ⚠️ **Divida NOMEADA**: comando nao e pool, e hoje sao DOIS caminhos declarados no fluxo; um terceiro obriga o genero a subir para a arvore. Gate de 3 ramos; os 7 gates do arco verdes. | 2026-09-07 | `CHANGELOG.md` § 2026-09-07 (4) |
| DLG-16 | **Encerrada SEM implementacao: a conferencia de prefixo comum perdeu o objeto.** Ela existia para impor a D5 no submit; com a D5 revogada (`DLG-31`) nao ha estado invalido a recusar — dois paths sem prefixo comum passaram a ser um registro LEGITIMO. O invariante que sobrevive nunca foi o prefixo, e **todo path e FOLHA** (D2), cuja deteccao ja mora no relatorio (`own > 0` em pasta, `DLG-15`). | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (25) |
| DLG-25 | **D13 implementada: a troca de forma virou ÉPOCA, e o conflito deixou de existir.** `GET /reports/agent-events/epochs` devolve um bloco por **run contiguo** (sessionizacao por transicao de `tags[dialog_form_id]`), e o `/tree` ganhou `form_id` para recortar. Medido ao vivo: sem recorte a arvore de `servico` da `single_vocabulary: false` e mostra `segunda_via` em dois caminhos; recortada na epoca carimbada da **`true`** e a duplicata some — o conflito nao e tratado, ele **deixa de existir**. A epoca **sem carimbo** e uma run legitima (`form_id: ""`), nao descartada nem atribuida: ela ainda mistura, e a tela DIZ isso, porque a forma daqueles eventos nunca foi gravada. ⚠️ **Limite nomeado:** o discriminador *run × forma* NAO foi exercido — nao houve rollback de hook nestes dados, entao a mutacao que agrupa por forma foi detectada por quebrar o SQL, nao pela proposicao. Gate: `probe_agent_event_epochs.sh` (4 ramos, INCONCLUSIVO proprio; o ramo D prende a armadilha do `if not x` — string vazia e epoca, nao ausencia de filtro) | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 (22) |

## `CLAUDE.md` — saude do arquivo de contexto

| id | tarefa | data | ancora |
|---|---|---|---|
| DOC-01 | **Higiene: as regras do proprio arquivo aplicadas ate o fim, e o alvo posto em revisao.** `2 203 -> 1 883` linhas. Sairam as quatro secoes de arco > 50 linhas (Sentiment Tracking · Operational Visibility · Arc 7 · Dialog Primitive), cada uma com resumo de 15-20 linhas e o destino criado ou atualizado no MESMO commit, e a secao `Pending`, que nascera antes do ledger e virara segunda casa do que este arquivo diz. **13 itens conferidos um a um antes de remover:** 7 ja estavam aqui, 15 ids novos foram escritos em `pending.md` sob grupo proprio. ⚠️ **`Postura de Engenharia` e `Security` NAO foram cortadas** — carregam o catalogo medido e pertencem pela tabela do proprio arquivo; com elas de pe, os 800 sao inalcancaveis sem otimizar a metrica contra o proposito, entao o alvo fica **em revisao, com a medicao escrita ao lado** | 2026-09-05 | `CHANGELOG.md` § 2026-09-05 DOC-01 |
| DOC-02 | **A meta virou 1 750 e nasceu CUMPRIDA — `1 883 -> 1 688`.** O 800 caiu por aritmetica, nao por gosto: apagar `Saude` + `Security` + `Postura` INTEIRAS daria 1 316, ainda 116 acima dele, entao era alvo abaixo do piso que as proprias regras protegem — promessa que a proxima sessao tentaria cumprir cortando o catalogo. Dois movimentos, os unicos que restavam: o **indice anotado de `docs/` (95 linhas) foi para `docs/INDEX.md`**, onde ja havia um portal (duas casas indexando o mesmo acervo, cobradas de TODA sessao no boot), e as **cinco secoes de arco acima do formato** (ContextStore, Audit LGPD, Outbound, Dialog, Scheduler) foram apertadas a 20 linhas, com o detalhe apendado ao destino no MESMO commit. Conferencia de perda: 26 afirmacoes distintivas, 0 perdidas | 2026-09-06 | `CHANGELOG.md` § 2026-09-06 DOC-02 |
