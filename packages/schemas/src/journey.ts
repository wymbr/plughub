/**
 * journey.ts — REMOVED (Arc 19 Fase F)
 *
 * Journey, JourneyType, and all associated schemas have been removed.
 * The Journey entity is superseded by the unified session model (Arc 19):
 * workflows run as channel_type=webhook sessions; hierarchy is visible in
 * the segment trace, not in a separate Journey entity.
 *
 * Arcs 10, 16, and 17 are preserved in CHANGELOG.md for historical reference.
 */
