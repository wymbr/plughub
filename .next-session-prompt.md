continuação projeto plughub.
Leia CLAUDE.md e .session-handoff.md na raiz, deixado pela sessão anterior.

REGRAS OPERACIONAIS (mesmas de sempre):
- O sandbox bash NÃO alcança o WSL: VOCÊ EDITA (Read/Write/Edit), EU RODO e colo as saídas.
- Ambiente = docker-compose.demo.yml. ClickHouse/Postgres db = plughub_demo (user plughub);
  agent-registry em plughub_registry.
- Serviços Python/TS BAKEIAM o source → build <svc> && up -d --force-recreate <svc>.
  Arquivo NOVO exige build --no-cache — e suíte antiga passando NÃO prova que a edição
  entrou na imagem: preflight com `python -c "import …; print(<símbolo novo>)"` antes.
  YAML de skill é exceção (mount ro → restart), mas editar YAML de skill semeado é NO-OP:
  use infra/scripts/deploy_skill_to_slot.sh.
- `up -d --force-recreate` APAGA o `pip install` do pytest no container — reinstalar antes
  de rodar suíte.
- `config-seed` tem IMAGEM PRÓPRIA: `build config-seed` ANTES do `run --rm config-seed`.
- O container do `redis` NÃO tem python3 — parser JSON em script roda no `routing-engine`.
- Smokes/portões/probes = script host em infra/test/*.sh (podem ficar VERMELHOS);
  ferramentas de deploy/manutenção em infra/scripts/.
- Ao me passar um bloco de comandos, NÃO ponha passo manual como comentário `#` no meio:
  vira comentário de shell e o passo some sem aviso.
- Inglês no código; PT em i18n/spec/dados de tenant.

MÉTODO (o que esta série de sessões provou que funciona):
- PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
  (`parametrize` multiplica casos; contar as chamadas de asserção, não o parágrafo).
- Veredicto RAMIFICA sobre o valor medido; nunca costurá-lo no meio de uma frase escrita
  para outro valor. Três ramos: presente-igual, presente-diferente, AUSENTE — e ausente é
  INCONCLUSIVO, nunca verde.
- Antes de comparar duas leituras, PROVAR QUE O LEITOR LÊ (preflight contra valor
  conhecido, abortando com código 3). Duas leituras quebradas são iguais entre si.
- Perguntar POR QUE a regra existe, contra a memória escrita do repo (migrations, ADRs,
  comentários). Isso já corrigiu o rumo de três fases seguidas (C, E duas vezes), e
  nenhuma teria sido pega por teste — o código fazia o que a spec (errada) dizia.
- Antes de REMOVER um efeito, ler o que ele fazia inteiro. Na Fase E, "o work_task_release
  já libera a vaga" era verdade e mesmo assim a supressão era regressão: o
  `remove_conversation` também restaura membership de SET e o espelho current_sessions.

TAREFA: **Fase F (D7)** do ADR docs/adr/adr-work-item-requeue-and-agent-affinity.md.
Fases A–E estão fechadas e validadas (ver CHANGELOG § topo, 5 entradas).

  **Resume terminal-uma-vez.** Sob a D2 o item de trabalho circula na fila até o
  prazo, e o `POST /api/work_queue/expire/:sessionId` do supervisor é a única
  forma de tirá-lo de cena antes disso. A corrida: o item pode estar reivindicado
  e sendo PREENCHIDO quando o supervisor encerra. O segundo a chegar — submit OU
  expire — precisa de recusa explícita, nunca aplicar em silêncio.

  O que já está no lugar e provavelmente se reusa:
  · o veredicto de 4 ramos da Fase A no submit, contra o registro durável do
    árbitro (`{t}:pool:{p}:claim_record:{sid}`). A F talvez seja o MESMO veredicto
    num segundo eixo (terminalidade), não um mecanismo novo — conferir antes de
    projetar qualquer coisa;
  · o vocabulário do fim, já no domínio de SEGMENTO e com produtor único
    (`_wrapup_close_reason`, orchestrator-bridge): `task_submitted` (entrega) ·
    `acw_expired` (prazo) · `acw_supervisor_closed` (supervisor).
  · o expire NÃO preenche no lugar do agente (D5 da ADR author-bound): encerra
    **sem disposição**. Não inventar `outcome` para ele.

Comece medindo antes de propor: quantos itens saem hoje por submit × por expire ×
por prazo, e se existe algum caso de duplo-terminal já gravado no demo. Escreva a
previsão antes de rodar. A fila do `formfill_demo` tem 4 itens parados (dois com
`RESERVATION EXPIRED` desde 2026-08-04) — amostra pronta para olhar
`work_item_deadline` e o caminho do expire.
