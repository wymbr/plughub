continuação projeto plughub.
Leia CLAUDE.md e .session-handoff.md na raiz, deixado pela sessão anterior.

REGRAS OPERACIONAIS (mesmas de sempre):
- O sandbox bash NÃO alcança o WSL: VOCÊ EDITA (Read/Write/Edit), EU RODO e colo as saídas.
- Ambiente = docker-compose.demo.yml. ClickHouse/Postgres db = plughub_demo (user plughub);
  agent-registry em plughub_registry.
- Serviços Python/TS BAKEIAM o source → build <svc> && up -d --force-recreate <svc>.
  Arquivo NOVO exige build --no-cache — e suíte antiga passando NÃO prova que a edição
  entrou na imagem: preflight com `python -c "import …; print(<símbolo novo>)"` antes.
  YAML de skill é exceção (mount ro → restart), mas editar YAML de skill semeado é NO-OP:
  use infra/scripts/deploy_skill_to_slot.sh.
- platform-ui NÃO tem volume mount: toda mudança de frontend exige `build platform-ui`.
  Hard refresh nunca basta — e MEDIR ANTES DO BUILD mede o bundle de ontem.
- `up -d --force-recreate` APAGA o `pip install` do pytest no container — reinstalar antes.
- `config-seed` tem IMAGEM PRÓPRIA: `build config-seed` ANTES do `run --rm config-seed`.
- O container do `redis` NÃO tem python3 — parser JSON em script roda no `routing-engine`.
- Smokes/portões/probes = script host em infra/test/*.sh (podem ficar VERMELHOS);
  ferramentas de deploy/manutenção em infra/scripts/.
- Ao me passar um bloco de comandos, NÃO ponha passo manual como comentário `#` no meio:
  vira comentário de shell e o passo some sem aviso.
- Inglês no código; PT em i18n/spec/dados de tenant. Toda string visível nos DOIS locales.

MÉTODO (o que esta série de sessões provou que funciona):
- **LER O HANDLER ANTES DE NOMEAR O DEFEITO; PERGUNTAR AO ENDPOINT ANTES DE EXPLICAR A
  TELA.** Regra nova (2026-08-04) e a mais cara: quatro hipóteses seguidas caíram numa
  única investigação, todas plausíveis e tiradas do repo, porque eu explicava o sintoma
  a partir do que esperava do código. A resposta estava num `curl` desde o começo. Prever
  o número não protege contra investigar a coisa errada.
- PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar.
- **Contar não é identificar**: `SCARD 1` não diz QUAL. Use `SMEMBERS` quando a identidade
  importa — um probe que conta onde devia identificar produz resultado ambíguo.
- Veredicto RAMIFICA sobre o valor medido; nunca costurá-lo no meio de uma frase escrita
  para outro valor. Três ramos: presente-igual, presente-diferente, AUSENTE — e ausente é
  INCONCLUSIVO, nunca verde.
- Antes de comparar duas leituras, PROVAR QUE O LEITOR LÊ (preflight contra valor
  conhecido). Duas leituras quebradas são iguais entre si.
- **Toda montagem sintética deixa rastro** (token morto, vaga presa, item inreivindicável).
  Anotar o que foi escrito e desfazer na mesma rodada — dois falsos diagnósticos da última
  sessão foram sobre lixo produzido por mim.
- Perguntar POR QUE a regra existe, contra a memória escrita do repo.
- **Quando o dono do produto enuncia a REGRA GERAL, testar a exceção contra ela.** Foi assim
  que caiu o `rank` 0 do item com reserva expirada — e, no caminho, apareceu um sort que
  ordenava por `queued_at_ms` (que RESETA na devolução à fila) enquanto a tela exibia idade
  de `first_queued_ms` (que não reseta): lista que parecia ordenada pelo número que
  mostrava, e não estava.
- **Contar não é identificar**: `SCARD 1` não diz QUAL sessão. Use `SMEMBERS` quando a
  identidade importa — ambíguo passa por medição.

ESTADO: o ADR docs/adr/adr-work-item-requeue-and-agent-affinity.md está FECHADO (A–F + F2).
Nada pendente nele. A inbox de pull mudou na mesma sessão e foi validada na tela: crachás de
reserva expirada/transbordo REMOVIDOS (a ausência já é a notação de "qualquer um pega"),
`rank` 0 só para `reservedToMe` dentro da janela, e desempate pelo `ageOf` EXIBIDO — item
devolvido à fila reaparece na posição da própria idade, não no fim.

TAREFA — escolher uma; as duas saíram da validação do F2 e estão em TODO.md
§ "Fila de trabalho humano … resíduos pós-v1" → "Achados de 2026-08-04":

  (a) **Recusa de claim efêmera demais.** Os dois caminhos avisam — inbox por erro fixo
      no painel, preview por `addToast`. Com as vagas cheias o operador clica, o toast
      passa, a tela não muda, e ele conclui "reivindiquei e veio vazio" (foi o que
      disparou uma investigação inteira na direção errada).
      **PRIMEIRO VERIFICAR se o toast realmente aparece** — encher a capacidade
      (reivindicar até `max_concurrent`), clicar, olhar. Só então decidir entre torná-lo
      persistente, marcar o item na lista, ou nada. Não consertar o que pode não estar
      quebrado: a primeira redação deste item afirmava que o preview engolia o motivo, e
      era falso.

  (b) **Vaga ocupada por sessão que nunca virou cartão.** `9d37879e` foi reivindicada,
      consumiu vaga e não apareceu no Console; o `release` confirmou posse real
      (`released:true, requeued:true`). Causa DESCONHECIDA — reproduzir antes de teorizar,
      e não confundir com a ocupação artificial que a montagem sintética do F2 produziu no
      mesmo dia. Mais caro: pode levar a sessão inteira.

Se nenhuma das duas, o próximo arco natural é outro — perguntar antes de começar.

LIXO CONHECIDO NO DEMO: `509d5441-3fee-416d-8baa-2317ab1aac7b` está no ZSET de
`formfill_demo` com o token de resume MORTO (apagado numa montagem sintética e não
restaurado). Ele renderiza o formulário normalmente quando reivindicado — só o submit
falharia. `ZREM tenant_demo:pool:formfill_demo:queue <sid>` resolve.
