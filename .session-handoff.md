continuação sessão plughub.

Recado da sessão anterior (2026-08-04): **Fases A, B, C e D do ADR de requeue
estão FECHADAS e validadas** — todas as quatro com e2e no Console, não só em
teste. Próximo ponto de entrada: **Fase E (D8)**.

ONDE O ARCO CHEGOU: a duplicação acabou, efetiva E visual. O item volta pelo
caminho de pull com o pacote intacto, reservado a quem caiu; o submit de quem não
detém toma 403; e a tela não redesenha mais formulário de item que está na fila.
Restam **E** (drop não publica `agent_done`; separar os mapas de `close_reason` —
fecha a lacuna 6) e **F** (resume terminal-uma-vez: corrida submit × expire).

LEIA PRIMEIRO, nesta ordem:
  1. `docs/adr/adr-work-item-requeue-and-agent-affinity.md` — estado do desenho.
     Atenção às DUAS emendas: **§ D6 Emenda** (posse não cabe no ledger) e
     **§ D2** (o `first_queued_ms` saiu do escopo, com o número medido).
  2. `CHANGELOG.md` § topo — as duas entradas (Fase A e Fase B).
  3. `TODO.md` § I5 → "um F5 no Console devolve à fila um item em trabalho".
  4. `TODO.md` § "Erros de método que se repetem" (topo) — segue valendo inteira.

ONDE ESTAMOS, em três frases. A **duplicação efetiva** está fechada: a posse tem
registro durável no árbitro (A), e o item volta pelo caminho de pull com o pacote
intacto (B). A **duplicação visual** continua — o Console redesenha o formulário
por replay do `conversation.assigned` na reconexão, e isso é a **D5 / Fase D**.
O `agent_done` falso e o segmento sem `close_reason` seguem: **Fase E**.

CORREÇÃO DA FASE C, feita por REVISÃO do dono do produto (não por medição):
`-int` **não transborda**. A primeira versão semeava `drop_reserve_window_internal_s
= 300`, tratando fila interna como "janela generosa" — é o **erro de categoria** que
a migration `20260730000000_pool_internal_queue` já nomeava por escrito: *"a
identidade do executor é parte da DEFINIÇÃO da tarefa, não uma preferência"*.
Transbordar wrap-up entregaria a outro agente a classificação de um atendimento
que ele não fez. Hoje: `-int` → reserva PERMANENTE (campo ausente), saída pelo
prazo ou pelo expire do supervisor; pooled (aprovação, demos) → 30 s.
  · **os 5 pytests da Fase C passavam AFIRMANDO os 300 s** — spec errada com selo
    verde. Não havia sintoma: o caminho normal nunca alcança a chave (wrap-up real
    chega com `assigned_to`, e a regra 1 não sobrescreve). O que a expôs foi a
    pergunta "por que a regra existe", contra a memória escrita do repo.

CONFIG APLICADA: `routing.drop_reserve_window_default_s = 30` confirmada no store
(`curl .../config/routing/drop_reserve_window_default_s?tenant_id=tenant_demo`).
⚠️ `config-seed` tem **imagem própria** — `build config-seed` ANTES do `run --rm`,
senão roda o seed.py antigo em silêncio. O run revelou deriva acumulada
(`inserted=2`: a minha + `survey.link_delivery`, que nunca fora aplicada) → virou
proposta de portão em `TODO.md` § "Portão de deriva do seed do config-api".

PROVADO NA UI (2026-08-04): claim → F5 → Submit no formulário órfão devolve
**403 `resume: work item is back in the queue — claim it before submitting`**.
O `DialogFormRenderer` exibia só `HTTP 403` e engolia o `detail` — corrigido, o
motivo agora chega ao operador. Para reproduzir:
`bash infra/test/smoke_formfill_renderer.sh` → reivindicar no Console → F5 → Submit.

Conferido também que o form NÃO perde campo no F5 (3 nós em
`dialog_formfill_demo`, os 3 na tela antes e depois). O que o F5 leva é o que
estava DIGITADO — estado do React, só existe no navegador. É o custo registrado no
ADR §5, e a mitigação decidida é a janela de reserva da Fase C, não persistir
rascunho.

FASE E (D8) — o próximo, e o que já se sabe:
  · **queda de transporte não é conclusão de trabalho**, mas hoje todo drop publica
    `agent_done` no `agent.lifecycle` e fecha segmento. Sob D2 (o item circula até
    submit/prazo) uma conexão instável produz uma PILHA de `agent_done` falsos e de
    segmentos por um único wrap-up — contamina contagem, AHT e a bancada de agentes.
  · o fim de segmento por queda precisa de um `close_reason` do **domínio de
    SEGMENTO** (vocabulário livre, onde já vivem `task_submitted`, `acw_expired`,
    `acw_supervisor_closed`).
  · ⚠️ **NÃO** acrescentar `agent_disconnect` ao `_TRANSPORT_TO_CLOSE_REASON`: o mapa
    serve DOIS domínios — `main.py:5755` = contato (enum fechado, `CloseReasonSchema`)
    e `:6401` = segmento (vocabulário livre). No `agent_disconnect` o contato NÃO
    fecha, então estender o compartilhado escolheria um domínio em silêncio. Separar
    os mapas fecha a **lacuna 6** junto.
  · o produtor concreto da lacuna 6 já está medido: todo F5 gera segmento sem
    `close_reason`, e o próprio sistema loga isso.

FASE D (D5) ✅ — como foi resolvida (para não refazer o diagnóstico):
  · o replay NÃO vinha do pub/sub: vinha de `pool:pending_assignment:{pool}` (TTL
    300 s, escrita pelo bridge, reentregue na reconexão em `server.ts`).
  · as duas guardas que já existiam ali não pegavam o caso — workflow SUSPENSA (sem
    `session:closed`) e `instance_id` BATENDO (é o mesmo agente).
  · guarda nova pergunta a posse ao árbitro; veredicto em função PURA
    `shouldDropOnPossession` (`lib/assignment-filter.ts`), 4 ramos iguais aos do
    submit e do drop. Testes: `possession-filter.test.ts` (9).
  · **aberto:** `pool:pending_assignment` não é apagado no `work_task_release` —
    vive até o TTL e é descartado na entrega. Conserto no produtor = follow-up.

FASE D — o que já se sabia (mantido como registro do caminho):
  · **o Console deve derivar "eu detenho este item" do CLAIM**, não do
    `conversation.assigned` que o mcp-server REPLAY-a na reconexão. Hoje esse
    replay parece recuperação e é parte do defeito: exibe formulário de item que o
    backend já devolveu à fila.
  · sintomas observados na tela (todos a MESMA causa): form órfão aberto ·
    "Return to queue" sobre item não detido · mesmo id em CONTACTS **e** em PULL
    QUEUES · toast "Novo contato atribuído" sobre item que voltou à fila.
  · a fonte de verdade já existe e já é servida: `POST /v1/work_queue/holder`
    devolve `{found, instance_id, claimant_user_id, via, in_queue}`. `in_queue`
    responde "ninguém detém" de forma POSITIVA.
  · o backend já está seguro (Fase A recusa o submit com 403) — a Fase D é sobre
    a tela parar de oferecer o que não pode ser feito.
  · ⚠️ **contato de cliente entra no arco** (decisão do dono do produto, 08-04).
    É o pedaço que mexe com atendimento ao vivo.

A LIÇÃO DO DIA, e é sobre INSTRUMENTO — três medições não decidiram nada, todas
pela mesma família (converter ausência/falha em afirmação), e **uma delas deu
verde**:
  · espécime morto respondeu `TTL -2` — mas o contato tinha sido finalizado na
    tela. Ausência em item morto não fala sobre caminho de escrita.
  · o pytest enfileirava chamando `add_queued_contact` DIRETO: exercita a função
    lida, não a rota do produto. Ficaria verde mesmo se a rota divergisse.
  · **FALSO VERDE**: o probe rodava `python3` no container do REDIS, que não o
    tem. O `|| echo ABSENT` fazia a mensagem de erro sair como valor; o veredicto
    comparou antes×depois, achou DUAS MENSAGENS DE ERRO IGUAIS e imprimiu
    "✅ FASE B ATIVA". O guarda testava `= "ABSENT"` e passou ao lado (o valor era
    o erro *terminando* em ABSENT).
    → Consertos: **preflight** (testa o parser contra JSON conhecido e aborta com
      código 3 ANTES de medir) e **veredicto sobre a FORMA do valor final**, nunca
      sobre igualdade antes×depois — duas leituras quebradas são sempre iguais.
    → Regra que fica: **comparar duas leituras só vale depois de provar que o
      leitor lê.** E o que decidiu de verdade foi `redis-cli GET` cru, lido com os
      olhos: o instrumento que não tem como quebrar é o que só imprime.

GATES DESTAS FASES (rodam verdes hoje):
  · `infra/test/smoke_claim_possession.sh`                      — 30 PASS, 3 estados
  · `routing-engine  … tests/test_claim_possession_record.py`   — 19
  · `channel-gateway … tests/test_resume_possession_check.py`   —  7
  · `orchestrator-bridge … tests/test_work_item_release_on_drop.py` — 14
  · manuais: `probe_fase_b_release_on_reload.sh` (precisa do Console),
    `probe_first_queued_on_real_item.sh`, e os da sessão da manhã
    (`probe_reclaim_duplication.sh`, `probe_requeue_culprit.sh`,
    `probe_invisibility_window.sh`, `probe_console_restore_after_reload.sh`)

REGRAS OPERACIONAIS (as de sempre + o que estas sessões acrescentaram)

· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e
  cola as saídas
· PREVER O NÚMERO antes de rodar, e escrever a previsão. **CONTAR, não estimar** —
  errei duas contagens de teste em 08-04 (`parametrize` multiplica os casos)
· **`up -d --force-recreate` APAGA o `pip install` do pytest no container.**
  Reinstalar antes de rodar suíte, senão sai `pytest: executable file not found`
· **o container do `redis` NÃO tem python3.** Parser JSON roda no `routing-engine`
· veredicto RAMIFICA sobre o valor medido; nunca costurá-lo no meio de uma frase
  escrita para outro valor. Três ramos: presente-igual, presente-diferente,
  AUSENTE (e ausente é INCONCLUSIVO, nunca verde)
· `grep -c` com ZERO matches sai com código 1 — numa cadeia `&&` isso MATA o
  resto do comando. Exit ≠ 0 não é vermelho
· **todo `docker exec` dentro de script interativo leva `< /dev/null`** — senão
  consome o stdin e os `read` voltam vazios SEM ERRO. Corolário: isso EXCLUI
  passar dado por pipe no mesmo comando; use `-e VAR`
· **`formfill_demo` é o caminho barato** para exercitar claim/pull/renderer (pool
  pull, mas NÃO `-int` nem author-bound — `assigned_to` vazio ali é normal)
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo (user plughub), agent-registry em
  plughub_registry (NÃO plughub_demo)
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 (interna) ·
  mcp-server-plughub 3100 · platform-ui 5174 · auth-api 3202 NO HOST
· config-api: o namespace `routing` NÃO devolveu `claim_lease_s` nas tentativas
  de 08-04. Conferir antes de usar config-api como fonte de régua
· serviços Python/TS bakeiam o source → `build <svc> && up -d
  --force-recreate <svc>`; arquivo NOVO exige `build --no-cache`
· mudança em @plughub/schemas → rebuild de schemas + agent-registry +
  skill-flow-service + mcp-server JUNTOS (senão o registry rejeita com 422)
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer. Só o
  segundo roda
· YAML de skill não basta editar nem publicar — use
  infra/scripts/deploy_skill_to_slot.sh
· pytest roda DENTRO do container, em `src/<pacote_snake>/tests/`. O pacote do
  routing-engine é `plughub_routing`
· smokes e portões em infra/test/ (o que pode ficar VERMELHO); ferramentas de
  deploy/manutenção em infra/scripts/
· inglês no código, PT em i18n/spec/dados de tenant
