continuação sessão plughub.

Recado da sessão anterior (2026-08-04, noite): **ADR de requeue FECHADO — arco A–F
completo**. A Fase F (D7) entrou, com pytest e smoke verdes contra Redis real.
Resta só o **F2**, que é UI pura.

ONDE O ARCO CHEGOU. A duplicação acabou (A+B+D), a devolução é reservada ao dono
(C), a queda de transporte não se declara mais conclusão de trabalho (E), e agora
o resume é **terminal-uma-vez**: o segundo a chegar — submit, supervisor ou prazo
— recebe **409 com a causa nomeada**, nunca um 200 silencioso nem o 404 que
mentia.

LEIA PRIMEIRO, nesta ordem:
  1. `CHANGELOG.md` § topo — Fase F, com a tabela previsto×medido.
  2. `docs/adr/adr-work-item-requeue-and-agent-affinity.md` § **D7 Emenda** — as
     três correções de premissa. As anteriores (D6, D2, D8) seguem valendo.
  3. `TODO.md` § "Erros de método que se repetem" (topo) — seguem valendo.

FASE F — o que ela ensinou, e que vale para a próxima

  · **A previsão que ERRA é a que ensina.** Previ `acw_expired = 0` supondo que
    os itens do demo são recriados dentro das 24 h. Medido: **2**, de 30/07, com
    **100 s e 111 s de segmento real** — o scanner encerrou por prazo um item que
    um humano segurava havia quase dois minutos. A janela da D7 já tinha sido
    atravessada com trabalho vivo na mão; só não houve colisão. Sem a previsão
    escrita antes, o "2" teria passado por ruído.

  · **O defeito não era onde a ADR o descrevia.** A D7 fala de fila pull. Mas os
    três gatilhos entram pela MESMA `handle_resume` (o endpoint do supervisor não
    tem rota própria — lê o ledger e faz este mesmo resume), e o hash
    `resume_tokens` é escrito por `suspend`, `delegate` E `collect`. Toda workflow
    suspensa já tinha dois retomadores possíveis. Pull só acrescenta o terceiro
    gatilho. A correção age no consumo do token e cobre os outros de graça.

  · **A corrida não precisa de réplicas.** O scanner de prazo roda no MESMO
    processo e no mesmo event loop do endpoint HTTP. Basta um interleave de
    `await` — e é por isso que ela é barata de construir num teste.

  · **A Fase A NÃO se reusava.** O roteiro supunha que a F fosse o veredicto de 4
    ramos num segundo eixo. O bloco A5 está gated em
    `if approver is not None and claim_instance_id`, e supervisor e scanner chegam
    sem `claim_instance_id` — o árbitro nunca é consultado nos dois terminais
    não-submit. Conferir antes de reusar salvou a fase de nascer errada.

  · **Um teste quase passou pelo motivo errado**, e o padrão é novo o bastante
    para valer registro: o fake de Redis não tinha `await` interno, então a
    primeira corrotina rodava até o fim sem ceder o controle e a segunda caía no
    caso SEQUENCIAL. Verde, reprovando contra o código antigo, e sem ter
    exercitado a corrida uma única vez. Conserto: `sleep(0)` em cada operação do
    fake + asserção exigindo `state == "in_flight"`.

  · **O smoke pegou o que o pytest não vê:** o detentor do lock lido sem
    decodificar sairia no 409 como `closed_by: "b'agent'"`. O fake devolve `str`;
    o cliente real pode devolver `bytes`.

  · **Consequência ACEITA (decisão do dono do produto):** o lock dá unicidade,
    não prioridade. Uma entrega já preenchida pode perder para uma expiração numa
    janela de segundos. O conserto, se um dia for preciso, **não é no lock** — é
    dar ao scanner um sinal de "item em preenchimento", que ele hoje não tem.

PRÓXIMO — F2: o Console lê o 409

  O 409 nomeado existe no fio e ninguém o lê. Hoje `expirePending`
  (`packages/platform-ui/src/modules/work-items/api.ts`) faz
  `throw new Error('HTTP 409: {json cru}')` e o operador vê o corpo bruto. Não é
  silencioso, então não é regressão — mas a promessa da fase (*"um supervisor
  encerrou"* em vez de *"sua sessão expirou"*) só se cumpre aqui.

  · corpo do 409: `{ error, state: "in_flight"|"terminal", session_id,
    closed_by, cause, closed_at }` em `detail`;
  · `cause` ∈ `task_done` · `acw_expired` · `acw_supervisor_closed`;
  · dois consumidores: `work-items/api.ts` (expire do supervisor) e o
    `DialogFormRenderer.tsx` (submit do agente que perdeu a corrida);
  · **i18n obrigatório nos DOIS locales** antes de considerar pronto (invariante
    do CLAUDE.md) — nenhuma string em JSX;
  · atenção: o mcp-server embrulha o 409 do gateway como
    `{error:"expire_failed", detail:<corpo do gateway>}` — o `detail` do
    Console vem aninhado por esse salto.

ACHADOS ABERTOS, NÃO BLOQUEANTES — não misturar com nada
  · **`source` asserido pelo cliente** na porta pública de resume: um externo
    pode declarar `source:"supervisor:x"` e obter `acw_supervisor_closed`. A F
    tornou a causa DURÁVEL (25 h), então deixou de ser aceitável por omissão.
    Item próprio em `TODO.md`, com o motivo de não ter sido fechado junto (o
    downgrade cego derrubaria o expire do supervisor, que tem 0 ocorrências no
    demo e portanto não reclamaria).
  · **`[agent-ws] Pool registration returned HTTP 401`** em todo pool do login.
    Item próprio em `TODO.md` desde a Fase E.

GATES QUE RODAM VERDES HOJE
  · `channel-gateway … tests/test_resume_terminal_once.py`               — 17
  · `channel-gateway … tests/test_resume_possession_check.py`            —  7
  · `channel-gateway … tests/test_webhook_adapter.py`                    — 37
  · `orchestrator-bridge … tests/test_segment_close_reason_domain.py`    — 15
  · `orchestrator-bridge … tests/test_work_item_release_on_drop.py`      — 14
  · `routing-engine … tests/test_agent_released_lifecycle.py`            —  4
  · `routing-engine … tests/test_claim_possession_record.py`             — 19
  · `infra/test/smoke_resume_terminal_once.sh`                           —  8
  · `infra/test/smoke_claim_possession.sh`                               — 30
  · sondas (exigem Console): `check_fase_e_drop_stamp.sh`,
    `probe_fase_b_release_on_reload.sh`
  · probes de medição, não gates: `probe_fase_e_drop_footprint.sh`,
    `probe_fase_f_terminal_footprint.sh`

COMO CRIAR A CONDIÇÃO DE PULL (a receita que funcionou, na ordem)
  1. `bash infra/test/smoke_formfill_renderer.sh` → devolve o `session_id`
  2. Console (`localhost:5174`) → logar → ativar o pool `formfill_demo`;
     conferir: `logs mcp-server-plughub | grep '\[agent-ws\]'` tem de mostrar
     `Human agent registered`
  3. reivindicar o item na inbox; conferir ANTES de seguir:
     `GET tenant_demo:pool:formfill_demo:claim_record:{sid}` e
     `SMEMBERS session:{sid}:human_agents` — os dois não vazios
  4. F5 → esperar **~6 s** (graça 2,5 s + processamento) → rodar a sonda
  (pular um checkpoint custa uma rodada inteira; custou duas em 08-04)

REGRAS OPERACIONAIS (as de sempre + o que esta sessão confirmou)

· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e
  cola as saídas
· PREVER O NÚMERO antes de rodar, e escrever a previsão. **CONTAR, não estimar**
  (17 e 44 bateram exatos em 08-04; o combinado de 61 NÃO teria provado o 17 —
  contar por arquivo é o que fecha)
· **arquivo NOVO exige `build --no-cache`.** Edição de arquivo existente entra no
  build normal
· **suíte antiga passando NÃO prova que a edição entrou na imagem.** Preflight
  antes: `python -c "import …; print(<símbolo novo>)"` — 3 estados (valor /
  AttributeError = imagem velha / ImportError = instrumento)
· **um dublê sem `await` interno não interlaça corrotinas** — um teste de corrida
  sobre `asyncio.gather` pode medir o caso sequencial e ficar verde. Forçar o
  yield e ASSERTAR qual ramo saiu
· **`up -d --force-recreate` APAGA o `pip install` do pytest.** Reinstalar antes
· **o container do `redis` NÃO tem python3.** Parser JSON roda no `routing-engine`
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO,
  nunca verde
· antes de comparar duas leituras, PROVAR QUE O LEITOR LÊ (preflight contra valor
  conhecido, aborta com código 3)
· `grep -c` com ZERO matches sai com código 1 — numa cadeia `&&` mata o resto
· **todo `docker exec` dentro de script interativo leva `< /dev/null`**
· **cuidado ao colar bloco com `#`** — vira comentário de shell e o passo manual
  no meio some sem aviso
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo (user plughub), agent-registry em
  plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 (interna) ·
  mcp-server-plughub 3100 · platform-ui 5174 · auth-api 3202 NO HOST
· serviços Python/TS bakeiam o source → `build <svc> && up -d --force-recreate <svc>`
· **platform-ui não tem volume mount** — toda mudança de frontend exige
  `build platform-ui`; hard refresh nunca basta (relevante para o F2)
· mudança em @plughub/schemas → rebuild de schemas + agent-registry +
  skill-flow-service + mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer. Só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· pytest roda DENTRO do container, em `src/<pacote_snake>/tests/`. O pacote do
  routing-engine é `plughub_routing`
· smokes/portões/probes em `infra/test/` (podem ficar VERMELHOS);
  ferramentas de deploy/manutenção em `infra/scripts/`
· inglês no código, PT em i18n/spec/dados de tenant
· em ClickHouse, alias de agregado NUNCA repete nome de coluna real (sufixo `_ref`)
