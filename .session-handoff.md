continuação sessão plughub.

Recado da sessão anterior (2026-08-05, tarde). Três itens fechados, e o padrão
importa mais que os itens: **três de quatro entradas antigas estavam mal
descritas**, cada uma de um jeito diferente. Uma já estava consertada, uma trazia
um **conserto errado embutido**, uma descrevia um trecho que **ninguém executava**.
A do meio é a mais perigosa — parece pronta para executar.

LEIA PRIMEIRO, nesta ordem:
  1. `CHANGELOG.md` § topo — duas entradas novas:
     "`force-complete` deixou de mentir" · "O runner e2e ganhou credencial de serviço".
  2. `TODO.md` § "Fixtures do e2e ainda falam AgentType" (item NOVO, com mapa pronto).
  3. `TODO.md` § "Lacuna 4 — `force-complete`" e § "Lacuna 3 — não era defeito".

═══ O QUE ENTROU (código) ═══

  · **e2e ganhou service token.** `RegistryClient` (3º parâmetro `serviceToken`,
    default `AGENT_REGISTRY_SERVICE_TOKEN`, omitido quando vazio) + os três
    `create*` colapsados num `createIgnoringConflict` que ANOTA 401/403 com
    `[x-service-token=enviado|AUSENTE]`. **Env adicionada ao `e2e-runner` do
    `docker-compose.demo.yml`** — sem isso o header no código é no-op.

  · **`force-complete` ramificado em três** (`mcp-server/server.ts`): item
    parqueado → 200 pelo caminho do prazo vencido (resume `decision:"timeout"`,
    Bearer repassado); pipeline `:running` sem item → **501** que NOMEIA a
    ausência de mecanismo; nada → 404. UI nas DUAS superfícies
    (`OrchestrationTab`, `ParticipantFilterBar`): Bearer + 404×501 como mensagens
    OPOSTAS. i18n nos dois locales.

  · **Comentário em `routing-engine/main.py` §1238-1262** — só comentário, sem
    rebuild necessário. Explica por que o `continue` de `dispatch_mode == "pull"`
    fica ANTES da varredura, para o próximo leitor não "consertar" o que está certo.

═══ O ACHADO QUE VALE MAIS QUE OS TRÊS ═══

  **A lacuna 4 descrevia o HANDLER; o defeito estava no CAMINHO.** Ela dizia que o
  `force-complete` "só reescreve uma chave Redis" — verdade sobre o handler, e
  irrelevante: as duas chamadas da UI iam **sem `Authorization`** num endpoint que
  exige `supervisor|admin`, logo tomavam **401 antes de chegar lá**. O botão nunca
  funcionou em lugar nenhum. O comportamento descrito só era alcançável por curl.

  **Regra que sai disso:** ao auditar um endpoint, ler também **quem o chama e com
  o quê**. Senão descreve-se com precisão um trecho que ninguém executa.

  Segundo achado da mesma leitura: **chave com TTL não serve de condição de
  existência.** O `404 session_not_found` vinha de `session:{sid}:meta` ausente —
  e o caso que motiva o botão é a sessão parada há muito tempo, cujo `meta` já
  expirou. O guarda escondia exatamente o alvo.

═══ CORREÇÕES DE ROTA (duas, ambas de método) ═══

  · **A receita de contorno virou canônica sem ninguém decidir.** O handoff
    anterior mandava rodar o e2e no HOST com `REDIS_URL=…6380`/`KAFKA_BROKERS=…9093`.
    **Existe um serviço `e2e-runner` no `docker-compose.demo.yml`** (§1659), com
    URLs internas, `TENANT_ID: tenant_demo`, `redis:6379`/`kafka:29092` — sem
    divergência de porta alguma. A receita de portas era gambiarra de alguém.
    **Ao herdar receita de handoff, perguntar: isto é o jeito certo ou o contorno
    de alguém?**

  · **Header no código é no-op sem o aplicador.** O `AGENT_REGISTRY_SERVICE_TOKEN`
    faltava no `environment:` do `e2e-runner`, e o arquivo mora na imagem (`COPY`,
    sem volume). Mesma família de skill YAML, slot e seed do config-api.

═══ FECHADO SEM CÓDIGO (o registro é que envelheceu) ═══

  · **Lacuna 3** (`fila pull nunca tem teto de espera`) — **não é defeito**, e o
    conserto que ela propunha seria regressão: (a) `_emit_queue_timeout` fecha um
    CONTATO (cortesia, `outcome=abandoned`, segmento sintético `role=queue`) e item
    de trabalho não tem cliente; (b) sem `queue_config.max_wait_s` o pool herdaria
    o default de fila muda (1800 s); (c) já há autoridade — `work_task_expire`
    (`router.py` §1107-1112) trata nomeadamente o nunca-reivindicado, apaga a lease
    e devolve a VAGA. **A fila pull TEM teto**: o `timeout_hours` do delegate.

  · **Lacuna 6, metade A** — "os dois domínios compartilham o mesmo mapa de
    `close_reason`" **já foi consertado** na Camada E:
    `_TRANSPORT_TO_SEGMENT_CLOSE_REASON` (`bridge/main.py:3256`) com teste
    dedicado de não-sobreposição (`test_segment_close_reason_domain.py:124`).
    Aberto segue **só o enum** (`contact-segment.ts:83` e `:135`, `z.string()` livre).

═══ O QUE NÃO ESTÁ PROVADO (dito de propósito) ═══

  · **A mudança de UI do `force-complete`** — o probe exerce o ENDPOINT por curl.
    Que a faixa certa apareça na tela em cada estado foi verificado por leitura.
  · **Leitor de `role`** (§1055 Fatia B) — segue não-exercitado. O cenário 10 não
    roda: barrado pelas fixtures, não mais pela autenticação.
  · **`/v1/workflow/instances/:id/cancel` = 410 hard** — não conferido nesta passada.

═══ PRÓXIMOS CANDIDATOS ═══

  1. **Fixtures do e2e ainda falam AgentType** (§ próprio no TODO, mapa pronto) —
     `/v1/agent-types` **não existe** no agent-registry (entidade aposentada na
     Fase 3d/C; identidade de agente = `skill_id`). O seed morre em `seed.ts:94`.
     `09`/`11` já migraram o avaliador; o executor (`agente_retencao_v1`) ficou em
     ~15 chamadas de 9 arquivos. Preferir migrar as identidades a semear uma skill
     com nome de agent_type. **Conferir antes se `01`–`04` usam mesmo esse id.**
  2. **Lacuna 2** — reaper de lease. Vaga ✅ e transbordo por idade ✅; sobra o
     agente que reivindica e some SEM teardown de WS: lease de 180 s existe, nada
     a varre, item invisível até o `timeout_hours` (24 h). Instrumento já existe
     (estado `orphaned` do relatório de pendências). Decidir política antes de codar.
  3. **Lacuna 6, metade B** — enum de `close_reason` de segmento. Toca
     `@plughub/schemas` ⇒ rebuild conjunto (agent-registry + skill-flow-service +
     mcp-server). Inventariar os literais VIVOS antes de fechar o domínio.
  4. **Fatia 3 — `work_item.expired`** — o evento **não existe em lugar nenhum**
     (zero ocorrências em `packages/`): nem schema, nem tópico, nem consumidor.
     É o maior. Dá histórico ao nunca-reivindicado.

═══ INSTRUMENTO NOVO ═══

  · `infra/test/probe_force_complete_branches.sh` — mede os TRÊS ramos
    (200/404/501) + controle negativo sem Bearer. Desenho que vale copiar:
    **preflight no artefato** (senão "404 em tudo" == "não rebuildou"); **ramo A é
    testemunha** (sem um 200 real, três 404 passam por "ramificou"); **A julga
    CONSEQUÊNCIA** (relê o ledger e exige o `resume_token` sumido — um 200 sem isso
    seria a mesma mentira em invólucro novo); **INCONCLUSIVO tem ramo próprio**
    (ledger ausente em 4 s ⇒ exit 2, porque "não parqueou a tempo" não é reprovação).

═══ GATES ═══

  · `bash infra/test/probe_force_complete_branches.sh` → **9/9 VERDE** (previsão
    escrita antes, todos os ramos bateram).
  · e2e, ramo que APROVA:
      DC="docker compose -f docker-compose.demo.yml"
      $DC run --rm e2e-runner ts-node runner.ts --only 10
    → seed passa de `/v1/skills`; morre adiante em `seed.ts:94` (fixtures).
  · e2e, ramo que NEGA (controle negativo):
      $DC run --rm -e AGENT_REGISTRY_SERVICE_TOKEN= e2e-runner \
        ts-node runner.ts --only 10
    → 401 COM `[x-service-token=AUSENTE …]`.
  · Rebuildados nesta sessão: `mcp-server-plughub`, `platform-ui`, `e2e-runner`.
    `routing-engine` NÃO precisou (só comentário).

═══ REGRAS OPERACIONAIS (as de sempre) ═══

· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **platform-ui não tem volume mount** — mudança de frontend exige `build`
· **teste também mora na imagem** — controle negativo por stash exige REBUILD;
  preferir teste DIFERENCIAL (as duas leituras lado a lado, afirmando divergência)
· arquivo NOVO exige `build --no-cache`; edição de existente entra no build normal
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· antes de comparar duas leituras, PROVAR QUE O LEITOR LÊ (e conferir o PREFIXO)
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **contar não é identificar** — `SCARD 1` não diz QUAL; use `SMEMBERS`
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3 — parser JSON roda no `routing-engine`
· usar `docker compose … exec -T redis`, nunca `docker exec redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **experimento sensível a tempo roda DENTRO de um processo**
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway — apontar login para lá devolve "login falhou" sem culpa da senha) ·
  **redis 6380 · kafka 9093 · postgres 5433** (remapeados no HOST; por dentro do
  compose são 6379/29092/5432)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ RECEITAS QUE FUNCIONARAM ═══

  Bearer de supervisor (o `force-complete` e o `expire` exigem):
    curl -s -X POST http://localhost:3202/auth/login \
      -H Content-Type:application/json \
      -d '{"email":"admin@plughub.local","password":"changeme_admin","tenant_id":"tenant_demo"}' \
      | jq -r .access_token

  Sessão COM item parqueado (para exercitar o ramo 200):
    curl -s -X POST http://localhost:8010/v1/channels/webhook/pool/formfill_demo_ia \
      -H Content-Type:application/json \
      -d '{"tenant_id":"tenant_demo","context":{"session.briefing_session_id":"sess_briefing_demo"}}'

  Conferir o que RODA (não o que está no repo):
    $DC exec -T mcp-server-plughub \
      grep -c nothing_to_complete /app/packages/mcp-server-plughub/dist/server.js
