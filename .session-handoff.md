continuação sessão plughub.

Recado da sessão anterior (2026-08-05, noite). Um item fechado — o candidato 1 do
handoff anterior. Mas o que vale mais é o que apareceu **ao executá-lo**: o item
errava em três eixos diferentes (contagem, escopo, causa), e o vermelho que sobrou
no fim é maior que o item inteiro.

LEIA PRIMEIRO, nesta ordem:
  1. `CHANGELOG.md` § topo — "O seed do e2e parou de falar de uma entidade aposentada".
  2. `TODO.md` § "`agent_ready` não inscreve instância em pool nenhum" (item NOVO,
     e o candidato mais forte para esta sessão).
  3. `TODO.md` § "Fixtures do e2e ainda falam AgentType" (reescrito; sobram 3 bolsões).

═══ O QUE ENTROU (código) ═══

  · **Seed do e2e destravado.** Removido o bloco morto de `createAgentType` do
    `seedBaseFixtures` (`/v1/agent-types` **não existe** — entidade aposentada na
    Fase 3d/C). Migradas **23 call sites** de `"agente_retencao_v1"` →
    `"skill_retencao_oferta_v1"` em 11 arquivos (01, 02, 03, 04, 06×5, 07×6, 08×3,
    09, 10, 11, regressions×2) + 2 comentários. Saída (a) do TODO, não a (b) barata.

  · **Avisos de rota aposentada** em `lib/http-client.ts`
    (`createAgentType`/`listAgentTypes`/`getAgentType`) e correção de um comentário
    que dizia "`/v1/agent-types` NÃO é gateado" — verdadeiro e enganoso: não é
    gateado porque **não existe**.

  · **`seedPerfFixtures` marcada, não removida** — duas afirmações falsas no bloco,
    deixadas visíveis: (1) "used by Scenario 5" mas **ninguém a chama**; (2) usa a
    rota morta. O cenário 05 monta `agent_perf_{i}_v1` a partir de fixtures que
    nunca foram semeadas em execução alguma.

  · Só `e2e-runner` foi rebuildado. Nenhum serviço tocado.

═══ O ACHADO QUE VALE MAIS QUE O ITEM ═══

  Com o seed destravado, o **cenário 01 rodou pela primeira vez**: 10 verdes e um
  vermelho — `Pool retencao_humano contains instance after agent_ready`,
  `available: []`. Três fatos medidos, em gravidade crescente:

  1. **O caminho não pode popular o SET.** `agent_ready` itera `pools` do hash da
     instância; o cliente HTTP devolve `pools: []` **sem ramo algum**
     (`registry-client.ts` §71) — nenhum `skill_id` produz outra coisa. Junto vêm
     `max_concurrent_sessions: 1` e `permissions: []`, igualmente fixos.
  2. **O SET não tem leitor.** `{tenant}:pool:{p}:available` tem UM `sadd`, quatro
     `srem` e **nenhuma leitura fora de teste** em todo o mcp-server. O routing-engine
     sequer conhece a chave (zero `:available` em `.py`). A resposta de *"o que
     grava, e quem lê"* é **ninguém**.
  3. **O teste unitário é verde por causa do dublê.** `runtime.test.ts` §158-159
     afirma `sismember == 1`, e passa porque `createStubRegistryClient` devolve
     `pools: ["retencao_humano","retencao_bot"]` — **forma que o cliente real não
     consegue retornar**. O stub não simplifica a produção: contradiz.

  **NÃO está provado que seja defeito de produção.** A hipótese concorrente e mais
  provável é vestígio: a inscrição em pool passou ao slot de deploy + bootstrap (o
  próprio comentário do `registry-client` §44-46 diz isso), e o demo aloca normal.
  As duas saídas têm sentidos opostos — ver o item no TODO antes de escolher.

═══ O INSTRUMENTO QUE REPROVOU (a parte útil) ═══

  Meu preflight era `grep -c createAgentType fixtures/seed.ts`, previsão 1.
  Devolveu **3** — o número **antigo**. `grep -c` conta linhas com a *palavra*, e os
  2 comentários que escrevi explicando a remoção contêm a palavra; somados à chamada
  que sobrou, reproduzem o total anterior. **Se o build não tivesse entrado, diria 3
  igual** — não podia reprovar.

  **Regra:** preflight sobre texto de código casa o **token sintático da chamada**
  (`grep -c 'registry.createAgentType('` → 3→1), nunca o identificador solto. A
  contaminação tem DIREÇÃO: quem remove código deixa nota explicando, então a palavra
  reaparece em prosa na medida em que some em código, e o contador **reconstrói o
  valor pré-mudança**. Alarme específico: preflight que devolve exatamente o número
  de antes — conferir o PADRÃO antes de suspeitar do build.

═══ GATES (previsão escrita antes de rodar) ═══

  DC="docker compose -f docker-compose.demo.yml"

  · `$DC build e2e-runner` → ok
  · `$DC run --rm e2e-runner ts-node runner.ts --only 10` → **8/8 VERDE**.
    Previsão das 8 asserções, correta. Importa **como** passou: `masked:true` ficou
    verde, então a cláusula de escape da asserção D (`evalHasOriginal || !isMasked`)
    **não** foi acionada — D passou porque o evaluator recebeu mesmo
    `original_content`. **O leitor de `role` da Fatia B §1055 foi exercitado pela
    primeira vez** (roster `session:{id}:participants`) e funciona. Fecha a cadeia
    §101 → e2e → §1055.
  · `$DC run --rm e2e-runner ts-node runner.ts --only 01` → **10/11**, vermelho único
    e previsto (acima). Não é regressão: antes o 01 morria no seed.

  ⚠️ **O e2e faz `flushTestData` no `tenant_demo`** (`lib/redis-client.ts` §40):
  apaga `tenant_demo:*` **e** `session:*` antes de CADA cenário. Contra o demo leva
  junto instâncias logadas, snapshots de pool, ContextStore, claims, tokens de resume
  e sessões vivas. Não estava escrito em lugar nenhum.

═══ PRÓXIMOS CANDIDATOS ═══

  1. **`agent_ready` × pool** (item novo no TODO) — o mais forte, porque é o único
     com evidência fresca. Primeiro passo é MEDIÇÃO, não código: *"quem inscreve uma
     instância de IA num pool hoje, no demo"*, via `MONITOR` filtrado por `SADD`
     durante um boot do bridge. Se for vestígio, o conserto derruba o `sadd`, o SET,
     a asserção do 01 e o stub — e outros testes vão ficar vermelhos: esse vermelho
     é o resultado, não o obstáculo.
  2. **Lacuna 2** — reaper de lease. Vaga ✅ e transbordo por idade ✅; sobra o agente
     que reivindica e some SEM teardown de WS. Decidir política antes de codar.
  3. **Lacuna 6, metade B** — enum de `close_reason` de segmento. Toca
     `@plughub/schemas` ⇒ rebuild conjunto. Inventariar os literais VIVOS antes.
  4. **Fatia 3 — `work_item.expired`** — o evento não existe em lugar nenhum. É o maior.
  5. Bolsões do e2e (nenhum bloqueia a suíte): `seedPerfFixtures`+cenário 05;
     `15_instance_bootstrap` (o `listAgentTypes` é a espinha, não uma renomeação);
     `fixtures/seed_demo.ts`.

═══ REGRAS OPERACIONAIS (as de sempre) ═══

· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de cada cenário (ver acima)
· **platform-ui não tem volume mount** — mudança de frontend exige `build`
· **teste também mora na imagem** — controle negativo por stash exige REBUILD;
  preferir teste DIFERENCIAL (as duas leituras lado a lado, afirmando divergência)
· arquivo NOVO exige `build --no-cache`; edição de existente entra no build normal
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **preflight sobre código casa o TOKEN da chamada, não o identificador** — senão os
  comentários da própria mudança recompõem o número antigo (aconteceu nesta sessão)
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· antes de comparar duas leituras, PROVAR QUE O LEITOR LÊ (e conferir o PREFIXO)
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **contar não é identificar** — `SCARD 1` não diz QUAL; use `SMEMBERS`
· **asserção com cláusula de escape só vale condicionada** — ex.: o `|| !isMasked` do
  cenário 10 faria D passar de graça se o mascaramento não disparasse. Ler o par.
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3 — parser JSON roda no `routing-engine`
· usar `docker compose … exec -T redis`, nunca `docker exec redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **experimento sensível a tempo roda DENTRO de um processo**
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway — apontar login para lá devolve "login falhou" sem culpa da senha) ·
  **redis 6380 · kafka 9093 · postgres 5433** (remapeados no HOST; por dentro do
  compose são 6379/29092/5432)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ RECEITAS QUE FUNCIONARAM ═══

  Bearer de supervisor (`force-complete` e `expire` exigem):
    curl -s -X POST http://localhost:3202/auth/login \
      -H Content-Type:application/json \
      -d '{"email":"admin@plughub.local","password":"changeme_admin","tenant_id":"tenant_demo"}' \
      | jq -r .access_token

  Conferir o que RODA (não o que está no repo):
    $DC exec -T mcp-server-plughub \
      grep -c nothing_to_complete /app/packages/mcp-server-plughub/dist/server.js
