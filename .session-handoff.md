continuação sessão plughub.

Recado da sessão anterior (2026-08-04, noite): a sessão pegou os dois achados
abertos do F2 e terminou com **um deles refutado, o outro DIAGNOSTICADO com
receita de reprodução determinística**, mais dois achados novos. O maior
resultado não é o código que entrou — é que o achado 2, que ontem era "aconteceu
uma vez, causa desconhecida", hoje tem procedimento que reproduz 3/3 e uma cadeia
medida ponta a ponta.

LEIA PRIMEIRO, nesta ordem:
  1. `TODO.md` § "Fila de trabalho humano … resíduos pós-v1" → **Achados de
     2026-08-04**. O item 1 foi reescrito (3ª vez) e o item 2 ganhou a seção
     "✅ DIAGNOSTICADO".
  2. `CHANGELOG.md` § topo — "Teto de capacidade visível no Console".
  3. `docs/arcos/platform-ui.md` § "Capacidade do agente na tela" — a tabela das
     DUAS fontes de capacidade.

O QUE ENTROU (UI, tudo já buildado e validado na tela)

  · `Header.tsx` — prop `maxConcurrent`; o crachá vira fração (`Serving 3/3`) e
    ganha cor de lotação (âmbar). Sem a prop, mantém o rótulo antigo.
  · `AgentAssistPage.tsx` — linha visível "All slots in use (3/3)" ao lado do
    botão desabilitado do preview. Cor = token `warning` (#D97706), NÃO
    `warning-text` (#92400E, marrom para texto SOBRE fundo âmbar — sobre branco
    lê como cinza; medido na tela).
  · `PullInboxPanel.tsx` — `claimError` em estado PRÓPRIO (antes o `setError` da
    recusa era apagado pelo `refresh()` da linha seguinte); faixa dispensável;
    limpa quando o ITEM sai da fila, não quando a lista recarrega. Props mortas
    `claimDisabled`/`claimDisabledReason` REMOVIDAS.
  · i18n nos dois locales. **Variável `{{n}}`, não `{{count}}`** — `count` liga a
    pluralização do i18next (o repo usa sufixo `_plural`) e estas chaves não têm
    formas plurais.
  · `infra/test/probe_claim_capacity_sources.sh` (NOVO) — põe lado a lado as duas
    fontes de capacidade e IDENTIFICA cada ocupante.
  · `infra/test/probe_queue_member_resurrect.sh` (NOVO) — observa se membro
    removido do ZSET volta sozinho; restaura o que removeu.

O ACHADO PRINCIPAL — "Return to queue" deixa o marcador de humano no bridge

  RECEITA (determinística, 3/3): reivindicar N itens → "Return to queue" em todos
  → reivindicar de novo. A ocupação sobe, e NENHUM cartão aparece.

  CADEIA MEDIDA:
   · MONITOR: os 3 `ZREM` de claim com deltas 7s e 4s, ocupação subindo 1→2→3.
   · bridge: 3× `Skipping duplicate routing … human_active=True`, deltas 7s e 4s.
     Mesmos eventos, casados pelo relógio.
   · mcp-server: NENHUM `Forwarding conversation.assigned` nesses instantes.
     O evento nunca chegou ao frontend (o que exclui a UI como causa).
   · `EXISTS session:{sid}:closed` = 0 — sessões VIVAS.

  CAUSA: o guard de dedup (`orchestrator-bridge/main.py` §3509-3519) descarta
  `conversations.routed` sem `conference_id` quando existe
  `session:{sid}:human_agent`. Esse marcador é escrito em §808 e apagado SÓ em
  caminhos de encerramento/queda (§2696, §6347, §6810, §7284). O
  `work_task_release` NÃO está entre eles.

  CONTROLE (produzido sem apagar chave nenhuma, ver TODO):
    marcador 0 (após F5, que passa pelo ramo §6810) → 3 claims → 3 cartões ✓
    marcador 1 (após return-to-queue)               → 3 claims → 0 cartões ✗
    após `release` da API                            → marcador SEGUE 1

  FORMA DO CONSERTO (não implementado): limpar `session:{sid}:human_agent` + o
  membro de `session:{sid}:human_agents` na devolução à fila, simétrico ao §6810.
  PREFERIR isso a afrouxar o guard — exceção no guard troca um caso mudo por
  outro (o spam de `participant_joined` que ele existe para impedir). O TTL do
  marcador é o da sessão: "some sozinho" é janela de HORAS, não conserto.

ACHADOS NOVOS, NÃO INVESTIGADOS

  · **Requeue carimba score NOVO no ZSET** (`ZADD … 1785881907400`), não o
    original. Contradiz a regra do dono do produto ("item devolvido preserva o
    timestamp e é ordenado pela espera"). A TELA está certa (o sort passou a usar
    o `ageOf` de `first_queued_ms`), mas o ZSET — que o árbitro e o drain leem
    para "cabeça da fila" — reseta.
  · ~~`release` responde `requeued:true` e o item não aparece no ZSET~~ —
    **REFUTADO na mesma rodada.** O probe de capacidade mostrou os três
    OCUPANDO vaga: o requeue aconteceu, e o re-claim seguinte os tirou do ZSET.
    Nada sumiu. Foi a 4ª reprodução do defeito principal (claim aceito, vaga
    gasta, marcador presente, cartão nenhum) — não um achado novo.

O QUE FOI REFUTADO (não reabrir sem dado novo)

  · **Achado 1 ("recusa de claim efêmera") — PREMISSA FALSA.** A inbox não tem
    botão de claim (a linha abre o preview); o `handlePull` só serve o
    auto-atendimento de wrap-up; e no preview o botão é `disabled={atCapacity}`,
    então com vagas cheias NÃO HÁ clique nem toast. Previsto×medido na tela, 4/4.
    O gap real era o teto invisível, e foi esse que entrou.
  · **Achado 1c (item reivindicado após sair do ZSET) — NÃO é ressurreição.**
    Membro removido não volta sozinho (90s, 18 amostras, sistema em repouso). Os
    únicos `ZADD` são requeues, cada um precedido do `EVAL` de ocupação
    DECREMENTANDO — ou seja, releases. No caso original, um release (inclusive o
    F5) repôs o item. Comportamento correto.

LIÇÃO DE MÉTODO DESTA SESSÃO

  A regra nova de ontem ("ler o handler antes de nomear o defeito") pagou na
  PRIMEIRA aplicação: ler dois handlers derrubou a premissa inteira do achado 1
  antes de escrever uma linha de conserto — era a 3ª redação daquele item, e as
  duas anteriores erraram igual, descrevendo a tela a partir do que se esperava
  do código.

  E apareceu um caso novo, meu, do mesmo tipo: afirmei "sessões vivas" lendo
  `tenant_demo:session:{sid}:closed` — chave com prefixo INVENTADO (o bridge usa
  `session:{sid}:closed`, sem tenant). Chave errada devolve 0, e 0 parecia
  medição. **Ao ler chave de Redis, conferir o prefixo NO CÓDIGO que a escreve.**

  Terceiro caso, no meu próprio instrumento: `probe_claim_capacity_sources.sh`
  imprimia "SEM DIVERGÊNCIA" comparando o árbitro CONSIGO MESMO — e disse verde
  exatamente na reprodução do achado 2 (3 ocupantes × 0 cartões). O número de
  cartões é entrada HUMANA que o script não tem. Corrigido: agora sai
  INCONCLUSIVO e diz qual número falta. *Um veredicto que não pode reprovar o
  caso principal é pior que veredicto nenhum.*

ESTADO DO DEMO AO FIM

  · ZSET de `formfill_demo`: 1 item (`54911855`, score novo `1785882454265`).
  · `9768b952`, `e786cf8e`, `509d5441`: **as 3 vagas do agente presas**, sem
    cartão no Console — o estado do defeito, deixado de propósito como
    reprodução viva. Para destravar: `POST /api/work_queue/release/{sid}`
    (pool `formfill_demo`, instance `human-bef14526-…`) nos três E APAGAR
    `session:{sid}:human_agent` + `session:{sid}:human_agents`, senão o próximo
    claim é engolido de novo. Apagar essas chaves é o WORKAROUND do defeito, não
    o conserto.
  · `509d5441` segue com o **resume token MORTO** (lixo herdado de anteontem):
    renderiza o form, mas o Submit falha. Usar "Return to queue", nunca Submit.
  · Rastros sintéticos meus, todos desfeitos ou anotados: o `ZREM` de
    `54911855` foi restaurado pelo próprio probe; nenhuma chave foi apagada à mão.

RECEITAS QUE FUNCIONARAM

  Condição de pull: `bash infra/test/smoke_formfill_renderer.sh` → devolve o
  session_id → Console (5174) → ativar pool `formfill_demo` → reivindicar.

  Ver a mutação acontecer (melhor que ler código quando o estado some):
    docker compose -f docker-compose.demo.yml exec -T redis \
      redis-cli MONITOR | grep --line-buffered -i -e zadd -e zrem -e "<chave>"
  Casar os eventos pelos DELTAS entre timestamps — foi assim que os 3 claims do
  MONITOR foram identificados com os 3 "Skipping duplicate routing" do bridge.

GATES (inalterados; a sessão foi UI + investigação)
  · platform-ui não tem runner de teste; o gate é o `tsc -b` dentro do `build`.
  · Os gates de backend listados no handoff anterior seguem válidos.

REGRAS OPERACIONAIS (as de sempre)

· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **platform-ui não tem volume mount** — toda mudança de frontend exige
  `build platform-ui`; medir antes do build mede o bundle de ontem
· arquivo NOVO exige `build --no-cache`; edição de existente entra no build normal
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· antes de comparar duas leituras, PROVAR QUE O LEITOR LÊ (e conferir o PREFIXO
  da chave no código que a escreve)
· **contar não é identificar** — `SCARD 1` não diz QUAL; use `SMEMBERS`
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3 — parser JSON roda no `routing-engine`
· o serviço redis do compose não tem `container_name` — usar
  `docker compose -f docker-compose.demo.yml exec -T redis`, nunca `docker exec redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· cuidado ao colar bloco com `#` — vira comentário e o passo manual some
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · auth-api 3202
· mudança em @plughub/schemas → rebuild de schemas + agent-registry +
  skill-flow-service + mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`
