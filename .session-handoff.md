continuação sessão plughub.

Recado da sessão anterior (2026-08-04, tarde): **Fase E do ADR de requeue FECHADA e
validada e2e no Console**. Restam A–F: só a **F (D7)**.

ONDE O ARCO CHEGOU. A duplicação acabou (A+B+D), a devolução é reservada ao dono
(C), e agora a queda de transporte **não se declara mais conclusão de trabalho**:
o segmento carrega `close_reason=agent_disconnect` e o evento de lifecycle é
`agent_released`, não `agent_done`. Falta **F**: resume terminal-uma-vez —
sob a D2 o item pode estar sendo preenchido quando o supervisor o encerra, e o
segundo a chegar (submit ou expire) precisa de recusa explícita.

LEIA PRIMEIRO, nesta ordem:
  1. `docs/adr/adr-work-item-requeue-and-agent-affinity.md` — atenção às TRÊS
     emendas: **§ D6 Emenda** (posse não cabe no ledger), **§ D2** (o
     `first_queued_ms` saiu do escopo), e **§ D8 Emenda** (a mais recente, e a
     que mais mudou o desenho — abaixo).
  2. `CHANGELOG.md` § topo — Fase E, com os números medidos.
  3. `TODO.md` § I5 e § "Erros de método que se repetem" (topo) — seguem valendo.

FASE E — o que ela ensinou, e que vale para a F

  · **A previsão escrita antes salvou o número.** O bruto era 69 de 86 segmentos
    sem `close_reason`; a lacuna 6 real são **14**, todos humanos (IA é 100 % NULL
    porque nenhum call site `native` passa o campo). Medir sem separar por
    `agent_type` teria dado um número grande, plausível e sobre outra coisa.

  · **A lacuna era maior que o F5.** Fila interna carimba 9/9 (`task_submitted`);
    pools de pull de contato, 0/12. O carimbo já existia — mas só pela porta do
    SUBMIT. Toda saída de item reivindicado que não fosse entrega saía muda.

  · **Duas premissas do ADR caíram, e nenhuma por teste:**
    (a) *"a pilha de `agent_done` contamina contagem/AHT/bancada"* — **nada
        analítico lê o `agent_done`** desde 2026-07-28 (`analytics-api/models.py`
        o mapeia a `None`, e o comentário da remoção diz que o ramo *"descartava
        100 % do agent_done do bridge, em silêncio"*). O dano é real, o veículo
        são os SEGMENTOS;
    (b) *"o drop deixa de publicar"* — **seria regressão.** Ler o
        `remove_conversation` mostrou que ele também restaura a membership dos
        SETs do pool (SADD ready_set / SREM busy_set) e o espelho
        `current_sessions`; o `work_task_release` não faz nada disso, e o
        `mark_busy` do claim tira o humano do ready_set a `max_concurrent=1`. O
        agente sairia invisível para push depois de cada F5, sem nada vermelho.
    → As duas vieram de **perguntar por que a regra existe**, contra a memória
      escrita do repo. É a terceira vez nesta série que isso decide uma fase.

  · **A sonda recusou verde DUAS vezes antes de validar**, e as duas recusas
    estavam certas: o F5 não tinha acontecido (o `#` da instrução virou
    comentário de shell) e depois o claim não tinha acontecido. Saída sempre
    `INCONCLUSIVO`, nunca "0 quedas → ✅".

  · **Erro meu de diagnóstico, registrado:** pedi o log do BRIDGE para investigar
    a ausência da queda. O produtor do `agent_disconnect` é o **mcp-server**
    (`server.ts`, graça de 2 500 ms, gated por
    `sismember session:{sid}:human_agents`). Log vazio no consumidor não fala
    sobre o produtor.

FASE F (D7) — o próximo, e o que já se sabe
  · **resume terminal-uma-vez.** Sob a D2 o item circula na fila até o prazo, e o
    `POST /api/work_queue/expire/:sessionId` do supervisor é a única forma de
    tirá-lo de cena antes disso. A corrida: o item pode estar reivindicado e
    sendo preenchido quando o supervisor encerra — o segundo a chegar (submit OU
    expire) tem de receber **recusa explícita**, nunca aplicar em silêncio.
  · o expire **não** preenche no lugar do agente (D5 da ADR author-bound):
    encerra **sem disposição**. O supervisor não finge ser o autor.
  · o vocabulário do fim já existe e é do domínio de SEGMENTO:
    `task_submitted` (entrega) · `acw_expired` (prazo) ·
    `acw_supervisor_closed` (supervisor) — `_wrapup_close_reason`, main.py.
  · a Fase A já tem o veredicto de 4 ramos no submit, contra o registro durável
    do árbitro (`claim_record`). A F provavelmente é o MESMO veredicto aplicado a
    um segundo eixo (terminalidade), não um mecanismo novo — conferir antes de
    projetar.
  · **medir antes de propor:** quantos itens hoje saem por expire × por submit, e
    se já existe algum caso de duplo-terminal no demo. A fila do `formfill_demo`
    tem **4 itens parados** (dois de 2026-08-04 já com `RESERVATION EXPIRED`) —
    amostra pronta para olhar `work_item_deadline` e o caminho do expire.

GATES QUE RODAM VERDES HOJE
  · `orchestrator-bridge … tests/test_segment_close_reason_domain.py`     — 15
  · `orchestrator-bridge … tests/test_work_item_release_on_drop.py`       — 14
  · `routing-engine … tests/test_agent_released_lifecycle.py`             —  4
  · `routing-engine … tests/test_claim_possession_record.py`              — 19
  · `channel-gateway … tests/test_resume_possession_check.py`             —  7
  · `infra/test/smoke_claim_possession.sh`                                — 30
  · sondas (exigem Console): `check_fase_e_drop_stamp.sh` (3/3 em 08-04),
    `probe_fase_b_release_on_reload.sh`
  · probe de medição, não gate: `probe_fase_e_drop_footprint.sh`

COMO CRIAR A CONDIÇÃO DE PULL (a receita que funcionou, na ordem)
  1. `bash infra/test/smoke_formfill_renderer.sh` → devolve o `session_id`
  2. Console (`localhost:5174`) → logar → ativar o pool `formfill_demo`;
     conferir: `logs mcp-server-plughub | grep '\[agent-ws\]'` tem de mostrar
     `Human agent registered`
  3. reivindicar o item na inbox; conferir ANTES de seguir:
     `GET tenant_demo:pool:formfill_demo:claim_record:{sid}` e
     `SMEMBERS session:{sid}:human_agents` — os dois não vazios
  4. F5 → esperar **~6 s** (graça 2,5 s + processamento) → rodar a sonda
  (pular um checkpoint custa uma rodada inteira; custou duas em 08-04)

ACHADO ABERTO, NÃO BLOQUEANTE
  · `[agent-ws] Pool registration returned HTTP 401` em todo pool do login,
    seguido de registro bem-sucedido. Degrada e loga (certo), mas 401 é falha de
    auth de serviço. Item próprio em `TODO.md`; **não misturar** com o arco de
    requeue.

REGRAS OPERACIONAIS (as de sempre + o que esta sessão confirmou)

· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e
  cola as saídas
· PREVER O NÚMERO antes de rodar, e escrever a previsão. **CONTAR, não estimar**
  (`parametrize` multiplica os casos — 15 e 4 bateram exatos em 08-04)
· **arquivo NOVO exige `build --no-cache`.** Edição de arquivo existente entra no
  build normal. Sintoma: `file or directory not found` no pytest, com as suítes
  antigas passando ao lado
· **suíte antiga passando NÃO prova que a edição entrou na imagem.** Preflight
  antes: `python -c "import …; print(<símbolo novo>)"` — 3 estados (valor /
  AttributeError = imagem velha / ImportError = instrumento)
· **`up -d --force-recreate` APAGA o `pip install` do pytest.** Reinstalar antes
· **o container do `redis` NÃO tem python3.** Parser JSON roda no `routing-engine`
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO,
  nunca verde
· antes de comparar duas leituras, PROVAR QUE O LEITOR LÊ (preflight contra valor
  conhecido, aborta com código 3)
· `grep -c` com ZERO matches sai com código 1 — numa cadeia `&&` mata o resto
· **todo `docker exec` dentro de script interativo leva `< /dev/null`**
· **cuidado ao colar bloco com `#`** — vira comentário de shell e o passo manual
  no meio some sem aviso (aconteceu em 08-04, custou uma rodada)
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo (user plughub), agent-registry em
  plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 (interna) ·
  mcp-server-plughub 3100 · platform-ui 5174 · auth-api 3202 NO HOST
· serviços Python/TS bakeiam o source → `build <svc> && up -d --force-recreate <svc>`
· mudança em @plughub/schemas → rebuild de schemas + agent-registry +
  skill-flow-service + mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer. Só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· pytest roda DENTRO do container, em `src/<pacote_snake>/tests/`. O pacote do
  routing-engine é `plughub_routing`
· smokes/portões/probes em `infra/test/` (podem ficar VERMELHOS);
  ferramentas de deploy/manutenção em `infra/scripts/`
· inglês no código, PT em i18n/spec/dados de tenant
· em ClickHouse, alias de agregado NUNCA repete nome de coluna real (sufixo `_ref`)
