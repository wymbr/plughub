continuação sessão plughub.

Recado da sessão anterior (2026-08-05): o **achado 2 foi CONSERTADO e validado na
tela**, com previsão escrita antes de cada medição e todas batendo. O arco de
resíduos pós-v1 da fila de pull tem agora 3 dos 5 itens fechados; sobraram dois,
ambos pequenos e ambos com o diagnóstico já feito.

LEIA PRIMEIRO, nesta ordem:
  1. `CHANGELOG.md` § topo — "Return to queue desmonta a presença do humano".
  2. `docs/guias/conference-mechanics.md` § **Mudança 32** — o mecanismo.
  3. `TODO.md` § "Fila de trabalho humano … resíduos pós-v1" → "Achados de
     2026-08-04": o item 2 está fechado (diagnóstico original preservado num
     `<details>` pelo valor de método); os itens **4** e **5** são os que sobraram.

O QUE ENTROU

  Transporte novo `agent_release_item`. O botão "Return to queue" chamava o
  `work_task_release` DIRETO no árbitro: a vaga voltava e a presença ficava
  (`session:{sid}:human_agent` + `session:{sid}:human_agents`), e o guard de dedup
  do `process_routed` engolia o `conversations.routed` do re-claim. Agora o
  mcp-server ANUNCIA `contact_closed(reason=agent_release_item)` em
  `conversations.events` depois do `released:true`, e o **bridge** desmonta pelo
  caminho já provado da queda. Quem escreveu o fato é quem o desfaz.

  · `orchestrator-bridge/main.py` — mapa de close_reason de SEGMENTO ·
    `_has_continuation` → `(True,"item_requeued")` (único caso em que
    `remaining==0` não é fim de contato) · `agent_released` com
    `_resolve_hold=False` · ramo próprio ANTES do marcador `session:closed`
    (sem re-rota, sem `_release_work_item`, sem `_mark_contact_ended`, sem
    `on_human_end`) · wrap-up de peer excluído.
  · `mcp-server-plughub/lib/work-queue.ts` — o anúncio mora no `releaseTask`, então
    vale para a rota do Console E para a tool MCP. `kafka` virou dependência
    OBRIGATÓRIA de `WorkQueueDeps`; opcional deixaria um caminho sem desmonte.
    Os DOIS registros (`createServer` e `startServer`) atualizados.
  · `schemas/platform-events.ts` — só comentário: o `AgentReleasedEventSchema`
    afirmava que o evento NÃO é publicado sobre item de fila pull. Era o desenho
    ORIGINAL da Fase E; o código o reverteu e o comentário não acompanhou.
  · 2 testes anexados a `test_work_item_release_on_drop.py` (16 passed) — anexados
    ao arquivo EXISTENTE de propósito, para não exigir `build --no-cache`.
  · `infra/test/probe_release_presence.sh` (NOVO, host).

ESCOPO MAIOR QUE O MÍNIMO, E POR QUÊ

  A instrução original era "limpar as duas chaves". A leitura do §6751 mostrou um
  SEGUNDO defeito da mesma raiz: só `remove_conversation` restaura a membership
  dos SETs do pool (SADD `ready_set` / SREM `busy_set`), que o
  `work_task_release` não toca — sem o `agent_released`, o agente ficava invisível
  ao push depois de CADA devolução. Não é liberação dupla: a ocupação é DERIVADA
  do `SCARD` do semáforo, logo o segundo SREM é idempotente. A correção mínima
  teria deixado esse defeito de pé, mudo.

  O guard NÃO foi afrouxado. Exceção nele trocaria um caso mudo por outro (o spam
  de `participant_joined`). Corrigir o ESTADO torna o guard verdadeiro.

VALIDAÇÃO (previsto × medido, tudo bateu)

  | probe de presença, MESMO item, antes × depois | VERMELHO → VERDE |
  | `Return to queue` no log                      | 3                |
  | `continuation=True (item_requeued)`           | 3×, remaining=0  |
  | `Skipping duplicate … human_active=True`      | 0 (antes: 3)     |
  | cartões no re-claim                           | 3 (antes: 0)     |
  | árbitro × tela                                | 3 × 3, mesmos ids|

  As idades dos cartões (4:10/4:08/4:06 × 0:15/0:13/0:11 do 1º claim) provam que a
  identidade do item sobreviveu ao ciclo — mesma sessão, não uma nova.

O INSTRUMENTO NOVO — por que ele fecha sem a tela

  `probe_release_presence.sh`. Item no ZSET **não tem dono** (o claim é um ZREM),
  logo item na fila COM marcador de presença é o defeito em estado puro: os dois
  lados estão no Redis e o veredicto não precisa de entrada humana — ao contrário
  do `probe_claim_capacity_sources.sh`, que só enxerga o árbitro e por isso sai
  INCONCLUSIVO por construção.

  Duas defesas embutidas, ambas nascidas de erro real:
   · **self-test de prefixo**: escreve `session:{sid}:human_agent` de valor
     conhecido e confere o EXISTS antes de medir. Prefixo errado devolve 0, e 0 é
     indistinguível do VERDE deste script.
   · **virgem × devolvido** (`score` do ZSET vs `first_queued`): numa fila
     recém-semeada "sem presença" é verdade por construção. Sem essa distinção o
     VERDE seria vazio, então o script sai INCONCLUSIVO e diz qual número falta.

  Foi rodado como CONTROLE NEGATIVO antes do build (VERMELHO na reprodução) — sem
  isso o VERDE depois não valeria nada.

O QUE SOBROU (TODO, itens 4 e 5)

  · **4 — requeue carimba score NOVO no ZSET.** A tela está certa (ordena por
    `ageOf` de `first_queued_ms`); o ZSET que o árbitro e o drain leem reseta.
    ATENÇÃO: é justamente `score − first_queued` que o probe usa para distinguir
    virgem de devolvido — consertar o score exige outro discriminador no probe.
  · **5 — janela de ordenação `conversations.events` × `conversations.routed`.**
    Tópicos independentes, sem garantia de ordem. Não apareceu na validação
    (intervalo humano × dezenas de ms). O candidato REAL é o caminho
    `auto_attend`, onde não há clique separando os dois eventos. **Medir antes de
    engenheirar contorno.**

LIÇÕES DE MÉTODO DESTA SESSÃO

  · **O rastro sobrevive ao mecanismo.** O lixo deixado como "reprodução viva" já
    não reproduzia nada 14 h depois: as 3 vagas seguiam presas, mas os marcadores
    tinham expirado (TTL = TTL da sessão). Medir o rastro não mede a causa —
    reprodução que depende de estado com TTL precisa ser refeita do zero na sessão
    que a usa. Eu previ que os 3 apareceriam com marcador e **errei**; a saída do
    probe corrigiu a previsão antes de eu construir em cima dela.
  · **Defeito no próprio instrumento, achado pela saída dele.** A seção de contexto
    do probe listava a sessão ACUSADA sob o rótulo "estado normal de item
    reivindicado" — a mesma saída dizendo violação e normalidade sobre o mesmo id.
    Corrigido na hora (filtro por `QUEUED_SIDS`). Rótulo que descreve o oposto da
    medição é pior que rótulo nenhum.
  · **Comentário que descreve um desenho revertido é uma mentira documentada.** O
    `AgentReleasedEventSchema` afirmava o contrário do que o código fazia desde a
    Fase E. Ao ler comentário como fonte, conferir contra o código que executa.
  · **Linha longa no terminal quebra e o shell executa os pedaços.** `pytest-asyn`,
    `cio`, `rator_bridge.main` — três erros num bloco só. Comandos curtos, e python
    multilinha por heredoc (`python - <<'PY'`).

ESTADO DO DEMO AO FIM

  · 3 sessões ATIVAS no Console (`c4a1d642`, `f1c6ff65`, `371630fc`), pool
    `formfill_demo`, todas reivindicadas pela 2ª vez e com o form renderizado.
    Fila `formfill_demo` VAZIA. Ocupação 3/3 — para liberar, "Return to queue" nas
    três (que agora limpa sozinho) ou Submit.
  · O lixo antigo (`9768b952`, `e786cf8e`, `509d5441`) foi liberado por
    `POST /api/work_queue/release/{sid}`; os três devolveram `requeued:false` (o
    JSON do contato já tinha expirado) — vaga voltou, item não. Estado limpo.
  · Nenhuma chave foi apagada à mão em nenhum momento. A única mutação sintética
    foi a chave de self-test do probe, removida pelo próprio script.

RECEITAS QUE FUNCIONARAM

  Criar item de pull: `bash infra/test/smoke_formfill_renderer.sh` (1 por execução)
  → Console (5174) → pool `formfill_demo` já ativo → reivindicar.

  Ver o ramo novo agir:
    docker compose -f docker-compose.demo.yml logs -f --tail=0 orchestrator-bridge \
      | grep -E "Return to queue|item_requeued|Skipping duplicate"

  Preflight de símbolo novo em serviço Python (heredoc, não `-c`):
    $DC exec -T orchestrator-bridge python - <<'PY'
    import plughub_orchestrator_bridge.main as m
    print(m._TRANSPORT_TO_SEGMENT_CLOSE_REASON)
    PY

  Preflight do mcp-server: o dist é
  `/app/packages/mcp-server-plughub/dist/lib/work-queue.js` (NÃO `/app/dist`).

GATES

  · `docker compose -f docker-compose.demo.yml exec -T orchestrator-bridge \
     python -m pytest -q -k work_item_release` → 16 passed.
    (`up -d --force-recreate` APAGA o pytest — `pip install -q pytest pytest-asyncio` antes.)
  · `bash infra/test/probe_release_presence.sh formfill_demo` → VERDE exige pelo
    menos um item DEVOLVIDO; virgem sai INCONCLUSIVO de propósito.
  · platform-ui não tem runner; o gate é o `tsc -b` dentro do `build`.

REGRAS OPERACIONAIS (as de sempre)

· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· **platform-ui não tem volume mount** — toda mudança de frontend exige
  `build platform-ui`; medir antes do build mede o bundle de ontem
· arquivo NOVO exige `build --no-cache`; edição de existente entra no build normal
  (por isso teste novo foi ANEXADO a arquivo existente)
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· antes de comparar duas leituras, PROVAR QUE O LEITOR LÊ (e conferir o PREFIXO
  da chave no código que a escreve)
· **contar não é identificar** — `SCARD 1` não diz QUAL; use `SMEMBERS`
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3 — parser JSON roda no `routing-engine`
· o serviço redis do compose não tem `container_name` — usar
  `docker compose -f docker-compose.demo.yml exec -T redis`, nunca `docker exec redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· cuidado ao colar bloco com `#` — vira comentário e o passo manual some
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · auth-api 3202
· serviços do compose usados aqui: `orchestrator-bridge`, `mcp-server-plughub`
· mudança em @plughub/schemas → rebuild de schemas + agent-registry +
  skill-flow-service + mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
  — e deps novas precisam entrar nos DOIS
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`
