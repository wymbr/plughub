# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-08-27.md`](passagem-2026-08-27.md), que está **encerrada**.
>
> A sessão anterior foi **discussão conceitual + medição** do tema *Sessão, Segmento e Journey*.
> Resultado: 4 decisões aceitas no ADR (D10–D13), 1 linha de ADR **refutada** por medição, e 3 defeitos
> vivos separados que estavam contados como um. **Nenhuma linha de código foi escrita.**
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes das passagens anteriores (08-26 → 08-27 → 08-28),
> mas **todo dado medido carrega carimbo `2026-08-21`** (ClickHouse e logs). Onde houver conflito,
> **confie no carimbo do dado**, não no nome do arquivo.

---

```
continuação sessão plughub.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **no FRONTEND são DOIS caches**: depois do build, `Ctrl+Shift+R` é OBRIGATÓRIO
· arquivo NOVO entra no build normal quando o Dockerfile copia o DIRETÓRIO
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· **janela de log só compara se as DUAS janelas existirem**; `--since` sem fuso é HORA LOCAL (−03)
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso**
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3; `curl` NÃO existe no ai-gateway (use o host)
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **nada de crase dentro de aspas duplas em bash** — vira substituição de comando
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server + skill-flow-worker JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· **para hooks/deploy/capacity/queue_config, perguntar ao agent-registry, NUNCA ao YAML**
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · mailing-api 3660 · **redis 6380 · kafka 9093 · postgres 5433**
· health da analytics-api e do ai-gateway é **`/v1/health`**
· analytics-api usa **PyJWT** (`import jwt`); auth-api usa **python-jose**
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ AS SEIS REGRAS QUE ESTA SESSÃO COMPROU ═══
· **UM ZERO SÓ JULGA SE O FILTRO QUE O PRODUZIU NÃO PUDER TÊ-LO FABRICADO.** Medi
  "segmentos `primary` travados" com `status='closed'` e deu **0** — parecia conserto. Era o filtro:
  os 15 abertos estão em sessões que **não existem em `sessions`**, excluídas por construção.
  *Zero precisa de testemunha tanto quanto ausência precisa.*
· **"PRESERVADO" NÃO É SINÔNIMO DE INERTE.** `queue_config.skill_id` é descrito na tela como
  *"preserved, but it does not resolve the deploy"* — verdade pela metade: ele também torna o objeto
  truthy e **suprime o tenant default** (`main.py:5735`). Campo legado pode ser load-bearing pelo lado
  negativo. Irmão de *"campo morto pode gatear tela viva"*.
· **ERREI 4 DE 6 PREVISÕES, E CADA ERRO MUDOU O DIAGNÓSTICO — não o escopo.** Isso é sinal de modelo
  mental errado, não de ruído. *Escrever a previsão foi o que transformou cada erro em informação em
  vez de em diagnóstico.*
· **DUAS CAUSAS DÃO O MESMO SINTOMA, E FOI O CONTADOR QUE AS SEPAROU.** 4 segmentos de fila abertos,
  mas só **2** ocorrências do guard `Skill already running` ⇒ a causa que eu tinha diagnosticado
  explicava no máximo metade. Consertar a de cima não moveria o número.
· **O NOME PODE SER MAIS LARGO QUE O CONTEÚDO.** `session_transitions` parece substrato genérico de
  estado; é livro-razão de suspend/resume com token. Eu recomendei desenho inteiro em cima do nome
  antes de ler o schema. Mesma família da colisão `SessionMeta` × `ReplayContext`.
· **AMOSTRA DE 1 GENERALIZADA.** Vi dupla ativação numa sessão e previ o mesmo padrão no par inteiro.
  Duas das três não tinham. *Um caso não estabelece a forma de nenhum caminho* — o ADR já dizia isso
  sobre composição de segmentos (§2.3) e eu repeti o erro.

═══ ESTADO (fechado nesta sessão) ═══
ADR `adr-journey-session-segment-model.md`: **D10–D13 ACEITAS**
  · D10 pool de ENTRADA (sessão) × pool que ATENDE (segmento); `attended_pool_ids` = projeção
  · D11 `session` = qualquer acesso; **"contato" é FILTRO, não nível**
  · D12 espera é fato de ROTEAMENTO, com produtor próprio (veículo = segmento, id determinístico)
  · D13 discriminador ternário por `spawn_reason`; `pools.purpose` sai do critério de contato
  · ⚠️ **linha *(espera)* da D9 REFUTADA** — `duration_ms` de `role='queue'` mede o flow do agente
TODO.md: 3 seções NOVAS (janela de espera · tenant default suprimido · dois vocabulários de papel),
  2 CORRIGIDAS (superação refutada · resíduo 1 stale), 1 CONSOLIDADA (15 órfãos = os 15 abertos)
Doc novo: `docs/product/fila-janela-de-espera-2026-08-21.md` (medição completa, 8 seções)
E2E: `--demo` em **10/17**, INALTERADO — não rodei.
Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1): **intocado**.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
sessions: `NULL` 422 · `trigger` 96 · total **518** (`collect`/`delegate` AUSENTES)
segments: `primary` 827 · `specialist` 108 · `queue` 52
abertos em sessão FECHADA: `primary` **0** · `specialist` **0** · `queue` **4** (todos pós-18/08)
órfãos (segmento sem linha em `sessions`): `primary` **15**, e mais nada
`retencao_humano`: push · SLA 300000 ms · `queue_config = {"skill_id":"skill_fila_v1"}` (sem `pool_id`)
espera real medida sem registro nenhum: **21,35 s** (sessão `81d194ad-…-ce81b30e8343`)

═══ PONTO DE ENTRADA — quatro defeitos, em ordem de retorno ═══
1. **Abandono na fila não fecha a sessão** (TODO § superação, item 3). `active` + `abandoned` na mesma
   linha é estado impossível; some das contagens de contato encerrado. **Reproduz em 10 s** derrubando
   o webchat enquanto na fila. Não medido em volume — comece contando.
2. **Tenant default suprimido pelo `skill_id` legado** (TODO § própria). Conserto pequeno, efeito
   grande: a tela para de mentir e o operador para de configurar fila que não roda. Três frentes
   independentes listadas na seção; a de config-time é a que evita o defeito, não só o corrige.
3. **Idempotência da ativação de fila**. `segment_id` determinístico + `SET NX` no marcador + posse no
   `DELETE`. **Gate possível pelo e2e** (`e2e-inbound-*`), com testemunha obrigatória: os 47 segmentos
   de fila que fecham têm de continuar fechando.
4. **Janela de espera com produtor no routing** (D12). É construção, não conserto — e traz efeito
   colateral declarado: reclassificar o agente de fila para `specialist` move o tempo dele para dentro
   de `agent_time_ms` e muda TMA/AHT do ambiente.

⚠️ **NÃO fazer agora:** a F4 (visão 2 do histórico unificado). A classe *outbound* segue com **zero
amostras** de `spawn_reason` 13 dias depois do F0 — construir a tela é o ramo que nada exercita.

⚠️ **Pendências de doc que esta sessão NÃO fez:** (a) `CLAUDE.md` não foi atualizado com D10–D13 —
decidir se cabe resumo de 15 linhas ou só link; (b) `conference-mechanics.md` § Histórico de Problemas
e Correções **não** recebeu entrada, e a regra do `CLAUDE.md` exige isso antes de considerar concluída
qualquer mudança em mecânica de conferência — vale ANTES de mexer nos defeitos 1 e 3.

⚠️ **Varredura ainda por fazer** (herdada da passagem anterior): as passagens de 08-19 a 08-25 podem
ter backlog órfão pelo mesmo mecanismo que a de 08-26 tinha. Não medi.
```
