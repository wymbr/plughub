# Prompt de passagem — próxima sessão

> ⛔ **ENCERRADA em 2026-08-21.** Sucedida por [`passagem-2026-08-31.md`](passagem-2026-08-31.md).
> A tarefa P3 foi cumprida; o arco da janela de espera (D12) está entregue e registrado no
> `CHANGELOG.md`. **Duas afirmações deste arquivo caíram por medição:** *"a proteção do
> `key=session_id` é acidental, o caminho emite um evento só"* e *"baseline INALTERADA desde 08-21 ⇒
> zero tráfego novo"*. Ver a passagem sucessora e `conference-mechanics.md` § Mudança 37.

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-08-29.md`](passagem-2026-08-29.md), que está **encerrada**.
>
> A sessão anterior foi a **primeira com código escrito** deste arco: P1 + P2 do produtor da janela de
> espera (D12). **Nada foi construído nem validado** — sem `build`, sem gate. Além do código, três
> decisões novas (D10.1, D14, D14.1), duas delas nascidas de perguntas do operador, não da leitura de
> código.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes (08-28 → 08-29 → 08-30), mas **todo dado medido
> carrega carimbo `2026-08-21`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: P3 — build + preflight + gate do produtor da janela de espera.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Uma pendência declarada por quem parou de
  trabalhar é a última coisa que ele lembra, não o estado do repo. Na sessão passada as DUAS dívidas
  de doc declaradas na passagem 08-28 estavam **pagas** — custou 2 greps descobrir, e teria custado
  uma sessão inteira refazer. **A ORDEM proposta também se confere:** a mesma passagem ordenava os
  defeitos ao contrário do que `conference-mechanics.md` e `TODO.md` diziam, os dois com razão dada.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **PREFLIGHT DE SÍMBOLO antes de medir** — importar o módulo no container e conferir o atributo;
  imagem que não pegou a mudança produz medição que parece "o conserto não funcionou"
· **no FRONTEND são DOIS caches**: depois do build, `Ctrl+Shift+R` é OBRIGATÓRIO
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**, nunca `FROM t FINAL AS alias` (erro de sintaxe no 23.8)
· **`sessions` NÃO tem `ingested_at`** (essa coluna é de `segments`)
· **`GET :3300/v1/pools` devolve ENVELOPE, não lista** — `json.load(...)['pools'|'data'|'items']`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **nada de crase dentro de aspas duplas em bash** — vira substituição de comando
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server + skill-flow-worker JUNTOS
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· **para hooks/deploy/capacity/queue_config, perguntar ao agent-registry, NUNCA ao YAML**
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o ai-gateway)
· inglês no código, PT em i18n/spec/dados de tenant

═══ AS TRÊS REGRAS QUE ESTA SESSÃO COMPROU ═══
· **`sumIf` SOBRE COLUNA `Nullable` DEVOLVE NULL, NÃO 0 — E O NULL CONTAMINA COMPARAÇÃO E DENOMINADOR.**
  Publiquei *"16 sessões afetadas, +1,19%"*; o certo era **27 e +3,44%**. Sessão cujo único tempo era o
  segmento que a mudança adiciona tinha `hoje = NULL`, então `depois > hoje` virava NULL e o `countIf`
  **pulava exatamente a população mais afetada**; e `avg` ignora NULL em cada coluna, então as duas
  médias saíram sobre denominadores diferentes. Conserto: `coalesce(sumIf(...), 0)`.
  *O tell que salvou: a ARITMÉTICA NÃO FECHAVA (28 segmentos não cabem em 16 sessões).* A confirmação
  veio como identidade exata: `16 + 11 (só-fila) = 27`.
· **NÚMERO IGUAL NÃO É CONJUNTO IGUAL — leia a contingência, não as marginais.** Duas contagens deram
  **5** (sessões com fila duplicada; sessões que nunca fecham com fila abandonada) e eu previ que
  fossem o mesmo conjunto. **Interseção = 1.** Populações independentes. Um `GROUP BY` cruzando os dois
  predicados custou um comando e desfez a hipótese.
· **A MÉTRICA TEM UM GRÃO, E MEDIR NO GRÃO ERRADO PRODUZ UM NÚMERO QUE NÃO EXISTE EM TELA NENHUMA.**
  Declarei o efeito colateral como "+3,44% de TMA" — média do TENANT. TMA se lê **por pool**, e por pool
  é **+4,8% num único pool**. Foi uma pergunta do operador (*"TMA não é por pool?"*) que expôs isso, e
  a mesma pergunta revelou o requisito da D10 que faltava no plano.

═══ ERROS DE PREVISÃO — o padrão importa mais que a lista ═══
Errei 5 de 9 previsões, e **todas na mesma direção**: supus que o ambiente segue os DEFAULTS e que os
fenômenos se AGRUPAM. Ele faz o contrário nas duas coisas.
  · afetadas: previ 28 → **27**        · `duration_ms=0`: previ ≥10 → **zero**
  · interseção dup∩travada: previ 5 → **1**   · magnitude por pool: previ ">>3,44%" → **4,8%**
  · distribuição de SLA: previ "maioria no default de 30 s" → **2 de 36**
Acertei: baseline (3 números), ≥2 segmentos de fila (5), `retencao_humano`=300 000, "há outro pool >1 h".

═══ ESTADO — CÓDIGO ESCRITO, NADA VALIDADO ═══
**P1 ✅ escrito** — `key=session_id` no publish de `conversations.participants` do routing
  (`mute_queue.py`). Tópico de 3 partições publicando sem chave; não mordia porque o evento é único
  (só `participant_left`), mas a proteção era ACIDENTAL.
**P2 ✅ escrito** — o desenho mudou ao implementar, e para melhor: **não foi preciso extrair função nem
  espalhar chamadas.** `resolve_mute_exit` já estava plugado em TODAS as saídas de fila; só se recusava
  a agir no tier atendido. A mudança foi **remover a recusa**.
  · `mute_queue.resolve_mute_exit` → **`resolve_queue_exit`** (o conteúdo ficou mais largo que o nome).
    `SREM(unadmitted)` virou bookkeeping; quem decide é o `first_queued_ms`, que existe nos DOIS tiers.
    **Sem carimbo não emite nada** — ausência honesta, nunca `duration_ms` fabricado.
  · `queue_wait_segment_id()` = `uuid5(NAMESPACE_URL, "plughub:queue-wait:{tenant}:{sid}")`
  · **DOIS pontos de saída que nenhuma das 4 chamadas cobria, agora cobertos:**
      `SessionClosedEventHandler` (contact_closed) → `abandoned` — **é o caminho do Problema 36.3**, o
        cliente que cai NA FILA; o handler não tinha producer, passou a receber `kafka_producer`;
      drain com agente disponível → `handoff` — escapava porque com agente de fila ativo o contato é
        SINALIZADO (LPUSH) em vez de re-publicado, e o roteamento não roda de novo.
  · bridge `activate_queue_agent`: `role='queue'`→**`'specialist'`** · `pool_id`→**`_flow_pool_id`** (D10)
    · `segment_id` determinístico (namespace `queue-agent`, distinto do `queue-wait`)
  · emitir duas vezes é **inócuo por construção** (carimbo apagado na 1ª + id determinístico)
**NÃO tocado de propósito:** o emissor próprio do `_emit_queue_timeout` (passo 3). Unificar agora daria
  emissão DUPLA. Lacuna nomeada: `max_wait` na fila ATENDIDA segue sem segmento de espera.
**CHANGELOG: SEM entrada, de propósito** — código não exercitado não é implementação concluída.
E2E: `--demo` em 10/17, INALTERADO — não rodei.

═══ DECISÕES NOVAS (registradas em ADR + CLAUDE.md + TODO) ═══
**Nome do veículo: `queue` := ESPERA** (reconfirma a emenda 2 da D12, que a passagem 08-29 reabrira).
  Decidido pelo INVENTÁRIO, não por gosto: `role='queue'` já carregava DOIS sentidos, e este caminho
  herda 19 segmentos já corretos, não mexe na query do Fila/SLA, e torna segmento de fila aberto
  **estruturalmente impossível** (o padrão de EVENTO ÚNICO do routing não tem estado aberto).
**D10.1** — `pool_id` do segmento de ESPERA = **DESTINO** (é a dimensão do Fila/SLA,
  `reports_query.py:5741`; movê-lo para o pool de fila colapsaria todas as esperas numa linha, já que a
  fila é a default do tenant). A fila que executou vai em campo **próprio** (`queue_pool_id`).
  *"Pool de fila sempre distinto do destino"* não é modelo alternativo — é o estado-alvo da CONFIG.
**D14** — **SLA é fato do SEGMENTO DE ESPERA, nunca da sessão.** Hoje `sla_target_ms` é coluna de
  `sessions` e os TRÊS leitores do repo leem dali (`query.py:240`, `reports_query.py:3802`, `:5743`);
  uma sessão carrega UM alvo, então contato que espera em duas filas perde a 2ª violação.
**D14.1** — **antes do grão, há um problema de IDENTIDADE.** O rótulo da UI diz *"Total service SLA"* /
  *"SLA total do atendimento"* (nos dois locales) e **todos os consumidores comparam com ESPERA**. O
  default do formulário (30 s) contradiz o próprio rótulo.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
baseline (INALTERADA desde 08-21 ⇒ zero tráfego novo; **ela mede o código ANTIGO**):
  469 closed/sem-fila · 27 suspended · **10** closed+ab (TESTEMUNHA) · 8 active · **5** never_closed+ab · 3 nulas
  população **522**
segmentos `role='queue'` = **52**, e são TRÊS produtores:
  bridge (native, `queue-{sid}`)      33 segs · **5 abertos** · média 117 638 ms · mede o FLOW
  `resolve_mute_exit` (system-queue)  18 segs · 0 abertos   · média  29 121 ms · mede a ESPERA
  `_emit_queue_timeout` (system)       1 seg  · 0 abertos   · 1 802 441 ms     · mede a ESPERA
  ⇒ **os 5 abertos são TODOS do bridge**; os 19 do routing têm zero (evento único não tem estado aberto)
contingência (46 sessões com fila) — **três populações INDEPENDENTES**:
  single/never_closed/abandonada = **4**  ← o defeito 36.2, SEM duplicação
  dup que FECHAM = 4 de 5 · abertos em sessão FECHADA = **4** (inverso de 36.2, não variante)
TMA (efeito colateral do P2, recomputação hipotética sobre histórico):
  tenant +3,44% · **`retencao_humano` 456 083 → 477 968 ms (+4,8%)** · 27 sessões · **11 hoje em ZERO**
  ⚠️ **não é retroativo** — linhas antigas mantêm `role='queue'`; o número DERIVA com tráfego novo
SLA dos 36 pools: **18 entre 15 s e 10 min** × **18 com ≥1 h** (5×1h, 9×24h, 3×48h, **1×7 DIAS**)
  default de 30 s usado por **2** pools · o grupo ≥1 h é EXATAMENTE processo/aprovação/workflow

═══ A TAREFA — P3 ═══
1. `$DC build routing-engine orchestrator-bridge` → `$DC up -d routing-engine orchestrator-bridge`
2. **PREFLIGHT** (previsão: `True True` e `1`):
   `$DC exec -T routing-engine python -c "from plughub_routing import mute_queue as q; print(hasattr(q,'resolve_queue_exit'), hasattr(q,'queue_wait_segment_id'))"`
   `$DC exec -T orchestrator-bridge python -c "import plughub_orchestrator_bridge.main as m; print(open(m.__file__).read().count('plughub:queue-agent'))"`
3. **CASO REAL (11 s):** escalar do `sac_ia` para a fila e dar F5 no webchat.
   **PREVISÃO:** hoje 1 segmento (só `sac_ia`), nenhum registro de espera → depois **2**, sendo o novo
   `role='queue'`, `outcome='abandoned'`, `duration_ms` ≈ espera real.
4. **TESTEMUNHA OBRIGATÓRIA:** os **10** que fecham continuam fechando; as **469** linhas
   closed/no_abandoned_queue não mudam de forma.
5. **PREVISÃO QUE PROTEGE CONTRA "NÃO APLICOU":** o P2 conserta a DUPLICAÇÃO e **não deve mover** o
   `never_closed = 5` nem os 4 segmentos abertos em sessão fechada — são populações independentes
   (interseção medida = 1). **Verde-e-imóvel ali é o resultado ESPERADO.**
6. Medir a **coorte NOVA**, não o agregado: a baseline reflete o código antigo.
7. Só depois do gate verde: entrada no `CHANGELOG.md` + `docs/arcos/system-queue.md:134` (cita o nome
   antigo `resolve_mute_exit`).

═══ NÃO fazer agora ═══
· **Migrar os 3 leitores de SLA para o segmento (D14) ANTES de decidir o que `sla_target_ms` É (D14.1)**
  — levaria número errado para grão melhor. E metade do parque está preenchida segundo a outra leitura.
· **Unificar o emissor do `_emit_queue_timeout`** — daria emissão dupla no relatório que este arco
  conserta. Fatia própria.
· **36.2 (as 5 que não fecham)** — causa não identificada; a duplicação NÃO explica (interseção 1).
· **F4 (visão 2 do histórico unificado)** — classe *outbound* segue com zero amostras de `spawn_reason`.

═══ ORDEM QUE EMERGIU (diferente da que as passagens traziam) ═══
defeito 2 (tenant default suprimido pelo `skill_id` legado) destrava DUAS coisas: a atribuição da D10
(hoje `_flow_pool_id` resolve para o próprio destino ⇒ a linha do bridge é NO-OP neste ambiente) e o
estado-alvo da D10.1. **Ele subiu na fila por dois caminhos independentes.**

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de três passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
```
