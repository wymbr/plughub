# Prompt de passagem — sessão de 2026-08-25

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a de 2026-08-24, que fechou
> as tarefas **1** (credencial) e **2** (sentimento de ponta a ponta) e, no caminho, descascou **cinco
> defeitos empilhados** — cada um invisível enquanto o anterior existia. Um deles não era de
> sentimento: o agente de fila era **surdo à mensagem do cliente**. A passagem
> [`passagem-2026-08-24.md`](passagem-2026-08-24.md) está **encerrada**.

---

```
continuação sessão plughub.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**:
  editar um arquivo de teste e rodar `pytest` no container mede a imagem antiga
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· arquivo NOVO entra no build normal quando o Dockerfile copia o DIRETÓRIO
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· **janela de log só compara se as DUAS janelas existirem**
· **`--since` do docker sem fuso é HORA LOCAL** — o host é −03
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso**
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3; usar `$DC exec -T redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **nada de crase dentro de aspas duplas em bash** — vira substituição de comando
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server + skill-flow-worker JUNTOS
· mudança em **@plughub/skill-flow-engine** → rebuild de skill-flow-service +
  skill-flow-worker (o engine é compilado DENTRO deles)
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · mailing-api 3660 · **redis 6380 · kafka 9093 · postgres 5433**
· health da analytics-api é **`/v1/health`**; do ai-gateway também
· analytics-api usa **PyJWT** (`import jwt`); auth-api usa **python-jose**
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ AS SEIS REGRAS QUE 08-24 COMPROU (quatro por erro próprio) ═══
· **O GATE TEM DE JULGAR PELO DISCRIMINADOR, NÃO POR UM SINAL CORRELACIONADO.**
  O `gate_sentiment_engine_half.sh` classificava a ORIGEM da medição por
  `pool=unknown` — e reprovou um contato REAL chamando-o de "chamada sintética
  do probe". Dois fatos independentes tinham sido colapsados num sinal só: DE
  ONDE veio a medição (decide-se pela FORMA DO ID) e se o pool pôde ser resolvido
  (achado próprio, mensagem própria). **Acusação confiante e falsa é pior que
  reprovar: manda consertar o que está certo.**
· **NÃO COMPARE VALOR ESTRUTURADO COMO SE FOSSE ESCALAR.** O ctx guarda um
  `ContextEntry` (`{value, confidence, source, …}`), não um número. `awk` avalia
  string não-numérica como `0`, então `s+0 < 0` foi FALSO sobre um score de −0.5
  e o gate anunciou "mediu o texto errado". Extraia o campo; e **ilegível tem de
  ser INCONCLUSIVO**, nunca um ramo escolhido por default.
· **SAÍDA BYTE-IDÊNTICA É SINAL DE QUE NADA MUDOU.** Rodei o pytest depois de
  corrigir um teste e veio a MESMA linha, o MESMO `160 passed, 1 failed`. Não era
  "o fix não pegou": era a imagem antiga. Antes de investigar um resultado que
  não mudou, pergunte se ele PODIA ter mudado.
· **`??` E `||` DISCORDAM NA STRING VAZIA — e duas derivações do MESMO valor têm
  de compartilhar a guarda.** `ctx.instanceId ?? "_default_"` (campo do hash) ×
  `instanceId ? …suffix : …` (chave do BLPOP) mandavam `""` para lados opostos.
  Quando um valor decide DOIS nomes que precisam casar, teste a relação entre
  eles, não cada literal: *o campo é `_default_` se e somente se a chave não tem
  sufixo*.
· **`getattr(obj, "campo", default)` TRANSFORMA DEFEITO DE FIAÇÃO EM DEGRADAÇÃO.**
  O provider era buscado em `inference_engine.providers`, atributo inexistente (é
  `_providers`), e o `{}` do default fazia a medição sair pelo log "sem provider
  LLM" — indistinguível de ambiente sem chave. **Ausência de ATRIBUTO e ausência
  de CONFIGURAÇÃO são fatos diferentes e precisam de portas diferentes.**
· **`producer.send` DO AIOKAFKA BLOQUEIA, NÃO LEVANTA.** Com o broker
  inalcançável ele espera metadata ~40 s até o request timeout. O `try` protege
  contra exceção, **nunca contra travamento** — "fire-and-forget: nunca levanta"
  era verdadeiro e insuficiente. Escrita local vem primeiro; emissão remota por
  último e com teto de tempo.
  *Corolário:* a mensagem de erro (`UnknownTopicOrPartitionError`) NOMEIA o tópico
  e induz ao diagnóstico errado. O tópico existia; confira a lista do broker antes
  de acreditar no texto do erro.

═══ ✅ O BLOQUEIO DE CREDENCIAL CAIU ═══
A chave do demo foi reposta e o `/v1/health` responde **200/`ok`**. Conclusões
tiradas deste ambiente sobre avaliação, copilot, contexto e sentimento voltaram a
valer.

**A causa do bloqueio vale lembrar:** o `docker-compose.demo.yml` não tinha
`env_file`, então `.env.demo` **nunca era lido** — a chave vinha exportada da shell
de quem subiu a stack. Hoje o serviço tem `env_file: - .env.demo` e a linha
`PLUGHUB_ANTHROPIC_API_KEY:` foi **removida** do bloco `environment:` (que vence o
`env_file` e era o que deixava o estado da shell mandar em silêncio).
⚠️ `.env.demo` NÃO expande variável: o valor tem de estar por extenso nas DUAS
linhas, mantidas iguais à mão.

═══ ESTADO (fechado em 2026-08-24, ver CHANGELOG) ═══
SENTIMENTO: medido de ponta a ponta, com DOIS gates.
  · `infra/test/probe_sentiment_producer.sh` — metade GATEWAY. Duas chamadas que
    diferem só em `customer_utterance`, com TESTEMUNHA NEGATIVA (a chamada sem o
    campo não pode escrever nada).
  · `infra/test/gate_sentiment_engine_half.sh` — metade ENGINE. Reprodução MANUAL:
    exige contato que ENFILEIRE (pool humano sem ninguém pronto).
  · Medição de referência: score **-0.50**, pool `sac_ia`, ctx + `sentiment_live`.
  · Declarado em `agente_fila_v1.responder_cliente` — **único** step `reason` sobre
    fala de cliente no repo. Enquanto for o único, sentimento só existe para contato
    que passou pela FILA.
AGENTE DE FILA: deixou de ser surdo ao cliente (`menu.ts:211`/`resolve.ts:192`,
  `??` → `||`). Ver `conference-mechanics.md` § Mudança 35.
AI-GATEWAY: `app.state.llm_providers` publicado no startup; ordem dos emissores
  invertida (Redis antes de Kafka, emit sob `wait_for` 5 s); helper único
  `sentiment_emitter.resolve_session_pool_id` (GET + json, 4 ramos nomeados).
  **168 testes verdes.**

═══ TAREFA — escolher, não fazer tudo ═══

1. **O Console mostra "Neutral" para um cliente com score -0.50** *(diagnosticado
   em 08-24, NÃO corrigido — o conserto tem escopo próprio)*. Medido nas duas
   fontes, lado a lado, na sessão `6e8b22ef`:

       session:{sid}:ai → partial_params.sentiment_score : null
       {tenant}:ctx:{sid} → session.sentimento.current   : {"value": -0.5, …}

   `mcp-server/src/tools/supervisor.ts:118` faz
   `Number(partials["sentiment_score"] ?? 0)`. **Dois defeitos numa linha:**
   (a) lê a fonte APOSENTADA — o `sentiment_score` auto-reportado pelo
   `output_schema`, que o 08-23 abandonou porque nenhum skill o declara; o valor
   medido vive no ContextStore e a tela nunca olha para lá. (b) o `?? 0`
   transforma NÃO-MEDIDO num ponto legítimo da escala, e `0.0` classifica como
   `neutral` (`ActionBar.tsx:39`). A `trajectory` (`:115-116`) tem o mesmo `?? 0`,
   então a tendência também é feita de zeros inventados.

   *O agravante:* a UI **se protegia** — o chip só renderiza com
   `sentimentScore !== null` (`ActionBar.tsx:286`). O `?? 0` do backend desarmou
   essa proteção antes que ela pudesse agir, e a tela ficou incapaz de dizer
   "não sei". Vale mais que sentimento: é o único lugar onde a plataforma exibe
   um número medido ao operador, e ele está mentindo com cara de normalidade.

   Decisões que o conserto pede (por isso não foi feito de carona): de qual fonte
   ler, e **o que exibir quando não há medição** — ausência do chip é honesta e
   silenciosa; um estado "não medido" explícito é mais caro e mais verdadeiro.

2. **Validar o copilot em runtime** *(desbloqueado)*. Os dois bugs (`system=`
   inexistente; `.text` por `.content`) foram achados por LEITURA em 08-23 e
   nunca rodaram. ⚠️ **Confira primeiro DE ONDE o copilot busca o provider** — se
   for pelo mesmo caminho que o sentimento usava (`inference_engine.providers`),
   ele está morto pelo mesmo motivo, e "corrigido" continua sendo hipótese.

3. **Normalizar a guarda `_default_` nos 5 leitores** *(defesa em profundidade da
   Mudança 35)*. A assimetria `!== "_default_"` × truthiness continua em
   `orchestrator-bridge/main.py:9180` e `mcp-server/server.ts:2471/2487/2497/3641`.
   Hoje não há produtor de campo vazio; o próximo caminho que ativar agente nativo
   sem `instance_id` reabre o mesmo buraco. Tratar `""` como `_default_` **e
   LOGAR** — silêncio aqui recria o defeito com outra grafia.

4. **`session:{id}:meta` fatias B e C** *(a C ficou com consumidor NOMEADO)*.
   B = recusar campo de outro dono (hoje o helper só LOGA `[session_meta] DONO
   VIOLADO`); exige os quatro escritores de canal passando pelo helper, incluindo
   um irmão em TypeScript (`bpm.ts`). **C ganhou urgência**: o `pool_id` do meta é
   o pool de ENTRADA, então o sentimento medido pelo agente de FILA é agregado sob
   o pool onde o contato começou (`sac_ia`, não `fila_humano`). Separar em
   `entry_pool_id` é decidir semântica de atribuição — não cabe de carona.
   › *Suspeita NÃO medida:* `session:{id}:wf_agent` nasce com o mesmo `_stl()` = 4h
     ao lado do site W6 e descreve a mesma workflow de até 48 h. Medir antes.

5. **Cobertura e2e da FILA** *(net-new, revelado por 08-24)*. Nenhum cenário em
   `packages/e2e-tests/scenarios/` exercita contato entrando em fila com o agente
   de fila respondendo — foi por isso que a surdez do agente de fila sobreviveu.
   O 07 chama `queue_context_get` direto por MCP, sem flow rodando. Enquanto não
   existir, `gate_sentiment_engine_half.sh` depende de reprodução manual.

6. **Gate re-executável da Camada F** (arco Detach). Validada por medição manual
   instrumentada; sem script versionado, arco completo é lembrança. A própria F4
   declara que a **lease** não foi medida (não há reaper; o transbordo é por idade).

7. **`mcp_audit_log`** *(dormente POR MEDIÇÃO)*. Ordem correta: (1) exercitar
   `invoke` de verdade e confirmar linha em `session_timeline`; (2) só então DDL +
   dual-write. **Não criar a tabela antes** — vazia com cara de pronta é
   indistinguível de "ninguém acessou". Resíduo junto: `invoke` emite
   `session_id: ""` quando a instância tem 0 ou 2+ sessões ativas
   (`external-agent.ts:194-215`) e o parser DESCARTA evento sem `session_id`
   (`models.py:713-718`).

8. Reexame item a item dos 9 em `Escopo reduzido` (§4.5 do doc de reversão) —
   rejulgar pelo mérito, NÃO reverter em bloco. **Não tocado desde 08-17.**

9. Provisionar o SFU (LiveKit) — pré-requisito de qualquer trabalho de WebRTC.

═══ RESÍDUOS MENORES (sem dono, nenhum é bloqueio) ═══
· **contador de credencial é BALDE de calendário, não janela deslizante**:
  `window = now // 86400` (`account_selector.py`). Na virada zera de vez, e
  `calls_ok=0` logo depois é indistinguível de "nunca funcionou" — que é a
  condição que dispara a nota de aviso do `main.py:671`
· **`calls_ok` SUBCONTA**: `record_usage` só roda quando o AccountSelector escolheu
  uma CONTA. Chamada pelo alias legado do provedor tem sucesso sem incrementar.
  No demo o `config_id` sai VAZIO, isto é, está tudo indo pelo alias legado
· **`extract_context_from_response` (`context.py`) é heurística de palavras-chave em
  PT** e ainda alimenta `intent`/`confidence`/`flags` do `/inference`. Como
  `/inference` não tem chamador, é dívida dormente — mas o comentário do arquivo
  promete "model fine-tuned per vertical", que é promessa sem produtor
· **pausa de IA é desfeita em 15 s**: o `agent_ready` da reconciliação do bootstrap
  republica `ready` para instância com pausa declarada. Conserto no
  `instance_bootstrap`
· **`bpm.test.ts` vermelho por dívida conhecida**: `conversation_escalate` agora
  RECUSA sem `session:{id}:meta` e o teste não semeia o meta
· **`npx vitest` no host valida `@plughub/schemas` de 2026-08-02** — o perigo é o
  simétrico: mudança de schema ficar VERDE porque o `dist/` velho aceita a forma
  antiga
· `queue_config` só existe em 1 pool do demo (`retencao_humano`) — as duas metades
  do gate da fila não cabem na MESMA janela sem mexer em config
· o e2e 07 tem uma asserção que **não pode reprovar** (`pass(...)` incondicional
  sobre o retorno do `queue_context_get`, `07_inbound_full.ts:133-137`)
· `session_timeline` está VAZIO e recebe linha de um único parser (o de `mcp.audit`)
  — ou seja `/v1/audit/mcp-calls` sempre devolve lista vazia neste ambiente
· o `on_failure` do `responder_cliente` volta para `aguardar_mensagem` **sem falar
  com o cliente**: falha de LLM na fila é indistinguível de "o agente ignorou"
```
