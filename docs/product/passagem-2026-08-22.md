# Prompt de passagem — sessão de 2026-08-22

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a de 2026-08-21, que fechou a
> **tarefa 2** inteira (capacidade × pausa, três defeitos) e **dois terços da tarefa 3** (os dois
> defeitos-consequência do `session:{id}:meta`). A passagem
> [`passagem-2026-08-21.md`](passagem-2026-08-21.md) está **encerrada**.

---

```
continuação sessão plughub.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· arquivo NOVO exige `build --no-cache`; edição de existente entra no build normal
  (exceção medida: `__tests__/` está no `exclude` do tsconfig do mcp-server, então
   arquivo de teste novo não afeta o build)
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· **janela de log só compara se as DUAS janelas existirem**
· **`--since` do docker sem fuso é HORA LOCAL** — o host é −03
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso**
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3; usar `$DC exec -T redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· YAML de registry é seed-if-absent: editar pool existente é NO-OP no DB rodando
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · mailing-api 3660 · **redis 6380 · kafka 9093 · postgres 5433**
· health da analytics-api é **`/v1/health`** (o `/health` sem prefixo dá 404)
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ TRÊS REGRAS QUE 08-20 E 08-21 COMPRARAM ═══
· **PROCURE A TESTEMUNHA DO DEFEITO NO LOG ANTES DE CONSERTAR** (08-20). Um fix num
  caminho sem tráfego fica verde sem mudar nada, e o verde é creditado a ele.
· **UM CASO PERMITIDO QUE PULA O PORTÃO NÃO SERVE DE CONTROLE** (08-20). Controle tem
  de mudar UM valor e percorrer o MESMO caminho.
· **DUAS CAUSAS EMPILHADAS PODEM DAR O MESMO NÚMERO** (08-21, nova). O pool publicava
  `available: 2` por DOIS motivos que coincidiam no valor: snapshot congelado (gancho
  morto por divergência de nome) E aritmética que somava capacidade de agente pausado.
  Consertar o de cima não moveria o número, e pareceria "não aplicou". **Antes de
  codar, calcule à mão o valor que o caminho consertado produziria**; se empata com o
  observado, há mais de uma causa. Foi um probe imprimindo "modelo de hoje" × "modelo
  correto" lado a lado que separou.
· *Corolário de instrumento, caro em 08-21:* o probe mentiu DUAS vezes em dez minutos
  — 36 `docker exec` sequenciais fabricaram "6 snapshots sem `available`" (eram
  leituras falhas; virou `MGET` único), e um `case` exigindo `"model":"…` sem espaço
  contou 0 `bootstrap_placeholder` enquanto o histograma ao lado contava 30
  (`json.dumps` escreve com espaço). **Duas contagens da mesma coisa na mesma tela é o
  que salvou as duas.**

═══ ESTADO (fechado em 2026-08-21, ver CHANGELOG) ═══
TAREFA 2 COMPLETA — capacidade × pausa, os três defeitos:
  · aritmética: `_RECOMPUTE_POOL_OCCUPANCY_LUA` lê `status`; instância inativa
    contribui capacidade ZERO e mantém a ocupação. `paused_capacity =
    Σ max(0, max_concurrent − ocupação)` POR INSTÂNCIA (2, não 3, com 1 sessão viva)
  · a linha FECHA: `total = busy + busy_elsewhere + paused_capacity + available`
  · gatilho: o consumidor casava `agent_paused`, nome que produtor NENHUM emite
  · a pausa NÃO limpa o `busy_set` (só o logout) — é lá que o pausado-com-sessão vive
  · gate `infra/test/gate_pause_capacity.sh` VERDE (P0 2 · P1 0 · P2 2)
  · `system_availability_check` parou de cair em `pools_available` sem o rollup;
    `available` virou OPCIONAL no tipo (o bootstrap o omite de propósito)
TAREFA 3 — dois terços: guard do supervisor fail-closed + resolvedor único de tenant
  no mcp-server (3 sites). Gate `gate_supervisor_tenant_guard.sh` VERDE, e ele estava
  VERMELHO contra a imagem anterior — discrimina de fato
n8n arco ABORTADO · não reabrir os baldes da triagem de 08-17

═══ TAREFA — escolher, não fazer tudo ═══

1. **Declarar a PARTIÇÃO de propriedade do `session:{id}:meta`** *(o que sobrou da
   tarefa 3, e é o coração dela)*. Seis escritores, três semânticas no bridge, chave
   interpolada à mão em ~50 sites, sem schema nem helper. **Medido em 08-21: 8/8 metas
   carregam `pool_id`**, que pela partição implícita é do BRIDGE — a violação é a
   regra, não a exceção. Conserto: helper `session_meta_merge(..., owner)` por
   linguagem que (i) rejeite campo de outro dono, (ii) preserve o MAIOR TTL (tratando
   `-1` como ausência), (iii) tenha semântica única de criação; mais schema com nome
   que não colida com o `SessionMeta` do ReplayContext. **Nenhum dos seis pode virar
   dono único** — os canais só cobrem webchat/webrtc, o bridge chega tarde, o trigger
   se abstém de `pool_id` por desenho.
   › *Sub-item independente:* TTL 24 h → 4 h. Medido 8/8 na faixa ≤ 4 h, 0 acima —
     mas isso **não prova truncamento** (webchat nasce com 4 h). Provar exige sessão de
     TRIGGER que passou por alocação. Não consertar antes de provar.

2. **Sentimento sem produtor** *(o mais grave herdado de 08-20, intocado)*.
   `ai-gateway/session.py:142-161` é o ÚNICO chamador de produção das três funções de
   sentimento e pendura no `/v1/inference`, que tem **0 requisições** contra 116 do
   `/v1/reason`. Tópico `sentiment.updated`, chave `session:{id}:sentiment`, tag
   `session.sentimento.current` e o consumidor da analytics-api descrevem um pipeline
   SEM FONTE. **Responder ANTES de codar:** `/v1/inference` é legado (⇒ sentimento
   precisa de produtor no caminho do `reason`) ou caminho vivo que o demo não exercita?
   O `HGET` numa chave string, na mesma função, é o SEGUNDO problema — não o primeiro.

3. **Auditoria MCP sem STORE** *(achado de 08-21)*. `system.tables` por `%audit%` OU
   `%mcp%`, sem filtro de database, voltou **VAZIA**: nem `mcp_audit_log` nem
   `audit_access_log` existem. O invariante "nenhum caller pode optar por sair do
   audit" é de LGPD, e a única borda em vigor (`invoke`, `source:
   "mcp_server_invoke"`) publica para um consumidor que não tem onde gravar. **Medir
   antes:** o consumer da analytics-api chega a tentar criar a tabela e degrada mudo,
   ou o DDL nunca é chamado? Tabela ausente e consumer que nunca rodou dão a mesma tela.

4. **Gate re-executável da Camada F** do arco Detach. Validada por medição manual
   instrumentada, reaproveitando smokes de B/D/R0/I5 por override de env
   (`smoke_internal_work_queue.sh:85-89`). Arco declarado completo sem gate versionado
   é lembrança, não verificação. A própria F4 declara que a **lease** não foi medida.

5. Reexame item a item dos 9 em `Escopo reduzido` (§4.5 do doc de reversão) — rejulgar
   pelo mérito, NÃO reverter em bloco. **Não tocado desde 08-17.**

6. Provisionar o SFU (LiveKit) — pré-requisito de qualquer trabalho de WebRTC.

═══ RESÍDUOS MENORES (sem dono, nenhum é bloqueio) ═══
· **pausa de IA é desfeita em 15 s**: o `agent_ready` da reconciliação do bootstrap
  republica `ready` para instância com pausa declarada. Não é regressão (antes a pausa
  de IA não agia em nada); o conserto é no `instance_bootstrap`
· **`bpm.test.ts` vermelho por dívida conhecida**: `conversation_escalate publica
  evento…` falha porque o tool agora RECUSA sem `session:{id}:meta` (arco P2, 08-18) e
  o teste não semeia o meta. Uma suíte vermelha por dívida aceita deixa de sinalizar
  regressão nova. Conserto: semear o meta + acrescentar o caso simétrico (meta ausente
  ⇒ recusa nomeada)
· **`npx vitest` no host valida `@plughub/schemas` de 2026-08-02**: o `dist/` não tem
  `mcp_server_invoke`, que o fonte declara — daí 2 falhas em `invoke-audit.test.ts` que
  NÃO são defeito. O perigo é o simétrico: mudança de schema ficar VERDE no host porque
  o `dist/` velho aceita a forma antiga. No container não acontece (o Dockerfile builda
  schemas antes). Decidir: pré-passo declarado, ou dizer que a suíte de host não julga
  contrato
· `queue_config` só existe em 1 pool do demo (`retencao_humano`) — as duas metades do
  gate da fila não cabem na MESMA janela sem mexer em config
· o e2e 07 tem uma asserção que **não pode reprovar** (`pass(...)` incondicional sobre
  o retorno do `queue_context_get`, `07_inbound_full.ts:133-137`)
```
