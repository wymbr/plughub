# Prompt de passagem — próxima sessão

> Convenção de nome: `passagem-AAAA-MM-DD-NN.md`. Sucede
> [`passagem-2026-08-26-01.md`](passagem-2026-08-26-01.md), que está **encerrada**: as duas frentes
> que ela abriu foram resolvidas — o **chip de PROCESSO** (decisão do dono: caminho **(a)**,
> implementado, provado por gate e por tela) e a triagem que ela pedia. A **V2 do arco ALLOWLIST
> continua sendo a próxima fase** e não foi tocada.
>
> ⚠️ Arquivos SEM o sufixo `-NN` são da série antiga e **o nome deles não é a data**. Confie no
> carimbo do dado dentro do arquivo.

---

```
continuação sessão plughub. A frente principal é a mesma que a passagem anterior deixou
em segundo lugar, e ela não andou:

  (1) V2 do arco ALLOWLIST — catálogo de TIPOS (D1). Próxima fase do ADR; destrava a
      metade ABERTA da V0 (os 3 inventários de categoria que discordam, com
      `iban`/`passport` fantasmas exibindo selo "Ativo" incondicional).
          docs/adr/adr-contextstore-allowlist.md   (§6 = as fases; V0/V1/V1b feitas)

  (2) TRÊS FUROS DE AUTORIZAÇÃO medidos ontem, com seção própria no TODO.md. Não são
      teoria: têm arquivo:linha e reprodução. Em ordem de gravidade:
        · `POST /supervisor/join` entra em sessão VIVA e ESCREVE no stream conferindo
          só o tenant (`supervisor.py:81`).
        · o ABAC de pool é OPT-IN DO CHAMADOR: sem header `Authorization`, o
          `pool_auth` devolve irrestrito SEM 401 (`:16-18`, `:133-135`); o caminho SSE
          degrada aberto até com token EXPIRADO (`:182-184`).
        · Analytics/Monitor têm portão de PAPEL a montante da ABAC, hardcoded e não
          editável na UI (`Sidebar.tsx:121`) — e COSMÉTICO, porque a rota não tem
          guard (`routes.tsx:122-147`).

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Já se pagou DEZESSEIS sessões
  seguidas. Nesta o preço veio três vezes, sempre na mesma forma: **a população que o
  instrumento olhou não continha o caso da pergunta**. Detalhe em § O QUE ESTA SESSÃO
  COMPROU — leia essa seção ANTES de escrever qualquer probe.
· **AS CINCO BASELINES, se for tocar código de serviço** (a 1ª MUDOU ontem):
    $DC exec -T analytics-api sh -c "cd /app/packages/analytics-api && python3 -m pytest src/plughub_analytics_api/tests -q 2>&1 | tail -3"
    $DC exec -T orchestrator-bridge sh -c "cd /app/packages/orchestrator-bridge && python3 -m pytest src/plughub_orchestrator_bridge/tests -q 2>&1 | tail -3"
    $DC exec -T session-replayer python3 tests/test_context_persister.py
    $DC exec -T routing-engine sh -c "cd /app/packages/routing-engine && python3 -m pytest src/plughub_routing/tests -q 2>&1 | tail -3"
    $DC exec -T mcp-server-plughub sh -c "cd /app/packages/mcp-server-plughub && npx vitest run 2>&1 | tail -5"
  Esperado: **620 passed** (era 617; +3 do marcador) · **102** · **7/7** ·
  **1 failed, 294** · **1 failed, 221**. Os dois vermelhos são conhecidos (Backlog).
· 🔴 **O AMBIENTE TEM UM CASO DE TESTE MONTADO À MÃO. NÃO O DESFAÇA.**
  `supervisor@plughub.local` está com **`accessible_pools = ["limite_ia","limite_retorno"]`**,
  concedidos pela tela de Acesso **de propósito** — é o único usuário que combina
  *alcança o Analytics* com *escopo estreito*, e sem ele o gate do chip não julga.
  Zerá-lo "para ver 120" apaga o caso. Confira antes de medir:
    ADMIN_EMAIL=supervisor@plughub.local ADMIN_PASS=changeme_supervisor bash infra/test/q_chip_row_dump.sh
· ⚠️ **Os outros dois usuários, para não perder tempo:** `admin` tem **22 pools** (43 com
  os espelhos `-int`) e alcança as 120 linhas da janela — **não exercita** nada de
  escopo. `operator` tem escopo estreito e **não alcança a tela** de Analytics (furo 2
  acima). Qualquer medição de ABAC de pool tem de dizer QUAL usuário usou.
· ⚠️ **`retencao_humano` segue com `queue_config.pool_id = fila_humano`, `max_wait_s: 1800`,
  `sla_target_ms: 300000`** — config deliberada, não reverta.
· 🆕 **PODE haver seed de teste sujando o ctx.** `infra/test/seed_withheld_demo.sh` injeta
  `caller.nome` + `session.demo.resume_token` e **não limpa sozinho**. "Maria Demo" num
  contexto é isto. Limpe com `<script> <sid> --clean`.
· 🔴 **NUNCA rode `REGISTRY_SYNC_RECONCILE=true` para publicar um skill.** Martelo
  cirúrgico: `bash infra/test/redeploy_limite_entrada.sh`, ou
  `infra/test/_publish_skill.py <yaml> <pool>`.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **UM COMANDO POR LINHA.** Duas linhas coladas juntas viraram
  `q_chip_row_dump.shADMIN_EMAIL=...` e o bash foi procurar um arquivo com esse nome
· **comandos CURTOS** — cadeia de curl com token → **vira script em `infra/test/`**
· $DC não vem definido, e o compose fica na RAIZ:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· ⚠️ **o serviço do MCP chama-se `mcp-server-plughub`** — `mcp-server` NÃO resolve
· **`$DC exec` no clickhouse EXIGE `-T`**
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· ⚠️ **arquivo NOVO só entra na imagem com `build --no-cache`**; editar existente entra normal
· **script em `infra/test/` roda do HOST** — não precisa de build nenhum
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS
· **no front há DOIS caches** — depois do `up -d`, `Ctrl+Shift+R` é obrigatório
· **PREFLIGHT DE SÍMBOLO antes de medir**, ESTRUTURAL, nunca `grep`
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· ⚠️ **em `jq`, NUNCA `// "default"` sobre campo booleano ou numérico** — o alternativo
  dispara em `false` e em `0` tanto quanto em `null`, e as três coisas significam coisas
  diferentes. Use `if has("k") then (.k|tostring) else "AUSENTE" end`
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· ⚠️ **alias de agregado NUNCA repete nome de coluna real** (`ILLEGAL_AGGREGATION`, 184)
· **para chamar um TOOL MCP não existe `POST /mcp`** — o transporte é SSE; molde em
  `infra/test/probe_supervisor_tool_masking.sh`; em TS, `packages/e2e-tests/lib/mcp-client.ts`
· portas: analytics-api 3500 · agent-registry 3300 · **config-api 3600** · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 ·
  auth-api 3202 · agent-assist-ui (LEGADO, vivo) 5173

═══ O QUE ESTA SESSÃO COMPROU ═══
· 🔴 **A POPULAÇÃO DE UM INSTRUMENTO SE DERIVA DO DADO — NUNCA SE ASSUME DO RECORTE.**
  Três ocorrências em uma sessão, todas minhas, todas na mesma forma:
    1. `q_scope_delta_stage2.sh` decidia "o chip encolheu" por **interseção não-vazia**
       — e imprimia `anon=4 auth=4` nas próprias linhas de prova. Veredicto contradito
       pelo dado que ele exibia.
    2. o gate NOVO do marcador repetiu o erro **uma tela depois**: tomou "linhas multi
       visíveis" como "processos com membro fora do escopo" (verdade só com o admin de 5
       pools; com 22 reprovou código CORRETO em dois ramos, 29 de 29 linhas).
    3. o `q_chip_row_dump.sh` pegava as PRIMEIRAS linhas com chip — para irrestrito, as
       mais recentes, todas de processo de UMA sessão, onde a invariante vale
       trivialmente. Previ um número sobre uma população que o script não trouxe.
  **A derivação certa vem de comparar os dois escopos linha a linha** (`auth_n < anon_n`).
  E o corolário que fecha: **sem nenhuma linha do caso, o gate NÃO julga — INCONCLUSIVO.**
  Escopo largo não refuta um defeito de escopo; apenas não o exercita.
· 🔴 **TELA É EVIDÊNCIA DO QUE ELA MOSTRA, NÃO DO QUE ELA CONTÉM.** Li `· 3` em duas
  telas e registrei no TODO um provável defeito de backend ("o chip publica só os
  acessos"). Medido: `total=5 acc=3 int=2 unk=0` — a invariante FECHA. O ` + 2` estava
  **cortado pela rolagem horizontal**, e a barra de rolagem estava visível na MESMA
  imagem. Antes de inferir estado de backend a partir de um render, garanta que o render
  foi visto inteiro.
· 🔴 **NÃO "MELHORE" UM SCRIPT QUE VOCÊ NÃO PODE EXECUTAR.** Enfiei um `if/else` dentro
  do pipe do jq para refinar a população e quebrei o script em 3 erros de compilação, na
  mão do dono. Neste setup **toda edição de script sai não verificada** — mudança tem de
  ser mínima e sintaticamente conservadora, e o refinamento vem depois de o básico rodar.
· **UMA MEDIÇÃO PODE MATAR DUAS HIPÓTESES OPOSTAS DE UMA VEZ.** A queixa *"o admin vê
  pools não liberados"* tinha duas leituras (requisição sem header × lista vazia =
  irrestrito). Nenhuma: o token traz 22 pools e eles ALCANÇAM as 120 linhas. O
  discriminador foi decompor o predicado em suas três cláusulas e contar cada uma —
  101 / 9 / **10** / **0 suspeitas**. A cláusula 3 (*"um pool meu ATENDEU"*) é o que
  engana o olho, e é desenho.
· **UM CAMPO COM TRÊS ESTADOS PRECISA DOS TRÊS NA SAÍDA.** `journey_has_scoped_out_members`
  é `true` (há mais) × `false` (**medi** e não há) × `null` (não medi). Colapsar `false` em
  `null` faria o front tratar quem tem visão completa como caso desconhecido; colapsar
  `null` em `false` afirmaria completude não verificada. O mesmo vale no `jq` que lê.

═══ ERROS DE PREVISÃO ═══
Acertos: **620 passed** exato · **4/4 na tela** do supervisor (as três classes de linha +
a testemunha negativa) · a causa comum da queixa de ABAC (previ "os 22 pools alcançam
tudo, não há furo" — confirmado, 0 suspeitas) · `INCONCLUSIVO` com o admin, como previsto.
❌ **ERRO 1: previ `int=0` no chip do admin (a quebra não fecharia). Veio `int=2`.** A
   invariante sempre esteve boa; o defeito que eu tinha registrado não existia.
❌ **ERRO 2: previsão NÃO TESTADA apresentada como previsão.** O dump trouxe outra
   população (processos de 1 sessão) e o número previsto nunca foi exercido. Dizer
   "errei" ali seria tão falso quanto "acertei".
❌ **ERRO 3: mandei dois comandos na mesma linha**, que o bash concatenou.

═══ ENTREGUE — não re-fazer ═══
**(a) — o marcador de existência do chip de processo)** `reports_query.py`
(`_attach_session_journey_chip`): a ABAC saiu do `WHERE` e virou o predicado dos `countIf`;
os quatro números continuam escopados e o fato novo é `count() > countIf(acc)` →
`journey_has_scoped_out_members`. Sem restrição o predicado colapsa em `1` ⇒ `false`
medido, nunca `null`. Front: `ProcessChip.tsx` (`hasProcess` com a segunda razão para
pivotar; sob o marcador a QUEBRA não é desenhada, porque ela também é escopada e uma
classe fora do alcance viraria `0`); `ListaTab.tsx` (rodapé passou a usar `hasProcess`, a
MESMA função dos chips); i18n `lista.processChipScopedHint` (en + pt-BR); `types.ts`.
3 testes novos (`TestJourneyChipScopedOutMarker`), um deles assertando que a ABAC está no
`countIf` e **não** no `WHERE` — a regressão que nenhum teste de valor pega.

**Quatro scripts novos em `infra/test/`** (rodam do host, sem build):
· `probe_process_chip_scoped_marker.sh` — o gate do arco. População derivada, 3 ramos +
  INCONCLUSIVO, reprova nos DOIS sentidos (marcador mudo e marcador aceso à toa).
· `q_scope_leak_check.sh` — decompõe `_session_scope_clause` nas três cláusulas, com
  testemunha de presença por cláusula.
· `q_chip_row_dump.sh` — os cinco campos do chip, crus, por linha.
· (`q_scope_delta_stage2.sh` foi CORRIGIDO, não criado.)

**(colaterais)** `CHANGELOG.md` § chip · `TODO.md` com 5 seções novas · ADR
`adr-historico-unificado-duas-visoes.md` D3 emendada (*"processo inteiro"* tem um limite
que a decisão não previu: a ABAC).

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
`/reports/sessions` 19–26/08:
| usuário | pools | linhas | processos que perdem membro |
|---|---|---|---|
| anônimo | — (irrestrito) | 120 | 0 (`false` em 120, `true`/`null` em 0) |
| admin | 22 (43 c/ espelhos) | 120 | 0 → gate INCONCLUSIVO |
| operator | estreito | 80 | **6** (`4→1 3→1 4→1 4→1 4→1 5→1`) |
| supervisor | `limite_ia`+`limite_retorno` | 32 | 6, e a tela mostra `3+`/`2+`/**`1+`** |
Quebra do chip (admin): `PRC-8c47326d` **total=5 acc=3 int=2 unk=0** ·
`PRC-faf611c6` **total=3 acc=2 int=1 unk=0** — invariante fecha nas duas.
`q_scope_leak_check.sh` (admin): cláusula 1=**101** · 2=**9** · 3=**10** · suspeitas=**0**.
baselines: **620** · 102 · 7/7 · 1+294 · 1+221.

═══ NÃO fazer agora ═══
· **Zerar/alterar `accessible_pools` do `supervisor`** — é o caso de teste do chip.
· **`REGISTRY_SYNC_RECONCILE=true`** — reverteria a config do `retencao_humano`.
· **Inverter `accessible_pools: [] → nenhum pool`** antes de tornar o irrestrito
  EXPLÍCITO e contar quem depende do vazio. Tranca admin e developer fora, e "ninguém vê
  nada" é tão mudo quanto "todo mundo vê tudo". Ordem no TODO.
· **Retomar a (d)** ("ABAC no conteúdo, lista aberta") na ordem em que ela foi proposta.
  Abrir as linhas primeiro ENTREGA os `session_id` de endpoints que não checam nada.
· **Inverter para deny-by-default (V4 do ALLOWLIST) antes da V3.**
· **"Consertar" a tela do admin porque a faixa da V1 não aparece** — é o desenho; admin é
  `supervisor_role` e pula o portão. Testar como `operator`, sempre.
· **Apagar `AnaliseTab.tsx` / `useAllConfig` "porque estão mortos"** — são FEATURES sem
  consumidor.

═══ Backlog NOVO / reaberto ═══
· 🔴 **Os três furos de autorização** (topo desta passagem) — seções próprias no `TODO.md`,
  com o plano em 3 passos para o `[] = irrestrito` (401 primeiro, irrestrito explícito
  depois, `[]` = nenhum por último). **Decisão do dono registrada: o admin respeita a ABAC
  como qualquer um; não há bypass por papel.** Precedente na casa: o ABAC do scheduler já é
  grant-first sem bypass de admin.
· 🟡 **(d) adiada, com a ORDEM INVERTIDA e a tabela dos ~8 endpoints de drill que hoje não
  aplicam ABAC nenhuma** (`sessions.py` inteiro não importa `Depends`; o `transcript.py`
  diz por escrito *"tenant isolation only"*). Se voltar, vira ADR.
· 🆕 **UX: o chip trunca.** É o ÚNICO pivô da visão 1 para a visão 2 e fica cortado em tela
  comum (foi o que me fez registrar um defeito inexistente). Mover PROCESS para antes de
  SEGMENTS, ou `sticky` à direita.
· 🔴 **`packages/e2e-tests/scenarios/17_context_store.ts` Parte E nunca exerceu o tool** —
  `POST ${mcpServerUrl}/mcp`, rota inexistente. Conserto: `McpTestClient`.
· 🔴 **`src/__tests__/bpm.test.ts` — `conversation_escalate` vermelho desde 2026-08-18, e o
  SERVIÇO está certo.** Melhor conserto: assertar a recusa.
· 🆕 **`invalidateContextMaskingCache` não tem call site nenhum** — promete um consumidor de
  `config.changed` que não existe; a janela real é o TTL de 60 s.
· 🔴 **Outra metade da V0: os 3 inventários de categoria** (`iban`/`passport` fantasmas com
  selo "Ativo"). Depende da **V2**.
· 🆕 **Seed × config viva divergem** no namespace de masking. ADR §D7.
· 🆕 **`agent-assist-ui` legado (5173) não ganhou a faixa da V1** — aditivo, não regride.
· 🔴 **`probe_f4_direction_and_classes.sh` sai `OK` com ramo INCONCLUSIVO** — `:161` decide
  sobre `$fail`, e os ramos inconclusivos usam `note`, que não o toca.
· 🆕 **A visibilidade do ContextStore é lida do pool de ENTRADA**, não do que atende.
  Contato transferido aplica a política do pool de ORIGEM — 1º consumidor em que a acepção
  errada custa **exposição de PII**.
· 🆕 **`catch { /* non-fatal — use default */ }` sem log** na leitura de `context_visibility`.
· 🆕 **Nenhum dos gates novos é chamado por runner** — agora somando os quatro desta sessão.
· 🔴 **`test_expire_returns_the_slot_even_after_the_lease_expired` VERMELHO**, anterior a dez
  sessões. `router.py:1158` declara aquele ramo como *a MEDIDA da lacuna 2*.
· 🆕 **`GET /config/dashboards?tenant_id=…` responde 404** — NÃO é a colisão de rota.
· 🆕 **`wrapup_hold_ttl_s` é LEITOR SEM CHAVE** (`kafka_listener.py:411`).
· 🆕 **`sla_unstamped` existe na API e NÃO tem superfície na UI.**
· 🆕 **`_cv_sla_series` IGNORA o retorno de `_apply_pool_scope`.** É ABAC. Medir antes.
· 🔴 **`sla_default_ms` NÃO TEM LEITOR** — remover exige mexer no
  `smoke_config_routing_orphan_keys.sh:118`, que a usa como CANÁRIO.
· **3 cópias restantes do `480_000`** · **`supervisor.ts` compara atendimento total com alvo
  de espera** · **`duration_ms` do segmento humano diverge dos próprios carimbos** ·
  **`_emit_queue_timeout` publica SEM `key=`** · **`/analise/sessions?session_id=…` não é
  honrado**.

═══ INCONFORMIDADES herdadas — estado ═══
· ✅ **Tela de Masking mostrando 0 regras** — FECHADA (V0).
· ✅ **Omissão muda no ContextStore** — FECHADA nas DUAS portas (V1 + V1b).
· ✅ **"COLUNA PROCESS sumiu"** — FECHADA. Metade 1 (`120→86`) era config de escopo;
  metade 2 (o chip) foi consertada pelo caminho (a), com gate e tela.
· ✅ **"chip `· 3` com 5 linhas"** — REFUTADO por medição; era a coluna cortada.
· ⚠️ **Não testado:** transfer com o cliente ainda CONECTADO.
· ⚠️ **`recipients` do release saiu com UM elemento** nas duas execuções reais.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens antigas em busca de backlog órfão (herdada de DEZOITO)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduo do defeito 2: `queue_config.skill_id` legado do `retencao_humano`
· **lente C** (faixas por personagem) — **SUSPENSA por decisão do dono em 2026-08-26**
```
