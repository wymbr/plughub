# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-09-06.md`](passagem-2026-09-06.md), que está **encerrada**.
>
> A sessão anterior entregou **UMA** fatia com gate verde, mutação nos dois lados e delta contado
> antes de codar: a **D14 (iii)**, que **fechou o arco D14 (i→ii→iii)**. Foi a primeira das três que
> MOVEU número.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes, mas **todo dado medido carrega carimbo
> `2026-08-25`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: escolher entre (TTL de `pool_config`) — dois escritores da mesma
chave com TTLs diferentes, que a D14-iii acabou de promover de "cache frio" a BURACO NO LEDGER —,
(lacuna 2) o teste vermelho do `claimed_via`, ou (Console/Transfer) a inconformidade herdada que o
operador relatou.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Uma pendência declarada por quem parou de
  trabalhar é a última coisa que ele lembra, não o estado do repo. Já se pagou NOVE sessões
  seguidas. **A ORDEM proposta também se confere.**
· **MEÇA AS DUAS BASELINES ANTES DE MUDAR QUALQUER COISA.** Dois comandos, e são baratos:
    $DC exec -T routing-engine sh -c "cd /app/packages/routing-engine && python3 -m pytest src/plughub_routing/tests -q 2>&1 | tail -3"
    $DC exec -T analytics-api sh -c "cd /app/packages/analytics-api && python3 -m pytest src/plughub_analytics_api/tests -q 2>&1 | tail -3"
  Esperado HOJE: **1 failed, 284 passed** (o vermelho é o do Backlog 3, conhecido) e **607 passed**
  (era 595; a D14-iii somou 12).
· **O `TODO.md` § "SLA está no grão errado" foi ATUALIZADO no fim da sessão passada** ((iii) ✅, arco
  completo). Está mais atual que qualquer resumo — inclusive que este.
· ⚠️ **O AMBIENTE SEGUE MEXIDO, e continua sendo bom que siga.** `retencao_humano` tem
  `queue_config.pool_id = fila_humano` (fila ATENDIDA), `max_wait_s: 1800`, `sla_target_ms: 300000`.
  Não reverta. Confira:
    python3 infra/test/q_queue_config_inventory.py
  Esperado: 36 pools · none 35 · legacy_only 0 · **addressable 1**. Nunca ao YAML (seed-if-absent).

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços; e confira que a
  linha colada não veio DUPLICADA (o erro se lê como erro de sintaxe do código, não da colagem)
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **`$DC exec` no clickhouse EXIGE `-T`** — o serviço tem `tty: true` e o `< /dev/null` colide
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **arquivo NOVO exige `build --no-cache`** (não vale p/ script em `infra/test/`, que roda do host)
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **PREFLIGHT DE SÍMBOLO antes de medir**, e ESTRUTURAL: `inspect.getsource` da função CARREGADA,
  nunca `grep` (que conta o COMENTÁRIO que documenta a remoção)
· **`$DC exec` NÃO começa em `/app`** — caminho ABSOLUTO no pytest
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· CH 23.8 **não faz subquery correlacionada** — use JOIN; prefira joins binários ANINHADOS
· CH é alcançável do HOST em `http://localhost:8123`, user/pass `plughub` — probe em Python puro
  (`q_sla_source_delta.py`) sai mais barato que imprimir comando `docker exec`
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 · auth-api 3202
  · agent-assist-ui (app LEGADO, vivo) 5173

═══ O QUE ESTA SESSÃO COMPROU ═══
· **PREDICADO DE RELATÓRIO SE COPIA, NUNCA SE REESCREVE DE MEMÓRIA.** O probe do delta reescreveu o
  `_sla_eligible` e excluiu a espera ABANDONADA do denominador — o predicado real não a exclui (só o
  `within_sla`). Saiu `retencao_humano` a **95,7%** enquanto a tela mostra **0,6364**. O instrumento
  era falseável, ramificado, com testemunha de presença, e mediu a **proposição adjacente**. O tell
  que o pegou: o número não batia com um valor JÁ MEDIDO em sessão anterior. Sem esse valor de
  referência, o delta errado teria ido para o CHANGELOG.
· **UM GATE QUE NÃO CONTÉM A POPULAÇÃO DISCRIMINANTE NÃO JULGA NADA.** `discord = 0` no ambiente
  (nenhuma espera com dois alvos diferentes) ⇒ qualquer teste de "as duas fontes concordam" passaria
  idêntico sobre o código velho. O gate teve de **INSERIR** o caso. Irmão do `0` no
  `test_sla_target_predicate` — e agora com dois precedentes, é regra, não coincidência.
· **ASSERÇÃO SOBRE SQL SE FAZ NO SQL EXECUTADO, NÃO NO FONTE.** `grep`/`inspect.getsource` contam o
  comentário que documenta a migração. Rodar a função com mock e ler `client.query.call_args_list`
  entrega o artefato executado. **Normalize o espaço em branco** — sem isso a asserção depende do
  ALINHAMENTO do SQL e reprova no dia em que alguém rodar um formatador (custou 1 vermelho).
· **CHAME A FUNÇÃO SÍNCRONA INTERNA, NUNCA O WRAPPER `async`.** O de `query_pools_queue` tem
  `except → {"error": "data_unavailable"}`: por ele, teste quebrado devolve envelope vazio e
  "não há dado" fica indistinguível de "a query falhou".
· **NÃO CARREGUE NÚMERO DE DOCUMENTO PARA DENTRO DA PREVISÃO** (herdado, aplicado de novo): medi o
  `pool_config`/o delta ANTES de escrever cada previsão.

═══ ERROS DE PREVISÃO (11 acertos, 1 erro) ═══
Acertos: baselines **1 failed/284** e **595** · `alvo_seg=1` · `discord=0` · denominador despenca ·
`retencao_humano` **64,7%** na re-rodada · total de testes novos **12 (607)** · gate v1/v2/v3 os três
exatos · mutação **1 failed, 606** COM o teste nomeado · gate vermelho em `pool_b` **1/1** · e o
colateral previsto de que a mutação zeraria o `sla_unstamped`.
❌ **ERRO:** previ `elig_sess` total ~48 e aderência ~75%; vieram **51** e **70,6%**. Causa única —
subestimei quantas abandonadas carregam alvo (eram 14 de 24). **Duas grandezas erradas, uma causa
só**: quando dois números erram juntos, procure a causa compartilhada antes de tratá-los como dois
enganos.

═══ ENTREGUE — não re-fazer ═══
**(D14 iii)** — os três leitores passaram ao alvo do SEGMENTO. `sla_source.py` (época única,
`SEGMENT_SLA_EPOCH = "2026-08-25 00:52:29"` — instante MEDIDO da 1ª espera carimbada, nunca `now()`
nem data de deploy). `query.py:get_pool_sla_1h` largou `sessions` inteira (⇒ `pool_id` deixa de ser o
pool de ENTRADA, D10; `avg`/`p95` se movem junto). `_cv_sla_series` migrado com o **eixo preservado**
em `sessions.opened_at` (paga um JOIN: o overlay tem de alinhar com os sinais de survey) e sem o
`coalesce(wait_time_ms,0)` que dava 100% a pool sem espera. `_per_wait` toma o alvo de
`w.w_sla_target`; `_sla_eligible` ganhou a época; **`sla_unstamped` novo** no `by_pool`.
Gates: `test_sla_reads_the_segment.py` (12) + `infra/test/gate_sla_segment_target.sh` (insere a
população discriminante, 3 veredictos) + `infra/test/q_sla_source_delta.py` (probe de delta).
🟢 Mutação (`w.w_sla_target` → `ss.sla_target_ms`) reprovou os DOIS lados com os nomes previstos.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
suíte routing-engine: **1 failed, 284 passed** · suíte analytics-api: **607 passed**
delta da (iii) no tenant demo: **72 esperas · 51 elegíveis a 70,6% → 1 elegível**
  `retencao_humano` **34 elegíveis a 64,7%** → 1 · `discord = 0` · `alvo_seg = 1`
`pool_config` de `retencao_humano`: `sla_target_ms=300000`, `queue_config.pool_id=fila_humano`,
  `max_wait_s=1800`, **TTL ~3 593 s** (⇒ o 86 400 do routing-engine segue morto)

═══ AS OPÇÕES DE TAREFA ═══
**(TTL de `pool_config`)** — **subiu de prioridade por consequência medida, não por opinião.** Dois
  serviços escrevem a MESMA chave com TTLs diferentes e o último vence:
  `routing-engine/config.py:52` **86 400** × `orchestrator-bridge/session_config.py:29` **3 600**. O
  bridge reconcilia a cada 5 min ⇒ o 24 h está morto. Agrava: `config-api/seed.py:288` afirma em
  prosa que são caches SEPARADOS (não são), `registry.py:13` documenta "TTL 24h", e o
  `changelog-2026-04-16` documenta o conserto 300 s→86 400 que o bridge desfez em silêncio.
  ⚠️ **A D14 mudou o que está em jogo:** a expiração agora produz **segmento de espera SEM ALVO**, que
  é buraco no LEDGER e **não se corrige por deploy**. A (iii) deu o instrumento —
  `by_pool[].sla_unstamped` conta exatamente essa população. **Comece medindo o `sla_unstamped` em
  tráfego real antes de mexer:** se vier 0, o defeito é latente e vale declará-lo; se vier > 0, o
  número é o argumento. ⚠️ Não confunda exposição com dano (lição da D14.1).
**(lacuna 2)** o teste vermelho do `claimed_via` — ver Backlog 3. Não é trocar a string.
**(Console/Transfer)** a inconformidade herdada abaixo — é a única com operador esperando.

═══ NÃO fazer agora ═══
· **"Consertar" o vermelho do `claimed_via` trocando `"semaphore"` por `"record"`.** Passa o teste e
  apaga a pergunta. Ver Backlog 3.
· **Mexer na época `SEGMENT_SLA_EPOCH` para "recuperar" a série histórica.** Um valor anterior ao
  medido joga esperas sem alvo do lado "defeito" do corte e fabrica `sla_unstamped`. A série antiga
  não volta — o `first_queued_ms` foi consumido na saída da fila.
· **Migrar as esperas antigas para carimbar o alvo retroativamente.** Impossível por construção.
· **Ramificar o carimbo por `agent_kind`** — decidido e gravado em teste. Espera é espera.
· **"Consertar" a multiplicidade de segmentos por F5** — REFUTADO (Mudança 39).
· **O `queue-agent` do bridge** (`main.py:5993`, `uuid5` sem discriminador de passagem). Inalcançável
  hoje: exige DOIS pools com fila atendida e só um tem endereço.
· **F4 (visão 2 do histórico unificado)** — classe *outbound* segue com zero amostras.

═══ Backlog NOVO / reaberto ═══
· 🔴 **3. `test_expire_returns_the_slot_even_after_the_lease_expired` VERMELHO, e é ANTERIOR a três
  sessões atrás** (medido: `router.py` não foi tocado). Asserta `claimed_via == "semaphore"`; o código
  devolve `"record"`, porque a emenda **D6** (2026-08-04) inseriu o registro durável de posse ANTES do
  semáforo (`router.py:1151`, e `:1157` é o ramo do semáforo). ⚠️ **Não é cosmético:**
  `router.py:1158` declara que a linha do `semaphore` é *"a MEDIDA da lacuna 2"* — com o `record`
  respondendo antes, **esse ramo ficou inalcançável neste cenário**, e o instrumento da lacuna 2 parou
  de ser exercido sem que nada além deste vermelho o dissesse. **Consertar = decidir onde vive a
  medida da lacuna 2.**
· 🆕 **`_apply_pool_scope` tem `column=` desde a D14-iii** (default preservado). ⚠️ E, ao usá-lo,
  notei que **`_cv_sla_series` IGNORA o retorno** da função — com `accessible_pools=[]` ela não
  filtra nada e a query roda irrestrita. **NÃO consertado de propósito**, porque não medi se `[]`
  chega ali: o JWT usa `[]` com o sentido OPOSTO ("todos os pools"), então a tradução pode nunca
  produzir o caso. **Medir antes de mexer** — é ABAC, e um "conserto" que esvazia relatório de quem
  tem acesso é pior que o buraco.
· 🆕 **`sla_unstamped` existe na API e NÃO tem superfície na UI.** Contador nasce hoje sem leitor;
  enquanto ninguém o exibir, o buraco do TTL é visível só por `curl`.
· 🆕 **`estimated_wait_factor` (0.7) e `congestion_sla_factor` (1.5) são o 2º e 3º órfão do namespace
  `routing`** — semeados, cacheados, testados, **sem leitor**: `main.py` fixa `* 0.7` e
  `saturated.py` fixa `* 1.5`. Com o `sla_default_ms` já conhecido, são **três** botões que prometem
  efeito e não têm.
· 🔴 **`sla_default_ms` NÃO TEM LEITOR** — ⚠️ remover exige mexer no
  `smoke_config_routing_orphan_keys.sh:118`, que a usa como **CANÁRIO** de namespace intacto.
· 🆕 **`max_queue_len` é declarado no front e NUNCA lido** (`AnalisePoolsPage.tsx:55`).
· 🆕 **`query_pools_queue` engole a exceção** e devolve `data: []` + `error`, indistinguível de "não
  há dado" na tela. O gate novo o pega; a tela, não.
· **3 cópias restantes do `480_000`**: `instance_bootstrap.py:709` · `mcp-server/tools/bpm.ts:281` ·
  `config-api/seed.py:158`.
· **`supervisor.ts` — `urgency` compara ATENDIMENTO TOTAL com alvo de ESPERA.** Não mede nada. É
  contrato de tool MCP com consumidores não mapeados — **mapeá-los é a fatia**.
· ⚠️ **`duration_ms` do segmento humano diverge da janela dos próprios carimbos** (26 448 ms/80 s;
  25 519 ms/60 s). **Sem causa** — a explicação "sai do último F5" caiu com a Mudança 39.
· **`sac_ia` tem 2 esperas de 0 ms** (Mudança 37, consta corrigida). Ou é dado pré-correção, ou a
  correção não pegou tudo.
· **Exibir tempo de contato / espera-antes-do-agente no Console** — levantado pelo operador, SEM
  decisão. São 3 fatos distintos (segmento · contato · espera) e a tela mostra 1.
· `admission.py:267` apaga `first_queued` sem emitir, p/ sessões fechadas no buffer MUDO — 7º ponto
  de saída, fora dos 6 call sites. A fila ATENDIDA não tem backstop equivalente.
· **`_emit_queue_timeout` publica em outbound e `conversations.events` SEM `key=`.** Tópicos
  diferentes da fatia B, dívida à parte. O `_TimeoutProducer` do teste tolera e DECLARA a tolerância.

═══ INCONFORMIDADE HERDADA, ainda aberta ═══
**Console não libera a tela após `Transfer`** (relatada 2026-08-24). Diagnóstico JÁ FECHADO por
refutação — não repetir: **não é wrap-up e não é config de dispatch** (o pool está medido em
`detached` **no agent-registry**, não no YAML) ⇒ **teardown ausente no caminho do `Transfer`**. Abre
por: `Transfer` e `Close` compartilham o teardown do Console, ou são dois caminhos? ⚠️ Conserto
provavelmente em DOIS apps (o `agent-assist-ui` vive na 5173). ⚠️ Não testado: transfer com o cliente
ainda CONECTADO. Registro em `TODO.md` § *Analytics e UI*.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de ONZE passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduos do defeito 2: linhas órfãs `queue_default_*` no config store;
  `queue_config.skill_id` legado do `retencao_humano`
```
