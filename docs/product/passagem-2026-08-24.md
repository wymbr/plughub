# Prompt de passagem — sessão de 2026-08-24

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a de 2026-08-23, que fechou
> as **duas metades de engenharia da tarefa 1** (health honesto de credencial) e o **desenho inteiro
> da tarefa 2** (medição de sentimento), e que encontrou **quatro defeitos que ninguém procurava** —
> todos da mesma família: valor plausível vindo de lugar nenhum. A passagem
> [`passagem-2026-08-23.md`](passagem-2026-08-23.md) está **encerrada**.

---

```
continuação sessão plughub.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· arquivo NOVO entra no build normal quando o Dockerfile copia o DIRETÓRIO
  (`COPY packages/<pkg> ./packages/<pkg>`); `--no-cache` só é preciso quando o
  Dockerfile lista arquivos
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· **janela de log só compara se as DUAS janelas existirem**
· **`--since` do docker sem fuso é HORA LOCAL** — o host é −03
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso**
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3; usar `$DC exec -T redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **nada de crase dentro de aspas duplas em bash** — vira substituição de comando
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server + skill-flow-worker JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · mailing-api 3660 · **redis 6380 · kafka 9093 · postgres 5433**
· health da analytics-api é **`/v1/health`** (o `/health` sem prefixo dá 404)
· analytics-api usa **PyJWT** (`import jwt`); auth-api usa **python-jose**
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ AS CINCO REGRAS QUE 08-23 COMPROU (três por erro próprio) ═══
· **GREP TRUNCADO LIDO COMO AUSÊNCIA.** Duas afirmações foram anunciadas como
  achados e as duas eram FALSAS: *"`mark_throttled` não tem chamador"* e
  *"`/inference` não alimenta `record_usage`"*. Ambas vieram de um resultado com
  `[Showing results with pagination]` tratado como conjunto completo. O código real
  chama as duas coisas (`inference.py:304`, `:288`/`:324`/`:363`). Pior: a
  afirmação falsa chegou a ser PUBLICADA numa nota do `/v1/health` — superfície de
  observabilidade é o pior lugar possível para um valor plausível e errado.
  **Antes de dizer "não existe", confira se a busca terminou.**
· **MOCK MAIS PERMISSIVO QUE O CONTRATO CODIFICA O BUG.** `AsyncMock` aceita
  qualquer assinatura e `MagicMock` fabrica qualquer atributo. A fixture do
  `copilot_emitter` devolvia `MagicMock` com `.text` — e o módulo chamava
  `provider.call(..., system=...)`, kwarg que `LLMProvider.call()` não tem, e lia
  `.text`, que `LLMResponse` não expõe. **Dois defeitos, cada um suficiente para
  silêncio total, e a suíte verde.** Fixture usa a classe REAL; asserte a FORMA da
  chamada (`assert "system" not in kwargs`), não só que ela aconteceu.
  *Corolário do mesmo dia:* mock infiel MAIS ESTRITO revela — foi a fixture em
  `bytes` (contra um cliente que usa `decode_responses=True`) que expôs
  `str(bytes)` corrompendo em silêncio. Permissivo esconde, estrito revela.
· **DEPENDÊNCIA TRANSITIVA NÃO DECLARADA É ESTADO HERDADO.** `httpx` era import de
  runtime (`llm_accounts_catalog.py:45`, alcançado por `main.py:40`) declarado
  APENAS em `optional-dependencies.dev`. O container subia porque `anthropic` o
  trazia transitivamente e a camada `RUN pip install` estava em cache. Qualquer
  edição no pacote invalida o `COPY`, o pip resolve de novo, e o serviço morre num
  import que nunca mudou. O rebuild foi o INSTRUMENTO, não a causa.
· **`src` PRESENTE ≠ `dist` EM EXECUÇÃO.** Container exibindo digest sha256 no lugar
  da tag, com `CREATED` mais antigo que o build, é a assinatura de imagem anterior.
  O discriminador barato é perguntar ao COMPILADO:
      $DC exec -T <svc> sh -c "grep -c '<trecho>' /app/.../dist/index.js" < /dev/null
  Ler o `src` dentro do container dá falso verde: ele é copiado, mas quem roda é o
  `dist`. A forma mais enganosa de tudo é `src` certo ao lado de `dist` velho.
· **O HEALTHCHECK DO COMPOSE TEM LATÊNCIA PRÓPRIA.** `docker ps` logo após o `up -d`
  mostra `(health: starting)`, que NÃO é veredicto. O tempo até `unhealthy` é
  `start_period + retries × interval` — no ai-gateway, `20 + 10×10 ≈ 2 min`. Julgue
  pelo `curl` (que responde o veredicto final na hora) e use o `docker ps` só para
  confirmar que o código HTTP chegou ao orquestrador.

═══ ⛔ BLOQUEIO DE AMBIENTE — CONTINUA, MAS AGORA SE ANUNCIA ═══
**A credencial de LLM deste demo segue RECUSADA** (`status_401`, "API key is
invalid"), e portanto todo step `reason` de todo skill continua caindo no
`on_failure` — ramo legítimo de fluxo, logo sem alarme no fluxo. Qualquer conclusão
tirada deste ambiente sobre avaliação, copilot, contexto ou sentimento está medindo
o ramo de erro.

**O que mudou:** isso deixou de exigir arqueologia de log.
    curl -s -o /dev/null -w '%{http_code}' localhost:3200/v1/health   → 503
    $DC ps ai-gateway                                                 → unhealthy
    bash infra/test/probe_llm_credential_health.sh                    → nomeia a causa
O corpo do health traz `credentials: invalid` por conta, `last_error_code`,
`throttled`, e contadores com janela declarada (`calls_ok` como testemunha ao lado
de `errors`). Chave em `docker-compose.demo.yml:702`.

═══ ESTADO (fechado em 2026-08-23, ver CHANGELOG) ═══
HEALTH DE CREDENCIAL: desfecho de cada chamada gravado no funil único de erro
  (`provider_error_handler`) + sonda de boot (1 chamada por conta por start,
  `PLUGHUB_LLM_BOOT_PROBE`). `unknown` NUNCA vira `ok`; `rate_limit` nunca vira
  `invalid`; chave ausente não reprova, chave presente e recusada reprova (503).
  Credencial e DISPONIBILIDADE são fatos separados — todas as contas throttled ⇒
  `degraded`, nunca `ok` (seria verde sobre capacidade zero).
  Gate `probe_llm_credential_health.sh` + `TestCredentialSummary` (8 casos).
SENTIMENTO: a plataforma passou a MEDIR. `ReasonStepSchema.customer_utterance`
  (ref `$.`/`@ctx.`, literal RECUSADO) → `ReasonRequest` → `sentiment_analyzer.py`
  (haiku, fora do turno) → os três emissores que já existiam.
  `sentiment_score: float | None`, com **None = não medido** e pipeline pulado.
  `tenant_id` passou a viajar nos dois chamadores TS.
COPILOT: dois bugs corrigidos (`system=` inexistente; `.text` por `.content`) — o
  módulo nunca produziu uma análise até aqui. **Não validado em runtime** (a
  credencial mascara).
httpx promovido a dependência de runtime do ai-gateway.
157 testes verdes no ai-gateway.

═══ TAREFA — escolher, não fazer tudo ═══

1. **Credencial de LLM** *(bloqueia 2, 3 e toda medição de IA)*. Só isso: repor a
   chave. A metade de engenharia está feita — quando a chave válida entrar, o
   probe deve virar `ok`/200 com `calls_ok ≥ 1`, e é a PRIMEIRA vez que essa
   transição será observável.

2. **Declarar `customer_utterance` num skill e medir de ponta a ponta**
   *(depende de 1)*. Nenhum skill declara o campo, então nada é medido ainda —
   ausência honesta, não regressão. Candidato natural: `agente_fila_v1`, o único
   conversacional. Verificar a cadeia inteira: ref resolvida → chega ao gateway →
   haiku responde → três emissores → `session.sentimento.current` com tenant
   CORRETO. Gate: `infra/test/probe_sentiment_producer.sh` deve sair de
   INCONCLUSIVO.

3. **Validar o copilot em runtime** *(depende de 1)*. Os dois bugs foram achados
   por LEITURA e corrigidos; nenhuma análise foi produzida ainda neste ambiente.
   Enquanto não rodar de verdade, "corrigido" é hipótese.

4. **`session:{id}:meta` fatias B e C** *(não depende de credencial)*. B = recusar
   campo de outro dono (hoje o helper só LOGA `[session_meta] DONO VIOLADO`);
   exige os quatro escritores de canal passando pelo helper, incluindo um irmão em
   TypeScript (`bpm.ts`). C = `pool_id` carrega DOIS fatos — pool de ENTRADA
   (`webchat.py:210`, `webrtc.py:484`) × pool que ATENDE (bridge; acepção que
   `webhook.py:1330` lê no resume); separar em `entry_pool_id`.
   › *Suspeita NÃO medida:* `session:{id}:wf_agent` nasce com o mesmo `_stl()` = 4h
     ao lado do site W6 e descreve a mesma workflow de até 48 h. Medir antes.

5. **Gate re-executável da Camada F** (arco Detach). Validada por medição manual
   instrumentada; sem script versionado, arco completo é lembrança. A própria F4
   declara que a **lease** não foi medida (não há reaper; o transbordo é por idade).
   O dono do ambiente confirmou que o wrap-up detached está funcionando.

6. **`mcp_audit_log`** *(dormente POR MEDIÇÃO)*. Ordem correta: (1) exercitar
   `invoke` de verdade e confirmar linha em `session_timeline`; (2) só então DDL +
   dual-write. **Não criar a tabela antes** — vazia com cara de pronta é
   indistinguível de "ninguém acessou". Resíduo junto: `invoke` emite
   `session_id: ""` quando a instância tem 0 ou 2+ sessões ativas
   (`external-agent.ts:194-215`) e o parser DESCARTA evento sem `session_id`
   (`models.py:713-718`).

7. Reexame item a item dos 9 em `Escopo reduzido` (§4.5 do doc de reversão) —
   rejulgar pelo mérito, NÃO reverter em bloco. **Não tocado desde 08-17.**

8. Provisionar o SFU (LiveKit) — pré-requisito de qualquer trabalho de WebRTC.

═══ RESÍDUOS MENORES (sem dono, nenhum é bloqueio) ═══
· **`calls_ok` SUBCONTA**: `record_usage` só roda quando o AccountSelector escolheu
  uma CONTA (`inference.py:287`, `reason.py:136`). Chamada que cai no alias legado
  do provedor tem sucesso sem incrementar. O health já diz isso quando o contador
  está em zero
· **`extract_context_from_response` (`context.py`) é heurística de palavras-chave em
  PT** e ainda alimenta `intent`/`confidence`/`flags` do `/inference`. Como
  `/inference` não tem chamador, é dívida dormente — mas o comentário do arquivo
  promete "model fine-tuned per vertical", que é promessa sem produtor
· **pausa de IA é desfeita em 15 s**: o `agent_ready` da reconciliação do bootstrap
  republica `ready` para instância com pausa declarada. Conserto no
  `instance_bootstrap`
· **`bpm.test.ts` vermelho por dívida conhecida**: `conversation_escalate` agora
  RECUSA sem `session:{id}:meta` e o teste não semeia o meta. Suíte vermelha por
  dívida aceita deixa de sinalizar regressão nova
· **`npx vitest` no host valida `@plughub/schemas` de 2026-08-02** — o perigo é o
  simétrico: mudança de schema ficar VERDE porque o `dist/` velho aceita a forma
  antiga
· `queue_config` só existe em 1 pool do demo (`retencao_humano`) — as duas metades
  do gate da fila não cabem na MESMA janela sem mexer em config
· o e2e 07 tem uma asserção que **não pode reprovar** (`pass(...)` incondicional
  sobre o retorno do `queue_context_get`, `07_inbound_full.ts:133-137`)
· `session_timeline` está VAZIO e recebe linha de um único parser (o de `mcp.audit`)
  — ou seja `/v1/audit/mcp-calls` sempre devolve lista vazia neste ambiente
```
