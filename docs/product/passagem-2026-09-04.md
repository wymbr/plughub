# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-09-03.md`](passagem-2026-09-03.md), que está **encerrada**.
>
> A sessão anterior entregou TRÊS fatias: **(B)** emissor unificado, **(D14.1 passo 4)** medido e
> decidido, **(D14-i)** entregue com gate verde. Sobrou a **D14 (ii)/(iii)** e um backlog nomeado.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes, mas **todo dado medido carrega carimbo
> `2026-08-24`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: escolher entre (D14-ii) mover `sla_target_ms` para o segmento de
espera — agora com EVIDÊNCIA NUMÉRICA —, (divergência do `0`) um predicado único para os dois
escritores do campo, ou (falseabilidade) fechar a dívida do gate da fatia B.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Uma pendência declarada por quem parou de
  trabalhar é a última coisa que ele lembra, não o estado do repo. Já se pagou SEIS sessões
  seguidas. **A ORDEM proposta também se confere.**
· **O `TODO.md` § "SLA está no grão errado" foi ATUALIZADO no fim da sessão passada** (D14-i ✅,
  D14.1 passo 4 decidido em (b), D14-ii com a evidência dos "10 sem alvo"). Está mais atual que
  qualquer resumo — inclusive que este.
· ⚠️ **O AMBIENTE SEGUE MEXIDO, e continua sendo bom que siga.** `retencao_humano` tem
  `queue_config.pool_id = fila_humano` (fila ATENDIDA) e `max_wait_s: 1800`. Não reverta. Confira:
    python3 infra/test/q_queue_config_inventory.py
  Esperado: 36 pools · none 35 · legacy_only 0 · **addressable 1**. Nunca ao YAML (seed-if-absent).

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· 🆕 **`$DC exec` no clickhouse EXIGE `-T`** — o serviço tem `tty: true` e o `< /dev/null` colide
  ("cannot attach stdin to a TTY-enabled container"). `$DC exec -T clickhouse …`
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **arquivo NOVO exige `build --no-cache`** (não vale p/ script em `infra/test/`, que roda do host)
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **`up -d` recria o container e leva junto o `pip install`** — reinstalar pytest antes de rodar gate
· **no frontend há DOIS caches** — depois do build, `Ctrl+Shift+R` é obrigatório
· **PREFLIGHT DE SÍMBOLO antes de medir** — e prefira checagem ESTRUTURAL (AST, aridade, valor) a
  busca textual: `grep` conta o COMENTÁRIO que documenta a remoção
· **`$DC exec` NÃO começa em `/app`** — caminho ABSOLUTO no pytest
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· CH 23.8 **não faz subquery correlacionada** — use JOIN; e prefira joins binários ANINHADOS a
  multi-join encadeado (o aninhado passou; o encadeado é risco)
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 · auth-api 3202
  · agent-assist-ui (app LEGADO, vivo) 5173

═══ O QUE ESTA SESSÃO COMPROU ═══
· **UM AVISO PODE EXPIRAR SEM QUE NINGUÉM O REVOGUE.** Dois docstrings e o TODO diziam que unificar
  o emissor daria "emissão dupla". Era verdade quando foi escrito e falso quando foi lido: a própria
  D12 reclassificou o segmento do agente de fila para `role='specialist'` e o aviso sobreviveu à
  mudança que o tornou falso. **Comentário que descreve estado anterior é da família do DDL que
  promete invariante sem produtor.** Meça o aviso antes de obedecê-lo.
· **"NÃO EXPRESSÁVEL" É UMA RESPOSTA VÁLIDA, E ÀS VEZES É A RESPOSTA.** O passo 4 queria escrever
  `null` em 34 pools. Medindo: `PoolConfig.sla_target_ms` é `int`, não `int | None`, e o listener
  converte ausência em fallback ANTES do modelo — não existe "ramo sem alvo". A opção do plano não
  era cara, era **impossível**.
· **CONTAR DIREITO REVELA DEFEITO DE GRAÇA.** A D14-i só queria parar de colapsar. Ao contar,
  apareceram três ausências que viravam cumprimento de SLA — e a decomposição "48 = 5 abertas + 10
  SEM ALVO + 33" entregou, como subproduto, a evidência numérica da D14-ii.
· **VALOR PLAUSÍVEL TEM CARA DE POSIÇÃO DEFENSÁVEL.** Escrevi no código que contar não-espera como
  aderente era "defensável". Duas horas depois, `limite_entrega` com 37 contatos, ZERO esperas e
  **100% de aderência** tornou a posição insustentável. A diferença entre as duas horas foi o
  número.

═══ ERROS DE PREVISÃO (5 acertos, 4 erros) ═══
Acertos: sessões com n≥2 (**8**) · Σ waits = **71** · `retencao_humano` 48/39 · decomposição
**5 · 10 · 48** (os três) · gate VERDE.
Erros: `close_reason='max_wait_exceeded'` previ 0, medido **1** · sessões `max_wait` previ ≥2, medido
**1** · discordantes previ 4–6, medido **3** · `sla_eligible` do `retencao_humano` previ 43, medido
**33**.
⚠️ **O erro que importa é o último**, e a causa não foi falta de dado: eu tinha ACABADO de escrever um
predicado de TRÊS condições (`q_count>0` ∧ `wait_ms IS NOT NULL` ∧ `sla_target_ms > 0`) e previ usando
DUAS. Ao prever, releia o predicado que você escreveu.
Viés observado: os três primeiros erros são **para cima** — superestimei a frequência de timeout e de
discordância desta população.

═══ ENTREGUE — não re-fazer ═══
**(B) emissor unificado do `max_wait_exceeded`** — `resolve_queue_exit` ganhou `close_reason` e o ramo
`else` do `_emit_queue_timeout` saiu; com ele, `uuid4()`, publish sem `key=` e a cobertura de um tier
só. Gate: 4 testes novos em `test_queue_wait_segment.py` (**14 passed**). Registro: `CHANGELOG.md` ·
`TODO.md` · `conference-mechanics.md` § **Mudança 40**.

**(D14.1 passo 4) DECIDIDO (b)** pelo dono: não anula nada, muda quem LÊ. Absorvido pela D14-i.

**(D14-i) relatório para de colapsar** — `_per_session` → `_per_wait`. `contacts`/`queued` em SESSÕES
(significado preservado), `waits` nova + `abandoned`/`handoff`/`avg`/`p95` em PASSAGENS. Três exclusões
de aderência: só espera **concluída, com alvo e não-abandonada** é julgável. Gate:
`infra/test/gate_queue_report_per_wait.sh` (**VERDE**). Registro: `CHANGELOG.md` · `TODO.md`.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
ledger: **71** segmentos `role='queue'` · **59** sessões · 10 pools
  ⇒ **12 esperas descartadas** pelo `maxIf` (17%)
sessões com 2+ esperas: **8** (6×2 · 1×3 · **1×5**); com pool/desfecho DIVERGENTE: **3**
  (`049167a2` outcome · `dce98532` pool · `27651d1b` ambos)
  ⚠️ a de 5 esperas é `18232569-…`, a mesma da hipótese de duplicação por F5 REFUTADA (Mudança 39):
  5 esperas = 5 quedas reais. O "conserto" daquela hipótese teria apagado 4 esperas legítimas
`retencao_humano`: **48 esperas = 5 abertas + 10 SEM ALVO + 33 julgáveis**; within 21 ⇒ **0,6364**
  (era 0,913) · 20 das 48 abandonadas
aderência DEPOIS: `sac_ia` 1,0 (2 esperas de **0 ms** — fantasmas da Mudança 37) · `formfill_demo_ia`
  1,0 (7/9) · `limite_processo` 1,0 (4/5) · `wrapup_detached_ia` 1,0 · `especialista_onboarding`
  **0,0** (2/2 abandonadas) · `retencao_humano-int` **0,0** · `limite_ia`/`aprovacao_credito`/`nps_ia`
  **null** (espera medida, sessão sem alvo)
`max_wait_exceeded` no ledger: **1** (`retencao_humano`, 1 802 441 ms = teto de 1800 s, 2026-08-18,
  tier MUDO) ⇒ exposição prospectiva ALTA, dano histórico **ZERO**

═══ AS OPÇÕES DE TAREFA ═══
**(D14-ii) `sla_target_ms` no SEGMENTO de espera.** Desbloqueada pela (i) e agora com número:
  **10 das 43 esperas concluídas** do `retencao_humano` são injulgáveis porque a SESSÃO não tem alvo
  — enquanto o POOL tem (300 000 ms) e a espera aconteceu nele.
  ⏳ **Decidir antes de codar** (já estava no TODO): alvo **copiado** para o segmento no fechamento da
  espera (denormalização, guarda "o alvo do dia") × **resolvido na leitura** pelo pool do segmento
  (não duplica, mas re-lê config de hoje para medir ontem).
  ⚠️ `sessions.sla_target_ms` vira **projeção**, nunca fonte de cálculo.
  ⚠️ (iii) migrar os três leitores (`query.py:240` · `reports_query.py:3803` · o novo `_sla_eligible`)
  é fatia PRÓPRIA — não entra junto.
**(divergência do `0`)** os dois escritores do `sla_target_ms` discordam: `kafka_listener.py:231` usa
  `is not None` (**preserva** 0), `registry.py:3149` usa `int(_sla_src or FALLBACK)` (**truthiness**
  ⇒ 0 → 480 000). Com `0` configurado, o Monitor mostra 8 min enquanto o motor trata como prioridade
  infinita. **Terceira ocorrência** da família `??`×truthiness (Mudanças 35 e 37). Conserto: UM
  predicado, compartilhado. ⚠️ Hoje ninguém escreve `0` — é portão fechado, não defeito ativo.
**(falseabilidade da fatia B)** o teste do tier ATENDIDO nunca foi visto vermelho **pelo motivo
  certo** — reprovou por defeito do harness. Custa `git stash` no `main.py` + build + rodar + pop.

═══ NÃO fazer agora ═══
· **Anular `sla_target_ms` em pool nenhum** — decidido (b): não se anula, muda quem lê. E `null` viraria
  480 000 silenciosamente; `0` viraria prioridade infinita + ETA **0 ms ao cliente** (`main.py:1075`).
· **"Consertar" a multiplicidade de segmentos por F5** — REFUTADO (Mudança 39). O discriminador de
  duplicação é SEGMENTO ÓRFÃO (`duration_ms IS NULL` > 1 por sessão viva), **nunca a contagem**.
· **O `queue-agent` do bridge** (`main.py:5993`, `uuid5` sem discriminador de passagem). Inalcançável
  hoje: exige DOIS pools com fila atendida e só um tem endereço. O discriminador dele **não pode ser
  wall-clock**, ou a idempotência morre.
· **F4 (visão 2 do histórico unificado)** — classe *outbound* segue com zero amostras.

═══ Backlog NOVO / reaberto ═══
· 🔴 **`sla_default_ms` NÃO TEM LEITOR** — semeada (`config-api/seed.py:157`), cacheada, emitida,
  testada e **exibida ao operador**. Editar não muda nada. ⚠️ Remover exige mexer no
  `smoke_config_routing_orphan_keys.sh:118`, que a usa como **CANÁRIO** de namespace intacto.
· 🆕 **`max_queue_len` é declarado no front e NUNCA lido** (`AnalisePoolsPage.tsx:55`). Mesma família.
· 🆕 **`query_pools_queue` engole a exceção** e devolve `data: []` + `error`, que na tela é
  indistinguível de "não há dado". O gate novo o pega; a tela, não.
· **3 cópias restantes do `480_000`**: `instance_bootstrap.py:709` · `mcp-server/tools/bpm.ts:281` ·
  `config-api/seed.py:158`.
· **`supervisor.ts` — `urgency` compara ATENDIMENTO TOTAL com alvo de ESPERA.** Não mede nada. É
  contrato de tool MCP com consumidores não mapeados — **mapeá-los é a fatia**.
· ⚠️ **`duration_ms` do segmento humano diverge da janela dos próprios carimbos** (26 448 ms/80 s;
  25 519 ms/60 s). **Sem causa** — a explicação "sai do último F5" caiu com a Mudança 39.
· **`sac_ia` tem 2 esperas de 0 ms** (Mudança 37, consta corrigida). Confirmadas de novo em 08-24 pelo
  `avg_wait_ms: 0.0`. Ou é dado pré-correção, ou a correção não pegou tudo.
· **Exibir tempo de contato / espera-antes-do-agente no Console** — levantado pelo operador, SEM
  decisão. São 3 fatos distintos (segmento · contato · espera) e a tela mostra 1.
· `admission.py:267` apaga `first_queued` sem emitir, p/ sessões fechadas no buffer MUDO — 7º ponto
  de saída, fora dos 6 call sites. A fila ATENDIDA não tem backstop equivalente.
· 🆕 **`_emit_queue_timeout` publica em outbound e `conversations.events` SEM `key=`.** Tópicos
  diferentes da fatia B, dívida à parte. O `_TimeoutProducer` do teste tolera e DECLARA a tolerância.

═══ INCONFORMIDADE HERDADA, ainda aberta ═══
**Console não libera a tela após `Transfer`** (relatada 2026-08-24). Diagnóstico JÁ FECHADO por
refutação — não repetir: **não é wrap-up e não é config de dispatch** (o pool está medido em
`detached` **no agent-registry**, não no YAML) ⇒ **teardown ausente no caminho do `Transfer`**. Abre
por: `Transfer` e `Close` compartilham o teardown do Console, ou são dois caminhos? ⚠️ Conserto
provavelmente em DOIS apps (o `agent-assist-ui` vive na 5173). ⚠️ Não testado: transfer com o cliente
ainda CONECTADO. Registro em `TODO.md` § *Analytics e UI*.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de OITO passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduos do defeito 2: linhas órfãs `queue_default_*` no config store;
  `queue_config.skill_id` legado do `retencao_humano`
```
