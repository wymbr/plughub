# Prompt de passagem — sessão de 2026-08-23

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a de 2026-08-22, que fechou a
> **fatia A do `session:{id}:meta`** (partição + prazo) e as **duas metades vivas da auditoria LGPD**
> (gate ABAC + trilha de acesso), e que **descobriu um bloqueio de ambiente maior que as duas tarefas
> juntas**. A passagem [`passagem-2026-08-22.md`](passagem-2026-08-22.md) está **encerrada**.

---

```
continuação sessão plughub.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· arquivo NOVO exige `build --no-cache`; edição de existente entra no build normal
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· **janela de log só compara se as DUAS janelas existirem**
· **`--since` do docker sem fuso é HORA LOCAL** — o host é −03
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso**
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3; usar `$DC exec -T redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **nada de crase dentro de aspas duplas em bash** — vira substituição de comando
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · mailing-api 3660 · **redis 6380 · kafka 9093 · postgres 5433**
· health da analytics-api é **`/v1/health`** (o `/health` sem prefixo dá 404)
· analytics-api usa **PyJWT** (`import jwt`); auth-api usa **python-jose**. As duas
  convivem no repo e `jose` NÃO está no container do analytics
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ AS QUATRO REGRAS QUE 08-22 COMPROU (todas por erro próprio) ═══
· **FONTE DECLARATIVA NÃO É ESTADO.** Registry é seed-if-absent: editar pool já
  semeado é NO-OP e o DB vence. Uma nota de 08-10 leu o YAML e o chamou de
  configuração; o CLAUDE.md de 08-20 seguiu a nota; os dois erraram. A config viva
  do `retencao_humano.on_human_end` é **`detached`**, não `inline`. Para `hooks`,
  `deploy` e `capacity`: **pergunte ao agent-registry** (`GET :3300/v1/pools/{id}`).
· **CONTROLE É PARAR O SERVIÇO, NÃO CHEGAR ANTES.** Para flagrar valor que outro
  serviço sobrescreve em <1 s, `stop` no consumidor (com `trap` para religar). Duas
  tentativas de fotografar por fora voltaram INCONCLUSIVAS e nenhuma era o defeito:
  cada `docker exec` custa ~0,5 s, e saturar capacidade não segura fila quando o
  skill suspende e devolve a vaga.
· **LIMIAR FIXO MEDE A LENTIDÃO DO PRÓPRIO PROBE.** "Caiu mais de 120 s" reprovou
  código JÁ CONSERTADO, porque o laço levou 122 s de relógio (o contador `i*2` só
  somava os `sleep`). Comparar com decaimento natural medido em `date +%s`, e
  IMPRIMIR esperado × observado × subtração. Sair do laço pelo EVENTO esperado,
  nunca por número de voltas.
· **BYPASS DE DEMO ESCONDE O CAMINHO DE PRODUÇÃO.** Com `open_access`/`_dev_mode`
  ligado o ramo real retorna antes; um 200 é compatível com "gate correto" E "gate
  ausente". O probe tem de DECLARAR que não julga essa metade → teste unitário com
  o bypass desligado. Foi assim que se pegou um `from jose import jwt` que só
  falharia em produção. E leia a flag do CONTAINER (`printenv`), não do config-api.
· *Corolários de instrumento, três em um dia:* artefato com TTL curto (300 s) só se
  mede contra tráfego recém-gerado — procurar chave velha mede EXPIRAÇÃO, não
  ausência de escrita · denominador não pode vir de DENTRO do fenômeno · não casar
  campo presente em DUAS formas de erro (`"detail"` existe no 422 e no 401, e o
  probe anunciou recusa do provedor como "corpo inválido").

═══ ⛔ BLOQUEIO DE AMBIENTE — LEIA ANTES DE MEDIR QUALQUER COISA DE IA ═══
**Nenhum step `reason` funciona neste demo.** Medido 2026-08-22, correlação
perfeita na vida inteira do processo do ai-gateway:

    POST /v1/reason ...... 124      upstream_model_error ... 124
    status_401 ........... 124      ("API key is invalid", anthropic)

O handler levanta ANTES de `main.py:357`, então nada pós-LLM roda: sentimento,
`intent`, `confidence`, estado do supervisor. E **todo step `reason` de todo skill
cai no `on_failure`** — ramo legítimo do fluxo, logo sem alarme. Atinge
`agente_fila_v1` (o único conversacional), `agente_contexto_ia_v1`,
`agente_copilot_v1`, `agente_avaliacao_v1` e os dois revisores.

**Consequência de método:** qualquer conclusão tirada deste demo sobre avaliação,
copilot ou contexto estava medindo o ramo de erro sem saber. Reler com desconfiança
achados que "explicavam" ausência de dado por outras causas.

Chave em `docker-compose.demo.yml:702` (`PLUGHUB_ANTHROPIC_API_KEY`, fallback
`ANTHROPIC_API_KEY`). Gate: `infra/test/probe_sentiment_producer.sh` (INCONCLUSIVO
com causa nomeada enquanto o provedor recusar).

═══ ESTADO (fechado em 2026-08-22, ver CHANGELOG — 5 commits, árvore limpa) ═══
`session:{id}:meta` FATIA A: partição declarada e MEDIDA (porta = tenant_id/channel/
  contact_id/customer_id/session_id/started_at/customer_participant_id · bridge =
  agent_type_id/instance_id/pool_id/user_login); helper `session_meta_merge` com 3
  modos e `EVAL` único; prazo só ESTENDE (`-1`/`-2` DEFINEM). O defeito real era o
  PRAZO, não perda de campo: 86397 → 14398 na alocação, com **7 tokens de resume
  condenados, 0 cobertos**. Gate `probe_meta_ttl_bridge_off.sh` + `SELFTEST=1`.
AUDITORIA LGPD: gate ABAC real (`_check_audit_access`, 4 ramos, recusa sem segredo)
  e `audit_access_log` criada e gravando — inclusive na RECUSA. Os dois existiam só
  no docstring desde maio; o CLAUDE.md os dava por entregues. `mcp_audit_log` NÃO
  foi criada de propósito (zero tráfego na borda `invoke`).
Wrap-up detached CONFIRMADO em produção pelo dono do ambiente e pela API.
n8n arco ABORTADO · não reabrir os baldes da triagem de 08-17

═══ TAREFA — escolher, não fazer tudo ═══

1. **Credencial de LLM** *(bloqueia 2, e contamina toda medição de IA)*. Não é só
   repor a chave: o ai-gateway responde **200 no health com credencial inválida**.
   Um health que exercitasse o provedor, ou um contador de `upstream_model_error`
   em tela, teria mostrado isso no primeiro dia. Hoje o único sintoma é fluxo
   tomando `on_failure`, indistinguível de fluxo funcionando conforme desenhado.

2. **Sentimento — desenho, DEPOIS da credencial**. O diagnóstico antigo ("sem
   produtor") foi refutado e o CLAUDE.md § Sentiment Tracking já está reescrito.
   O que sobra é decisão de contrato: `/v1/reason` recebe `input` OPACO e o gateway
   não distingue fala do cliente de `pipeline_state`; `main.py:352` só aproveita
   `sentiment_score` se o `output_schema` declarar, e **nenhum skill declara** ⇒
   sempre `0.0` = neutro = indistinguível de não-medido. Rumo decidido com o dono:
   extração no **ai-gateway** (é lá que `inference.py:164-168` já isola a fala), com
   um campo novo no `ReasonRequest` que NOMEIE a fala do cliente. Precedente:
   `resolve.ts:301`. **Não** pedir que cada skill declare o campo. Defeito adjacente:
   nenhum chamador de `/v1/reason` envia `tenant_id` (`engine-runner.ts:204`,
   `skill-flow-service/index.ts:249`) ⇒ escreveria sob tenant vazio.

3. **`session:{id}:meta` fatias B e C**. B = recusar campo de outro dono (hoje o
   helper só LOGA `[session_meta] DONO VIOLADO`); exige os quatro escritores de
   canal passando pelo helper, incluindo um irmão em TypeScript (`bpm.ts`).
   C = `pool_id` carrega DOIS fatos — pool de ENTRADA (`webchat.py:210`,
   `webrtc.py:484`) × pool que ATENDE (bridge; acepção que `webhook.py:1330` lê no
   resume); separar em `entry_pool_id`, família de `elapsed_time_ms` ×
   `agent_time_ms` (D9).
   › *Suspeita NÃO medida:* `session:{id}:wf_agent` nasce com o mesmo `_stl()` = 4 h
     ao lado do site W6 e descreve a mesma workflow de até 48 h. Medir antes.

4. **Gate re-executável da Camada F** (arco Detach). Validada por medição manual
   instrumentada; sem script versionado, arco completo é lembrança. A própria F4
   declara que a **lease** não foi medida (não há reaper; o transbordo é por idade).
   O dono do ambiente confirmou que o wrap-up detached está funcionando e se ofereceu
   para ajudar a medir.

5. **`mcp_audit_log`** *(dormente POR MEDIÇÃO, não por opinião)*. Ordem correta,
   escrita no TODO: (1) exercitar `invoke` de verdade e confirmar linha em
   `session_timeline`; (2) só então DDL + dual-write + branch em `_write_row`.
   **Não criar a tabela antes** — vazia com cara de pronta é indistinguível de
   "ninguém acessou". Resíduo a medir junto: `invoke` emite `session_id: ""` quando
   a instância tem 0 ou 2+ sessões ativas (`external-agent.ts:194-215`) e o parser
   DESCARTA evento sem `session_id` (`models.py:713-718`).

6. Reexame item a item dos 9 em `Escopo reduzido` (§4.5 do doc de reversão) —
   rejulgar pelo mérito, NÃO reverter em bloco. **Não tocado desde 08-17.**

7. Provisionar o SFU (LiveKit) — pré-requisito de qualquer trabalho de WebRTC.

═══ RESÍDUOS MENORES (sem dono, nenhum é bloqueio) ═══
· **pausa de IA é desfeita em 15 s**: o `agent_ready` da reconciliação do bootstrap
  republica `ready` para instância com pausa declarada. Conserto no `instance_bootstrap`
· **`bpm.test.ts` vermelho por dívida conhecida**: `conversation_escalate` agora RECUSA
  sem `session:{id}:meta` e o teste não semeia o meta. Suíte vermelha por dívida aceita
  deixa de sinalizar regressão nova
· **`npx vitest` no host valida `@plughub/schemas` de 2026-08-02** — o perigo é o
  simétrico: mudança de schema ficar VERDE porque o `dist/` velho aceita a forma antiga
· `queue_config` só existe em 1 pool do demo (`retencao_humano`) — as duas metades do
  gate da fila não cabem na MESMA janela sem mexer em config
· o e2e 07 tem uma asserção que **não pode reprovar** (`pass(...)` incondicional sobre
  o retorno do `queue_context_get`, `07_inbound_full.ts:133-137`)
· `session_timeline` está VAZIO e recebe linha de um único parser (o de `mcp.audit`) —
  ou seja `/v1/audit/mcp-calls` sempre devolve lista vazia neste ambiente
```
