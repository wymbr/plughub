# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-09-10.md`](passagem-2026-09-10.md), que está **encerrada**.
>
> A sessão anterior encerrou a ordem de trabalho. Esta escolheu três fatias
> (**1.4 chip × cabeçalho**, **1.3 Transfer**, **F5 ContextStorePersister**), fechou as
> três, e **abriu um arco novo a pedido do dono** — que é o próximo passo declarado.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes, mas **todo dado medido carrega
> carimbo `2026-08-26`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: **o próximo passo ESTÁ declarado**, ao contrário da
passagem anterior. O dono pediu, ao fim da sessão de 08-26, que se abrisse um arco próprio
para **ContextStore como ALLOWLIST** — com ADR completo, escrito numa sessão limpa. O
briefing já está pronto e é o ponto de partida:

    TODO.md § "🆕 ARCO PROPOSTO — ContextStore como ALLOWLIST: campo sem regra não é acessível"

Ele separa o que está DECIDIDO do que o ADR precisa DISCUTIR (sete perguntas, com os
trade-offs). **A tarefa é escrever o ADR, não implementar.** O valor está em fechar as
perguntas abertas, não em codar as decididas.

⚠️ **Comece pela pergunta 1 — ela já vem respondida em parte, e o resto dela é a fatia mais
barata e mais visível do arco:** as regras de masking VÊM do config-api e ESTÃO em vigor
(14 delas, nível global), mas a tela `Configuration › Masking › Regras de Context Store`
mostra **"Nenhuma regra configurada — usando defaults do sistema"** e `0 regras
configuradas`. A política funciona; **a tela mente**. Foi essa tela que fez o dono
desconfiar (*"não sei se seguem"*) e abriu o arco inteiro.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Já se pagou TREZE sessões
  seguidas. Nesta, a passagem anterior trazia um comando de baseline que **nunca havia
  rodado** — apontava para `/app/packages/orchestrator_bridge` (underscore) e o diretório é
  `orchestrator-bridge` (hífen). O "96 passed" era número herdado sem comando que o
  produzisse. **Quando herdar um número, rode o comando que o produz ANTES de confiar nele.**
· **AS QUATRO BASELINES, se for tocar código de serviço** (⚠️ caminhos CONFERIDOS em 08-26):
    $DC exec -T analytics-api sh -c "cd /app/packages/analytics-api && python3 -m pytest src/plughub_analytics_api/tests -q 2>&1 | tail -3"
    $DC exec -T orchestrator-bridge sh -c "cd /app/packages/orchestrator-bridge && python3 -m pytest src/plughub_orchestrator_bridge/tests -q 2>&1 | tail -3"
    $DC exec -T session-replayer python3 tests/test_context_persister.py
    $DC exec -T routing-engine sh -c "cd /app/packages/routing-engine && python3 -m pytest src/plughub_routing/tests -q 2>&1 | tail -3"
  Esperado: **617 passed** · **102 passed** · **7/7** · **1 failed, 294 passed** (o vermelho
  é o do Backlog 3, conhecido). ⚠️ routing-engine **não foi re-medido em 08-26**.
· ⚠️ **A suíte do session-replayer passou a EXISTIR em 08-26.** O `tests/` nunca era copiado
  na imagem — por isso o pacote não aparecia em baseline nenhuma. Agora roda em `/app/tests/`.
· ⚠️ **O AMBIENTE SEGUE MEXIDO, e continua sendo bom que siga.** `retencao_humano` com
  `queue_config.pool_id = fila_humano`, `max_wait_s: 1800`, `sla_target_ms: 300000`; admin
  com `accessible_pools = []`. Não reverta. Confira:
    python3 infra/test/q_queue_config_inventory.py
  Esperado: 36 pools · none 35 · legacy_only 0 · **addressable 1**. Nunca ao YAML.
· 🆕 **A config de masking também está mexida (08-26): 14 → 23 regras**, com globs de SUFIXO
  aplicados como **override de TENANT** (antes só havia o global). Aplicado por
  `infra/test/apply_masking_suffix_rules.sh`, que é idempotente e imprime o valor anterior.
· 🔴 **E NUNCA rode `REGISTRY_SYNC_RECONCILE=true` para publicar um skill.**
  Martelo cirúrgico: `bash infra/test/redeploy_limite_entrada.sh`, ou
  `infra/test/_publish_skill.py <yaml> <pool>`.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — e N linhas coladas de uma vez NÃO são N comandos: `A=… TOK=$(…) cmd`
  vira atribuição PREFIXANDO `cmd`. Cadeia de curl com token → **vira script em `infra/test/`**
· $DC não vem definido, e o compose fica na RAIZ:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· ⚠️ **o serviço do MCP chama-se `mcp-server-plughub`** — `mcp-server` NÃO resolve
· **`$DC exec` no clickhouse EXIGE `-T`**
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· ⚠️ **arquivo NOVO só entra na imagem com `build --no-cache`**; editar existente entra normal
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS
· **mudança de env no compose exige `up -d`**, não só `build`
· **no front há DOIS caches** — depois do `up -d`, `Ctrl+Shift+R` é obrigatório
· **script em `infra/test/` roda do HOST** — não precisa de build
· **PREFLIGHT DE SÍMBOLO antes de medir**, ESTRUTURAL (`import … ; print(callable(…))`),
  nunca `grep`
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO —
  ⚠️ **e o exit tem de refletir isso** (ver "o que esta sessão comprou")
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· ⚠️ **alias de agregado NUNCA repete nome de coluna real** (`ILLEGAL_AGGREGATION`, 184)
· portas: analytics-api 3500 · agent-registry 3300 · **config-api 3600** · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 ·
  auth-api 3202 · agent-assist-ui (LEGADO, vivo) 5173

═══ O QUE ESTA SESSÃO COMPROU ═══
· **UM CONSERTO PODE QUEBRAR UM CONTRATO QUE ELE NÃO MENCIONA.** O wrap-up unificado
  (2026-07-27) tirou o wrap-up de agente da conferência e, com ele, o único produtor do
  `posatt_segment_complete` que o Console esperava depois de um Transfer. Nada ficou
  vermelho: o front continuou fiel a um COMENTÁRIO que deixara de ser verdade. **Ao mover
  onde algo roda, procure quem dependia de onde ele rodava.**
· **DUAS REFUTAÇÕES CERTAS PODEM APONTAR PARA LONGE DA CAUSA.** O diagnóstico herdado dizia
  *"não é wrap-up e não é config de dispatch"* — as duas corretas — e a causa **era** o
  wrap-up, por um caminho que nenhuma das duas hipóteses testava.
· 🔴 **UM TESTE DE IGUALDADE SÓ JULGA SE A POPULAÇÃO CONTIVER O CASO EM QUE A ≠ B.** Comparar
  a aba Contexto como admin × operator deu as duas telas IDÊNTICAS — e isso não julgava nada:
  `admin` é `supervisor_role` (pula o portão) e a sessão não tinha nenhum tag PII. Só depois
  de INJETAR o discriminador a política pôde ser medida. **Antes de comparar, pergunte o que
  na população faria os dois lados diferirem.**
· 🔴 **O MEU PRÓPRIO LEITOR QUEBRADO QUASE VIROU CONCLUSÃO ARQUITETURAL.** Um `jq` lendo
  `.entries.context_rules.value` (envelope que é do corpo do **PUT**, não da resposta do
  **GET**) devolveu vazio, e a leitura natural seria *"a chave não existe ⇒ a página de
  Masking é decorativa"*. **FALSO** — havia 14 regras em vigor. Leitor quebrado e ausência
  real produzem saída IDÊNTICA. **Trate o veredicto da sua própria ferramenta como hipótese.**
· 🔴 **UM GATE PODE SAIR "OK" COM O RAMO PRINCIPAL INCONCLUSIVO.** O primeiro check de
  falseabilidade do `probe_chip_breakdown.sh` (com `J=` inexistente) mostrou o ramo que mede
  a divergência saindo INCONCLUSIVO e o veredicto global imprimindo **OK**. **Rastreie ramo
  inconclusivo e saia 2, nunca 0** — senão a amostra que sumir apaga a medição em silêncio.
  ⚠️ **`probe_f4_direction_and_classes.sh` tem a MESMA forma e NÃO foi corrigido.**
· **UMA RESTRIÇÃO QUE NÃO RESTRINGE NÃO REPROVA.** `pip install aiokafka>=0.10.0` **sem
  aspas** é redirecionamento: o pip recebe `aiokafka` e o shell cria um arquivo `=0.10.0`. As
  cinco specs do session-replayer eram inertes havia meses, e a evidência estava visível em
  `ls /app` o tempo todo. Ninguém olhou porque o build ficava VERDE.
· **UM PACOTE CUJA SUÍTE NINGUÉM ALCANÇA TEM COBERTURA QUE NUNCA É EXERCIDA.** O `tests/` do
  session-replayer nunca era copiado na imagem — e era por isso que ele não aparecia em
  baseline nenhuma. **Quando um pacote não está nas baselines, pergunte se é por escolha.**
· **ERRO É BARULHENTO SÓ SE O `except` NOMEAR O MOTIVO.** Errei o nome do serviço no
  `MCP_SERVER_URL` e o log disse `Name or service not known` a cada fechamento. Com
  `except: pass`, a tabela ficaria vazia e o arco pareceria entregue.
· **DUAS GRAFIAS DA MESMA COISA, DE NOVO — e a minha era a quinta.**
  `http://mcp-server-plughub:3100` existia em quatro serviços; escrevi `mcp-server`.
  **Conte os sites antes de escrever o próximo.**
· **REGRA DE MASKING QUE CASA DEMAIS FALHA TÃO FEIO QUANTO A QUE CASA DE MENOS** — só do lado
  invisível, porque ninguém abre chamado por ver `***`. Por isso o glob de sufixo casa em
  **fronteira de segmento** (`tag.endsWith("." + s)`), nunca substring.
· **PERSISTÊNCIA ≠ EXIBIÇÃO.** `applyContextMaskingDynamic` faz DUAS coisas (portão de
  namespace por pool × mascaramento de PII); só a segunda pertence a um registro durável.
  Aplicar a primeira faria a config de UI de um pool **apagar história em silêncio**.

═══ ERROS DE PREVISÃO (7 acertos exatos, 3 erros) ═══
Acertos: **617** e **102** nas suítes · **7/7** no persister · `2+2` no processo de referência ·
aritmética fechando em 108/108 · `segment_count ≥ 2` na continuidade do Transfer · **14 → 23**
regras de masking · `***25` no CPF, exato.
❌ **ERRO 1: assumi `aiohttp` no session-replayer por analogia com o bridge.** Não existe lá;
   o serviço usa `urllib` em executor, e o comentário do `_fetch_config_value` diz *"no extra
   dependency"*. **Herdar por semelhança é herdar sem medir.**
❌ **ERRO 2: previ que os 4 `close_reason` nulos incluiriam os transfers de hoje.** Eram
   fixtures `sess-e2e-*`. Construí o padrão a partir de UMA amostra colhida enquanto o
   fechamento ainda estava em voo (`segment_count` 4 → 6, `close_reason` null → `agent_hangup`
   minutos depois). **`status: "closed"` NÃO significa que a linha parou de mudar.**
❌ **ERRO 3: escrevi a quinta grafia de um host que já tinha quatro.**

═══ ENTREGUE — não re-fazer ═══
**(1.4 · chip × cabeçalho)** o chip `PRC-…` publica os MESMOS dois domínios do cabeçalho da
visão 2 (`· 3 + 2`), com terceira classe só quando > 0. Backend: `countIf` sobre a mesma
`_DIRECTION_EXPR` (+ join `_ch`, que ela exige). Front: `ProcessChip.tsx`, **um componente**
para a coluna da lista e o selo do breadcrumb — antes eram duas cópias, que já divergiram uma
vez (4 × 8 caracteres). Tooltip reusa as chaves i18n **do cabeçalho**.
Gate: `probe_chip_breakdown.sh` (4 ramos, 3 estados).

**(1.3 · Transfer)** `_publish_segment_release()` no ramo de transfer do bridge: o release da
tela é fato do **FIM DO SEGMENTO**, não da conclusão do wrap-up (amarrar ao wrap-up reabriria o
G1 na UI). Zero diff no front e no mcp-server — reusa o contrato que os dois já implementam.
Validado ao vivo 2× (10:58 e 11:04 UTC), release 1 ms após o fim do segmento.
Registrado como **Mudança 42** em `conference-mechanics.md`.

**(F5 · `ContextStorePersister`)** ARCO DE FASES DO HISTÓRICO UNIFICADO COMPLETO. Snapshot do
ctx (sessão + processo) no `session_closed`, mascarado em grau operator pelo mcp-server via
`POST /internal/context-snapshot`. Gate `probe_context_snapshot_endpoint.sh` (6 ramos).

**(colaterais, consertados)** Dockerfile do session-replayer (aspas nas specs + `tests/` na
imagem) · texto do default de `Context Store Visibility` (dizia `service, journey, session`; o
código é `["service","session"]`) · 9 regras de masking por SUFIXO + suporte a sufixo no
matcher, com profundidade no score.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
`/reports/sessions` janela 19–26/08: **108** contatos · **29** com processo N > 1 · **0** não
classificadas · aritmética do chip fecha em **108/108**
processo `8c47326d…`: 5 sessões = **3 acessos + 2 internas** · `d62d7121…`: 4 = **2 + 2**
suítes: analytics-api **617** · bridge **102** · session-replayer **7/7** · routing-engine
**não re-medido**
masking: **14 → 23** regras (override de tenant criado) · ContextStore vivo: **24 sessões,
1 processo** · `session_context_snapshot`: 2 linhas (11 e 20 entradas, 0 ocultas)
`accessible_pools`: admin **[]** (= TODOS) · operator `{formfill_demo, retencao_humano,
aprovacao_credito}` · supervisor `{}`

═══ A PRÓXIMA TAREFA (declarada pelo dono) ═══
**Escrever o ADR do arco "ContextStore como ALLOWLIST".** Briefing completo em `TODO.md`.
Ordem sugerida: **(1)** a tela de Masking que mostra 0 com 14 em vigor — barato, visível, e é a
origem da desconfiança; **(2)** a omissão deixar de ser muda (contar, não omitir — vale sozinha
mesmo que a inversão nunca aconteça); **(3–5)** semear a allowlist, virar o default, tela do
pool vira seletor. ⚠️ **O pré-requisito é inegociável:** virar deny-by-default com a omissão
ainda muda troca vazamento de PII por quebra muda de UI.

═══ NÃO fazer agora ═══
· **`REGISTRY_SYNC_RECONCILE=true`** — reverteria a config do `retencao_humano`.
· **Re-derivar a direção na UI** — foi removida de lá de propósito.
· **Voltar a escrever o access token num `useEffect`** — defeito de ordem de efeitos, e não
  fica vermelho em teste nenhum.
· **Reintroduzir um nível de sessão dentro do `AnaliseJourneysPage`.**
· **Mexer em `accessible_pools` por `psql`** — use `infra/test/set_user_pools.sh`.
· **Trocar `maskContextForPersistence` por `applyContextMaskingDynamic`** — o portão de
  namespace NÃO pode podar o registro durável. Ramo E do gate existe para isso.
· **Resolver a raiz canônica da journey em Python** — é o union-find do mcp-server, e uma
  foto na raiz errada não fica vermelha.
· **Persistir o ctx em grau supervisor** — recria o cofre que a R7 recusou.
· **"Consertar" o vermelho do `claimed_via` trocando `"semaphore"` por `"record"`.** Backlog 3.
· **Mexer na época `SEGMENT_SLA_EPOCH`** para "recuperar" a série. Não volta.
· **Apagar `AnaliseTab.tsx` "porque está morto".** É uma FEATURE sem consumidor.

═══ Backlog NOVO / reaberto ═══
· 🔴 **A tela de Masking mostra `0 regras configuradas` com 14 em vigor.** As regras vêm do
  config-api e funcionam. `MaskingPage` lê `maskingEntries['context_rules']?.value ??
  maskingEntries['context_rules']` e o `??` deveria cair no objeto cru — investigar o fetch.
· 🔴 **Seed × config viva DIVERGIRAM nos dois sentidos** (`session.vencimento_cartao` só no
  seed; `session.cpf_titular` só vivo). Nenhuma das duas é fonte de verdade com confiança.
· 🔴 **`probe_f4_direction_and_classes.sh` pode sair OK com ramo INCONCLUSIVO** — mesma forma
  que foi corrigida no `probe_chip_breakdown.sh`.
· 🆕 **A visibilidade do ContextStore é lida do pool de ENTRADA**, não do que atende
  (`server.ts:1421`, via `session:{id}:meta`). Contato transferido aplica a política do pool de
  ORIGEM. **Consumidor novo da fatia C** de `session-meta-ownership.md`, e o primeiro em que a
  acepção errada custa **exposição de PII**.
· 🆕 **`catch {}` sem log na leitura de `context_visibility`** (`server.ts:1452`).
· 🆕 **Constante × texto de UI sem mecanismo** — `DEFAULT_OPERATOR_NAMESPACES` é descrita em
  dois textos i18n e já divergiu. Corrigida, sem trava. Como travar sem inventar um teste que
  não pode reprovar?
· 🆕 **`GET /config/dashboards?tenant_id=…` responde 404, 4+ vezes por carga de página.**
· 🆕 **O ramo "não classificadas" segue com 0 população** — existe sem nunca ter sido exercido.
· 🆕 **Nenhum dos gates novos é chamado por runner** (`probe_chip_breakdown`,
  `probe_context_visibility`, `probe_context_snapshot_endpoint`, `sweep_ctx_tags`) — mesma
  dívida de quase todo `infra/test/`, agora com mais quatro.
· 🆕 **`agente_portabilidade_intake_v1` tem o MESMO defeito de ramo que o 2b consertou** —
  zero `journey_merge`. Quantos skills mais resolvem pendência e ramificam por `policy`?
· 🔴 **3. `test_expire_returns_the_slot_even_after_the_lease_expired` VERMELHO**, anterior a
  sete sessões. `router.py:1158` declara aquele ramo como *a MEDIDA da lacuna 2*.
· 🆕 **`wrapup_hold_ttl_s` é LEITOR SEM CHAVE** (`kafka_listener.py:411`).
· 🆕 **`sla_unstamped` existe na API e NÃO tem superfície na UI.**
· 🆕 **`_cv_sla_series` IGNORA o retorno de `_apply_pool_scope`.** É ABAC. Medir antes.
· 🔴 **`sla_default_ms` NÃO TEM LEITOR** — remover exige mexer no
  `smoke_config_routing_orphan_keys.sh:118`, que a usa como CANÁRIO.
· **3 cópias restantes do `480_000`** · **`supervisor.ts` compara atendimento total com alvo de
  espera** · **`duration_ms` do segmento humano diverge dos próprios carimbos** ·
  **`_emit_queue_timeout` publica SEM `key=`**.

═══ INCONFORMIDADES herdadas — estado ═══
· ✅ **Console não libera a tela após `Transfer`** — FECHADA em 2026-08-26 (Mudança 42).
· ⚠️ **Não testado:** transfer com o cliente ainda CONECTADO.
· ⚠️ **`recipients` do release saiu com UM elemento** nas duas execuções reais — a segunda
  forma de nomear o humano (participant_id) tem teste unitário e **nenhuma população real**.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-26 em busca de backlog órfão (herdada de QUINZE passagens)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduo do defeito 2: `queue_config.skill_id` legado do `retencao_humano`
· **lente C** (faixas por personagem) — **SUSPENSA por decisão do dono em 2026-08-26**: não é
  essencial e traz risco de desenho. Não é mais "próxima fatia".
```
