# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-08-31.md`](passagem-2026-08-31.md), que está **encerrada**.
>
> A sessão anterior escolheu a opção **(C)** e a fechou: o defeito 2 virou um conserto maior do que o
> título dele dizia, e foi medido nos DOIS cenários de produto. Sobraram (A) e (B), e a (A) ficou
> mais concreta porque o cenário 2 exibiu o caso que ela trata.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes (08-29 → 08-30 → 08-31 → 09-01), mas **todo
> dado medido carrega carimbo `2026-08-24`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: escolher entre (A) discriminar o id do segmento de espera —
pré-requisito da D14 —, (B) unificar o emissor do `_emit_queue_timeout`, ou (D14.1) decidir o que
`sla_target_ms` É. A opção (C) das passagens anteriores foi ENTREGUE.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Uma pendência declarada por quem parou de
  trabalhar é a última coisa que ele lembra, não o estado do repo. Já se pagou TRÊS sessões seguidas
  (08-29, 08-30, 08-31: 3 greps em 2 minutos liberaram a sessão inteira para medir). **A ORDEM
  proposta também se confere.**
· **O `TODO.md` § "janela de espera" e § "tenant default" foram reescritos no fim da sessão passada.**
  Estão mais atuais que qualquer resumo — inclusive que este.
· ⚠️ **O AMBIENTE FOI MEXIDO NA VALIDAÇÃO.** O `retencao_humano` recebeu
  `queue_config.pool_id = fila_humano` para provar o cenário 2, e **pode ter ficado assim**. Isso muda
  tier, TMA e atribuição. **Pergunte ao agent-registry antes de medir qualquer coisa de fila:**
    curl -s localhost:3300/v1/pools/retencao_humano -H 'x-tenant-id: tenant_demo' | python3 -m json.tool | grep -A4 queue_config
  Nunca ao YAML (é seed-if-absent; o DB vence).

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **arquivo NOVO exige `build --no-cache`** (edição de existente entra no build normal) — por isso os
  4 testes novos foram para DENTRO de `test_queue_wait_segment.py`, e funcionou
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **PREFLIGHT DE SÍMBOLO antes de medir** — importar o módulo no container e conferir o atributo
· **`$DC exec` NÃO começa em `/app`** — use caminho ABSOLUTO no pytest
· **no FRONTEND são DOIS caches**: depois do build, `Ctrl+Shift+R` é OBRIGATÓRIO
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· **`GET :3300/v1/pools` devolve ENVELOPE** — e já traz `deployed_skill_id` por pool (dispensa um GET
  de slots por pool; foi assim que o inventário saiu numa requisição)
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **nada de crase dentro de aspas duplas em bash**; para python multi-linha em uma linha, use $'...\n...'
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service + mcp-server +
  skill-flow-worker JUNTOS
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· **para hooks/deploy/capacity/queue_config, perguntar ao agent-registry, NUNCA ao YAML**
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 (rota `/config/{ns}?tenant_id=`,
  SEM `/v1`) · dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 ·
  platform-ui 5174 · **auth-api 3202** (3200 é o ai-gateway)
· inglês no código, PT em i18n/spec/dados de tenant

═══ AS QUATRO REGRAS QUE ESTA SESSÃO COMPROU ═══
· **"TEM CONFIG" ≠ "TEM ENDEREÇO".** Objeto que mistura endereço com política não pode ser testado por
  PRESENÇA. `queue_config` carregava três fatos (`pool_id`=endereço · `max_wait_s`=política ·
  `skill_id`=endereço legado morto) e QUATRO call sites perguntavam "há quem atenda?" com
  `if queue_config:`. Preço: licença de IA retida numa espera que ninguém atendia. Virou invariante no
  CLAUDE.md § pool endereçável.
· **BUSCA TEXTUAL EM FONTE CONTA O COMENTÁRIO — inclusive `inspect.getsource` dentro do container.**
  Removi `_flow_pool_id = queue_pool_id or pool_id`, escrevi um comentário dizendo que removi, e
  propus o preflight `'or pool_id' in source` prevendo False. Veio True. O conserto é contar a
  ATRIBUIÇÃO e as DUAS formas de uma vez: `s.count('… or pool_id')` vs `s.count('…\n')` → `0 1`.
· **INSTRUMENTO COM JANELA: ausência fora da janela é INCONCLUSIVA, não vermelha.**
  `queue:agent_active:{sid}` é apagado no fim do contato; `EXISTS` depois disso dá 0 e não reprova
  nada. Idem `grep -c` no log ANTES do contato acontecer (saiu 0/0, depois 0/1). Antes de ler,
  pergunte se a janela do instrumento cobre o fenômeno.
· **MEDIR ANTES DE ATRIBUIR CRÉDITO.** O segmento de fila apareceu na tela e parecia mérito do
  conserto; a série por dia mostrou linhas em 08-10, 08-11, 08-13, 08-18, 08-19 e 08-21 — o produtor é
  a D12, de sessões anteriores. Um resultado bom no fim da sua mudança não é seu por proximidade.

═══ ERROS DE PREVISÃO ═══
Errei 2 de 17.
  · **fator 8 no teto do whatsapp**: li o RÓTULO do ramo ("Fila ATENDIDA" × "Fila MUDA") em vez do
    teste (`max_wait_s > 0`, que nunca consultou tier). Corrigido ANTES de virar código — a regra que
    o usuário decidiu já era a implementada, só o rótulo mentia.
  · **preflight do bridge**: ver a 2ª regra acima.
Acertei: inventário (5 previsões), preflight de símbolo, `8 passed`, falseabilidade, os 4 números do
contato real do cenário 1, a série por dia, e os 3 segmentos do cenário 2.

═══ ESTADO — DEFEITO 2 ENTREGUE, NOS DOIS CENÁRIOS ═══
Tier de fila decidido por **ENDEREÇO** (`queue_config.pool_id`), predicado único
`mute_queue.queue_address`, usado por routing e bridge. Política (`max_wait_s`) é do POOL e vale nos
dois tiers; canal é tolerância física (default de quem não declarou) e `0` é VETO. Default de tenant
REMOVIDO (vocabulário pré-slot, zero usuários, promessa falsa na tela). `queue_pool_id or pool_id`
virou recusa alta. UI: rótulo honesto + aviso em config-time + `max_wait_s` enviável sem endereço.
Registro: `CHANGELOG.md` 2026-08-24 · `CLAUDE.md` § pool endereçável · `system-queue.md` §
Esclarecimento estrutural · `queue-attended-model.md` Fase C item (4) · `TODO.md` (duas seções).
Gate: `test_queue_wait_segment.py` **8 passed** (4 D12 + 4 tier, estes puros).
Probe: `infra/test/q_queue_config_inventory.py`.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
inventário (36 pools): `queue_config` em **1** · **addressable 0** · default de tenant `""`/`""` ·
  `fila_humano` (agent_kind **ai**) com slot promovido `skill_fila_v1` e ninguém apontando p/ ele
cenário 1 (sem pool de fila, sessão `a7275f7f…`): ERROR 0 · INFO 1 · `unadmitted` **sim** ·
  `admission:kind:ai` **não**
cenário 2 (`pool_id=fila_humano`, sessão `…13315bc9968c`): `unadmitted` **não**; 3 segmentos —
  `primary sac_ia native escalated_human 2 761` · `queue retencao_humano system abandoned 176 431` ·
  `specialist fila_humano native abandoned 176 424`
  ⇒ **D10 saiu do no-op**; as duas linhas de 176 s são a MESMA janela por dois ângulos
  ⇒ o `+4,8 %` de TMA em `retencao_humano` NÃO ocorre quando há pool de fila endereçado
`queue_max_wait_by_channel`: voice 300 · webrtc 300 · webchat 1800 · whatsapp 14400 (**nenhum 0** ⇒ o
  veto de canal é exposição LATENTE, ninguém sofre)
segmentos `role='queue' agent_type='system'` por dia: 08-10:5 · 08-11:6 · 08-13:1 · 08-18:3 ·
  08-19:4 · 08-21:5 · 08-24:1

═══ AS OPÇÕES DE TAREFA ═══
**(A) Discriminar o id do segmento de espera — PRÉ-REQUISITO DA D14.**
  `queue_wait_segment_id` é `uuid5(tenant, session_id)`, **sem discriminador de passagem nem de pool**.
  Sessão que espera em DUAS filas emite dois segmentos que COLAPSAM numa linha, vencendo a última.
  Conserto provável: pool de destino no namespace do `uuid5` — barato, mas **muda a identidade das
  linhas já gravadas** (declarar a descontinuidade).
  ⚠️ Segue **DEDUTIVO** — a população não tem caso de duas filas. Primeiro passo é PRODUZIR o caso
  (transferência entre pools com espera nos dois), não codar.
  🔎 **Novo desde 08-24:** o cenário 2 exibe o padrão irmão — `queue` e `specialist` cobrindo a MESMA
  janela com ids distintos. Serve de modelo do que se quer (dois fatos, duas linhas) e de material de
  teste, porque agora a fila atendida volta a existir neste ambiente.
**(B) Unificar o emissor do `_emit_queue_timeout`.** Fatia própria; junto com o P2 daria emissão dupla.
  Lacuna viva: `max_wait_exceeded` na fila **ATENDIDA** segue sem segmento de espera.
  ⚠️ **Subiu de prioridade por fato novo:** enquanto o `retencao_humano` não tinha fila atendida
  funcional, a lacuna era inalcançável neste ambiente. Com o cenário 2 provado, ela é reproduzível —
  basta um contato esperando além do `max_wait_s`.
**(D14.1) Decidir o que `sla_target_ms` É.** Bloqueia a D14, que a (A) destrava. O rótulo diz "Total
  service SLA" nos dois locales e TODOS os consumidores comparam com ESPERA; o default do formulário
  (30 s) contradiz o rótulo. **18 de 36 pools estão com ≥ 1 hora** (processo/aprovação — não podem
  violar SLA) contra 18 entre 15 s e 10 min. Todo agregado de SLA hoje mistura duas populações
  incomparáveis. É decisão + inventário, pouco código.

═══ NÃO fazer agora ═══
· **Migrar os 3 leitores de SLA (D14)** — bloqueado pela (A) e, antes dela, pela (D14.1).
· **36.2 (as 5 que não fecham)** — causa não identificada; a duplicação NÃO explica (interseção 1).
· **F4 (visão 2 do histórico unificado)** — classe *outbound* segue com zero amostras de `spawn_reason`.

═══ Resíduos nomeados do defeito 2 (nenhum é bloqueio) ═══
· linhas órfãs de `queue_default_agent_type_id`/`_skill_id` no config store — inertes (sem leitor),
  saem com DELETE explícito se quiser higiene
· `queue_config.skill_id` legado do `retencao_humano` — preservado de propósito e rotulado IGNORADO
  na tela; some quando alguém limpar
· confirmação VISUAL da tela do pool (rótulo novo + aviso de deploy ausente) não foi reportada

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de CINCO passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
```
