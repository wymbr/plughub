# Prompt de passagem — próxima sessão

> ⚠️ **ENCERRADA.** Sucedida por [`passagem-2026-08-30.md`](passagem-2026-08-30.md), que traz o P1/P2
> escritos (não validados) e as decisões D10.1 / D14 / D14.1. **A tarefa 36.3 desta passagem foi
> executada** — o que resta dela é o gate, e está na sucessora.

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-08-28.md`](passagem-2026-08-28.md), que está **encerrada**.
>
> A sessão anterior foi **medição pura** sobre o defeito 1 daquela passagem (*abandono na fila*).
> Resultado: o defeito 1 foi **reenquadrado** (certo no conteúdo, errado na evidência), **3 achados
> separados** registrados como `Problema 36`, **1 achado refutado** por medição, e a **ordem de ataque
> passou a ser consequência do dado**. **Nenhuma linha de código foi escrita.**
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes (08-26 → 08-27 → 08-28 → 08-29), mas **todo
> dado medido carrega carimbo `2026-08-21`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: 36.3 — produtor da janela de espera (D12).

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **no FRONTEND são DOIS caches**: depois do build, `Ctrl+Shift+R` é OBRIGATÓRIO
· arquivo NOVO entra no build normal quando o Dockerfile copia o DIRETÓRIO
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· **janela de log só compara se as DUAS janelas existirem**; `--since` sem fuso é HORA LOCAL (−03)
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso**
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3; `curl` NÃO existe no ai-gateway (use o host)
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **nada de crase dentro de aspas duplas em bash** — vira substituição de comando
· **ClickHouse: `FROM t AS alias FINAL`**, nunca `FROM t FINAL AS alias` (erro de sintaxe no 23.8)
· **`sessions` NÃO tem `ingested_at`** (essa coluna é de `segments`)
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server + skill-flow-worker JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· **para hooks/deploy/capacity/queue_config, perguntar ao agent-registry, NUNCA ao YAML**
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · mailing-api 3660 · **redis 6380 · kafka 9093 · postgres 5433**
· health da analytics-api e do ai-gateway é **`/v1/health`**
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ AS QUATRO REGRAS QUE ESTA SESSÃO COMPROU ═══
· **ERREI 4 PREVISÕES SEGUIDAS ANTES DE ACERTAR A 5ª, E CADA ERRO MUDOU O DIAGNÓSTICO.** Previ, em
  ordem: (1) que o par `active`+`abandoned` existia como linha — não existe; (2) que o `abandoned`
  vinha de segmento de fila nas sessões travadas — nenhuma tinha; (3) que a sessão de hoje ainda
  estaria viva no Redis — estava morta como as outras; (4) que o par estava noutro tenant ou noutra
  versão de linha — estava em `tenant_demo`, versão única, com TUDO nulo. *Escrever a previsão foi o
  que transformou cada erro em informação em vez de em conclusão publicada.*
· **EU MESMO FABRIQUEI O ZERO QUE A REGRA ANTERIOR MANDAVA DESCONFIAR.** Contei
  `status='active' AND outcome='abandoned'` e deu **0**, e usei o 0 como refutação. O par nunca existiu
  como coluna: as duas palavras são DERIVAÇÕES em camadas diferentes. *A regra de 08-21 dizia "um zero
  só julga se o filtro que o produziu não puder tê-lo fabricado" — e eu a repeti como vítima.*
· **ESTADO IMPOSSÍVEL NA TELA NÃO É ESTADO IMPOSSÍVEL NO DADO.** Antes de caçar a linha corrompida,
  descobrir de ONDE vem cada palavra que a tela desenha. Aqui eram dois arquivos, dois processos e
  duas regras defensáveis isoladamente.
· **"NÃO APARECE NA TELA" EXIGE BATER NA API ANTES DE VIRAR DEFEITO.** Um quarto achado nasceu e
  morreu: a API devolvia a linha (`alvo=1`) e um `Ctrl+Shift+R` levou 72 → 103 contatos. *O tell que
  quase passou batido: o total NÃO tinha mudado apesar de duas sessões novas.*

═══ ESTADO (fechado nesta sessão) ═══
`docs/guias/conference-mechanics.md` § **Problema 36** (NOVO, 3 sub-achados + query re-executável +
  o não-achado registrado). Irmão do Problema 34, lacuna da Mudança 24.
`TODO.md`: item 3 da § *superação* CORRIGIDO; seção NOVA *"Abandono tem DOIS vocabulários"*.
`CLAUDE.md`: invariante dos três níveis EMENDADO com D10–D13 (a D11 contradizia o texto que estava lá:
  dizia `session = UM contato`, agora `session = UM ACESSO`).
E2E: `--demo` em **10/17**, INALTERADO — não rodei.
Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1): **intocado**.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
população `sessions` tenant_demo: **522** (era 518 na passagem anterior; cresceu com os testes)
cross-tab `closed_at` × fila abandonada:
  never_closed + queue_abandoned  = **5**   ← o defeito
  closed       + queue_abandoned  = **10**  ← a TESTEMUNHA (intermitência ~⅓, não ausência)
  never_closed sem fila abandonada = 8 `active` + 3 nulas + 27 `suspended`  ← família à parte
as 8 `active`: **keys=0 / ttl=-2** no Redis, contra **56** chaves `session:*` vivas
`close_reason` com ZERO ocorrência apesar de estar no domínio: `session_timeout`, `no_resource`,
  `system_error` · e `agent_closed` (14×) NÃO está no domínio
sessões com `customer_disconnect`: **13**
reload na fila (2 reproduções): fecha em 11 s e 24 s, `escalated_human`/`customer_disconnect`,
  **1 segmento**, segmento de fila NUNCA nasce

═══ A TAREFA — 36.3, produtor da janela de espera (D12) ═══
**Por que esta primeiro:** é a raiz dos outros dois. Enquanto o segmento de espera não nascer, não há
onde pendurar o fechamento (36.2) nem a contagem de abandono (vocabulário duplo). Consertar só a
leitura faria as duas telas CONCORDAREM sobre um contato que continua sem registro nenhum de espera.

**LER ANTES DE PROPOR** (nesta ordem, e só isto):
 1. `docs/adr/adr-journey-session-segment-model.md` § D12 (e a D9, cuja linha *(espera)* está REFUTADA)
 2. `docs/product/fila-janela-de-espera-2026-08-21.md` (medição completa, 8 seções)
 3. `docs/guias/conference-mechanics.md` § Problema 36 (o que já está registrado — a correção
    ATUALIZA essa entrada, não cria outra)

**A PRIMEIRA DECISÃO NÃO É CÓDIGO, É NOME.** `role='queue'` **já existe e já significa outra coisa**:
é o segmento do AGENTE DE FILA (o flow que conversa com quem espera), não a espera do cliente. Foi
medindo isso que a linha *(espera)* da D9 caiu. Logo o produtor novo precisa de veículo próprio, e
decidir qual é o primeiro trabalho:
  · papel novo (ex. `wait`) num segmento com id determinístico?  ou
  · reclassificar o agente de fila e liberar o nome `queue`?
Ver D12: *veículo = segmento, id determinístico*.

⚠️ **EFEITO COLATERAL DECLARADO, e ele depende da escolha acima.** `agent_time_ms` filtra
`role IN ('primary','specialist') AND duration_ms IS NOT NULL` (`reports_query.py:867-874`). Um papel
NOVO fica fora por construção — nada muda. **Reclassificar o agente de fila para `specialist` põe o
tempo dele DENTRO de `agent_time_ms` e muda TMA/AHT do ambiente.** Os dois caminhos são defensáveis;
o que não é defensável é escolher sem contar o antes.

⚠️ **SEGUNDO EFEITO, e este é BEM-VINDO mas tem de ser previsto.** O relatório Fila/SLA admite por
`q_count > 0` (`reports_query.py:5762`). Assim que o segmento de espera passar a existir, contatos que
hoje são INVISÍVEIS ao relatório entram — numerador **e** denominador. **A taxa de abandono vai mudar,
e mudar é o certo.** Contar a base ANTES é obrigatório, senão o acerto vira "regressão".

**TESTEMUNHA OBRIGATÓRIA do gate:** os **10** que fecham continuam fechando; as **469** linhas
`closed/closed/no_abandoned_queue` não mudam de forma. Reprodução: e2e `e2e-inbound-*` + o caso manual
(escalar do `sac_ia` para a fila e dar F5 no webchat — 11 s).

**A BASE A CONTAR ANTES DE TOCAR EM QUALQUER COISA** é a query de volume que está no § Problema 36 do
`conference-mechanics.md` (copiar de lá, já validada). Rodar, colar o resultado, e só então propor.

═══ NÃO fazer agora ═══
· **36.2 (a intermitência dos 5 em 15)** — sem forma conhecida; precisa de log do bridge nos 5 casos
  nomeados. Depois do 36.3, que pode inclusive dissolvê-la.
· **Unificar o vocabulário de abandono** — trava numa DECISÃO DE PRODUTO ainda não tomada (*cair
  enquanto espera conta como abandono?*) e depende do 36.3.
· **F4 (visão 2 do histórico unificado)** — a classe *outbound* segue com zero amostras de
  `spawn_reason`; construir a tela é o ramo que nada exercita.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de duas passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
```
