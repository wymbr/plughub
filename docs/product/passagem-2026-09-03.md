# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-09-02.md`](passagem-2026-09-02.md), que está **encerrada**.
>
> A sessão anterior escolheu a **(D14.1)** e a fechou de ponta a ponta — decisão, inventário,
> contrato, rótulo, default. Sobraram **(B)** e **(D14-i)**, e a D14-i está agora **desbloqueada**.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes, mas **todo dado medido carrega carimbo
> `2026-08-24`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: escolher entre (D14-i) parar de colapsar a sessão numa linha de
fila no relatório Fila/SLA — agora DESBLOQUEADA —, ou (B) unificar o emissor do
`_emit_queue_timeout`. A (A) e a (D14.1) foram ENTREGUES e MEDIDAS.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Uma pendência declarada por quem parou de
  trabalhar é a última coisa que ele lembra, não o estado do repo. Já se pagou CINCO sessões
  seguidas. **A ORDEM proposta também se confere.**
· **O `TODO.md` § "SLA está no grão errado" foi REESCRITO no fim da sessão passada** (D14.1 decidida,
  correlação com `purpose` riscada, 5 passos enfileirados, 3 deles já ✅). Está mais atual que
  qualquer resumo — inclusive que este.
· ⚠️ **O AMBIENTE SEGUE MEXIDO, e continua sendo bom que siga.** `retencao_humano` tem
  `queue_config.pool_id = fila_humano`. É o **cenário 2**, e é ele que torna o caso de duas filas
  reproduzível. Não reverta sem motivo. Confira:
    python3 infra/test/q_queue_config_inventory.py
  Esperado: 36 pools · none 35 · legacy_only 0 · **addressable 1**. Nunca ao YAML (seed-if-absent).

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **arquivo NOVO exige `build --no-cache`**
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **`up -d` recria o container e leva junto o `pip install`** — reinstalar pytest antes de rodar gate
· **no frontend há DOIS caches** — depois do build, `Ctrl+Shift+R` é obrigatório
· **PREFLIGHT DE SÍMBOLO antes de medir** — e prefira checagem ESTRUTURAL (aridade, valor) a busca
  textual: `grep`/`inspect.getsource` contam o COMENTÁRIO que documenta a remoção
· **`$DC exec` NÃO começa em `/app`** — caminho ABSOLUTO no pytest
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 · auth-api 3202
  · agent-assist-ui (app LEGADO, vivo) 5173

═══ O QUE ESTA SESSÃO COMPROU ═══
· **EXPOSIÇÃO E DANO SÃO GRANDEZAS SEPARADAS, e a lição foi paga DUAS vezes no mesmo dia.** Virou
  invariante no `CLAUDE.md` § Postura de Engenharia pela manhã (aging inerte: 16 esperas em pools de
  alvo absurdo, dano ZERO porque duravam 8 segundos) e foi **violada duas horas depois** (7 segmentos
  por contato chamados de duplicação sem contar quantos estavam ÓRFÃOS — zero estavam).
· **CAMPO DECLARADO NUM SENTIDO E CONSUMIDO NOUTRO: o desempate é MEDIR OS CONSUMIDORES, não ler o
  rótulo nem o contrato.** A metade "atendimento total" do `sla_target_ms` tinha rótulo, comentário
  de contrato Zod, 5 superfícies de UI — e **nenhum consumidor vivo**, porque o produtor devolvia
  constantes. Foi isso que tornou a decisão barata e matou a opção "partir em dois campos".
· **O DISCRIMINADOR CERTO COSTUMA JÁ EXISTIR NO REPO.** Duas vezes: `queue_address` (tier da fila) e
  `participant_joined_at` consumido por GETDEL (janela aberta). Antes de inventar flag, procure a
  chave cujo CICLO DE VIDA já significa o fato.
· **DEFAULT DUPLICADO É SINTOMA; BOTÃO SEM LEITOR É O DOENTE.** A varredura atrás do "default duplo"
  achou `sla_default_ms`: semeada, cacheada, emitida, testada, **exibida ao operador** — e sem
  nenhum leitor.

═══ ERROS DE PREVISÃO ═══
Numéricas: **7 de 8** no inventário (faixas 18/0/18/0, default 30 s em 2 pools, tier 1/35,
correlação NÃO-limpa com `purpose`). **Errei o `480_000 → 0`** (medido 2).
Na testemunha do aging: previ **latente**, medido **vivo-mas-inócuo** — 16 de 63 esperas em pools de
alvo ≥1 h, com dano zero. Foi o erro que gerou o invariante.

⚠️ **DUAS AFIRMAÇÕES MINHAS FORAM REFUTADAS POR MEDIÇÃO — não repita nenhum dos dois caminhos:**
  · **"F5 duplica segmento"** — FALSO. Detalhe abaixo, e em `conference-mechanics.md` § Mudança 39.
  · **"o default 480 000 vazou para o store"** — FALSO. `demo_ia` e `sac_ia` **declaram** o valor no
    `tenant_demo.yaml:159,166`. Coincidência com a constante do runtime, não vazamento.
As duas seguiram o mesmo padrão: parei de medir e comecei a explicar.

═══ ESTADO — (D14.1) ENTREGUE ═══
**Decisão:** `sla_target_ms` := **alvo de ESPERA em fila** (dono do produto). É **alvo** (soft): o
aging cresce até ele e o breach acelera depois — o contato SOBE na fila. Quem **encerra** é o TETO
(`queue_config.max_wait_s` · `queue_max_wait_by_channel`, onde **`0` é VETO**), via
`_emit_queue_timeout` → `max_wait_exceeded`, varredura a cada 5 s.

Passos 1–3 do plano ✅ (contrato Zod · rótulo nos 2 locales · `SLA_TARGET_MS_FALLBACK` nomeado e
BARULHENTO em `routing/models.py`). Passos 4–5 abertos (ver § opções).
Registro: `CHANGELOG.md` 2026-08-24 · `CLAUDE.md` § Routing Algorithm **4b** + § Postura de
Engenharia · `TODO.md` § D14.1 · `queue-attended-model.md` · `conference-mechanics.md` § Mudança 39.
Instrumentos novos: `q_sla_target_inventory.py` · `q_sla_band_wait_witness.sh`.

**Console:** a barra de SLA foi REMOVIDA das 7 superfícies + o produtor de constantes
(`server.ts`). O relógio ⏱ passou a ancorar no `assigned_at` do servidor (`parseAssignedAt`, nos
DOIS apps), com degradação barulhenta.

⚠️ **NÃO FEITO, e foi oferecido:** o build final não foi confirmado. Rodar antes de medir qualquer
coisa: `$DC build agent-registry routing-engine mcp-server-plughub platform-ui` + `up -d`.
*(O rótulo novo — "Queue wait target (ms)" — foi confirmado NA TELA pelo operador, então
platform-ui está de pé; os outros três não foram verificados.)*

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
inventário `sla_target_ms` (36 pools): wait_plausible **18** (15 s–10 min) · grey **0** ·
  process **18** (≥1 h: 5×1h, 9×24h, 3×48h, 1×**7 dias** `limite_entrega`) · absent 0
defaults: `30_000` em 2 pools · `480_000` em 2 (declarados no YAML) · 32 configurados à mão
discriminador REAL (não é `purpose`, que separa 2 de 18): **como o pool é ENTRADO** —
  espera de contato 18 · `webhook` 12 · `pull` 4 · I/O de `delegate` **2**
  ⚠️ os 2 de delegate (`limite_retorno`, `portabilidade_confirmacao`) **não têm discriminador no
  registro do pool** — são `push` + canal de cliente como os 18; só o VALOR os separa, o que é
  circular para migração automática
pela definição (fila HUMANA), o campo vale para **2 pools de 36**: `retencao_humano` (5 min) e
  `especialista_onboarding` (10 min). Os 4 humanos `pull` já estão em prazo-de-processo (1d/2d) —
  o parque foi configurado seguindo uma distinção que o código não faz
63 segmentos `role='queue'`, 9 pools, 56 sessões: **43** fila humana push · **19** pool de IA ·
  **1** fila pull ⇒ **32%** do Fila/SLA não pertence a ele
aging inerte: 16 das 63 esperas em pools de alvo ≥1 h, `sla_ratio` entre 0,00005 e 0,0017 ⇒
  mecanismo VIVO, dano **ZERO** (esperas de 5 a 14 s). Limítrofe: `aprovacao_credito`, 4,9 min
  contra alvo de 2 dias — 1 item na fila, nada a ordenar

═══ AS OPÇÕES DE TAREFA ═══
**(D14-i) Parar de colapsar a sessão numa linha de fila.** DESBLOQUEADA pela D14.1.
  `reports_query.py:5752-5755` faz `anyIf(pool_id, role='queue')`, `anyIf(outcome,…)`,
  `maxIf(duration_ms,…)` por sessão. A 2ª espera é **descartada na leitura**, e onde as duas
  discordam o `anyIf` **sorteia**.
  ⚠️ **O conserto NÃO é `sum()`** — somar esperas contra alvos diferentes é o que a D14 recusa.
  ⚠️ **Decidir ANTES de codar:** com uma linha por segmento, o que a coluna `contacts` conta? Hoje
  conta SESSÕES. Per-segmento ela mudaria de significado numa tela que o operador já lê.
  ⚠️ **Os números de conformidade VÃO mudar** — contar por pool ANTES, prever, e declarar. Os 32%
  de contaminação medidos dizem a direção, não o valor.
  ⚠️ Sessões com pool e ZERO segmentos de fila (5 medidas em 08-14) **não podem sumir** do relatório.
**(B) Unificar o emissor do `_emit_queue_timeout`.** Lacuna viva: `max_wait_exceeded` na fila
  **ATENDIDA** segue sem segmento de espera (o passo 3 só cobre o tier MUDO). ⚠️ Aquele emissor ainda
  usa `uuid4()` **e publica sem `key=`** — os dois defeitos que o `resolve_queue_exit` já pagou.
  Cuidado conhecido: trocar para `emit_segment=True` sem remover o passo 3 dá emissão DUPLA.
**(D14.1 passo 4) O que fazer com os 34 pools cujo valor não é espera.**
  ⚠️ **PRÉ-REQUISITO NÃO MEDIDO:** o que `scorer.py:177`, `decide.py:287`, `saturated.py:92` e
  `routing/main.py:1055` fazem com `null`/`0`. `max(sla,1)` sugere degradação limpa, mas o
  `saturated` MULTIPLICA e o `main.py:1055` publica **ETA ao CLIENTE**. Medir antes de anular.

═══ NÃO fazer agora ═══
· **Migrar os 3 leitores de SLA para o segmento (D14 ii/iii)** — depende da (D14-i).
· **"Consertar" a multiplicidade de segmentos por F5** — REFUTADO, ver § abaixo.
· **O `queue-agent` do bridge** (`main.py`, mesma colisão de id da fatia A). Hoje inalcançável:
  exige DOIS pools com fila atendida e só um tem endereço. Fatia própria; o discriminador dele
  **não pode ser wall-clock**, ou a idempotência morre.
· **F4 (visão 2 do histórico unificado)** — classe *outbound* segue com zero amostras.

═══ ⚠️ REFUTADO — não re-derivar (custou meia sessão) ═══
**Sintoma real e reproduzível:** o relógio do Console zera a cada F5, e um contato acumula MUITOS
segmentos `primary` (medidos **8** em `18232569-…`, 6 em `af64c36b-…`, 6 em `fb66eed5-…`). O gatilho
confirma: 7→8 e 2→3 num único F5.

**Diagnóstico sedutor pela leitura do código, e FALSO:** `activate_human_agent` roda de novo a cada
reconexão e `setex session:{sid}:segment:{inst}` sobrescreveria o id anterior, orfanando-o.

**O número que refuta:** `countIf(duration_ms IS NULL)` = **ZERO abertos** naquelas sessões. Todos
fecham. F5 além do `UNREGISTER_GRACE_MS` (**2,5 s**, `mcp-server/server.ts:2879`) é queda GENUÍNA →
`contact_closed(agent_disconnect)` → segmento **fechado** + GETDEL da chave de join → contato volta à
fila → re-rota. F5 **dentro** do grace nem chega lá: o unregister é cancelado e a tela é restaurada
pelo replay de `pool:pending_assignment`, que não cria segmento.
⇒ **N segmentos = N quedas reais = N janelas de participação.** O relógio zerando está CERTO.

⚠️ **Se a suspeita voltar, o discriminador é SEGMENTO ÓRFÃO** (`duration_ms IS NULL` > 1 por sessão
viva), **nunca a contagem**.

═══ Backlog NOVO / reaberto ═══
· 🔴 **`sla_default_ms` NÃO TEM LEITOR** — semeada (`config-api/seed.py:157`), cacheada
  (`routing_config.py:47`), emitida (`kafka_emitter.py:11`), testada e **exibida ao operador** na
  tela do namespace `routing` (`ROTEIRO_TESTES.md:209`). Editar não muda nada. ⚠️ Remover exige
  mexer no `smoke_config_routing_orphan_keys.sh:118`, que a usa como **CANÁRIO** de namespace
  intacto.
· **3 cópias restantes do `480_000`**, com donos diferentes: `instance_bootstrap.py:709` ·
  `mcp-server/tools/bpm.ts:281` · `config-api/seed.py:158`.
· **`supervisor.ts` tem 2ª dívida nomeada e não consertada:** o `urgency` compara ATENDIMENTO TOTAL
  com alvo de ESPERA. Não mede nada. Não alterado por ser contrato de tool MCP com consumidores não
  mapeados — **mapeá-los é a fatia**.
· ⚠️ **`duration_ms` do segmento humano diverge da janela dos próprios carimbos** (26 448 ms/80 s;
  25 519 ms/60 s, duas medições). **Voltou a NÃO TER CAUSA** — a explicação "sai do último F5" caiu
  junto com a refutação.
· **`sac_ia` tem 2 esperas de 0 ms** — a espera fantasma da Mudança 37, que consta como corrigida.
  Ou é dado pré-correção, ou a correção não pegou tudo.
· **Exibir tempo de contato / espera-antes-do-agente no Console** — levantado pelo operador, SEM
  decisão. São 3 fatos distintos no modelo (segmento · contato · espera) e a tela mostra 1.
· `admission.py:267` apaga `first_queued` sem emitir, para sessões fechadas no buffer MUDO — 7º
  ponto de saída, fora dos 6 call sites. A fila ATENDIDA não tem backstop equivalente.

═══ INCONFORMIDADE HERDADA, ainda aberta ═══
**Console não libera a tela após `Transfer`** (relatada 2026-08-24). Diagnóstico JÁ FECHADO por
refutação — não repetir: **não é wrap-up e não é config de dispatch** (o pool está medido em
`detached`, e a tela permanece mesmo após o wrap-up finalizado) ⇒ **teardown ausente no caminho do
`Transfer`**. Abre por: `Transfer` e `Close` compartilham o teardown do Console, ou são dois
caminhos? ⚠️ Conserto provavelmente em DOIS apps (o `agent-assist-ui` vive). ⚠️ Não testado:
transfer com o cliente ainda CONECTADO. Registro em `TODO.md` § *Analytics e UI*.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de SETE passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduos do defeito 2: linhas órfãs `queue_default_*` no config store;
  `queue_config.skill_id` legado do `retencao_humano`
```
