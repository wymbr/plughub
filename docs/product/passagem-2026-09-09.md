# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-09-08.md`](passagem-2026-09-08.md), que está **encerrada**.
>
> A sessão anterior fechou o **passo 1** (TTL de `pool_config`). Esta fechou o **passo 2**
> (medição + decisão) e o **2b** que o passo 2 revelou — dois gates novos, vermelho→verde num
> deles, e **nenhuma linha de código de serviço tocada**.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes, mas **todo dado medido carrega carimbo
> `2026-08-25`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: seguir a ORDEM PROPOSTA no topo do `TODO.md`
(§ "🧭 Ordem de trabalho PROPOSTA"). Os passos 1, 2 e 2b estão **FECHADOS**; o próximo é o
**passo 3 — F4 (visão 2 de processos) + os resíduos da F3**, na mesma passada. É a primeira
fatia de TELA desta linha de trabalho, e é maior que as três anteriores juntas.

⚠️ **A ordem é PROPOSTA, não decidida — e ela se confere.** Leia a seção inteira antes de pegar
o passo 3. **Não repita o plano aqui nem em outro arquivo** — uma casa só.

⚠️ **Ela JÁ SE CORRIGIU DUAS VEZES.** Na sessão de 08-25 (manhã), a regra do `sla_unstamped`
estava incompleta. Nesta, o passo 2 descobriu um **terceiro** bloqueio que o F4 não declarava —
pertença — e ele precedia os dois declarados. **Ao fechar um passo, pergunte o que a resposta
tornou visível**, não só se o passo fechou.

Alternativas fora da ordem, se o dono preferir: (Console/Transfer) a inconformidade herdada — a
única com operador esperando; ou (lacuna 2) o teste vermelho do `claimed_via`.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Já se pagou ONZE sessões seguidas.
  Nesta, a passagem anterior dizia que o `delegate = 0` era "não explicado" e apontava
  `webhook.py:1604` — o carimbo estava em `:1610`/`:1646` e a explicação estava escrita em DOIS
  comentários do próprio repo. Um grep antes de medir teria poupado a pergunta inteira.
· **AS TRÊS BASELINES, se você for tocar em código de serviço:**
    $DC exec -T routing-engine sh -c "cd /app/packages/routing-engine && python3 -m pytest src/plughub_routing/tests -q 2>&1 | tail -3"
    $DC exec -T analytics-api sh -c "cd /app/packages/analytics-api && python3 -m pytest src/plughub_analytics_api/tests -q 2>&1 | tail -3"
    $DC exec -T orchestrator-bridge sh -c "cd /app/packages/orchestrator-bridge && python3 -m pytest src/plughub_orchestrator_bridge/tests -q 2>&1 | tail -3"
  Esperado (medido em 08-25 de manhã, **não re-medido nesta sessão**): **1 failed, 294 passed**
  (o vermelho é o do Backlog 3, conhecido) · **607 passed** · **96 passed**.
  ⚠️ Esta sessão **não** as rodou, e a omissão foi deliberada: nada de código de serviço mudou
  (um YAML de skill, sete arquivos em `infra/test/`, três docs). Se a sua fatia é a F4, ela mexe
  em `platform-ui` — e aí a baseline que importa é o **build do front**, não estas três.
· ⚠️ **O AMBIENTE SEGUE MEXIDO, e continua sendo bom que siga.** `retencao_humano` tem
  `queue_config.pool_id = fila_humano` (fila ATENDIDA), `max_wait_s: 1800`, `sla_target_ms: 300000`.
  Não reverta. Confira:
    python3 infra/test/q_queue_config_inventory.py
  Esperado: 36 pools · none 35 · legacy_only 0 · **addressable 1**. Nunca ao YAML (seed-if-absent).
· 🔴 **E NUNCA rode `REGISTRY_SYNC_RECONCILE=true` para publicar um skill.** Ele reaplica o YAML
  sobre **skills, pools E deploy-slots do tenant inteiro** e reverteria exatamente a config acima.
  O martelo cirúrgico existe: `bash infra/test/redeploy_limite_entrada.sh` (um skill, um pool),
  ou `infra/test/_publish_skill.py <arquivo.yaml> <pool>` para qualquer outro par.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **`$DC exec` no clickhouse EXIGE `-T`** — o serviço tem `tty: true` e o `< /dev/null` colide
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **script em `infra/test/` roda do HOST** — não precisa de build; `.py` auxiliar entra no
  container por `$DC cp` (sobrevive a `restart`, NÃO a `up -d`)
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **PREFLIGHT DE SÍMBOLO antes de medir**, e ESTRUTURAL, nunca `grep`
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· ⚠️ **crase dentro de string com aspas duplas em bash é substituição de comando** — mordeu
  nesta sessão (`offer: command not found` dentro da própria mensagem de erro)
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· CH 23.8 **não faz subquery correlacionada** — use JOIN; prefira joins binários ANINHADOS
· CH é alcançável do HOST em `http://localhost:8123`, user/pass `plughub`
· portas: analytics-api 3500 · agent-registry 3300 · **config-api 3600** · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 · auth-api 3202
  · agent-assist-ui (app LEGADO, vivo) 5173

═══ O QUE ESTA SESSÃO COMPROU ═══
· **"RODAR O FLUXO QUE MENCIONA O CAMPO" ≠ "ATINGIR A LINHA QUE O ESCREVE", E O ZERO DAS DUAS É
  IDÊNTICO.** O plano mandava *"rodar o cenário `limite_entrega` e reler `spawn_reason`"* — não
  podia funcionar: o rótulo nasce no ENGAJAMENTO (`handle_collect_engage`), não no parqueamento,
  e o `collect` é lazy. Ler o produtor ANTES de rodar mostrou isso em cinco minutos. **Antes de
  rodar um cenário para popular um campo, leia o CALL SITE, não o arquivo do fluxo.**
· **CONTE QUANTAS AUSÊNCIAS DIFERENTES DÃO O MESMO ZERO.** Eram QUATRO no `spawn_reason`:
  produtor mudo · cenário não rodado · rodado e parado antes do clique · sessão ainda aberta (o
  carimbo só chega no fechamento). Cada uma virou ramo nomeado do gate. Com um veredicto só, três
  delas teriam sido lidas como a primeira.
· **UM GATE QUE SÓ EXERCITA O RAMO QUE FUNCIONA NÃO PODE REPROVAR O RAMO QUE NÃO FUNCIONA.** A F1
  constava ✅ *"provada por aresta ativa em `journey_aliases`"*. A aresta existia — vinda do ramo
  `auto`. O ramo `offer` nunca uniu nada, por onze dias, com o gate verde ao lado.
· **PROVAR UM PROVA A FORMA, NÃO O NÚMERO.** A decisão do dono foi *"são N acessos"*; o gate
  ganhou uma SEGUNDA consulta de propósito. O modo de falha que só ela pega (pertença chaveada por
  cliente, ou token de merge consumido no primeiro uso) passaria no passo anterior.
· **O INSTRUMENTO ERRA COM A CARA DO ALVO.** Três defeitos meus se disfarçaram de defeito do
  produto nesta sessão: leitor que fatiava texto num `yaml_snapshot` que é objeto JSON (e varria
  `previous` junto com `current`); cliente WS que só respondia `interaction.request` e ficava mudo
  numa pergunta de texto livre — **o probe acusou o fluxo**; e a crase no bash. **Quando o
  veredicto acusar o alvo, verifique primeiro se o instrumento chegou até ele.**
· **PERTENÇA É FATO DE OUTRO ESCOPO QUE POLÍTICA.** O conserto do 2b não mudou lógica nenhuma —
  mudou de QUAL fato o merge depende. Amarrado à `policy`, ele fazia um subconjunto arbitrário
  dos acessos virar membro.

═══ ERROS DE PREVISÃO (muitos acertos, 3 erros) ═══
Acertos: `collect` 0→1 · a linha fechando em 1 s · passo 1 = `ANTIGO` no vermelho-primeiro · os
quatro HTTP 200 do redeploy · passo 1 = `NOVO` depois · aresta ativa com actor certo · relatório
2 e depois 3 · `delegate` seguindo 0.
❌ **ERRO 1: previ `ANTIGO` e veio `INDETERMINADO`.** O `yaml_snapshot` é objeto JSON, não texto
   YAML — meu fatiador não tinha o que fatiar. O leitor **avisou que era ele**, e foi o que
   salvou o diagnóstico.
❌ **ERRO 2: previ que o cliente WS responderia a pergunta do CPF.** Menu `interaction: text` sem
   `masked_fields` NÃO vira `interaction.request` (`menu.ts:167-168`) — chega como mensagem comum
   e espera `msg.text`. São DUAS superfícies de pergunta, e eu conhecia uma.
❌ **ERRO 3: previ ✅3/❌3 no vermelho; veio ✅2/❌3.** Dois passos imprimem `✓` sem incrementar o
   contador. O que importava (quais ❌) bateu exato — mas **prever na unidade do instrumento
   significa prever o número que ele IMPRIME**.

═══ ENTREGUE — não re-fazer ═══
**(passo 2 — medição)** `spawn_reason='collect'` **tem produtor**, provado ponta a ponta (0→1).
O zero era ausência de população. `delegate = 0` é **DESENHO** (`delegate()` sempre roda como
conferência, reusando o `session_id` do pai) — não re-medir. Texto do F4 decidido pelo dono:
**acessos do cliente, e são N**. Gate: `infra/test/probe_spawn_reason_collect.sh` (8/0) +
`infra/test/_ws_engage.py`.
**(passo 2b — pertença)** `avaliar_pendencia(found)` → `unificar_journey` →
`avaliar_politica_retomada` em `skill_limite_entrada_v1.yaml`; ramo `auto` vai direto a
`retomar_resultado`. Um site só. Gate `infra/test/probe_journey_merge_status_access.sh`
**vermelho→verde** (✅2❌3 → ✅6❌0) + `_ws_chat.py` (cliente webchat scriptado, duas superfícies).
Deploy cirúrgico: `redeploy_limite_entrada.sh` + `_publish_skill.py`.
As-built dos dois no `CHANGELOG.md`; ADR do histórico unificado atualizado (a tabela de pertença
agora confere com o código).

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
`spawn_reason` no tenant: `<NULL>` **450** · `trigger` **110** · `collect` **1** · `delegate` **0**
`journey_aliases`: 15 → **18** (as três do gate de 2b)
`/reports/journeys` do processo do gate: **3** (processo + 2 consultas)
suítes: **não re-medidas nesta sessão** (nada de código de serviço mudou) — ver ANTES DE TUDO

═══ AS OPÇÕES DE TAREFA ═══
**(passo 3 — F4 + resíduos da F3)** a fatia da ordem. Visão 2: pivô, árvore/cronologia num
  componente com toggle, internas dobradas. Resíduos da F3, na MESMA passada porque são a mesma
  superfície: `/analise/sessions?session_id=…` ignorado (três telas linkam pra lá), filtro por
  direção de verdade (parâmetro novo sobre `spawn_reason`), e duas páginas mortas
  (`ContactsPage.tsx`, `AnaliseContatosPage.tsx`).
  ⚠️ **O cabeçalho conta ACESSOS DO CLIENTE** (`spawn_reason` NULL/`collect`), não sessões. As
  internas são etapa dobrada, nunca linha de contagem.
**(Console/Transfer)** a inconformidade herdada abaixo — a única com operador esperando.
**(lacuna 2)** o teste vermelho do `claimed_via` — ver Backlog 3. Não é trocar a string.

═══ NÃO fazer agora ═══
· **`REGISTRY_SYNC_RECONCILE=true`** — ver ANTES DE QUALQUER COISA. Reverteria a config do
  `retencao_humano` sem avisar.
· **Re-medir o `delegate = 0`.** É desenho, está explicado em dois comentários do repo.
· **Rodar o cenário `limite_entrega` esperando ver `collect` aparecer sozinho.** Sem clique não
  nasce sessão, e a entrega do link não existe.
· **Baixar o TTL de `pool_config` alegando limpeza** (a limpeza é por DELETE explícito).
· **"Consertar" o vermelho do `claimed_via` trocando `"semaphore"` por `"record"`.** Backlog 3.
· **Mexer na época `SEGMENT_SLA_EPOCH`** para "recuperar" a série. Não volta.
· **Migrar esperas antigas** para carimbar o alvo retroativamente. Impossível por construção.
· **Ramificar o carimbo de SLA por `agent_kind`** — decidido e gravado em teste.
· **"Consertar" a multiplicidade de segmentos por F5** — REFUTADO (Mudança 39).
· **O `queue-agent` do bridge** (`main.py:5993`). Inalcançável hoje.

═══ Backlog NOVO / reaberto ═══
· 🆕 **`agente_portabilidade_intake_v1` tem o MESMO defeito de ramo que o 2b acabou de consertar**
  — zero ocorrências de `journey_merge`, vai de `avaliar_politica_retomada` direto a
  `retomar_processo`. Estava no `TODO.md` como *"fatia pequena, fora do caminho crítico"*, e a
  medição do 2b mostra que a classe do defeito é maior do que "faltou num skill". **Mesma
  correção, mesmo gate parametrizado.** Primeira pergunta: quantos skills mais resolvem pendência
  e ramificam por `policy`?
· 🆕 **O cliente que CLICA no link do `collect` não recebe nada.** A sessão roteia para
  `limite_retorno`, que ramifica em `@ctx.session.customer_present` — tag que
  `handle_collect_engage` **não semeia**. Medido: `complete resolved` em 1 s, tela vazia. Hoje
  mascarado porque ninguém clica (entrega de link é trilha não construída) — **dois defeitos se
  cobrindo**, e consertar a entrega desmascara o outro. ⚠️ **É DECISÃO, não obviedade:** o
  discriminador foi desenhado para falhar ADIANDO; ligar a tag troca o lado seguro do erro.
· 🆕 **`handle_delegate` + rota `/v1/channels/webhook/delegate` = caminho morto com rota VIVA.**
  Nada o chama (`delegate()` é sempre conferência). Decidir entre remover e declarar.
· 🆕 **`main.py:960` promete o que a função não faz** — o docstring de `POST …/webhook/collect`
  diz *"creates a ROUTED child contact session"* e o handler é lazy. Promessa sem produtor, do
  lado do docstring de rota.
· 🆕 **Entrega real do link do `collect`** (`webhook.py:2055`, `TODO(J4c fase 2)`). Enquanto não
  existir, `collect` só cresce por gate — o produtor funciona e a classe não tem população real.
· 🆕 **Os DOIS probes novos não são chamados por runner nenhum** — como quase todo gate em
  `infra/test/`. Mesma dívida do `gate_pool_config_ttl_source.sh` e da Camada F.
· 🔴 **3. `test_expire_returns_the_slot_even_after_the_lease_expired` VERMELHO, e é ANTERIOR a
  cinco sessões atrás.** Asserta `claimed_via == "semaphore"`; o código devolve `"record"`, porque
  a emenda **D6** (2026-08-04) inseriu o registro durável de posse ANTES do semáforo
  (`router.py:1151`, e `:1157` é o ramo do semáforo). ⚠️ **Não é cosmético:** `router.py:1158`
  declara que a linha do `semaphore` é *"a MEDIDA da lacuna 2"* — com o `record` respondendo antes,
  esse ramo ficou inalcançável neste cenário. **Consertar = decidir onde vive a medida da lacuna 2.**
· 🆕 **`wrapup_hold_ttl_s` é LEITOR SEM CHAVE** (`kafka_listener.py:411`) — resolve sempre para o
  `90` inline e não aparece na tela. Semear, ou assumir o 90 e tirar o `.get`. **Decidir antes.**
· 🆕 **`queue_default_agent_type_id` / `queue_default_skill_id` continuam no config store.**
  Saíram do código e do seed em 08-24; o store é DB-owned. Conserto = DELETE pela API oficial.
· 🆕 **`ai_gateway_ttl_s` e `channel_gateway_ttl_s` no namespace `session`, ignoradas pelo bridge.**
  Não medi se ai-gateway/channel-gateway as leem. **Medir antes de concluir qualquer coisa.**
· 🆕 **`sla_unstamped` existe na API e NÃO tem superfície na UI.**
· 🆕 **`_cv_sla_series` IGNORA o retorno de `_apply_pool_scope`.** NÃO consertado de propósito: é
  ABAC e o JWT usa `[]` com o sentido OPOSTO. Medir antes de mexer.
· 🔴 **`sla_default_ms` NÃO TEM LEITOR** — ⚠️ remover exige mexer no
  `smoke_config_routing_orphan_keys.sh:118`, que a usa como **CANÁRIO** de namespace intacto.
· 🆕 **`estimated_wait_factor` (0.7) e `congestion_sla_factor` (1.5)** — 2º e 3º órfão do
  namespace `routing`: `main.py` fixa `* 0.7` e `saturated.py` fixa `* 1.5`.
· 🆕 **`max_queue_len` é declarado no front e NUNCA lido** (`AnalisePoolsPage.tsx:55`).
· 🆕 **`query_pools_queue` engole a exceção** e devolve `data: []` + `error`, indistinguível de
  "não há dado" na tela. O gate o pega; a tela, não.
· **3 cópias restantes do `480_000`**: `instance_bootstrap.py` · `mcp-server/tools/bpm.ts:281` ·
  `config-api/seed.py:158`.
· **`supervisor.ts` — `urgency` compara ATENDIMENTO TOTAL com alvo de ESPERA.** Contrato de tool
  MCP com consumidores não mapeados — **mapeá-los é a fatia**.
· ⚠️ **`duration_ms` do segmento humano diverge da janela dos próprios carimbos** (26 448 ms/80 s;
  25 519 ms/60 s). **Sem causa** — a explicação "sai do último F5" caiu com a Mudança 39.
· **`sac_ia` tem 2 esperas de 0 ms** (Mudança 37, consta corrigida). Ou é dado pré-correção, ou a
  correção não pegou tudo.
· **Exibir tempo de contato / espera-antes-do-agente no Console** — levantado pelo operador, SEM
  decisão. São 3 fatos distintos (segmento · contato · espera) e a tela mostra 1.
· `admission.py:267` apaga `first_queued` sem emitir, p/ sessões fechadas no buffer MUDO — 7º
  ponto de saída, fora dos 6 call sites. A fila ATENDIDA não tem backstop equivalente.
· **`_emit_queue_timeout` publica em outbound e `conversations.events` SEM `key=`.**
· **`probe_journey_limite.sh` não pode reprovar na dimensão de F1** — conta por proveniência
  enquanto `/reports/journeys` conta proveniência ∪ alias. Usá-lo como gate de journey compra
  confiança sem dar nada. *(O 2b acabou de mostrar o custo real dessa família.)*

═══ INCONFORMIDADE HERDADA, ainda aberta ═══
**Console não libera a tela após `Transfer`** (relatada 2026-08-24). Diagnóstico JÁ FECHADO por
refutação — não repetir: **não é wrap-up e não é config de dispatch** (o pool está medido em
`detached` **no agent-registry**, não no YAML) ⇒ **teardown ausente no caminho do `Transfer`**. Abre
por: `Transfer` e `Close` compartilham o teardown do Console, ou são dois caminhos? ⚠️ Conserto
provavelmente em DOIS apps (o `agent-assist-ui` vive na 5173). ⚠️ Não testado: transfer com o
cliente ainda CONECTADO. Registro em `TODO.md` § *Analytics e UI*.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de TREZE passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduo do defeito 2: `queue_config.skill_id` legado do `retencao_humano`
· F5 do arco de histórico (`ContextStorePersister`) — fase própria, desenho fechado no ADR §3
```
