# Prompt de passagem — próxima sessão

> 🔒 **ENCERRADA.** Sucedida por [`passagem-2026-08-26-01.md`](passagem-2026-08-26-01.md), que
> inaugura a convenção `passagem-AAAA-MM-DD-NN.md` — **data real** + contador da sessão no dia.
> Esta e todas as anteriores (sem sufixo `-NN`) são da série sequencial antiga, cujo **nome não é
> a data**: este arquivo foi escrito em **2026-08-26**, e existe um `passagem-2026-08-26.md` que é
> de uma sessão muito anterior. Ao procurar a passagem de um dia, confie no carimbo do dado.
>
> ⚠️ **Duas coisas escritas aqui foram REFUTADAS por medição na sessão seguinte:**
> `admin com accessible_pools = []` (são **5 pools**), e a "triagem de UM CLIQUE" do chip de
> PROCESSO, que não pode responder o que promete (o cabeçalho da lista é sempre
> `meta.total_contacts` — `ListaTab.tsx:45`). Ver a sucessora.

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-09-11.md`](passagem-2026-09-11.md), que está **encerrada**.
>
> A sessão anterior abriu o arco "ContextStore como ALLOWLIST" a pedido do dono. Esta
> **escreveu o ADR** (com o modelo mudando em três pontos na conversa com o dono) e
> **entregou as duas primeiras fases, V0 e V1**, cada uma com gate visto vermelho
> antes de verde.
>
> ⚠️ **Nota de data, terceira vez:** o arquivo segue a série de nomes, mas **todo dado
> medido carrega carimbo `2026-08-26`**. A série já drifta quase um mês. Onde houver
> conflito, **confie no carimbo do dado** — e considere consertar a série de nomes.

---

```
continuação sessão plughub. TAREFA: o arco ALLOWLIST está com ADR escrito e as duas
primeiras fases entregues. **O dono NÃO declarou a próxima** — a sessão terminou com
três itens abertos e nenhum deles é o "óbvio próximo". Leia o ADR e escolha COM ele:

    docs/adr/adr-contextstore-allowlist.md   (status: proposto; §6 = as fases V0–V5)

Os três abertos, em ordem de custo:
  (a) COLUNA PROCESS em /analise/sessions sumiu — falta UM CLIQUE para triar (abaixo)
  (b) SEGUNDA PORTA: o tool `supervisor_state` devolve o ctx CRU. Medido: ZERO skills
      o consomem ⇒ fechar é barato AGORA e caro quando algo depender dela
  (c) V2 — catálogo de TIPOS. É a fatia que destrava a outra metade da V0 e o mapa

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Já se pagou CATORZE sessões
  seguidas. Nesta, duas referências herdadas do `TODO.md` estavam **obsoletas em ~215
  linhas** (`server.ts:1421` e `:1452` — o arquivo deslocou quando a F5 inseriu
  `/internal/context-snapshot` em `:1524`). **Número herdado que você não conferiu é
  lembrança, não medição.**
· **AS QUATRO BASELINES, se for tocar código de serviço** (⚠️ **NÃO re-medidas em
  08-26 nesta sessão** — a sessão tocou TypeScript do mcp-server e o front, que não
  têm suíte nas baselines; `build` verde é compilação, não teste):
    $DC exec -T analytics-api sh -c "cd /app/packages/analytics-api && python3 -m pytest src/plughub_analytics_api/tests -q 2>&1 | tail -3"
    $DC exec -T orchestrator-bridge sh -c "cd /app/packages/orchestrator-bridge && python3 -m pytest src/plughub_orchestrator_bridge/tests -q 2>&1 | tail -3"
    $DC exec -T session-replayer python3 tests/test_context_persister.py
    $DC exec -T routing-engine sh -c "cd /app/packages/routing-engine && python3 -m pytest src/plughub_routing/tests -q 2>&1 | tail -3"
  Esperado: **617 passed** · **102 passed** · **7/7** · **1 failed, 294 passed** (o
  vermelho é o do Backlog 3, conhecido). routing-engine **não é re-medido há 2 sessões**.
· ⚠️ **O AMBIENTE SEGUE MEXIDO, e continua sendo bom que siga.** `retencao_humano` com
  `queue_config.pool_id = fila_humano`, `max_wait_s: 1800`, `sla_target_ms: 300000`;
  admin com `accessible_pools = []`. Não reverta. Confira:
    python3 infra/test/q_queue_config_inventory.py
  Esperado: 36 pools · none 35 · legacy_only 0 · **addressable 1**. Nunca ao YAML.
· 🆕 **PODE haver seed de teste sujando o ctx.** `infra/test/seed_withheld_demo.sh`
  injeta `caller.nome` + `session.demo.resume_token` e **NÃO limpa sozinho**. Se vir
  "Maria Demo" num contexto, é isto. Limpe com `<script> <sid> --clean`.
· 🔴 **E NUNCA rode `REGISTRY_SYNC_RECONCILE=true` para publicar um skill.**
  Martelo cirúrgico: `bash infra/test/redeploy_limite_entrada.sh`, ou
  `infra/test/_publish_skill.py <yaml> <pool>`.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — e N linhas coladas de uma vez NÃO são N comandos: `A=… TOK=$(…) cmd`
  vira atribuição PREFIXANDO `cmd`. Cadeia de curl com token → **vira script em `infra/test/`**
· $DC não vem definido, e o compose fica na RAIZ:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· ⚠️ **o serviço do MCP chama-se `mcp-server-plughub`** — `mcp-server` NÃO resolve
· **`$DC exec` no clickhouse EXIGE `-T`**
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· ⚠️ **arquivo NOVO só entra na imagem com `build --no-cache`**; editar existente entra normal
· **script em `infra/test/` roda do HOST** — não precisa de build nenhum
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS
· **no front há DOIS caches** — depois do `up -d`, `Ctrl+Shift+R` é obrigatório
· **PREFLIGHT DE SÍMBOLO antes de medir**, ESTRUTURAL (`import … ; print(callable(…))`),
  nunca `grep`
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO —
  e o exit tem de refletir isso
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· ⚠️ **alias de agregado NUNCA repete nome de coluna real** (`ILLEGAL_AGGREGATION`, 184)
· portas: analytics-api 3500 · agent-registry 3300 · **config-api 3600** · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 ·
  auth-api 3202 · agent-assist-ui (LEGADO, vivo) 5173

═══ O QUE ESTA SESSÃO COMPROU ═══
· 🔴 **UMA HIPÓTESE HERDADA PODE APONTAR UM PASSO ADIANTE DO DEFEITO.** O briefing dizia
  que a tela de Masking mostrava `0 regras` por causa do `??` em `MaskingPage.tsx:160`.
  Esse `??` **nunca chegava a ser exercido**: o `fetch` recebia `index.html` e o
  `safeJson` lançava antes. Consertar a hipótese teria produzido um fix plausível que
  não muda nada. **Medir antes de consertar não é ritual — é o que separa as duas.**
· 🔴 **MEIA CORREÇÃO PARECE PRONTA.** O mesmo bloco de nginx já fora consertado uma vez,
  raciocinando **só sobre ESCRITA** (`PUT …/context_rules`, 2 segmentos). A LEITURA de
  namespace também tem 1 segmento, igual à rota de página — e ficou quebrada, com o
  comentário do conserto anterior por cima, parecendo resolvido. **Quem separa por
  contagem de segmentos tem de perguntar quais VERBOS têm aquela forma.**
· 🔴 **ESCREVI UM RAMO QUE NÃO PODIA REPROVAR — DENTRO DO GATE FEITO PARA PEGAR ESSA
  FAMÍLIA.** `select(($s|keys) | index(.) != null)` é SEMPRE verdadeiro: dentro de
  `index(.)` o `.` é reavaliado contra a entrada do pipe (o próprio array), acha na
  posição 0, e `0 != null` passa. Acusou "3 tags em ambos" com os conjuntos disjuntos.
  Conserto: `as $tag` + **preflight do próprio detector** (hit=1 / miss=0, senão
  INCONCLUSIVO).
· 🔴 **E FOI OUTRO RAMO QUE O PEGOU.** `E` (lista) e `F` (aritmética) medem a mesma
  propriedade por caminhos diferentes, e a **discordância entre eles** foi a evidência.
  Um gate só com o E teria publicado um defeito inexistente. **Quando uma propriedade
  importa, meça-a por dois caminhos — a redundância não é desperdício, é o detector de
  instrumento cego.**
· **DEV E CONTAINER DIVERGINDO É ESCONDERIJO.** O proxy do vite discrimina por `Accept`
  desde sempre; o defeito de rota **não existia em desenvolvimento**. Quando um defeito
  só aparece no container, a primeira pergunta é o que o DEV faz de diferente.
· **O DANO INVISÍVEL VEM JUNTO COM O VISÍVEL.** As regras de categoria morriam no MESMO
  fetch e caíam em `DEFAULT_DISPLAY_RULE` — e **default parece certo**. Só o contador de
  regras do ContextStore denunciou, porque `0` não é um valor plausível.
· 🔴 **`admin` NÃO JULGA POLÍTICA DE MASKING.** Ele é `supervisor_role`: pula o portão e
  não casa regra de papel `operator`. A faixa da V1 **não aparece** para ele, e está
  certo. Testar como `operator`, sempre. (Eu mandei "abra a aba Contexto" sem dizer o
  papel — instrução incompleta que custou uma rodada.)
· **O CONSOLE SÓ LISTA CONTATOS DO AGENTE LOGADO.** Não existe "abrir o contato de outro
  agente". Para ver política na tela, o contato tem de NASCER no papel que se quer testar.
· **INJEÇÃO FORA DE BANDA NÃO ACORDA TELA JÁ CARREGADA.** `HSET` direto no Redis não
  publica `supervisor_state.updated` ⇒ só aparece após refresh. Não é defeito; é
  propriedade do atalho de teste.

═══ ERROS DE PREVISÃO (acertos e 3 erros) ═══
Acertos: **3/3 exatos** no probe de rota (`5174/masking → 200 text/html` ·
`5174/audit_policy → 200 application/json` · `3600/masking → 200 application/json`) ·
interseção página×namespace = **{masking}**, só ela · ramos A/B/C/D/F do
`probe_context_withheld` · a faixa na tela com as duas causas separadas.
❌ **ERRO 1: previ `OK` na 1ª execução pós-build do `probe_context_withheld`.** Saiu
   REPROVADO — por defeito do **meu próprio ramo E**, não do código. Prever o resultado
   de um instrumento que você acabou de escrever é prever duas coisas ao mesmo tempo.
❌ **ERRO 2: mandei o operator "abrir o contato no Console".** Impossível — o Console só
   lista os contatos DELE. A instrução assumia uma capacidade que o produto não tem.
❌ **ERRO 3: não previ o refresh.** Disse "a aba refaz o fetch em `supervisor_state.updated`"
   sem notar que o `HSET` do seed não publica evento nenhum.

═══ ENTREGUE — não re-fazer ═══
**(ADR)** `docs/adr/adr-contextstore-allowlist.md`, **proposto**. Fecha as 7 perguntas do
briefing. Modelo decidido COM o dono, e mudou em três pontos na conversa: **(1)** categoria
abstrata virou **TIPO** — declaração única com formato × máscara-por-papel × classe LGPD,
qualquer uma podendo ser vazia (funde as TRÊS METADES medidas: `MaskingRule`+regex tem
detecção sem papel/canal; `MaskingDisplayRule` tem canal sem papel; `ContextMaskingType` tem
papel sem detecção/canal); **(2)** hierarquia de negócio entra na CHAVE, em
`escopo.dominio.campo` — o escopo FICA no 1º segmento porque hoje ele roteia hash+TTL em 3
casas; **(3)** legado vira **alias contado e datado** no próprio nó do mapa, com a NOVA como
canônica. Mais: **quatro políticas, um vocabulário** (W escrita · R-agente · R-humano · P
persistência).

**(V0)** Colisão de rota no nginx do platform-ui: `masking` é nome de página **e** namespace,
e `location` casa a URI **sem a query string**. Discriminador passou a ser o `Accept`
(igual ao vite). Gate `probe_config_route_collision.sh` — lê a lista de páginas do PRÓPRIO
Dockerfile e a de namespaces do PRÓPRIO config-api (colisão nova reprova sozinha), com
testemunha de presença e **contra-teste** (o conserto ingênuo passaria no ramo principal e
quebraria a navegação).

**(V1)** `applyContextMaskingDynamic` devolve `MaskedContextView` (`entries`+`total`+
`by_rule`+`by_pool_scope`); endpoint publica `customer_context.context_withheld`; aba
Contexto ganha a faixa *"N campo(s) oculto(s) por política"* nomeando as tags. **Duas listas**
porque as causas se consertam em telas diferentes. Ocultos **não** entram em `entries` (dois
números, dois significados — confirmado na tela: "3 fields" com 2 retidos). `agent.*` fora do
total. Gate `probe_context_withheld.sh` (6 ramos; **D** = testemunha negativa, **F** =
aritmética) + `seed_withheld_demo.sh` (mesma injeção, sem limpar, para olhar a tela).

**(colaterais)** `TODO.md` § ARCO PROPOSTO virou ponteiro para o ADR, preservando a evidência ·
linha do ADR no índice do `CLAUDE.md` · duas referências obsoletas corrigidas.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
rota: **12 páginas SPA × 17 namespaces**, interseção = `{masking}` · testemunha `agent_activity`
withheld (sessão `f0427a16…`, operator): **total 13 = 10 entregues + 1 by_rule + 2 by_pool_scope**
· admin: **0 e 0**
masking: **23 regras** em vigor, visíveis na tela após a V0
inventários de categoria: **3 discordam** — enum 7 · com regex **4** · tela **6** (com `iban`
e `passport`, que não existem em lugar nenhum e mostram selo "Ativo" incondicional)
blast radius do ctx: **486 ocorrências em 97 arquivos**, ~40 deles YAML de skill
escrita no ctx: **12 `HSET` diretos** em produção (bridge 6 · channel-gateway 4 · routing 2)
`applyContextMaskingDynamic`: **1** call site · tool `supervisor_state`: ctx **CRU**, **0 skills**
`/analise/sessions` 19–26/08: **84 contatos** (a passagem anterior mediu **108** — ver backlog)

═══ NÃO fazer agora ═══
· **`REGISTRY_SYNC_RECONCILE=true`** — reverteria a config do `retencao_humano`.
· **Inverter para deny-by-default (V4) antes da V3.** A V1 pagou o pré-requisito, mas a
  lista real de campos ainda não existe — virar a chave por adivinhação apaga tela em
  produção, não em teste.
· **"Consertar" a tela do admin porque a faixa não aparece.** É o desenho.
· **Trocar `maskContextForPersistence` por `applyContextMaskingDynamic`** — o portão de
  namespace NÃO pode podar o registro durável.
· **Apagar a rota de SPA do `/config` "porque o fetch precisa do proxy".** Passa no ramo
  principal do gate e quebra a navegação — o contra-teste existe para isso.
· **Mexer em `accessible_pools` por `psql`** — use `infra/test/set_user_pools.sh`.
· **Mexer na época `SEGMENT_SLA_EPOCH`** para "recuperar" a série. Não volta.
· **Apagar `AnaliseTab.tsx` "porque está morto".** É uma FEATURE sem consumidor.
· **Apagar `useAllConfig` "porque não tem consumidor".** Mesmo caso — mas saiba que
  `GET /config` (sem segmento) também cai no SPA e devolveria HTML se alguém a usasse.

═══ Backlog NOVO / reaberto ═══
· 🔴 **COLUNA PROCESS sumiu de `/analise/sessions`** (relatado pelo dono, ANTES do build
  desta sessão — nada que fizemos toca esse caminho). Sintoma: a coluna existe, todas as
  linhas em `—`, **inclusive** uma linha `limite processo → aprovacao credito` com ícone 🔗.
  E um segundo número mudou: **108 → 84 contatos** na mesma janela, o que o chip **não pode**
  causar. **TRIAGEM DE UM CLIQUE, faça-a primeiro:** marque *"Include internal sessions"* —
  se der **108**, a contagem está sã e o problema é só o chip; se ficar em ~84, mudou a
  POPULAÇÃO e o chip pode estar certo. São duas investigações diferentes.
· 🔴 **Outra metade da V0: os 3 inventários de categoria** (`iban`/`passport` fantasmas com
  selo "Ativo"; `address`/`health` no enum sem nunca disparar). Depende de qual lista vira
  canônica ⇒ é decisão da **V2**.
· 🔴 **Segunda porta:** tool `supervisor_state` devolve o ctx CRU (`tools/supervisor.ts:163-177`
  → `:380`). **0 skills o consomem** (só e2e 17, mock e skill-flow-service de teste) ⇒ barato
  agora, caro depois.
· 🆕 **Seed × config viva divergem, e agora dá para VER**: a tela lista
  `session.numero_cartao` → `session.cpf_titular` → `session.limite_solicitado`, e o
  `seed.py` traz `vencimento_cartao` nessa posição e **não** traz `cpf_titular`. ADR §D7.
· 🆕 **`agent-assist-ui` legado (5173) não ganhou a faixa da V1** — aditivo, não regride.
· 🔴 **`probe_f4_direction_and_classes.sh` sai `OK` com ramo INCONCLUSIVO** — CONFIRMADO por
  leitura nesta sessão: `:161` decide sobre `$fail`, e os ramos inconclusivos (`:114`, `:129`)
  usam `note`, que não toca `$fail`.
· 🆕 **A visibilidade do ContextStore é lida do pool de ENTRADA** (`server.ts:1636` +
  `:1654-1668`), não do que atende. Contato transferido aplica a política do pool de ORIGEM.
  Fatia C de `session-meta-ownership.md`, e o 1º consumidor em que a acepção errada custa
  **exposição de PII**.
· 🆕 **`catch { /* non-fatal — use default */ }` sem log** em `server.ts:1667`.
· 🆕 **Nenhum dos gates novos é chamado por runner** (`probe_config_route_collision`,
  `probe_context_withheld`, mais os quatro da sessão anterior) — dívida de quase todo
  `infra/test/`, agora com mais dois.
· 🔴 **3. `test_expire_returns_the_slot_even_after_the_lease_expired` VERMELHO**, anterior a
  oito sessões. `router.py:1158` declara aquele ramo como *a MEDIDA da lacuna 2*.
· 🆕 **`GET /config/dashboards?tenant_id=…` responde 404** — NÃO é a colisão de rota
  (`dashboards` não é nome de página); é outro defeito, no config-api.
· 🆕 **`wrapup_hold_ttl_s` é LEITOR SEM CHAVE** (`kafka_listener.py:411`).
· 🆕 **`sla_unstamped` existe na API e NÃO tem superfície na UI.**
· 🆕 **`_cv_sla_series` IGNORA o retorno de `_apply_pool_scope`.** É ABAC. Medir antes.
· 🔴 **`sla_default_ms` NÃO TEM LEITOR** — remover exige mexer no
  `smoke_config_routing_orphan_keys.sh:118`, que a usa como CANÁRIO.
· **3 cópias restantes do `480_000`** · **`supervisor.ts` compara atendimento total com alvo de
  espera** · **`duration_ms` do segmento humano diverge dos próprios carimbos** ·
  **`_emit_queue_timeout` publica SEM `key=`**.

═══ INCONFORMIDADES herdadas — estado ═══
· ✅ **Tela de Masking mostrando 0 regras** — FECHADA (V0).
· ✅ **Omissão muda no ContextStore** — FECHADA na porta HTTP (V1). Tool segue aberto.
· ⚠️ **Não testado:** transfer com o cliente ainda CONECTADO.
· ⚠️ **`recipients` do release saiu com UM elemento** nas duas execuções reais — a segunda
  forma de nomear o humano (participant_id) tem teste unitário e **nenhuma população real**.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-26 em busca de backlog órfão (herdada de DEZESSEIS)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduo do defeito 2: `queue_config.skill_id` legado do `retencao_humano`
· **lente C** (faixas por personagem) — **SUSPENSA por decisão do dono em 2026-08-26**
```
