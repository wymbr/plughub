# Prompt de passagem — sessão de 2026-08-27

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a de 2026-08-26, que fez o
> **reexame dos 9 em `Escopo reduzido`** (item 8 da passagem anterior), podou **11 seções fechadas**
> do `TODO.md` e o reagrupou em **15 temas**. A passagem [`passagem-2026-08-26.md`](passagem-2026-08-26.md)
> está **encerrada**.
>
> **Esta é mais curta que as anteriores de propósito.** O backlog saiu de dentro da passagem e foi
> para o `TODO.md`, que agora é a fonte única de trabalho pendente. Passagem carrega **regras +
> estado**; item que só existe na passagem morre com ela.

---

```
continuação sessão plughub.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **no FRONTEND são DOIS caches**: depois do build, `Ctrl+Shift+R` é OBRIGATÓRIO
· arquivo NOVO entra no build normal quando o Dockerfile copia o DIRETÓRIO
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· **janela de log só compara se as DUAS janelas existirem**; `--since` sem fuso é HORA LOCAL (−03)
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso**
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3; `curl` NÃO existe no ai-gateway (use o host)
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **nada de crase dentro de aspas duplas em bash** — vira substituição de comando
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server + skill-flow-worker JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · mailing-api 3660 · **redis 6380 · kafka 9093 · postgres 5433**
· health da analytics-api e do ai-gateway é **`/v1/health`**
· analytics-api usa **PyJWT** (`import jwt`); auth-api usa **python-jose**
· tabela de campanhas é `evaluation.campaigns`, PK `id` (não `campaign_id`)
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ AS SEIS REGRAS QUE 08-26 COMPROU ═══
· **SCRIPT QUE APLICA UMA DECISÃO NÃO É SCRIPT QUE A TOMA.** Prometi rodar
  `prune_todo_closed.py` "para ver o que sairia" — ele não detecta nada: a lista `CLOSED` é um
  registro manual de 2026-08-03. Rodou e removeu ZERO. O trabalho real era a conferência seção a
  seção contra o CHANGELOG. *Ferramenta com nome de detector pode ser um livro-caixa.*
· **TÍTULO QUE DECLARA CONCLUSÃO MENTE POR DENTRO — 8 DE 10.** As seções diziam ✅ no cabeçalho e
  carregavam pendência viva enterrada no corpo (a rodada anterior teve 3 de 7; a armadilha PIOROU).
  A remoção é por seção INTEIRA, então o resgate tem de ser feito ANTES, à mão.
· **ANTES DE RESGATAR, PERGUNTE SE JÁ TEM CASA.** Metade dos resíduos já vivia em outra seção
  aberta; movê-los teria criado duplicata — o defeito que a poda existe para evitar. Dois foram
  para dentro da § I5 (cujo cabeçalho já os nomeava), não para seção nova.
· **CAMPO MORTO PODE GATEAR TELA VIVA.** `review_workflow_skill_id` é config morta *e* é o gate do
  painel de curadoria do Arc 13 (`CampaignsPage.tsx:1359`). E como NENHUMA campanha tem o valor
  esperado, o painel **nunca renderiza**. "Morto" e "gateando algo vivo" não são exclusivos.
· **ARQUIVO ≠ ID.** `agente_revisor_v1.yaml:80` declara `id: skill_revisao_v1`. Quem apaga por nome
  de arquivo não vê o que sai do banco; quem apaga por id não acha o arquivo. Classificar por id,
  com o arquivo mapeado.
· **O CONTADOR DA MINHA PRÓPRIA FERRAMENTA SUBCONTAVA.** `len(out)` contava elementos de lista, não
  linhas, e reportou "+15" para 30 linhas físicas. *Valor plausível dentro do instrumento de
  medição* — o `wc -l` foi quem denunciou. Conserte o contador, não só o conteúdo.

═══ ESTADO (fechado em 2026-08-26, ver CHANGELOG) ═══
TODO.md: **5.470 → 4.355 linhas**, 79 seções em **15 temas** + 4 fixadas no topo
  (direção vigente · reexame · resíduos resgatados · erros de método).
  · 11 seções podadas (−1.145 linhas); R1–R8 resgatados antes da remoção
  · backlog que só existia na passagem foi MIGRADO para cá (7 seções novas)
  · dois scripts de manutenção: `prune_todo_closed.py` (corrigido) e
    `group_todo_by_theme.py` (novo — move byte a byte, aborta em padrão ambíguo)
REEXAME DOS 9: 4 cortes mantidos · 2 revertidos (12, 16) · 3 reatribuídos (15, 17, 55).
  Tarefas A0–A7 (item 9) · B1/B2 (produto) · C1–C4 (reatribuição) · D1 (itens 10 e 11).
E2E: `--demo` em **10/17**, INALTERADO — não rodei em 08-26.
  Vermelhos: 12 (produto) · 13/14/18 (sem endpoint — virou R4) · 15/16/17 (testes mortos).
CREDENCIAL LLM: **resolvida** — `/v1/health` responde `anthropic: ok`, `credentials: ok`,
  `last_ok_age_s` não-nulo. A seção do "401 em 124 de 124" foi podada por medição.

═══ PONTO DE ENTRADA — escolha um item do TODO.md ═══
Não há lista de tarefas nesta passagem por decisão: ela vive no `TODO.md`, agrupada por tema.
Quatro sugestões de maior retorno, com o porquê:

1. **A1 — trocar o discriminador do painel de curadoria** (§ Reexame dos 9). Destrava o A4 e o
   resto do item 9. Ficou BARATO: zero campanhas com `treplica`, logo não há migração de dado.
   Mas tem metade de produto embutida — destravar o painel é mudança de comportamento.
2. **R4 — 13/14/18 sem endpoint de webhook** (§ Resíduos resgatados). ⚠️ O achado importa mais
   que o conserto: os cenários **já não funcionavam** — a mudança trocou "201 sobre sessão morta"
   por "404 honesto". Trocar a URL sem conferir o que cada um assere devolve o verde falso.
   Endereçar por **pool**, nunca por skill.
3. **15/16/17 — três cenários testando o que não existe** (§ Infra de teste). Vermelho permanente
   ensina a ignorar a cor; e o 17 ainda tem três `pass()` de "skipped", que é o defeito inverso.
4. **B1/B2 — as duas decisões de produto reabertas** pela reversão (§ Reexame dos 9). B1 = nível
   (a) de Business in Any Media, sob a pergunta "quanto vira config + interpretador genérico".

⚠️ **Varredura que ficou por fazer:** as passagens de 08-19 a 08-25 podem ter backlog órfão pelo
mesmo mecanismo que a de 08-26 tinha. Não medi. É barato conferir e caro descobrir tarde.
```
