# Prompt de passagem — sessão de 2026-08-19

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a sessão de 2026-08-18,
> que fechou P1/P3/P4 e a F1 do P2 do
> [`handoff-segmentos-abertos-2026-08-18.md`](handoff-segmentos-abertos-2026-08-18.md).

---

```
continuação sessão plughub.
Leia docs/product/handoff-segmentos-abertos-2026-08-18.md (Pendência 2, fases F2/F3)

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
  (`lib/redis-client.ts` §40) — contra o demo leva junto instâncias logadas,
  snapshots, ContextStore, claims, tokens de resume e sessões vivas
· **platform-ui não tem volume mount** — mudança de frontend exige `build`
· **teste também mora na imagem** — controle negativo por stash exige REBUILD;
  preferir teste DIFERENCIAL (as duas leituras lado a lado, afirmando divergência)
· arquivo NOVO exige `build --no-cache`; edição de existente entra no build normal
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada** — senão o resultado não pode julgá-la
· **preflight sobre código casa o TOKEN da chamada, não o identificador** — senão os
  comentários da própria mudança recompõem o número antigo
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· antes de comparar duas leituras, PROVAR QUE O LEITOR LÊ (e conferir o PREFIXO)
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso** — verde calado
  vende cobertura que não tem (mordeu em 2026-08-18, no gate da F1)
· **quoting que atravessa shell→SQL→saída→SQL tem quatro chances de errar e uma de
  acertar** — montar listas no bash, não no SQL (mordeu no purge)
· **`MONITOR` nomeia o CULPADO pelo IP do cliente** — mapear com
  `docker inspect -f '{{.Name}} {{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' $(docker ps -q)`
· **contar não é identificar** — `SCARD 1` não diz QUAL; use `SMEMBERS`
· **asserção com cláusula de escape só vale condicionada** — ler o par
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3 — parser JSON roda no `routing-engine`
· usar `docker compose … exec -T redis`, nunca `docker exec redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **experimento sensível a tempo roda DENTRO de um processo**
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · **redis 6380 · kafka 9093 · postgres 5433** (remapeados no HOST)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ ESTADO (fechado em 2026-08-18, ver CHANGELOG) ═══
P1 expurgo aplicado · baseline do probe de segmentos abertos agora é ZERO (825→816)
P3 família B `primary` MEDIDA (12/12) · `specialist` sem amostra viva
P4 77 `create_task` → `_spawn` · atribuição provada 6/6
P2 F1 (a ORDEM) medida ao vivo · **F2 e F3 são o trabalho desta sessão**
n8n arco ABORTADO · §5 executada · não reabrir os baldes da triagem de 08-17

═══ TAREFA ═══
P2/F2 + F3 — o ENDEREÇAMENTO do agente de fila.

O desenho já está FECHADO por dois invariantes, não é decisão a retomar:
  · "o POOL é a unidade endereçável — nunca o `skill_id`" (CLAUDE.md § Invariantes)
  · "produção = snapshot do slot `current` do POOL. Ponto." (docstring de
    `resolve_flow_for_agent`, orchestrator-bridge/main.py:585, que registra que os
    fallbacks para `skill.flow` foram removidos em 2026-07-13 por serem VAZAMENTO)
⇒ `queue_config` passa a endereçar um POOL de fila com deploy próprio.
   Resolver por `skill_id` reabriria o vazamento fechado. Não refazer essa análise.

F2 — o campo:
  `queue_config.pool_id` em schemas + agent-registry + UI + i18n (2 locales).
  `skill_id` sobrevive como endereço LEGADO. No bridge, `_activate_queue_agent`
  passa o POOL DE FILA ao `activate_native_agent` e mantém o POOL DE DESTINO no
  segmento — são dois fatos hoje colapsados na mesma variável `pool_id`
  (`:5670` é dimensão de relatório, "onde o contato esperou"; `:5693` é o deploy a
  executar). É o invariante "never store a narrower-scope fact in a wider-scope field".

F3 — o deploy:
  dar pool de fila com slot promovido ao `retencao_humano` (hoje declara
  `skill_fila_v1`, que existe e está published, mas o pool não tem slot e o agente
  NUNCA roda). Sem F3, F2 é campo novo sem consumidor — a própria consequência 1
  que o P2 existe para fechar.

Gate: `infra/test/gate_queue_segment_not_born_without_flow.sh <T0>`.
  ⚠️ Ele hoje só exercita a metade da RECUSA. A F3 destrava a metade da PRESENÇA:
  com um pool de fila realmente deployado, a mesma janela passa a conter um pool que
  recusa e um que ativa. Fechar as duas metades é critério de conclusão da F3.
  Reprodução é MANUAL: T0 ANTES de abrir o contato, pool de destino SEM ninguém logado.

═══ PONTA SOLTA a medir antes de assumir qualquer coisa ═══
Em 2026-08-18 20:17:00 o routing enfileirou `e7d62017` em `retencao_humano-int`
(canal webhook) e o bridge não emitiu NENHUMA linha — nem a recusa da F1, nem o
`Sem agente de fila para pool=…` promovido a INFO no mesmo dia. Duas leituras, e não
sei qual: (a) esse caminho de pull não passa por `_activate_queue_agent`; (b) o
evento não chega ao bridge. É observação, não achado. Medir antes de desenhar a F2
em cima.

═══ DEPOIS DA F2/F3, na ordem ═══
1. `do_not_contact` da Fase 3b do Outbound — smoke ESCRITO E NUNCA VALIDADO, num
   veto de contato com pessoa. Virou o item mais urgente daquela seção depois que a
   justificativa n8n saiu.
2. Reexame item a item dos 9 em `Escopo reduzido` (§4.5 do doc de reversão) —
   rejulgar pelo mérito, NÃO reverter em bloco.
3. `specialist` de suspend/resume: segue sem amostra viva (exige
   delegate-as-conference, que nenhum pool webhook do demo alcança hoje).
```
