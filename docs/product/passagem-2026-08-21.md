# Prompt de passagem — sessão de 2026-08-21

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a de 2026-08-20, que validou o
> gate do `do_not_contact`, resolveu dois conflitos DOC×DOC por medição e mediu os dois achados sem dono
> — todos os quatro itens da TAREFA daquela passagem, **menos** o reexame dos 9 em `Escopo reduzido`.
> A passagem [`passagem-2026-08-20.md`](passagem-2026-08-20.md) está **encerrada**.

---

```
continuação sessão plughub.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova:
    export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build:
    docker images --format '{{.Repository}} {{.CreatedSince}}' | grep <svc>
    docker ps --format '{{.Names}} | {{.Image}} | {{.Status}}' | grep <svc>
  (container mostrando IMAGE ID em vez da tag = está rodando imagem velha)
· arquivo NOVO exige `build --no-cache`; edição de existente entra no build normal
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· **janela de log só compara se as DUAS janelas existirem** — conferir `head -1`/`tail -1`
· **`--since` do docker sem fuso é HORA LOCAL** — o host é −03
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso**
· **contar não é identificar** — `total_instances` do snapshot é CAPACIDADE, não gente
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3; usar `$DC exec -T redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· YAML de registry é seed-if-absent: editar pool existente é NO-OP no DB rodando
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · mailing-api 3660 · **redis 6380 · kafka 9093 · postgres 5433**
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ DUAS REGRAS QUE A SESSÃO DE 08-20 COMPROU ═══
· **PROCURE A TESTEMUNHA DO DEFEITO NO LOG ANTES DE CONSERTAR.** Ia consertar um
  `HGET` numa chave string no ai-gateway; antes, contei o log da falha que ele
  necessariamente produziria: ZERO em 24.106 linhas. O endpoint que alcança o bloco
  (`/v1/inference`) tem 0 requisições. O conserto teria ficado verde sem mudar nada, e
  o verde teria sido creditado a ele. Corolário: **a testemunha ausente vale mais que o
  fix** — foi ela que destapou que a trilha de sentimento não tem produtor vivo.
· **UM CASO PERMITIDO QUE PULA O PORTÃO NÃO SERVE DE CONTROLE.** O gate do opt-out
  tinha veto (passo 2) e permissão (passo 3), e mesmo assim não podia reprovar: o
  permitido era campanha `transactional`, que retorna ANTES de consultar o cadastro. Um
  bug que vetasse todo mundo ficava verde. Controle tem de mudar UM valor e percorrer o
  MESMO caminho.

═══ ESTADO (fechado em 2026-08-20, ver CHANGELOG) ═══
Outbound 3b VALIDADO — `smoke_outbound_fase3b.sh` 5/5, com testemunha (cliente sem
  opt-out, mesma campanha e canal) e opt-out por canal acrescentados. Ramo não
  exercitado e declarado na saída: degradação com o identity fora do ar
Conflito DOC×DOC do Detach A–F RESOLVIDO por medição: o CLAUDE.md estava certo, o
  TODO.md § ~4303 era plano de 07-23 estagnado (marcado como tal, não apagado).
  Correção que caiu da medição: o demo roda `dispatch: inline`, não `detached`
Arc 15 CORRIGIDO: o ✅ cobria o canal, não a mídia — não há serviço LiveKit em compose
  algum, nem env, nem SDK na imagem; roda inteiro em `_dev_mode`/mock
Os 2 achados sem dono MEDIDOS e registrados no TODO.md (duas seções novas no topo)
n8n arco ABORTADO · não reabrir os baldes da triagem de 08-17

═══ TAREFA — escolher, não fazer tudo ═══

1. **Sentimento sem produtor** *(o mais grave descoberto em 08-20)*. `session.py:142-161`
   é o ÚNICO chamador de produção das três funções de sentimento, e pendura no
   `/v1/inference`, que tem 0 requisições contra 116 do `/v1/reason`. Tópico
   `sentiment.updated`, `session:{id}:sentiment`, tag `session.sentimento.current`,
   faixas por tenant e consumidor da analytics-api descrevem um pipeline SEM FONTE.
   **Pergunta a responder ANTES de codar:** `/v1/inference` é legado (⇒ sentimento
   precisa de produtor no caminho do `reason`) ou caminho vivo que o demo não exercita?
   Pede tráfego real, não leitura de código. O `HGET` da mesma função é o SEGUNDO
   problema, não o primeiro. Detalhe: TODO.md § `session:{id}:meta`, item 1.

2. **Capacidade de pool não conhece PAUSA.** Dois defeitos empilhados; a ORDEM importa.
   (a) a aritmética: `_RECOMPUTE_POOL_OCCUPANCY_LUA` (`registry.py:630-742`) soma
   `max_concurrent` por pertencimento a set, sem ler `status`, e a pausa não limpa o
   `busy_set`; (b) o gancho existe e nunca roda — publica-se `agent_pause`, o consumidor
   casa `agent_paused` (`kafka_listener.py:356`). **Consertar (b) primeiro troca número
   congelado por número recalculado e igualmente errado.** Bancada de reprodução da fila
   abaixo. **Previsão a escrever antes:** o pool sai de `available: 2` para `0`.
   Detalhe: TODO.md § "A capacidade de pool não conhece PAUSA".

3. **`session:{id}:meta` — declarar a partição de propriedade.** Seis escritores (não
   quatro; `delegate`/`collect` NÃO escrevem — o achado herdava um comentário errado). A
   partição implícita está certa e não está escrita. Dois defeitos independentes e
   baratos, ambos do padrão que o conserto do escalate estabeleceu: o guard de tenant
   que se auto-anula (`analytics-api/supervisor.py:99`) e o `"tenant_demo"` fabricado na
   transferência REST do Console (`server.ts:2236`). **Antes de chamar o primeiro de
   exposição, CONTAR quantos metas vivos não têm `tenant_id`.**

4. **Gate re-executável da Camada F** do arco Detach. A F foi validada por medição manual
   instrumentada, reaproveitando smokes de B/D/R0/I5 por override de env
   (`smoke_internal_work_queue.sh:85-89`). Arco declarado completo sem gate versionado é
   lembrança, não verificação. A própria F4 declara que a **lease** não foi medida.

5. Reexame item a item dos 9 em `Escopo reduzido` (§4.5 do doc de reversão) — rejulgar
   pelo mérito, NÃO reverter em bloco. **Único item de 08-20 que não foi tocado.**

6. Provisionar o SFU (LiveKit) — pré-requisito de qualquer trabalho de WebRTC, incluindo
   a decisão do bridge PSTN→WebRTC. Não se decide topologia de mídia sobre um SFU que
   não existe.

═══ BANCADA DE REPRODUÇÃO DA FILA (para a tarefa 2) ═══
1. pausar o agente humano no Console (pausa é hard filter; o snapshot NÃO é recomposto
   e vai continuar dizendo `available: 2` — é justamente o defeito)
2. T0=$(date -u +%FT%TZ); echo $T0
3. curl -s -X POST http://localhost:8010/channel/webhook/crm-callback \
     -H 'content-type: application/json' \
     -d '{"tenant_id":"tenant_demo","customer_id":"cus_x"}'
   (o endpoint `crm-callback` resolve para `retencao_humano`)
4. bash infra/test/gate_queue_segment_not_born_without_flow.sh "$T0"
5. Retomar no Console → drain sinaliza → escalada → segmento fecha

═══ RESÍDUOS MENORES (sem dono, nenhum é bloqueio) ═══
· `queue_config` só existe em 1 pool do demo (`retencao_humano`) — as duas metades do
  gate da fila não cabem na MESMA janela sem mexer em config
· `system_availability_check` não recebeu a lição da F5b: cai em
  `pools_available > 0 ? "available" : "unknown"` (`operational.ts:326`) sobre o número
  desconfiável, num tool que decide oferta de canal ao CLIENTE
· TTL de `session:{id}:meta` é truncado 24h→4h no primeiro `process_routed`, porque
  nenhum escritor compara TTL antes de sobrescrever
```
