# Prompt de passagem — sessão de 2026-08-26

> ✅ **BACKLOG MIGRADO em 2026-08-26 — esta passagem não é mais fonte de trabalho pendente.**
> As tarefas 1, 3, 4, 5 e 6 e os resíduos menores que não tinham seção viraram seções do
> `TODO.md`, dentro dos temas criados na reorganização do mesmo dia (a tarefa 2 virou **R4**; a 7 e
> a 8 já tinham casa; o *reexame dos 9* foi **feito**). O que continua valendo aqui é o que **não**
> é backlog: as regras operacionais, as regras de método que a sessão de 08-25 comprou, e o estado
> da suíte e2e no dia.
>
> **Regra que isto estabelece:** passagem carrega *regras + estado*; trabalho pendente mora no
> `TODO.md`. Item que só existe na passagem morre com ela.

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a de 2026-08-25, que
> fechou as tarefas **1** (o Console mentia sobre sentimento) e **5** (cobertura e2e da fila) e,
> no caminho, mediu a suíte e2e inteira pela primeira vez: **`--demo` saiu de "não roda" para
> 10/17**. A passagem [`passagem-2026-08-25.md`](passagem-2026-08-25.md) está **encerrada**.

---

```
continuação sessão plughub.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **no FRONTEND são DOIS caches**: depois do build, `Ctrl+Shift+R` é OBRIGATÓRIO — o
  `index.html` cacheado aponta para o bundle de hash ANTIGO (custou um diagnóstico em 08-25)
· arquivo NOVO entra no build normal quando o Dockerfile copia o DIRETÓRIO
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· **janela de log só compara se as DUAS janelas existirem**
· **`--since` do docker sem fuso é HORA LOCAL** — o host é −03
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso**
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3; usar `$DC exec -T redis`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **nada de crase dentro de aspas duplas em bash** — vira substituição de comando
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server + skill-flow-worker JUNTOS
· mudança em **@plughub/skill-flow-engine** → rebuild de skill-flow-service + skill-flow-worker
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · mailing-api 3660 · **redis 6380 · kafka 9093 · postgres 5433**
· health da analytics-api é **`/v1/health`**; do ai-gateway também
· analytics-api usa **PyJWT** (`import jwt`); auth-api usa **python-jose**
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ AS SEIS REGRAS QUE 08-25 COMPROU ═══
· **UM DEFAULT NO PRODUTOR DERRUBA A GUARDA DO CONSUMIDOR SEM DEIXAR RASTRO.**
  A Console dizia "Neutral" para um cliente medido em −0.50. `Number(partials[…] ?? 0)`:
  fonte aposentada + `?? 0` convertendo NÃO-MEDIDO num ponto legítimo da escala. O
  agravante é que a UI **se protegia** (`sentimentScore !== null`) e o default do
  backend desarmou a proteção ANTES que ela pudesse agir. Ao consertar ausência,
  pergunte quem já guardava contra ela.
· **A IMPLEMENTAÇÃO QUE DESENHA A TELA PODE SER A OUTRA.** A passagem apontava
  `tools/supervisor.ts:118`; o cálculo tinha DUAS cópias independentes e idênticas, e a
  Console consome a do endpoint HTTP (`server.ts`). Consertar a apontada teria deixado a
  tela mentindo com o commit no lugar. **Antes de consertar, ache TODOS os produtores.**
· **VERDE RÁPIDO DEMAIS É HIPÓTESE, NÃO RESULTADO.** O cenário 29 fechou 8/8 em 1059 ms
  com uma chamada de LLM dentro — não fecha. A asserção lia o WebSocket e casava com a
  SAUDAÇÃO, anterior à fala do cliente. Passaria com o step morto. Corrigida para julgar o
  ARTEFATO (`resposta_ia` no pipeline_state): 1059 ms → **4078 ms**. *O instrumento que
  denunciou não foi uma asserção, foi o relógio.*
· **ENV DE SERVIÇO AUSENTE VIRA UM `localhost` BEM-FORMADO E ERRADO — três vezes num dia.**
  `WEBCHAT_JWT_SECRET` (parecia defeito de auth: `invalid_token`), `CONFIG_API_URL`
  (parecia serviço fora do ar: `fetch failed`, fatal antes do 1º cenário),
  `PLUGHUB_WORKFLOW_CHANNEL_GATEWAY_URL` (parecia gateway caído: 502). Em TODOS a
  informação que faltava era **o nome da variável**. ⚠️ Confira o `env_prefix` do Settings:
  o workflow-api usa `PLUGHUB_WORKFLOW_`, então um `CHANNEL_GATEWAY_URL` sem prefixo é
  ignorado em silêncio. O `runner.ts` agora audita as 13 URLs no arranque e diz qual declarar.
· **UM MECANISMO PLAUSÍVEL QUE EXPLICA O SINTOMA NÃO É PROVA DE QUE FOI ELE QUE AGIU.**
  No cenário 12, li `webchat.py:320-329` (queda do WS → `client_disconnect` → `_close`) e
  declarei a causa. Instrumentado o campo que faltava, o `conn.session_ended` veio com
  **`reason: "session_expired"`** — outra causa. A leitura estava certa sobre o que aquele
  caminho FAZ e errada sobre ser ele o PERCORRIDO. É "valor plausível" numa hipótese.
· **PREVER COMO SE A CAMADA VISÍVEL FOSSE A ÚLTIMA É O MODO DE FALHA.** Previ 13/17 no
  `--demo` e vieram 10/17: a env consertou o 502 de 13/14/18 e revelou embaixo um
  `404 No webhook endpoint … configured for this tenant`. O CHANGELOG registra o padrão
  *"cada correção só revela a próxima"* DUAS vezes na mesma sessão — e a previsão foi feita
  como se ele não fosse se aplicar. Nomear o padrão não imuniza contra ele.

═══ ESTADO (fechado em 2026-08-25, ver CHANGELOG) ═══
CONSOLE × SENTIMENTO: consertado ponta a ponta e VERIFICADO NOS PIXELS.
  · fonte única `mcp-server/src/lib/session-sentiment.ts`, consumida pelas DUAS
    implementações (tool MCP + endpoint HTTP que a Console usa)
  · fonte canônica = ContextStore (hash CRU, nunca o snapshot mascarado — o filtro é por
    namespace configurável POR POOL e apagaria o sentimento em silêncio)
  · `current: null` = NÃO MEDIDO; nenhuma superfície renderiza sem valor
  · 4 superfícies no platform-ui + o app LEGADO `agent-assist-ui` (porta 5173 do compose)
  · gate `infra/test/gate_console_sentiment_source.sh` (re-executável, testemunha negativa)
  · 15 testes unitários; conferido na tela com contato real (chip presente × ausente)
E2E — CENÁRIO 29 (fila atendida): **8/8 em 4078 ms**, fora do `--demo` por decisão
  (depende de precondição de ambiente que ele MEDE e sobre a qual reprova).
  Roda com `--queue` ou `--only 29`. As duas asserções que pegam a surdez do agente de
  fila (campo `_default_` presente + lista-fantasma ausente) não dependem de LLM.
E2E — SUÍTE: `--demo` saiu de **não-roda** para **10/17**.
  Verdes: 01–04, 06, 07, 08–11.
  Vermelhos e sua CLASSE:
    · **12** — produto: reconexão perde o stream (ver tarefa 1)
    · **13, 14, 18** — `404 No webhook endpoint '<flow_id>' configured for this tenant`
      (o 502 anterior era env e foi corrigido; isto é a camada seguinte — seed)
    · **15, 16** — testam `/v1/agent-types`, **entidade APOSENTADA** (o próprio
      `fixtures/seed.ts:93-115` documenta a remoção em prosa). Teste morto.
    · **17** — chama `POST /mcp`, rota que NÃO EXISTE (HTML 404). Teste morto. ⚠️ Tem três
      `pass()` de "skipped" que transformam indisponibilidade em verde (linhas 312-317).

═══ TAREFA — escolher, não fazer tudo ═══

1. **Webchat: a promessa de reconexão pode não existir** *(produto; contradição doc × código)*.
   `CLAUDE.md` § WebChat promete *"Reconnect via cursor: zero messages lost"*. MEDIDO no
   cenário 12: após queda do WS e reconexão 200 ms depois, `session:{sid}:stream` está
   **VAZIO**, o cliente recebe `conn.session_ended{reason:"session_expired"}` e
   `cursor_no_reauth == cursor_enviado`.
   **NÃO medido, e é o primeiro passo:** quem apaga o stream, e quem emite `session_expired`
   (grep pelo literal → produtor). ⚠️ Não repita meu erro: `client_disconnect`
   (`webchat.py:320-329`) explica o sintoma e **não foi** o caminho.
   Decisão de PRODUTO, com custo próprio (janela de graça de quanto? o que acontece com AHT e
   com o `close_reason` da analítica?). As duas saídas honestas: implementar a graça **ou
   apagar a promessa do doc**. O 12 fica vermelho até a decisão — um verde aqui exigiria
   enfraquecer justamente a asserção que julga a garantia.

2. **13/14/18 — endpoints de webhook não semeados** *(3 cenários, 1 causa)*. Os cenários
   disparam por `flow_id` (`flow_approval_test_v1`, `flow_nps_collect_v1`,
   `flow_worker_chain_test_*`) e o tenant demo não tem endpoint de webhook para eles.
   Decidir: semear no `fixtures/seed.ts` (e então é fixture) ou aceitar que o `--demo` não
   cobre workflow. ⚠️ Endereçar por **pool**, nunca por skill — ver o invariante no CLAUDE.md.

3. **15/16/17 — três cenários testando o que não existe mais.** 15 e 16 falam com
   `/v1/agent-types` (entidade aposentada); 17 fala com `POST /mcp` (rota inexistente).
   Apagar ou migrar é decisão sua — o que não pode é ficarem vermelhos permanentes, porque
   vermelho permanente ensina a ignorar a cor. Junto: os três `pass()` de "skipped" do 17.

4. **Automatizar a medição de sentimento sobre o cenário 29.** O 29 já produz o contato de
   FILA reprodutível — a metade cara. Faltam duas asserções: `{tenant}:ctx:{sid}` →
   `session.sentimento.current` medido, e o agregado `{tenant}:pool:{p}:sentiment_live`.
   Substitui a reprodução MANUAL do `gate_sentiment_engine_half.sh`. ⚠️ Asserção com NOME
   PRÓPRIO (como o `D1`), senão "sem credencial" volta a ser indistinguível de "não mediu".

5. **Validar o copilot em runtime** *(herdado, não tocado)*. Os dois bugs (`system=`
   inexistente; `.text` por `.content`) foram achados por LEITURA em 08-23 e nunca rodaram.
   ⚠️ Confira primeiro DE ONDE o copilot busca o provider — se for pelo caminho que o
   sentimento usava (`inference_engine.providers`), está morto pelo mesmo motivo.

6. **Normalizar a guarda `_default_` nos 5 leitores** *(herdado)*. A assimetria
   `!== "_default_"` × truthiness segue em `orchestrator-bridge/main.py:9180` e
   `mcp-server/server.ts:2471/2487/2497/3641`. Hoje não há produtor de campo vazio; o
   próximo caminho que ativar agente nativo sem `instance_id` reabre o buraco. Tratar `""`
   como `_default_` **e LOGAR**. 💡 Agora dá para testar: o cenário 29 exercita exatamente
   esse caminho.

7. **`session:{id}:meta` fatias B e C** *(herdado)*. B = recusar campo de outro dono. C = o
   `pool_id` do meta é o pool de ENTRADA, então sentimento medido pelo agente de FILA agrega
   sob o pool onde o contato começou. › *Suspeita não medida:* `session:{id}:wf_agent` nasce
   com o mesmo `_stl()` = 4 h e descreve workflow de até 48 h.

8. **Gate re-executável da Camada F** (arco Detach) · **`mcp_audit_log`** (dormente por
   medição; exercitar `invoke` ANTES do DDL) · **reexame dos 9 em `Escopo reduzido`** ·
   **provisionar o SFU (LiveKit)** — todos herdados, nenhum tocado.

═══ RESÍDUOS MENORES (sem dono, nenhum é bloqueio) ═══
· `sentiment.trajectory` é `[]` e `trend` é `null`: **`session:{id}:sentiment` — o array de
  histórico que o CLAUDE.md documenta — NÃO TEM PRODUTOR**. Ou escrever (~15 linhas no
  `sentiment_emitter`, e o helper já recebe `trajectory` como parâmetro), ou apagar a promessa
· `intent.confidence ?? 0` é o irmão do defeito consertado, em campo diferente
· **`packages/agent-assist-ui/` é um SEGUNDO Console vivo** (porta 5173, sem profile). Todo
  conserto de Console precisa ser feito duas vezes. Decisão pendente: aposentar ou assumir
· código morto em `platform-ui/…/ContactList.tsx` (`sentimentColor` e `sentimentScore` sem uso)
· cenário 03 imprime dois warnings de `docker: not found` e passa mesmo assim — o kill/restart
  de container é pulado, e o teste vira injeção de estado no Redis. Verde honesto, mas menor
  do que o nome promete
· contador de credencial é BALDE de calendário (`now // 86400`), e `calls_ok` SUBCONTA
  (só incrementa quando o AccountSelector escolheu CONTA; no demo vai tudo pelo alias legado)
· `extract_context_from_response` (`context.py`) é heurística de palavras-chave em PT, e
  alimenta `intent`/`confidence`/`flags` do `/inference` — que não tem chamador
· pausa de IA é desfeita em 15 s pelo `agent_ready` da reconciliação do bootstrap
· `bpm.test.ts` vermelho por dívida conhecida (`conversation_escalate` recusa sem meta)
· `npx vitest` no host valida `@plughub/schemas` de 2026-08-02
· o `on_failure` do `responder_cliente` volta a `aguardar_mensagem` **sem falar com o
  cliente**: falha de LLM na fila é indistinguível de "o agente ignorou" pelo lado do cliente
```
