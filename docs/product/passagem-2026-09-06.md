# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-09-05.md`](passagem-2026-09-05.md), que está **encerrada**.
>
> A sessão anterior entregou **UMA** fatia com gate verde e E2E em tráfego real: a **D14 (ii)**,
> alvo de SLA no segmento de espera. As duas opções que ela não escolheu (TTL de `pool_config`,
> lacuna 2) seguem intactas e ganharam uma terceira: a **(iii)**, agora desbloqueada.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes, mas **todo dado medido carrega carimbo
> `2026-08-24`** (o E2E saiu às 00:52 UTC de 25-08, que é ainda dia 24 em BRT). Onde houver
> conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: escolher entre (D14-iii) migrar os três leitores de SLA para o
alvo do segmento — recém-desbloqueada, e é ela que MOVE os números —, (TTL de `pool_config`) dois
escritores da mesma chave com TTLs diferentes, ou (lacuna 2) o teste vermelho do `claimed_via`.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Uma pendência declarada por quem parou de
  trabalhar é a última coisa que ele lembra, não o estado do repo. Já se pagou OITO sessões
  seguidas. **A ORDEM proposta também se confere.**
· **MEÇA AS DUAS BASELINES ANTES DE MUDAR QUALQUER COISA.** Dois comandos, e são baratos:
    $DC exec -T routing-engine sh -c "cd /app/packages/routing-engine && python3 -m pytest src/plughub_routing/tests -q 2>&1 | tail -3"
    $DC exec -T analytics-api sh -c "cd /app/packages/analytics-api && python3 -m pytest src/plughub_analytics_api/tests -q 2>&1 | tail -3"
  Esperado HOJE: **1 failed, 284 passed** (o vermelho é o do Backlog 3, conhecido) e **595 passed**.
  Sem baseline, "quebrei?" não tem resposta — foi o erro de método de duas sessões atrás.
· **O `TODO.md` § "SLA está no grão errado" foi ATUALIZADO no fim da sessão passada** ((ii) ✅, a
  sub-pergunta da D14.1 fechada, e a (iii) com um cuidado de ORDEM novo). Está mais atual que
  qualquer resumo — inclusive que este.
· ⚠️ **O AMBIENTE SEGUE MEXIDO, e continua sendo bom que siga.** `retencao_humano` tem
  `queue_config.pool_id = fila_humano` (fila ATENDIDA), `max_wait_s: 1800`, `sla_target_ms: 300000`.
  Não reverta. Confira:
    python3 infra/test/q_queue_config_inventory.py
  Esperado: 36 pools · none 35 · legacy_only 0 · **addressable 1**. Nunca ao YAML (seed-if-absent).

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços; e confira que a
  linha colada não veio DUPLICADA (o erro se lê como erro de sintaxe do código, não da colagem)
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **`$DC exec` no clickhouse EXIGE `-T`** — o serviço tem `tty: true` e o `< /dev/null` colide
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **arquivo NOVO exige `build --no-cache`** (não vale p/ script em `infra/test/`, que roda do host)
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **PREFLIGHT DE SÍMBOLO antes de medir**, e ESTRUTURAL: `inspect.getsource` da função CARREGADA,
  nunca `grep` (que conta o COMENTÁRIO que documenta a remoção)
· **`$DC exec` NÃO começa em `/app`** — caminho ABSOLUTO no pytest
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· CH 23.8 **não faz subquery correlacionada** — use JOIN; prefira joins binários ANINHADOS
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 · auth-api 3202
  · agent-assist-ui (app LEGADO, vivo) 5173

═══ O QUE ESTA SESSÃO COMPROU ═══
· **A ALLOWLIST DO CONSUMIDOR É O PONTO CEGO DE TODA COLUNA NOVA.**
  `analytics_api.models.parse_participant_event` monta o `segment_row` campo a campo: o que não
  estiver no dict é **descartado em silêncio**. Produtor correto, consumidor verde, coluna `NULL`, e
  nada vermelho em lugar nenhum. Ao acrescentar campo a evento, o teste que importa não é o do
  produtor — é o que prova que o campo **atravessa o parser**.
· **MUTE OS DOIS LADOS, NÃO UM.** Falseabilidade de uma fatia que cruza dois serviços é DUAS
  proposições. A mutação no produtor não diz nada sobre a allowlist, e vice-versa. Custou duas
  rodadas de build e pagou: as duas reprovaram, com os testes nomeados previstos.
· **UM TESTE PODE REPROVAR PELO MOTIVO CERTO E MEDIR A COISA ERRADA.** A testemunha negativa do
  parser usava `seg["campo"]` e levantava `KeyError` quando o campo saía da allowlist — reprovava
  medindo **presença de chave**, não a proposição que declara ("ausente vira `None`, não `0`").
  Corrigida para `.get()`. Foi a MUTAÇÃO que expôs isso, não a leitura do teste.
· **NÃO CARREGUE NÚMERO DE DOCUMENTO PARA DENTRO DA PREVISÃO.** Era o viés dos dois erros da sessão
  anterior. Aplicado de propósito no E2E: em vez de prever `300000` a partir da passagem, medi o
  `pool_config` primeiro **e só então** escrevi a previsão. Custa um comando.
· **CICLO DE IMPORT É REQUISITO DE DESENHO, NÃO OBSTÁCULO.** `registry` importa `mute_queue`, logo o
  inverso é proibido — e a saída preguiçosa (grafar a chave Redis uma segunda vez) não fica
  vermelha: devolve `None` e vira "pool sem alvo". A chave subiu para `models`, que não importa nada.

═══ ERROS DE PREVISÃO (8 acertos, 0 erros) ═══
Acertos: baseline routing **1 failed/278** · analytics depois **595** · routing depois
**1 failed/284** · mutação produtor **2 failed/283 com o teste nomeado** · mutação allowlist
**2 failed/593 com os dois nomeados** · coluna no CH **2 linhas** · E2E **300000** · e a
meta-previsão de que a testemunha negativa reprovaria **pela via errada** (`KeyError`).
⚠️ **Zero erro não é motivo para relaxar o método — é motivo para desconfiar do escopo.** Toda
previsão desta sessão foi sobre mecanismo que eu tinha ACABADO de ler. A previsão difícil é a que
atravessa código que não se leu, e esta sessão não teve nenhuma dessas. A (iii) terá.

═══ ENTREGUE — não re-fazer ═══
**(D14 ii)** — `analytics.segments.sla_target_ms` (`Nullable(Int64)`), carimbada por
`mute_queue.resolve_queue_exit` a partir do `{t}:pool_config:{p}` via
`resolve_wait_sla_target_ms` — **UM site**, não os seis chamadores. Sem `SLA_TARGET_MS_FALLBACK`
(ausência vira `null`: o cache expira em ~1 h e alvo fabricado no ledger não se corrige por deploy).
`models.pool_config_key()` nasceu como definição única da chave. Campo no
`ConversationParticipantEventSchema` (piso `.positive()`, nulável) e na allowlist do parser.
Gates: `test_queue_wait_sla_stamp.py` (6) · `test_segment_sla_column.py` (9, inclui **guard de
aridade `_SEGMENT_COLS` × `_segment_row`** que serve a QUALQUER coluna futura) · 2 no `test_consumer`.
**Decisões do dono, gravadas como teste:** (1) pool de IA **tem** alvo — espera é espera, sem ramo
por `agent_kind`; (2) alvo **copiado** no fechamento, não resolvido na leitura.
🟢 E2E em tráfego: espera de **10 065 ms** com **`sla_target_ms=300 000`**, 5 esperas anteriores `\N`.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
suíte routing-engine: **1 failed, 284 passed** · suíte analytics-api: **595 passed**
`pool_config` de `retencao_humano`: `sla_target_ms=300000`, `queue_config.pool_id=fila_humano`,
  `max_wait_s=1800`, **TTL 3 593 s** (⇒ o 86 400 do routing-engine segue morto; ver opção 2)
esperas `role='queue'` no tenant: as 5 anteriores ao deploy estão todas com `sla_target_ms` NULL
`retencao_humano`: 48 esperas = 5 abertas + **10 SEM ALVO** + 33 julgáveis (medição da (i), intacta)

═══ AS OPÇÕES DE TAREFA ═══
**(D14-iii) migrar os três leitores** — `query.py:240` · `reports_query.py:3803` · `_sla_eligible`
  (`reports_query.py:5901`) — de `sessions.sla_target_ms` para o alvo do SEGMENTO. É a fatia que
  **MOVE os números**; as duas anteriores não moveram nenhum, de propósito.
  ⏳ **UMA decisão do dono antes de codar:** a (ii) é **forward-only** — linha antiga tem `NULL` e não
  há migração possível (o `first_queued_ms` é consumido na saída). Trocar a fonte muda o denominador
  (`countIf(sla_target_ms > 0)`) e **esvazia a série histórica**. Duas saídas defensáveis:
    (a) ler o segmento **com fallback à sessão** durante a transição — preserva a série, mas mistura
        duas fontes num mesmo número, e ninguém saberá qual respondeu em cada linha;
    (b) **cortar a série** numa data declarada — honesto e legível, mas descarta o histórico.
  ⚠️ Escolher em silêncio é o que não pode. E **contar antes, por pool**, senão a mudança de número
  vira "o relatório quebrou".
  ⚠️ `reports_query.py:5740-5760` já não colapsa (a (i) entregou `_per_wait`) — confira antes, porque
  esta passagem afirma e não mediu nesta sessão.
**(TTL de `pool_config`)** dois serviços escrevem a MESMA chave com TTLs diferentes e o último vence:
  `routing-engine/config.py:52` **86 400** × `orchestrator-bridge/session_config.py:29` **3 600**. O
  bridge reconcilia a cada 5 min ⇒ **o 24 h do routing-engine está morto** (re-medido nesta sessão:
  TTL 3 593). Agrava: `config-api/seed.py:288` afirma em prosa que são caches SEPARADOS (não são),
  `registry.py:13` documenta "TTL 24h", e o `changelog-2026-04-16` documenta o conserto 300 s→86 400
  que o bridge desfez em silêncio. ⚠️ **Ganhou consequência nova com a (ii):** a expiração agora
  produz **segmento de espera sem alvo** — não é mais só cache frio, é buraco no ledger que não se
  corrige depois.
**(lacuna 2)** o teste vermelho do `claimed_via` — ver Backlog 3. Não é trocar a string.

═══ NÃO fazer agora ═══
· **"Consertar" o vermelho do `claimed_via` trocando `"semaphore"` por `"record"`.** Passa o teste e
  apaga a pergunta. Ver Backlog 3.
· **Migrar as esperas antigas para carimbar o alvo retroativamente.** Impossível por construção, não
  por falta de tempo: o `first_queued_ms` é consumido na saída da fila e o alvo do dia se perdeu.
· **Anular `sla_target_ms` em pool nenhum** — decidido (b): não se anula, muda quem lê.
· **Ramificar o carimbo por `agent_kind`** — decidido e gravado em teste. Espera é espera.
· **"Consertar" a multiplicidade de segmentos por F5** — REFUTADO (Mudança 39). O discriminador de
  duplicação é SEGMENTO ÓRFÃO (`duration_ms IS NULL` > 1 por sessão viva), **nunca a contagem**.
· **O `queue-agent` do bridge** (`main.py:5993`, `uuid5` sem discriminador de passagem). Inalcançável
  hoje: exige DOIS pools com fila atendida e só um tem endereço.
· **F4 (visão 2 do histórico unificado)** — classe *outbound* segue com zero amostras.

═══ Backlog NOVO / reaberto ═══
· 🔴 **3. `test_expire_returns_the_slot_even_after_the_lease_expired` VERMELHO, e é ANTERIOR a duas
  sessões atrás** (medido: `router.py` não foi tocado). Asserta `claimed_via == "semaphore"`; o código
  devolve `"record"`, porque a emenda **D6** (2026-08-04) inseriu o registro durável de posse ANTES
  do semáforo (`router.py:1151`, e `:1157` é o ramo do semáforo). ⚠️ **Não é cosmético:**
  `router.py:1158` declara que a linha do `semaphore` é *"a MEDIDA da lacuna 2"* — com o `record`
  respondendo antes, **esse ramo ficou inalcançável neste cenário**, e o instrumento da lacuna 2
  parou de ser exercido sem que nada além deste vermelho o dissesse. Casa com a lacuna que a Camada F
  já declarava (*"a lease não foi medida"*). **Consertar = decidir onde vive a medida da lacuna 2.**
· 🆕 **`sessions.sla_target_ms` é PROJEÇÃO desde a (ii), mas nada impede que volte a ser lida como
  fonte.** Enquanto a (iii) não migrar os leitores, a regra existe só em prosa (CLAUDE.md, CHANGELOG,
  `conference-mechanics.md` § 41) — mesma família de "invariante sem mecanismo que a imponha".
· 🆕 **`estimated_wait_factor` (0.7) e `congestion_sla_factor` (1.5) são o 2º e 3º órfão do namespace
  `routing`** — semeados, cacheados, testados, **sem leitor**: `main.py` fixa `* 0.7` e
  `saturated.py` fixa `* 1.5`. Com o `sla_default_ms` já conhecido, são **três** botões que prometem
  efeito e não têm.
· 🔴 **`sla_default_ms` NÃO TEM LEITOR** — ⚠️ remover exige mexer no
  `smoke_config_routing_orphan_keys.sh:118`, que a usa como **CANÁRIO** de namespace intacto.
· 🆕 **`max_queue_len` é declarado no front e NUNCA lido** (`AnalisePoolsPage.tsx:55`).
· 🆕 **`query_pools_queue` engole a exceção** e devolve `data: []` + `error`, indistinguível de "não
  há dado" na tela. O gate novo o pega; a tela, não.
· **3 cópias restantes do `480_000`**: `instance_bootstrap.py:709` · `mcp-server/tools/bpm.ts:281` ·
  `config-api/seed.py:158`.
· **`supervisor.ts` — `urgency` compara ATENDIMENTO TOTAL com alvo de ESPERA.** Não mede nada. É
  contrato de tool MCP com consumidores não mapeados — **mapeá-los é a fatia**.
· ⚠️ **`duration_ms` do segmento humano diverge da janela dos próprios carimbos** (26 448 ms/80 s;
  25 519 ms/60 s). **Sem causa** — a explicação "sai do último F5" caiu com a Mudança 39.
· **`sac_ia` tem 2 esperas de 0 ms** (Mudança 37, consta corrigida). Ou é dado pré-correção, ou a
  correção não pegou tudo.
· **Exibir tempo de contato / espera-antes-do-agente no Console** — levantado pelo operador, SEM
  decisão. São 3 fatos distintos (segmento · contato · espera) e a tela mostra 1.
· `admission.py:267` apaga `first_queued` sem emitir, p/ sessões fechadas no buffer MUDO — 7º ponto
  de saída, fora dos 6 call sites. A fila ATENDIDA não tem backstop equivalente.
· **`_emit_queue_timeout` publica em outbound e `conversations.events` SEM `key=`.** Tópicos
  diferentes da fatia B, dívida à parte. O `_TimeoutProducer` do teste tolera e DECLARA a tolerância.

═══ INCONFORMIDADE HERDADA, ainda aberta ═══
**Console não libera a tela após `Transfer`** (relatada 2026-08-24). Diagnóstico JÁ FECHADO por
refutação — não repetir: **não é wrap-up e não é config de dispatch** (o pool está medido em
`detached` **no agent-registry**, não no YAML) ⇒ **teardown ausente no caminho do `Transfer`**. Abre
por: `Transfer` e `Close` compartilham o teardown do Console, ou são dois caminhos? ⚠️ Conserto
provavelmente em DOIS apps (o `agent-assist-ui` vive na 5173). ⚠️ Não testado: transfer com o cliente
ainda CONECTADO. Registro em `TODO.md` § *Analytics e UI*.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de DEZ passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduos do defeito 2: linhas órfãs `queue_default_*` no config store;
  `queue_config.skill_id` legado do `retencao_humano`
```
