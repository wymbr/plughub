# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-09-07.md`](passagem-2026-09-07.md), que está **encerrada**.
>
> A sessão anterior entregou **UMA** fatia com gate verde, mutação nomeada nos dois lados e as
> baselines medidas antes de tocar em código: o **TTL de `pool_config`**, passo 1 da ordem de
> trabalho. Ela fechou o passo 1 e **corrigiu a própria ordem** em dois pontos.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes, mas **todo dado medido carrega carimbo
> `2026-08-25`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: seguir a ORDEM PROPOSTA no topo do `TODO.md`
(§ "🧭 Ordem de trabalho PROPOSTA"). O passo 1 (TTL de `pool_config`) está **FECHADO**; o próximo
é o **passo 2 — destravar o F4**, que é MEDIÇÃO + DECISÃO, não código: rodar o cenário
`limite_entrega` (parqueamento), reler `spawn_reason`, e a decisão de texto do dono
`contatos` × `acessos do cliente`. Meia sessão.

⚠️ **A ordem é PROPOSTA, não decidida — e ela se confere.** Leia a seção inteira antes de pegar o
passo 2. **Não repita o plano aqui nem em outro arquivo** — uma casa só.

⚠️ **E ela JÁ SE CORRIGIU uma vez, na sessão passada.** A regra *"`sla_unstamped = 0` rebaixa o
item 1 na hora"* estava **incompleta**: um contador só decide se a população que o torna legível
existir. Veio `0` sobre `elig = 1` ⇒ INCONCLUSIVO, não latente. A cláusula foi acrescentada ao
`TODO.md`. **Ao aplicar qualquer critério escrito em cima de um contador, pergunte antes qual
população o torna legível** — vale para o passo 2, cujo bloqueio é literalmente "zero amostras".

Alternativas fora da ordem, se o dono preferir: (lacuna 2) o teste vermelho do `claimed_via`, ou
(Console/Transfer) a inconformidade herdada — a única com gente esperando.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Uma pendência declarada por quem parou
  de trabalhar é a última coisa que ele lembra, não o estado do repo. Já se pagou DEZ sessões
  seguidas — e na passada a passagem apontou o **arquivo errado** do lado do bridge
  (`session_config.py:29` em vez de `instance_bootstrap.py:71`), o que só apareceu por grep.
· **MEÇA AS TRÊS BASELINES ANTES DE MUDAR QUALQUER COISA.** São três agora, não duas:
    $DC exec -T routing-engine sh -c "cd /app/packages/routing-engine && python3 -m pytest src/plughub_routing/tests -q 2>&1 | tail -3"
    $DC exec -T analytics-api sh -c "cd /app/packages/analytics-api && python3 -m pytest src/plughub_analytics_api/tests -q 2>&1 | tail -3"
    $DC exec -T orchestrator-bridge sh -c "cd /app/packages/orchestrator-bridge && python3 -m pytest src/plughub_orchestrator_bridge/tests -q 2>&1 | tail -3"
  Esperado HOJE: **1 failed, 294 passed** (o vermelho é o do Backlog 3, conhecido) · **607 passed**
  · **96 passed**. A do bridge é NOVA — a passagem anterior não a tinha, e por isso a sessão
  passada começou sem saber se quebrava algo lá.
· ⚠️ **O AMBIENTE SEGUE MEXIDO, e continua sendo bom que siga.** `retencao_humano` tem
  `queue_config.pool_id = fila_humano` (fila ATENDIDA), `max_wait_s: 1800`, `sla_target_ms: 300000`.
  Não reverta. Confira:
    python3 infra/test/q_queue_config_inventory.py
  Esperado: 36 pools · none 35 · legacy_only 0 · **addressable 1**. Nunca ao YAML (seed-if-absent).

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **`$DC exec` no clickhouse EXIGE `-T`** — o serviço tem `tty: true` e o `< /dev/null` colide
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **arquivo NOVO exige `build --no-cache`** (não vale p/ script em `infra/test/`, que roda do host)
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **PREFLIGHT DE SÍMBOLO antes de medir**, e ESTRUTURAL (`hasattr`, `model_fields`,
  `inspect.getsource` da função CARREGADA), nunca `grep` — que conta o COMENTÁRIO que documenta a
  remoção
· **`$DC exec` NÃO começa em `/app`** — caminho ABSOLUTO no pytest
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· CH 23.8 **não faz subquery correlacionada** — use JOIN; prefira joins binários ANINHADOS
· CH é alcançável do HOST em `http://localhost:8123`, user/pass `plughub`
· config-api: token `demo_config_admin_token` (header `X-Admin-Token`); o PUT com
  `"tenant_id": null` escreve o default GLOBAL, que é onde o seed escreve
· portas: analytics-api 3500 · agent-registry 3300 · **config-api 3600** · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 · auth-api 3202
  · agent-assist-ui (app LEGADO, vivo) 5173

═══ O QUE ESTA SESSÃO COMPROU ═══
· **CHAVE COM DOIS ESCRITORES NÃO TEM DOIS VALORES — TEM O DO ESCRITOR MAIS FREQUENTE.** O
  diagnóstico herdado dizia "último escritor vence, e o bridge reconcilia a cada 5 min". Errado na
  cadência e, por isso, errado no tamanho: quem sobrescreve é o `_heartbeat_tick`, **a cada 15 s**.
  **O instrumento é PARAR o serviço suspeito** — se o valor decai e não reseta, ele é o renovador
  único. (3587 → 3546 → 3462 com o bridge parado; 3594 ao religar.)
· **CONFIG NÃO LIDA FALHA POR CAMADAS, E TODAS DEGRADAM PARA O MESMO DEFAULT.** O namespace
  `session` NUNCA foi lido pelo bridge: env ausente no compose + porta 3500 (analytics-api) no
  default hardcoded + GET sem `?tenant_id=`. Três causas, cada uma suficiente. **Consertar a de
  baixo não move nada e parece "não aplicou".** O tell que pegou: minha hipótese bateu no GÊNERO
  (warning de degradação para default) e errou a ESPÉCIE (422 × falha de conexão) — ao acertar
  "quase", conte as camadas acima da sua hipótese.
· **DEGRADAÇÃO QUE NÃO NOMEIA O QUE PERDE EFEITO NÃO É LIDA.** O aviso
  *"using cached/default values"* existia e gritou por meses. Agora lista as chaves afetadas.
· **UM RAMO DE DIAGNÓSTICO PODE ESTAR ERRADO SEM O GATE FICAR ERRADO.** Prever a mutação revelou
  que o gate reprovaria certo e diria a causa errada (*"o heartbeat parou?"* quando o problema é
  código). Separado em três ramos por VALOR: default conhecido × acima × abaixo. **Ao escrever um
  gate, preveja a mutação e leia a MENSAGEM que ele imprimiria, não só a cor.**
· **UM TESTE ANTIGO VERMELHO APÓS REFACTOR "EQUIVALENTE" É COSTURA MOVIDA, NÃO TESTE VELHO.**

═══ ERROS DE PREVISÃO (11 acertos, 2 erros) ═══
Acertos: as três baselines · inventário `addressable 1` · `waits 72` · `elig 1` · `unst 0` · os
quatro TTLs do experimento de parada · bridge 96 · routing 294 · mutação **6 failed, 90 passed**
com os seis nomes · gate 3595→4241→86397 · os seis valores do namespace batendo com o `_DEFAULTS`.
❌ **ERRO 1: previ 422 no reload do bridge; veio falha de CONEXÃO.** Duas causas acima da minha.
❌ **ERRO 2: previ `1 failed, 293 passed` no routing; veio `2 failed, 292`.** Meu `_CACHES` como
   atributo de CLASSE congelou o singleton no import e tornou `patch("…routing_config")` inerte.
   O teste antigo reprovou **com razão**.

═══ ENTREGUE — não re-fazer ═══
**(TTL de `pool_config`)** — fonte única no Config API (namespace `session`, `pool_config_ttl_s`,
**86 400**), lida pelos DOIS escritores **no momento da escrita** (nunca capturada no import, senão
`config.changed` não vale sem restart). Constante `_POOL_CONFIG_TTL_S` REMOVIDA do bridge;
`Settings.pool_config_ttl_seconds` REMOVIDA do routing-engine (`PLUGHUB_POOL_CONFIG_TTL_SECONDS`
anotada como sem efeito no `ecosystem.config.js`). `CONFIG_API_URL` acrescentada ao bridge no
compose; default corrigido 3500 → 3600; `configure_tenant` + `params` no `SessionConfigCache`;
`config.changed` do routing-engine passa a tratar `session`; `except: pass` da renovação agora loga.
Gates: `test_pool_config_ttl_single_source.py` (10 no bridge + 10 no routing, valor discriminante
**4242**) + `infra/test/gate_pool_config_ttl_source.sh` (ponta a ponta, ~90 s, restaura no EXIT).
🟢 Mutação reprovou os dois lados com os nomes previstos.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
suítes: routing-engine **1 failed, 294 passed** · analytics-api **607** · bridge **96**
`/reports/pools/queue` (tenant demo, janela ampla): **72 esperas · elig 1 · unst 0** ⇒ INCONCLUSIVO
namespace `session` no store: os 6 conhecidos IDÊNTICOS ao `_DEFAULTS` (ligar a leitura não mudou
  comportamento) + 4 que o bridge ignora (`ai_gateway_ttl_s`, `channel_gateway_ttl_s` e as duas
  `queue_default_*` órfãs)
TTL de `{t}:pool_config:{p}` hoje: **86 400**, renovado a cada 15 s pelo bridge

═══ AS OPÇÕES DE TAREFA ═══
**(passo 2 — destravar o F4)** medição + decisão, sem código. Ver `TODO.md` § Ordem de trabalho.
  ⚠️ O *"acesso outbound tem ZERO amostras"* é contagem com **dois mundos que ela não separa**
  (produtor mudo × ninguém rodou o cenário), e a F0 ligou o gate do `collect` no MESMO dia da
  medição. A mesma rodada responde o `delegate = 0` não explicado (`webhook.py:1604`).
**(lacuna 2)** o teste vermelho do `claimed_via` — ver Backlog 3. Não é trocar a string.
**(Console/Transfer)** a inconformidade herdada abaixo — a única com operador esperando.

═══ NÃO fazer agora ═══
· **Baixar o TTL de `pool_config` alegando limpeza.** A limpeza é por DELETE explícito em
  `_reconcile_pool_configs`; o TTL é backstop só para "pool removido enquanto o bridge está morto".
  Se expirar, `get_candidate_pools` devolve VAZIO e todo contato é enfileirado.
· **"Consertar" o vermelho do `claimed_via` trocando `"semaphore"` por `"record"`.** Ver Backlog 3.
· **Mexer na época `SEGMENT_SLA_EPOCH` para "recuperar" a série histórica.** A série antiga não
  volta — o `first_queued_ms` é consumido na saída da fila.
· **Migrar esperas antigas para carimbar o alvo retroativamente.** Impossível por construção.
· **Ramificar o carimbo de SLA por `agent_kind`** — decidido e gravado em teste.
· **"Consertar" a multiplicidade de segmentos por F5** — REFUTADO (Mudança 39).
· **O `queue-agent` do bridge** (`main.py:5993`, `uuid5` sem discriminador de passagem).
  Inalcançável hoje: exige DOIS pools com fila atendida e só um tem endereço.
· **Construir o F4 antes do passo 2** — repete a armadilha que a F3 já pagou (ANI/DNIS construídas
  e removidas por falta de população). E **não há regressão pressionando**: a Vista Processos
  existe (`AnaliseJourneysPage`).

═══ Backlog NOVO / reaberto ═══
· 🔴 **3. `test_expire_returns_the_slot_even_after_the_lease_expired` VERMELHO, e é ANTERIOR a
  quatro sessões atrás.** Asserta `claimed_via == "semaphore"`; o código devolve `"record"`, porque
  a emenda **D6** (2026-08-04) inseriu o registro durável de posse ANTES do semáforo
  (`router.py:1151`, e `:1157` é o ramo do semáforo). ⚠️ **Não é cosmético:** `router.py:1158`
  declara que a linha do `semaphore` é *"a MEDIDA da lacuna 2"* — com o `record` respondendo antes,
  esse ramo ficou inalcançável neste cenário. **Consertar = decidir onde vive a medida da lacuna 2.**
· 🆕 **`wrapup_hold_ttl_s` é LEITOR SEM CHAVE** (`kafka_listener.py:411`) — não está no `_DEFAULTS`
  do `routing_config.py` nem no seed, resolve sempre para o `90` inline e não aparece na tela. É a
  metade que a heurística dos órfãos não pega (procura-se chave sem leitor). Conserto: semear, ou
  assumir o 90 como constante e tirar o `.get`. **Decidir qual antes de codar.**
· 🆕 **`queue_default_agent_type_id` / `queue_default_skill_id` continuam no config store** (medidas
  vazias em 2026-08-25). Saíram do código e do seed em 2026-08-24; o store é DB-owned, então
  remover do seed não remove a linha. Conserto = DELETE pela API oficial.
· 🆕 **`gate_pool_config_ttl_source.sh` não é chamado por runner nenhum** — como quase todo gate em
  `infra/test/`. Gate que ninguém roda é lembrança, não verificação (é a mesma dívida da Camada F).
· 🆕 **`ai_gateway_ttl_s` e `channel_gateway_ttl_s` estão no namespace `session` e o bridge as
  ignora.** Não medi se ai-gateway/channel-gateway as leem. **Medir antes de concluir qualquer
  coisa** — podem ser legítimas (namespace compartilhado) ou dois órfãos a mais.
· 🆕 **`sla_unstamped` existe na API e NÃO tem superfície na UI.** Enquanto ninguém o exibir, o
  buraco do TTL é visível só por `curl`.
· 🆕 **`_cv_sla_series` IGNORA o retorno de `_apply_pool_scope`** — com `accessible_pools=[]` a query
  roda irrestrita. **NÃO consertado de propósito**: não medi se `[]` chega ali (o JWT usa `[]` com o
  sentido OPOSTO, "todos os pools"). É ABAC — medir antes de mexer.
· 🔴 **`sla_default_ms` NÃO TEM LEITOR** — ⚠️ remover exige mexer no
  `smoke_config_routing_orphan_keys.sh:118`, que a usa como **CANÁRIO** de namespace intacto.
· 🆕 **`estimated_wait_factor` (0.7) e `congestion_sla_factor` (1.5)** são o 2º e 3º órfão do
  namespace `routing`: `main.py` fixa `* 0.7` e `saturated.py` fixa `* 1.5`.
· 🆕 **`max_queue_len` é declarado no front e NUNCA lido** (`AnalisePoolsPage.tsx:55`).
· 🆕 **`query_pools_queue` engole a exceção** e devolve `data: []` + `error`, indistinguível de "não
  há dado" na tela. O gate o pega; a tela, não.
· **3 cópias restantes do `480_000`**: `instance_bootstrap.py` · `mcp-server/tools/bpm.ts:281` ·
  `config-api/seed.py:158`.
· **`supervisor.ts` — `urgency` compara ATENDIMENTO TOTAL com alvo de ESPERA.** Contrato de tool MCP
  com consumidores não mapeados — **mapeá-los é a fatia**.
· ⚠️ **`duration_ms` do segmento humano diverge da janela dos próprios carimbos** (26 448 ms/80 s;
  25 519 ms/60 s). **Sem causa** — a explicação "sai do último F5" caiu com a Mudança 39.
· **`sac_ia` tem 2 esperas de 0 ms** (Mudança 37, consta corrigida). Ou é dado pré-correção, ou a
  correção não pegou tudo.
· **Exibir tempo de contato / espera-antes-do-agente no Console** — levantado pelo operador, SEM
  decisão. São 3 fatos distintos (segmento · contato · espera) e a tela mostra 1.
· `admission.py:267` apaga `first_queued` sem emitir, p/ sessões fechadas no buffer MUDO — 7º ponto
  de saída, fora dos 6 call sites. A fila ATENDIDA não tem backstop equivalente.
· **`_emit_queue_timeout` publica em outbound e `conversations.events` SEM `key=`.**

═══ INCONFORMIDADE HERDADA, ainda aberta ═══
**Console não libera a tela após `Transfer`** (relatada 2026-08-24). Diagnóstico JÁ FECHADO por
refutação — não repetir: **não é wrap-up e não é config de dispatch** (o pool está medido em
`detached` **no agent-registry**, não no YAML) ⇒ **teardown ausente no caminho do `Transfer`**. Abre
por: `Transfer` e `Close` compartilham o teardown do Console, ou são dois caminhos? ⚠️ Conserto
provavelmente em DOIS apps (o `agent-assist-ui` vive na 5173). ⚠️ Não testado: transfer com o
cliente ainda CONECTADO. Registro em `TODO.md` § *Analytics e UI*.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de DOZE passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduo do defeito 2: `queue_config.skill_id` legado do `retencao_humano` (a parte das linhas
  órfãs `queue_default_*` foi MEDIDA — ver Backlog acima)
```
