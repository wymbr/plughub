# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-08-30.md`](passagem-2026-08-30.md), que está **encerrada**.
>
> A sessão anterior fechou o **P3**: build, preflight, gate manual em 3 contatos reais e gate
> re-executável. O arco da janela de espera (D12) está **entregue e registrado**. Achou-se um defeito
> no próprio conserto (portão morto), e uma refutação minha teve de ser **corrigida no mesmo dia**
> porque se apoiava em evidência que o fix eliminou — o rastro ficou nos docs de propósito.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes (08-29 → 08-30 → 08-31), mas **todo dado
> medido carrega carimbo `2026-08-21`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: escolher entre (A) discriminar o id do segmento de espera —
pré-requisito da D14 —, (B) unificar o emissor do `_emit_queue_timeout`, ou (C) o defeito 2 (tenant
default suprimido pelo `skill_id` legado), que destrava a D10 e a D10.1.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Uma pendência declarada por quem parou de
  trabalhar é a última coisa que ele lembra, não o estado do repo. Isto já se pagou DUAS sessões
  seguidas: na 08-29 as duas dívidas de doc da 08-28 estavam pagas; na 08-30 tudo o que a passagem
  declarava escrito estava mesmo escrito — 3 greps confirmaram em 2 minutos e liberaram a sessão
  inteira para medir. **A ORDEM proposta também se confere.**
· **O `TODO.md` § "janela de espera" foi reescrito no fim da sessão passada.** Ele está mais atual que
  qualquer resumo — inclusive que este.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **arquivo NOVO exige `build --no-cache`** (edição de existente entra no build normal) — confirmado
  de novo na sessão passada com o arquivo de teste
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **PREFLIGHT DE SÍMBOLO antes de medir** — importar o módulo no container e conferir o atributo
· **`$DC exec` NÃO começa em `/app`** — use caminho ABSOLUTO no pytest, ou vem
  `ERROR: file or directory not found`, que é INCONCLUSIVO, não vermelho
· **no FRONTEND são DOIS caches**: depois do build, `Ctrl+Shift+R` é OBRIGATÓRIO
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**, nunca `FROM t FINAL AS alias` (erro de sintaxe no 23.8)
· **`sessions` NÃO tem `ingested_at`** (essa coluna é de `segments`) — para coorte nova, marque o
  instante com `export T0=$(date -u '+%F %T')` e filtre `segments.ingested_at >= '$T0'`
· **`GET :3300/v1/pools` devolve ENVELOPE, não lista** — `json.load(...)['pools'|'data'|'items']`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· **nada de crase dentro de aspas duplas em bash** — vira substituição de comando
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server + skill-flow-worker JUNTOS
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· **para hooks/deploy/capacity/queue_config, perguntar ao agent-registry, NUNCA ao YAML**
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o ai-gateway)
· inglês no código, PT em i18n/spec/dados de tenant

═══ AS TRÊS REGRAS QUE ESTA SESSÃO COMPROU ═══
· **GUARDA SOBRE VALOR DECODIFICADO TESTA `if not x`, NUNCA `is None`.** `_decode` devolve `""` para
  chave ausente, então `is None` compara com um valor que a fonte **não produz**: o ramo fica morto e
  o caminho segue como se o dado existisse. Foi assim que o produtor NOVO passou a emitir uma espera
  fantasma de 0 ms em TODO contato roteado direto. É a **Mudança 35 outra vez** (`""` do lado errado
  de uma guarda), no mesmo mecanismo, quatro dias depois ⇒ virou invariante no CLAUDE.md § Postura.
· **A TESTEMUNHA CERTA PARA UM DEFEITO É CEGA PARA O QUE O CONSERTO CRIA.** A query canônica do 36.2
  ficou verde e correta — ela conta `outcome='abandoned'`, e a fantasma é `handoff`. O defeito novo só
  apareceu porque se mediu a população que **NÃO deveria ter linha**. Ao criar um produtor, o teste
  que importa não é "registrou o fato", é "**não** registrou o não-fato".
· **EVIDÊNCIA PODE MORRER COM O PRÓPRIO FIX — e reescrever a conclusão em silêncio é pior que errar.**
  Refutei *"o evento é único"* com TRÊS emissões medidas; duas eram as fantasmas, e pós-fix o caso
  emite uma vez só. A conclusão sobrevive por outro caminho (duas filas), a evidência não. Os três
  docs carregam a emenda à vista.

═══ ERROS DE PREVISÃO ═══
Errei 2 de 9, e as duas do mesmo jeito: **supus que o ambiente estava parado**.
  · baseline: previ 469/522 → **474/528** (5 sessões nasceram entre o carimbo da baseline e o corte;
    a aritmética fecha, o pressuposto de "zero tráfego novo" é que não valia)
  · caminho do pytest: previ relativo ao repo → `exec` não começa em `/app`
Acertei: preflight (2), ramo (a) do caso real, testemunhas (5 baldes), 3→1 na coorte pós-fix,
falseabilidade (1 failed, e o teste CERTO), 4 passed.

═══ ESTADO — ARCO DA JANELA DE ESPERA (D12) ENTREGUE ═══
**P1 ✅ · P2 ✅ · P3 ✅** — construído, medido, corrigido, documentado. `CHANGELOG.md` tem a entrada;
`conference-mechanics.md` § **Mudança 37**; `system-queue.md` item 4 renomeado.
· produtor da espera vivo nos DOIS tiers (`resolve_queue_exit`), incluindo os 2 pontos de saída que
  nenhuma das 4 chamadas cobria (`SessionClosedEventHandler` = caminho do 36.3; drain com agente)
· **defeito encontrado e corrigido no P3:** `if raw is None` → `if not raw` (portão morto)
· gate re-executável: `packages/routing-engine/src/plughub_routing/tests/test_queue_wait_segment.py`
  (4 testes, Redis real, skip explícito lendo as DUAS variáveis; **falseabilidade conferida**)
· `infra/test/q_baseline_36_2.sql` — a testemunha do 36.2 virou arquivo

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
coorte de 3 contatos reais, antes × depois do fix: linhas `role='queue'` **3 → 1**
  · escalou→F5 na fila: `queue/abandoned/47 327 ms` (e 8 273 ms no 1º caso) — SOBREVIVEU
  · atendido com NPS, e menu inicial+F5: `queue/handoff/0` → **sem linha** (fantasma: 2 de 2)
testemunhas do 36.2 (`infra/test/q_baseline_36_2.sql`), população **528**:
  474 closed/sem-fila · 27 suspended · **11** closed+ab (era 10) · 8 active · **5** never_closed+ab
  (IMÓVEL, como previsto) · 3 nulas · nenhuma linha antiga migrou de balde
gate: `4 passed in 0.45s`; com o portão revertido: `1 failed, 3 passed`, e o que falha é
  `test_no_stamp_emits_nothing`
⚠️ **TMA: o `+4,8 %` em `retencao_humano` NÃO foi observado em dado novo** — é recomputação
  hipotética sobre histórico, e **não é retroativo** (linhas antigas mantêm `role='queue'`). Não
  confundir previsão com medição quando alguém reabrir o relatório.

═══ AS TRÊS OPÇÕES DE TAREFA ═══
**(A) Discriminar o id do segmento de espera — PRÉ-REQUISITO DA D14, e a ordem inverteu.**
  `queue_wait_segment_id` é `uuid5(tenant, session_id)`, **sem discriminador de passagem nem de pool**.
  Sessão que espera em DUAS filas emite dois segmentos que **colapsam numa linha**, vencendo a última.
  Mesma família do resíduo de `participation_intervals`. A D14 existe exatamente para esse contato ⇒
  **pôr `sla_target_ms` no segmento não adianta enquanto os dois segmentos forem um só.**
  Conserto provável: pool de destino no namespace do `uuid5` — barato, mas **muda a identidade das
  linhas já gravadas** (declarar a descontinuidade).
  ⚠️ **DEDUTIVO, NÃO OBSERVADO** — a população atual não tem caso de duas filas. Primeiro passo é
  PRODUZIR o caso (transferência entre pools com espera nos dois), não codar.
**(B) Unificar o emissor do `_emit_queue_timeout`.** Fatia própria; junto com o P2 daria emissão
  dupla. Lacuna viva: `max_wait_exceeded` na fila **ATENDIDA** segue sem segmento de espera.
**(C) Defeito 2 — tenant default suprimido pelo `skill_id` legado.** Destrava DUAS coisas por
  caminhos independentes: a atribuição da D10 (hoje `_flow_pool_id` resolve para o próprio destino ⇒
  a linha do bridge é **no-op** neste ambiente) e o estado-alvo da D10.1.

═══ NÃO fazer agora ═══
· **Migrar os 3 leitores de SLA (D14)** — bloqueado pela (A), e antes disso pela **D14.1**: o rótulo
  da UI diz *"Total service SLA"* nos dois locales enquanto todos os consumidores comparam com
  ESPERA, e o default do formulário (30 s) contradiz o próprio rótulo. **Decidir o que
  `sla_target_ms` É antes de mudar onde ele mora.**
· **36.2 (as 5 que não fecham)** — causa não identificada; a duplicação NÃO explica (interseção 1), e
  a imobilidade prevista se confirmou.
· **F4 (visão 2 do histórico unificado)** — classe *outbound* segue com zero amostras de `spawn_reason`.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de QUATRO passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
```
