# Prompt de passagem — sessão de 2026-08-20

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a de 2026-08-19, que fechou o
> **P2 inteiro** (F2 + F3) e o defeito de tenant que a F3 destapou. O handoff
> [`handoff-segmentos-abertos-2026-08-18.md`](handoff-segmentos-abertos-2026-08-18.md) está **encerrado**:
> P1, P2, P3 e P4 fechadas (P3 com `specialist` declaradamente sem amostra viva).

---

```
continuação sessão plughub.

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova:
    export DC="docker compose -f docker-compose.demo.yml"
· **e2e roda DENTRO**: `$DC run --rm e2e-runner ts-node runner.ts --only N`
· **e2e apaga `tenant_demo:*` e `session:*`** antes de CADA cenário
· **nenhum serviço monta o fonte** — mudança exige `build`
· **`build` verde NÃO recria container** — mordeu em 2026-08-19: imagem de 5 min,
  container de 1 h, e o `PUT` no registry devolvia 200 descartando o campo novo em
  SILÊNCIO (Zod strip). Sempre `$DC up -d <svcs>` DEPOIS do build, e conferir:
    docker images --format '{{.Repository}} {{.CreatedSince}}' | grep <svc>
    docker ps --format '{{.Names}} | {{.Image}} | {{.Status}}' | grep <svc>
  (container mostrando IMAGE ID em vez da tag = está rodando imagem velha)
· arquivo NOVO exige `build --no-cache`; edição de existente entra no build normal
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· **previsão de DELTA exige a BASE contada**
· **janela de log só compara se as DUAS janelas existirem** — mordeu em 2026-08-19:
  24 eventos do routing × 2 linhas do bridge era artefato de container recriado.
  Conferir `head -1`/`tail -1` do log de cada serviço ANTES de comparar contadores
· **`--since` do docker sem fuso é HORA LOCAL** — o host é −03; `--since 20:06`
  filtrou tudo e devolveu 0, que parecia medição
· **preflight sobre código casa o TOKEN da chamada, não o identificador**
· **um gate que renomeia o token que ele próprio parseia fica incapaz de reprovar** —
  o gate da fila casava `pool=`, que a F2 renomeou para `destino=`
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **um gate que não exercitou uma das metades tem de DIZER isso**
· **contar não é identificar** — e `total_instances` do snapshot de pool é
  CAPACIDADE (Σ max_concurrent), não gente: 1 humano logado lê como "3". Use SMEMBERS
· `up -d --force-recreate` APAGA o `pip install` do pytest — reinstalar antes
· o container do `redis` NÃO tem python3; usar `$DC exec -T redis`, nunca `docker exec`
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· `schemas` NÃO é serviço do compose (compila dentro do build de cada consumidor)
· mudança em @plughub/schemas → rebuild de agent-registry + skill-flow-service +
  mcp-server JUNTOS
· mcp-server tem REGISTRO DUPLO de tools: createServer E startServer; só o 2º roda
· YAML de skill não basta editar nem publicar — `infra/scripts/deploy_skill_to_slot.sh`
· YAML de registry é seed-if-absent: editar pool existente é NO-OP no DB rodando
· ambiente = docker-compose.demo.yml, tenant `tenant_demo`,
  ClickHouse/Postgres db = plughub_demo, agent-registry em plughub_registry
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 ·
  dialog-api 3760 · channel-gateway 8010 · routing-engine 3550 ·
  mcp-server-plughub 3100 · platform-ui 5174 · **auth-api 3202** (3200 é o
  ai-gateway) · **redis 6380 · kafka 9093 · postgres 5433** (remapeados no HOST)
· inglês no código, PT em i18n/spec/dados de tenant
· i18n: toda string visível nos DOIS locales, sempre por `t()`

═══ ESTADO (fechado em 2026-08-19, ver CHANGELOG) ═══
P2 COMPLETO: F2 (`queue_config.pool_id`) + F3 (`fila_humano` era deploy ÓRFÃO, não
  faltava deploy). Gate com as duas metades medidas em janelas espelhadas
`conversation_escalate` não fabrica mais `tenant_id`; trigger de webhook externo
  escreve `session:{id}:meta`. Validado diferencialmente (5 chaves `default:*` → 0,
  e um segmento `primary` onde antes não havia)
A "ponta solta" do handoff anterior NÃO existia — era comparação de janelas de log
n8n arco ABORTADO · não reabrir os baldes da triagem de 08-17

═══ BANCADA DE REPRODUÇÃO DA FILA (quente, use se precisar) ═══
1. pausar o agente humano no Console (pausa é hard filter; o snapshot NÃO é
   recomposto pela pausa e vai continuar mentindo `available: 2` — ver achado abaixo)
2. T0=$(date -u +%FT%TZ); echo $T0
3. curl -s -X POST http://localhost:8010/channel/webhook/crm-callback \
     -H 'content-type: application/json' \
     -d '{"tenant_id":"tenant_demo","customer_id":"cus_x"}'
   (o endpoint `crm-callback` resolve para `retencao_humano`)
4. bash infra/test/gate_queue_segment_not_born_without_flow.sh "$T0"
5. Retomar no Console → drain sinaliza → escalada → segmento fecha

═══ TAREFA — escolher, não fazer tudo ═══
Nenhum item abaixo é continuação obrigatória do P2; ele está fechado.

1. `do_not_contact` da Fase 3b do Outbound — smoke ESCRITO E NUNCA VALIDADO, num
   veto de contato com pessoa. Era o item mais urgente antes do P2 aparecer.
2. **CONFLITO DOC×DOC não resolvido**: o `CLAUDE.md` § "Detach de hooks" afirma
   "Camada F ✅ ARCO A–F COMPLETO" e o `TODO.md` mantém E2 e F abertos. O próprio
   CLAUDE.md manda resolver **por medição**, não escolhendo o mais recente. Não foi
   tocado nesta sessão.
3. Reexame item a item dos 9 em `Escopo reduzido` (§4.5 do doc de reversão) —
   rejulgar pelo mérito, NÃO reverter em bloco.
4. `specialist` de suspend/resume: segue sem amostra viva (exige
   delegate-as-conference, que nenhum pool webhook do demo alcança hoje).

═══ ACHADOS ABERTOS de 2026-08-19 (nenhum é bloqueio; todos têm dono nenhum) ═══
· **`agent_pause` não recompõe o snapshot do pool.** Durante toda a reprodução o
  Monitor afirmou `available: 2` num pool com SET de alocação VAZIO (`updated_at`
  congelado). Mesma família que a F3a consertou via `refresh_snapshots_for_instance`;
  este caminho ficou de fora. O `system_availability_check` usa esse número para
  decidir oferta de canal ao CLIENTE — é valor plausível num lugar que decide produto.
· **`session:{id}:meta` não tem escritor único.** Hoje: o canal na conexão, o
  delegate/collect, o bridge na alocação e (desde 08-19) o trigger de webhook externo.
  Quatro escritores para uma chave cujos leitores tratam ausência como default. O
  conserto do escalate fechou o caso mais caro; o padrão continua.
· **`queue_config` só existe em 1 pool do demo** (`retencao_humano`), então as duas
  metades do gate não cabem na MESMA janela sem mexer em config. Foram medidas em
  janelas espelhadas, cada uma declarando a metade que não exerceu.
```
