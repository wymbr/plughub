# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-09-04.md`](passagem-2026-09-04.md), que está **encerrada**.
>
> A sessão anterior entregou DUAS fatias, ambas com gate verde: **(falseabilidade da fatia B)** e
> **(predicado único do `sla_target_ms`)**. Sobrou a **D14 (ii)/(iii)** e três achados novos com
> dono próprio.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes, mas **todo dado medido carrega carimbo
> `2026-08-24`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: escolher entre (D14-ii) mover `sla_target_ms` para o segmento de
espera — desbloqueada e com evidência numérica —, (TTL de `pool_config`) dois escritores da mesma
chave com TTLs diferentes, ou (lacuna 2) o teste vermelho do `claimed_via`.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Uma pendência declarada por quem parou de
  trabalhar é a última coisa que ele lembra, não o estado do repo. Já se pagou SETE sessões
  seguidas. **A ORDEM proposta também se confere.**
· 🆕 **MEÇA A BASELINE DA SUÍTE ANTES DE MUDAR QUALQUER COISA.** Foi o erro de método desta sessão:
  rodei a suíte só DEPOIS, veio `1 failed`, e só não virou meia hora de investigação porque o
  vermelho era atribuível por estrutura (arquivo que eu não tinha tocado). Sem baseline, "quebrei?"
  não tem resposta. Um comando:
    $DC exec -T routing-engine sh -c "cd /app/packages/routing-engine && python3 -m pytest src/plughub_routing/tests -q 2>&1 | tail -3"
  Esperado HOJE: **1 failed, 278 passed** — o vermelho é conhecido (ver Backlog item 3).
· **O `TODO.md` § "SLA está no grão errado" foi ATUALIZADO no fim da sessão passada** (passo 4 ✅
  fechado, + subseção "Achados adjacentes da fatia do predicado"). Está mais atual que qualquer
  resumo — inclusive que este.
· ⚠️ **O AMBIENTE SEGUE MEXIDO, e continua sendo bom que siga.** `retencao_humano` tem
  `queue_config.pool_id = fila_humano` (fila ATENDIDA) e `max_wait_s: 1800`. Não reverta. Confira:
    python3 infra/test/q_queue_config_inventory.py
  Esperado: 36 pools · none 35 · legacy_only 0 · **addressable 1**. Nunca ao YAML (seed-if-absent).

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços. 🆕 E confira que
  a linha colada não veio DUPLICADA: aconteceu nesta sessão e o erro se lê como erro de sintaxe do
  código, não da colagem
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **`$DC exec` no clickhouse EXIGE `-T`** — o serviço tem `tty: true` e o `< /dev/null` colide
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **arquivo NOVO exige `build --no-cache`** (não vale p/ script em `infra/test/`, que roda do host)
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **`up -d` recria o container e leva junto o `pip install`** — reinstalar pytest antes de rodar gate
· **no frontend há DOIS caches** — depois do build, `Ctrl+Shift+R` é obrigatório
· **PREFLIGHT DE SÍMBOLO antes de medir** — e prefira checagem ESTRUTURAL (AST, aridade, valor) a
  busca textual: `grep` conta o COMENTÁRIO que documenta a remoção
· **`$DC exec` NÃO começa em `/app`** — caminho ABSOLUTO no pytest
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· CH 23.8 **não faz subquery correlacionada** — use JOIN; prefira joins binários ANINHADOS
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 · auth-api 3202
  · agent-assist-ui (app LEGADO, vivo) 5173

═══ O QUE ESTA SESSÃO COMPROU ═══
· **PARA CONFERIR FALSEABILIDADE, MUTE DE PROPÓSITO — `git stash` É O INSTRUMENTO ERRADO.** Com a
  fatia já commitada o stash é NO-OP, o teste segue verde, e **verde por ausência de mudança é
  indistinguível de teste que não pode reprovar** — que é a proposição sob exame. E o preflight tem
  de ser `inspect.getsource` da função CARREGADA, não `grep`: "o build não pegou" e "o teste é
  inútil" produzem os dois um verde e são conclusões OPOSTAS. Sem esse ramo, a rodada não julga.
· **INVENTÁRIO POR MENÇÃO SUBCONTA; CONTE POR DERIVAÇÃO.** A passagem dizia "os dois escritores do
  `sla_target_ms` discordam". Grepando o padrão de ATRIBUIÇÃO em vez do nome: **quatro sites, quatro
  comportamentos**. E os dois esquecidos eram os **mudos** — foi o silêncio deles que os manteve
  fora de toda investigação anterior. Um deles era o defeito ativo.
· **A FATIA PODE SER ABERTA PELO CASO ERRADO E AINDA ASSIM VALER.** O `0` era portão fechado (o Zod é
  `.positive()`, ninguém consegue configurá-lo). Quem mordia era a AUSÊNCIA, no site da ETA, com
  gatilho de RELÓGIO (TTL de cache). Ao inventariar, pergunte de cada site *"e com ausente?"* e *"e
  com zero?"* **separadamente** — são dois ramos e costumam ter gatilhos de natureza diferente.
· **UM CONTRATO QUE NÃO SABE DIZER "AUSENTE" EMPURRA O PRODUTOR PARA O VALOR PLAUSÍVEL.**
  `estimated_wait_ms` era `nonnegative()` **obrigatório**: `null` inexpressável, `0` legítimo. O
  produtor publicava `0`. Não foi desleixo do produtor — foi o único vocabulário que ele tinha.
· **UMA FUNÇÃO SÓ NÃO FAZ FONTE ÚNICA.** `_queue_position_and_eta` era declarada "FONTE ÚNICA" e
  tinha dois consumidores discordando: um guardava e omitia, o outro publicava `0`. **A unicidade
  estava no cálculo, não no vocabulário.**

═══ ERROS DE PREVISÃO (6 acertos, 2 erros) ═══
Acertos: falseabilidade **4 de 4** (mute VERDE · attended VERMELHO **com a mensagem prevista** ·
sem-carimbo VERDE · AST VERDE) · gate do predicado **23 passed** · preflight `MUT`/`ORIG` ·
**36** chaves de `pool_config`.
Erros:
· previ TTL "alto (~86 400)", medido **3 592**. Causa: **carreguei o número do docstring para dentro
  da previsão em vez de medir.** O docstring (`registry.py:13`) diz 24 h e está errado há tempo.
· previ "suíte existente sem regressão", veio **1 failed**. O número estava certo sobre a MINHA
  mudança e a previsão estava errada sobre o mundo — **eu nunca tinha estabelecido a baseline.**
  É o erro de método da sessão, e virou a 2ª linha do § ANTES DE QUALQUER COISA.
Viés observado: os dois erros são de **confiar em documento no lugar de medir** — um doc de código,
uma suposição sobre o estado do repo. Nenhum foi erro de aritmética.

═══ ENTREGUE — não re-fazer ═══
**(falseabilidade da fatia B)** — mutação nomeada em `_emit_queue_timeout` (chamada a
`resolve_queue_exit` re-gatilhada por `queue:agent_active`) ⇒ **1 failed, 3 passed**, vermelho com
*"fila ATENDIDA fechada por max_wait produziu 0 segmentos de espera"*. Revertido (`ORIG`, 14 passed).
Reconciliação: o comentário em `test_queue_wait_segment.py:328` afirmava medição que era **dedução**;
o vermelho que o autor vira era o do HARNESS. Agora tem citação no próprio teste.
🟢 Subproduto: a fatia B deixou testemunha — `main.py:711` loga `wait_segment=%s`, e sob a mutação
saiu `wait_segment=False` ao lado de `queue_agent=True`.

**(predicado único do `sla_target_ms`)** — `models.resolve_sla_target_ms(raw, where=…) -> int | None`,
nunca `0`, logando QUAL violação (ausente × não-positivo × ilegível × booleano). Quatro sites
migrados; `SLA_TARGET_MS_FALLBACK` aplicado **no call site** dos dois que precisam de `int` (a opção
"Optional de ponta a ponta" segue sendo a (a) da D14.1, registrada e recusada). Contrato corrigido:
`QueuePositionUpdatedEventSchema.estimated_wait_ms`/`sla_target_ms` agora nuláveis, com pisos
deliberadamente diferentes (alvo `.positive()` · ETA `.nonnegative()`). Gate:
`test_sla_target_predicate.py` (**23 passed**), tabela + guarda AST.
⚠️ **A linha que dá valor ao gate é a do `0`** — igualdade entre sites só julga se a população contiver
o caso em que eles DISCORDAVAM.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
suíte routing-engine: **1 failed, 278 passed** (o vermelho é o do Backlog 3, ANTERIOR a esta sessão)
`{tenant_demo}:pool_config:*` → **36 chaves**, TTL **3 592 s** (todas iguais ⇒ reescritas em lote
  pelo bridge ~8 s antes); TTL efetivo é **1 h**, não os 24 h que 4 documentos afirmam
inventário de fila: 36 pools · none 35 · legacy_only 0 · addressable 1 (`retencao_humano`→`fila_humano`)
`sla_target_ms` — sites que DERIVAM o campo da fonte crua: **4** (2 em `main.py`, e os 2 eram mudos)
cobertura de `_queue_position_and_eta` e `_pool_sla_target` antes desta sessão: **ZERO testes**

═══ AS OPÇÕES DE TAREFA ═══
**(D14-ii) `sla_target_ms` no SEGMENTO de espera.** Desbloqueada pela (i), com número
  (**10 das 43 esperas concluídas** do `retencao_humano` são injulgáveis porque a SESSÃO não tem
  alvo enquanto o POOL tem 300 000 ms) e **agora sem herdar predicado divergente** — a fatia do
  predicado entrou de propósito ANTES, porque a (ii) CARIMBA o alvo no ledger e alvo carimbado
  errado deixa de ser corrigível por deploy.
  ⏳ **DUAS decisões do dono antes de codar, não uma:**
    (1) **pool de IA tem alvo de espera?** — `TODO.md:1492-1495`, sub-pergunta da D14.1 nunca
        respondida. Dos 63 segmentos `role='queue'` medidos, **19 estão em pools de IA**. Sem isso a
        (ii) não sabe o que escrever neles.
    (2) alvo **copiado** no fechamento da espera (denormaliza, guarda "o alvo do dia") × **resolvido
        na leitura** pelo pool do segmento (não duplica, mas re-lê config de hoje para medir ontem).
  ⚠️ `sessions.sla_target_ms` vira **projeção**, nunca fonte de cálculo.
  ⚠️ (iii) migrar os três leitores (`query.py:240` · `reports_query.py:3803` · `_sla_eligible`) é
  fatia PRÓPRIA — não entra junto.
**(TTL de `pool_config`)** dois serviços escrevem a MESMA chave com TTLs diferentes e o último vence:
  `routing-engine/registry.py:3298` **86 400** × `orchestrator-bridge/instance_bootstrap.py:477,939`
  **3 600**. O bridge reconcilia a cada 5 min ⇒ **o 24 h do routing-engine está morto**. Agrava:
  `config-api/seed.py:288` afirma em prosa que são caches SEPARADOS (não são), e o
  `changelog-2026-04-16` documenta o conserto 300 s→86 400 que o bridge desfez em silêncio.
  ⚠️ A fatia do predicado já tirou o dano mais caro desta expiração (a ETA de 0 ms), então isto agora
  é **correção de modelo**, não de sangramento.
**(lacuna 2)** o teste vermelho do `claimed_via` — ver Backlog 3. Não é trocar a string.

═══ NÃO fazer agora ═══
· **"Consertar" o vermelho do `claimed_via` trocando `"semaphore"` por `"record"`.** Passa o teste e
  apaga a pergunta. Ver Backlog 3.
· **Anular `sla_target_ms` em pool nenhum** — decidido (b): não se anula, muda quem lê.
· **"Consertar" a multiplicidade de segmentos por F5** — REFUTADO (Mudança 39). O discriminador de
  duplicação é SEGMENTO ÓRFÃO (`duration_ms IS NULL` > 1 por sessão viva), **nunca a contagem**.
· **O `queue-agent` do bridge** (`main.py:5993`, `uuid5` sem discriminador de passagem). Inalcançável
  hoje: exige DOIS pools com fila atendida e só um tem endereço.
· **F4 (visão 2 do histórico unificado)** — classe *outbound* segue com zero amostras.

═══ Backlog NOVO / reaberto ═══
· 🔴 **3. `test_expire_returns_the_slot_even_after_the_lease_expired` VERMELHO, e é ANTERIOR a esta
  sessão** (medido: `router.py` não foi tocado). Asserta `claimed_via == "semaphore"`; o código
  devolve `"record"`, porque a emenda **D6** (2026-08-04) inseriu o registro durável de posse ANTES
  do semáforo (`router.py:1140`, que o diz literalmente). ⚠️ **Não é cosmético:** `router.py:1158`
  declara que a linha do `semaphore` é *"a MEDIDA da lacuna 2"* — com o `record` respondendo antes,
  **esse ramo ficou inalcançável neste cenário**, e o instrumento da lacuna 2 parou de ser exercido
  sem que nada além deste vermelho o dissesse. Casa com a lacuna que a Camada F já declarava (*"a
  lease não foi medida"*). **Consertar = decidir onde vive a medida da lacuna 2 agora.**
· 🆕 **`estimated_wait_factor` (0.7) e `congestion_sla_factor` (1.5) são o 2º e 3º órfão do namespace
  `routing`** — semeados, cacheados, testados, **sem leitor**: `main.py` fixa `* 0.7` e
  `saturated.py` fixa `* 1.5`. Com o `sla_default_ms` já conhecido, são **três** botões que prometem
  efeito e não têm.
· 🔴 **`sla_default_ms` NÃO TEM LEITOR** — ⚠️ remover exige mexer no
  `smoke_config_routing_orphan_keys.sh:118`, que a usa como **CANÁRIO** de namespace intacto.
· 🆕 **`max_queue_len` é declarado no front e NUNCA lido** (`AnalisePoolsPage.tsx:55`).
· 🆕 **`query_pools_queue` engole a exceção** e devolve `data: []` + `error`, indistinguível de "não
  há dado" na tela. O gate novo o pega; a tela, não.
· **3 cópias restantes do `480_000`**: `instance_bootstrap.py:709` · `mcp-server/tools/bpm.ts:281` ·
  `config-api/seed.py:158`.
· **`supervisor.ts` — `urgency` compara ATENDIMENTO TOTAL com alvo de ESPERA.** Não mede nada. É
  contrato de tool MCP com consumidores não mapeados — **mapeá-los é a fatia**.
· ⚠️ **`duration_ms` do segmento humano diverge da janela dos próprios carimbos** (26 448 ms/80 s;
  25 519 ms/60 s). **Sem causa** — a explicação "sai do último F5" caiu com a Mudança 39.
· **`sac_ia` tem 2 esperas de 0 ms** (Mudança 37, consta corrigida). Ou é dado pré-correção, ou a
  correção não pegou tudo.
· **Exibir tempo de contato / espera-antes-do-agente no Console** — levantado pelo operador, SEM
  decisão. São 3 fatos distintos (segmento · contato · espera) e a tela mostra 1.
· `admission.py:267` apaga `first_queued` sem emitir, p/ sessões fechadas no buffer MUDO — 7º ponto
  de saída, fora dos 6 call sites. A fila ATENDIDA não tem backstop equivalente.
· **`_emit_queue_timeout` publica em outbound e `conversations.events` SEM `key=`.** Tópicos
  diferentes da fatia B, dívida à parte. O `_TimeoutProducer` do teste tolera e DECLARA a tolerância.

═══ INCONFORMIDADE HERDADA, ainda aberta ═══
**Console não libera a tela após `Transfer`** (relatada 2026-08-24). Diagnóstico JÁ FECHADO por
refutação — não repetir: **não é wrap-up e não é config de dispatch** (o pool está medido em
`detached` **no agent-registry**, não no YAML) ⇒ **teardown ausente no caminho do `Transfer`**. Abre
por: `Transfer` e `Close` compartilham o teardown do Console, ou são dois caminhos? ⚠️ Conserto
provavelmente em DOIS apps (o `agent-assist-ui` vive na 5173). ⚠️ Não testado: transfer com o cliente
ainda CONECTADO. Registro em `TODO.md` § *Analytics e UI*.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de NOVE passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduos do defeito 2: linhas órfãs `queue_default_*` no config store;
  `queue_config.skill_id` legado do `retencao_humano`
```
