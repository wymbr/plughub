# Prompt de passagem — próxima sessão

> Cole o bloco abaixo ao abrir a sessão nova (Opus, sessão limpa). Sucede a
> [`passagem-2026-09-01.md`](passagem-2026-09-01.md), que está **encerrada**.
>
> A sessão anterior escolheu a opção **(A)** e a fechou. Sobraram (B) e (D14.1) — e **nasceu uma
> terceira**, que não existia na passagem anterior porque só apareceu ao consertar o produtor.
>
> ⚠️ **Nota de data:** o arquivo segue a série de nomes, mas **todo dado medido carrega carimbo
> `2026-08-24`**. Onde houver conflito, **confie no carimbo do dado**.

---

```
continuação sessão plughub. TAREFA: escolher entre (B) unificar o emissor do `_emit_queue_timeout`,
(D14.1) decidir o que `sla_target_ms` É, ou (D14-i) parar de colapsar a sessão numa linha de fila no
relatório Fila/SLA. A opção (A) foi ENTREGUE e MEDIDA em contato real.

═══ ANTES DE QUALQUER COISA ═══
· **CONFIRA AS ⚠️ DESTA PASSAGEM POR GREP ANTES DE AGIR.** Uma pendência declarada por quem parou de
  trabalhar é a última coisa que ele lembra, não o estado do repo. Já se pagou QUATRO sessões
  seguidas. **A ORDEM proposta também se confere.**
· **O `TODO.md` § "janela de espera" e § "SLA está no grão errado" foram reescritos no fim da sessão
  passada.** Estão mais atuais que qualquer resumo — inclusive que este.
· ⚠️ **O AMBIENTE SEGUE MEXIDO, e agora é bom que siga.** `retencao_humano` tem
  `queue_config.pool_id = fila_humano` (medido 2026-08-24, ainda lá). É o **cenário 2**, e é ele que
  torna o caso de duas filas reproduzível. Não reverta sem motivo. Confira:
    python3 infra/test/q_queue_config_inventory.py
  Esperado: 36 pools · none 35 · legacy_only 0 · **addressable 1**. Nunca ao YAML (seed-if-absent).

═══ REGRAS OPERACIONAIS (as de sempre) ═══
· o sandbox bash NÃO alcança o WSL — eu edito (Read/Write/Edit), você roda e cola
· **comandos CURTOS** — linha longa quebra no terminal e o shell roda os pedaços
· $DC não vem definido na shell nova, e o compose fica na RAIZ do repo:
    cd ~/projects/plughub && export DC="docker compose -f docker-compose.demo.yml"
· **nenhum serviço monta o fonte** — mudança exige `build`. **Vale para TESTE também**
· **arquivo NOVO exige `build --no-cache`** — por isso os 2 testes novos foram para DENTRO de
  `test_queue_wait_segment.py`, e funcionou (de novo)
· **`build` verde NÃO recria container** — sempre `$DC up -d <svcs>` DEPOIS do build
· **`up -d` recria o container e leva junto o `pip install`** — reinstalar pytest antes de rodar gate
· **PREFLIGHT DE SÍMBOLO antes de medir** — e prefira checagem ESTRUTURAL (aridade, valor) a busca
  textual: `grep`/`inspect.getsource` contam o COMENTÁRIO que documenta a remoção
· **`$DC exec` NÃO começa em `/app`** — caminho ABSOLUTO no pytest
· PREVER O NÚMERO antes de rodar, e escrever a previsão. CONTAR, não estimar
· veredicto RAMIFICA sobre o valor medido; três ramos, e AUSENTE é INCONCLUSIVO
· **um contador de AUSÊNCIA precisa de um contador-TESTEMUNHA de presença ao lado**
· **ClickHouse: `FROM t AS alias FINAL`**; db = `plughub_demo` (nunca `analytics`)
· todo `docker exec` dentro de script interativo leva `< /dev/null`
· portas: analytics-api 3500 · agent-registry 3300 · config-api 3600 · dialog-api 3760 ·
  channel-gateway 8010 · routing-engine 3550 · mcp-server 3100 · platform-ui 5174 · auth-api 3202

═══ O QUE ESTA SESSÃO COMPROU ═══
· **IDENTIDADE DERIVADA PRECISA DO DISCRIMINADOR DO FENÔMENO, NÃO DO CONTÊINER.** `uuid5` determinístico
  é a forma certa de tornar emissão repetida inócua — e inútil se a chave descreve a coisa errada.
  `queue_wait_segment_id` identificava a SESSÃO; o fato é a PASSAGEM. Virou invariante no CLAUDE.md
  § Postura de Engenharia.
· **A PREMISSA FALSA ESTAVA NO DOCSTRING DA PRÓPRIA FUNÇÃO** (*"uma sessão tem UMA passagem pela
  fila"*). Comentário que promete invariante sem mecanismo que a imponha — idêntico ao DDL de
  `participation_intervals` (Problema 34), só que em Python.
· **PERDA DE DADO ORDENA A FILA DE TRABALHO ACIMA DE DEFEITO DE EXIBIÇÃO.** Foi o que decidiu fazer (A)
  antes de (D14.1), contra a cadeia de dependências: o colapso do relatório é consertável a qualquer
  momento; a linha de espera perdida não volta (o carimbo é apagado na saída).
· **ESCOLHER O DISCRIMINADOR É ESCOLHER O ESCOPO.** O pool foi recusado por ser fato do CALL SITE
  (`event.pool_id or ""`), não da passagem — daria dois ids para uma passagem.

═══ ERROS DE PREVISÃO ═══
Numéricas: **0 de 8**. Acertei inventário (4 baldes), defaults, log (total + duplicados), colisão
(2 saídas → 1 linha + id idêntico ao previsto), id do bridge, preflight (`3 True`), gate (`10 passed`)
e o pós-conserto (2 saídas → 2 linhas, ids distintos, fórmula antiga não bate).

Errei DUAS de julgamento, as duas corrigidas por leitura antes de virar código:
  · **achei que a devolução à fila num pool pull daria o caso de duas passagens de graça.** Não dá:
    `work_task_claim` não está nos 6 call sites de `resolve_queue_exit`, e
    `test_claim_possession_record.py:346` **afirma de propósito** que o carimbo sobrevive à devolução.
    Uma passagem, não duas.
  · **vendi (A) como "mexer no namespace do uuid5".** Ao medir o consumidor, (A) sozinha **não melhora
    nenhuma tela** — o relatório descarta a linha resgatada. Isso não invalidou a fatia (o ledger é o
    pré-requisito da D14), mas mudou o que se promete dela.

═══ ESTADO — (A) ENTREGUE ═══
`queue_wait_segment_id(tenant, session, first_queued_ms)`, 3º param obrigatório; call site passa o
`first_ms` de que a duração deriva; `except ValueError` do parse deixou de ser mudo.
Registro: `CHANGELOG.md` 2026-08-24 · `CLAUDE.md` § Postura de Engenharia ·
`conference-mechanics.md` § **Mudança 38** · `TODO.md` (duas seções).
Gate: `test_queue_wait_segment.py` **10 passed** (8 + 2 novos, que são um PAR e não se separam).
Probes novos: `q_human_pool_paths.py` · `q_queue_collision_witness.sh` · `q_queue_multirow_impact.sh`.

⚠️ **NÃO FEITO, e foi oferecido:** a falseabilidade dos 2 testes novos foi argumentada por construção,
**não conferida empiricamente**. O experimento custa 2 builds: remover só `:{int(first_queued_ms)}` do
f-string (mantendo a aridade 3) ⇒ previsão **`1 failed, 9 passed`**, vermelho em
`test_two_passages_get_distinct_ids`. Se for barato, pague — as outras sessões pagaram.

═══ NÚMEROS MEDIDOS — não re-medir sem motivo ═══
inventário (36 pools): none 35 · legacy_only 0 · **addressable 1** (`retencao_humano` →
  `{max_wait_s:1800, pool_id:"fila_humano", skill_id:"skill_fila_v1"}`) · defaults `''`/`''`
pools humanos: **6** — `retencao_humano` (push) · `especialista_onboarding` (push) ·
  `retencao_humano-int`, `aprovacao_credito`, `aprovacao_deploy`, `formfill_demo` (pull) · `unknown` 0
ANTES (`9403a14b-…6a937dae41c4`): 2 saídas → **1 linha** `role='queue'`; id gravado
  `4a539b95-458d-56a5-…` = `uuid5` previsto. Espera de **24 118 ms perdida** (sobrou a de 85 009 ms)
DEPOIS (`27651d1b-…dc9a3d1a0c0c`): 2 saídas → **2 linhas**; `3cc267dd-376c-5fdd-…` (43 791 ms,
  handoff) e `b4d51ae4-6220-5c4a-…` (80 980 ms, abandoned) ⇒ **124 771 ms** no ledger
exposição do `anyIf` hoje: 61 linhas `role='queue'`; 50 sessões com 1 · 4 com 2 · 1 com 3;
  **2 das 5 multi-linha DISCORDAM** (`049167a2…` em outcome · `dce98532…` em pool)

═══ RECEITA PARA REPRODUZIR O CASO DE DUAS FILAS (custou uma sessão descobrir) ═══
1. contato no `sac_ia` → escala para `retencao_humano` (deixe esperar)
2. logue humano em `retencao_humano` (push) e assuma → fecha a passagem 1 com `handoff`
3. transfira para `especialista_onboarding` (push, ninguém logado) → passagem 2
4. cliente desconecta → `abandoned`
5. `infra/test/q_queue_collision_witness.sh <session_id>`
⚠️ NÃO tente pela devolução à fila em pool pull — é UMA passagem, por decisão testada.

═══ AS OPÇÕES DE TAREFA ═══
**(D14-i) Parar de colapsar a sessão numa linha de fila.** NOVA — só existe porque (A) landou.
  `reports_query.py:5740-5760` faz `anyIf(pool_id, role='queue')`, `anyIf(outcome, …)`,
  `maxIf(duration_ms, …)` por sessão. Consequência viva: a 2ª linha de espera é **descartada na
  leitura** (tela mostra 80 980 de 124 771) e o `anyIf` **sorteia** onde as duas discordam — 2 de 5
  sessões hoje. Era resíduo congelado; agora cresce com o tráfego.
  ⚠️ **O conserto NÃO é `sum()`** — somar esperas contra alvos diferentes é o que a D14 recusa.
  ⚠️ Depende da **(D14.1)**: não se decide a granulação da comparação sem decidir o que o alvo mede.
**(D14.1) Decidir o que `sla_target_ms` É.** Bloqueia a (D14-i) e a D14. Rótulo diz "Total service
  SLA" nos dois locales, TODOS os consumidores comparam com ESPERA, e o default do formulário (30 s)
  contradiz o rótulo. **18 de 36 pools com ≥ 1 hora** (processo/aprovação) contra 18 entre 15 s e
  10 min ⇒ todo agregado de SLA mistura duas populações incomparáveis. Decisão + inventário.
**(B) Unificar o emissor do `_emit_queue_timeout`.** Lacuna viva: `max_wait_exceeded` na fila
  **ATENDIDA** segue sem segmento de espera. ⚠️ **Ganhou 2º motivo:** aquele emissor ainda usa
  `uuid4()` **e publica sem `key=`** — os dois defeitos que o `resolve_queue_exit` já pagou. Cuidado
  conhecido: trocar para `emit_segment=True` sem remover o passo 3 dá emissão DUPLA.

═══ NÃO fazer agora ═══
· **Migrar os 3 leitores de SLA (D14 ii/iii)** — depende de (D14-i), que depende de (D14.1).
· **O `queue-agent` do bridge** (`main.py:5963`, mesma colisão, confirmada). Hoje **inalcançável**:
  exige DOIS pools com fila atendida e só um tem endereço. Fatia própria, com pergunta extra — o
  discriminador dele NÃO pode ser wall-clock, ou a idempotência morre.
· **36.2 (as 5 que não fecham)** — causa não identificada.
· **F4 (visão 2 do histórico unificado)** — classe *outbound* segue com zero amostras.

═══ LACUNA DE VALIDAÇÃO — o arco INTEIRO foi medido só em CONTATO, nunca em PROCESSO ═══
Levantada pelo operador em 2026-08-24. D12, defeito 2 e (A) foram validados com sessões de
atendimento (`sac_ia` → humano → transferência). **Nenhuma medição usou sessão de workflow/webhook**,
e o caminho de fila NÃO as exclui.

O que foi medido em resposta, e o que não:
· **o caminho de fila é canal-agnóstico** (`main.py:792-854` só consulta o canal para
  `channel_max_wait_s`) ⇒ processo cujo pool não tem instância livre enfileira igual, ganha
  `first_queued_ms` e emite `role='queue'`. Processos ESTÃO no mecanismo.
· **o defeito 2 é no-op para processo** — MEDIDO: os 35 pools sem `queue_config` (todos os webhook
  entre eles) eram mudos ANTES (`if queue_config:` com null) e são mudos DEPOIS (`queue_address`→"").
  Só o `retencao_humano` trocou de lado, e é humano.
· **(A) é identidade só, sem ramo por canal** ⇒ risco baixo por construção. **NÃO medido em processo.**

Dois achados novos, nenhum confirmado no dado:
· ⚠️ **`queue_max_wait_by_channel` não tem `webhook`** (medido: voice 300 · webrtc 300 · webchat 1800
  · whatsapp 14400) ⇒ processo cai no default 1800 s. Inofensivo hoje, mas `webhook` é o canal que
  alguém zeraria pensando *"processo não espera"* — e **`0` é VETO**: fecharia todo workflow
  enfileirado na porta com `max_wait_exceeded`.
· ⚠️ **o relatório Fila/SLA parece não separar contato de processo**: `spawn_reason` não aparece na
  função (`reports_query.py:5740-5840`); as ocorrências no arquivo são de outras funções (939, 988,
  1500). Se confirmado, processo que enfileira entra na estatística de fila de ATENDIMENTO — as duas
  populações misturadas, que é o que a D11/D13 nomeia. **Indício estrutural, não medido.**

**Experimento (barato, e fecha as duas):** disparar um pool webhook cuja capacidade esteja esgotada
(ou reduzir `max_concurrent` do deploy para 0/1 e disparar duas vezes) ⇒ o 2º trigger enfileira.
Conferir (i) se nasce segmento `role='queue'`, (ii) se a sessão aparece no `/reports/pools/queue`,
(iii) se o drill mostra a espera. Instrumento pronto: `infra/test/q_queue_collision_witness.sh`.
⚠️ Prever o número antes; e a testemunha é a sessão de CONTATO ao lado, que deve continuar aparecendo.

═══ INCONFORMIDADE REGISTRADA — Console não libera a tela após `Transfer` ═══
Relatada pelo operador em 2026-08-24, com diagnóstico JÁ FECHADO por refutação (não repetir o
caminho): a tela mantém transcrição + contexto após o `Transfer`, com `Session closed`, enquanto o
`Close` libera. **Não é wrap-up e não é config de dispatch** — o wrap-up sempre restaura o histórico
ao exibir (padronizado quando `inline`/`detached` virou configurável, com histórico reduzido próprio),
o pool está medido em `detached` (`hooks.on_human_end[0].pool = "wrapup_detached_ia"`), e a tela
permanece **mesmo após o wrap-up finalizado**. ⇒ **teardown ausente no caminho do `Transfer`.**
Abre por: `Transfer` e `Close` compartilham o teardown do Console, ou são dois caminhos?
⚠️ Conserto de Console provavelmente precisa ser feito DUAS vezes (o `agent-assist-ui` vive).
⚠️ Não testado: transfer com o cliente ainda CONECTADO (aí o contato continua e "limpar" muda de
sentido). Registro completo em `TODO.md` § *Analytics e UI*.

═══ Backlog NOVO, fora do arco de fila ═══
· **`duration_ms` do segmento humano diverge da janela dos próprios carimbos.** DUAS medições
  independentes: 26 448 ms para 80 s (11:44:50→11:46:10) e 25 519 ms para 60 s (13:22:55→13:23:55).
  Duas ocorrências não é ruído. Hipóteses não testadas: wrap-up fora da conta (correto e mal
  rotulado) × carimbo de fim errado.
· `admission.py:267` apaga `first_queued` sem emitir, para sessões fechadas no buffer MUDO — 7º ponto
  de saída, fora dos 6 call sites. A fila ATENDIDA não tem backstop equivalente.

═══ Pendências herdadas, NÃO fechadas ═══
· varredura das passagens 08-19 a 08-25 em busca de backlog órfão (herdada de SEIS passagens atrás)
· Reexame dos 9 (A0–A7, B1/B2, C1–C4, D1)
· resíduos do defeito 2: linhas órfãs `queue_default_*` no config store; `queue_config.skill_id`
  legado do `retencao_humano`; confirmação VISUAL da tela do pool
```
